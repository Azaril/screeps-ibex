# Ibex Incremental Rewrite Plan — 2026-06-09 (updated 2026-06-11)

> Built from [`../reviews/ibex-review-report.md`](../reviews/) and the [`../design/`](../design/) ADRs. **Incremental & confidence-driven**; back-compat not required (serialized state may be dropped per step). Don't break the running bot mid-increment.

## 0. Status, Phases & Confidence (2026-06-10)

**This document is the roadmap; execution happens in phases**, each tracked as a living document under [`../execution/`](../execution/) (one doc per phase, task IDs in commits). A phase is a working-size slice of one or more increments; the increment table in §3 is the source of truth for sequencing and seams.

- **Phase 0 — COMPLETE** ([`phase-0.md`](../execution/phase-0.md), exit audit §6: all criteria met except #8's formal operator sign-off, which remains the operator's call): the verification substrate. Delivered the harness stack (`screeps-server-kit` + `screeps-ibex-eval` + `screeps-rest-api` + `screeps-prospector` + `screeps-pack` npm-free deploy), host test lane + first ~33 kernel/pin tests, supplanted-code cleanup (~950-line orphan squad missions deleted), the proposed-fixes Group A critical fixes, the `segments.rs` registry (seg-55 cost-matrix wipe FIXED, live-proven), BASELINE-0/1. **Phase 0 absorbed most of Increment 0 and the Inc-1/2/4 quick-win subsets** — see the Status column in §3 and the §7 changelog.
- **Phase 1 — SUBSTANTIALLY COMPLETE** ([`phase-1.md`](../execution/phase-1.md), exit audit §2.7: all criteria met except #3-partial — seeded N=9 gate mode carried to Phase 2 — and #10's operator sign-off): Increment-0 remainder + Increment 1 → **M0 + M1 REACHED** (2026-06-11). Delivered: seg-57 versioned metrics + colony-health score + scenario/fault-injection harness; `CpuGovernor` + tier-scaled pathfinding ops pool; JS-boundary containment (serialize never skipped — live panic acceptance); guarded intent sink (all 23 combat sites) + intent differ; `StageClass` scheduler seam; war cadences off 1; planner/renew/power-bank/visual riders. D1/D2 (0007 snapshot + re-match) deferred to Phase 2. BASELINE-2 recorded (health 0.5640). All builds warning-free (standing bar).
- **Phase 2 — NEXT (to be planned):** candidates from the carried list (phase-1.md §2.0 (j)): D1/D2, seeded determinism + gate aggregation, B5 cache-warm remainder, Inc-2 rescoped tail (IBEX-049), then Inc 3 (stable-ID SquadStore). Plan it when entered — don't pre-invest.
- **Confidence policy** (how to read §3): increments **0–2 are HIGH confidence** (designs settled, implementable now — planned in detail). **3–6 are MEDIUM** (decisions made, details firm enough to sequence, but expect refinement as earlier increments teach us). **7–9 are LOW** (directional; Inc 9 is explicitly an evidence-gated menu, not a queue). Low-confidence rows are deliberately under-planned — they will be re-planned when their gates approach; don't invest in detail there yet.
- **Reset-anytime policy (operator decision, 2026-06-10):** during the rewrite, serialized state may be dropped at **any** increment — no migration paths, no version bridging, no reset choreography. Rationale: versioning formats while the data structures are still churning buys migration debt, not safety. **Serialization is solved holistically ONCE, late** — at the Increment-7 entry gate, after 0001 (identity), 0003/0008 (job/squad shapes), and the FSM (Inc 6) have stabilized what actually gets persisted. Until then the standing requirement is only the §1 non-negotiable: a decode failure or schema change is a **loud, logged reset**, never a silent empty world. (This rescopes Inc 2 and Inc 5 — see their rows.)

## 1. North-Star & Objective Function
- **Win conditions:** survive · expand · score. Ibex is a competently-designed, feature-complete economic bot, but it is not yet top-tier: it cannot reliably convert an economic lead into territory, expansion is route-blind, and — decisively — **it can permanently die to its own CPU use** (review §Exec, Field Report C). The rewrite optimizes for converting "viable on a single quiet colony" into "viable under sustained multi-colony war."
- **"Colony health" score** (review §9 — *the* objective function the rewrite optimizes and self-improvement diffs against). Four weighted terms, **survival-dominating**, fixed-weight & per-scenario-normalized so the score is reproducible and regression-diffable:
  1. **Survival** (gate + score) — avoided extinction: no death-spiral, no unrecoverable deser failure, spawn alive. A spiral run scores ~0 here **regardless of every other term**.
  2. **CPU headroom** — mean/p95 of `(tick_limit − cpu_used)/tick_limit` **including intent cost**, plus long-tick rate.
  3. **Economic growth** — slope of GCL + stored energy + RCL-progress.
  4. **Military win-rate** — rooms held vs lost, **squad cohesion rate** (fraction of combat ticks with all members in-range — directly measures Field Report A), targets killed vs waves spent.
- **Non-negotiables (hard invariants every increment must preserve):**
  - **Never die to our own CPU** — bucket-aware load-shedding must keep the colony making progress under pressure; the MIN_PATHFIND_OPS floor stays so creeps never fully freeze (IBEX-003/016).
  - **Never silently lose state** — deserialization failure and segment overflow must become *loud, deterministic* events (log-and-reset, watermark), never a spontaneous empty world (IBEX-004/014). Under the §0 reset-anytime policy, state drops themselves are free at any increment — **loudness is the entire requirement**: every reset has a logged cause.
  - **Never break the running bot mid-increment** — strangler-fig: one stable seam per step, harness-verified before the next.

## 2. Architectural Pillars → ADRs
Target/decision column is the review §8 per-pillar recommendation (authoritative). Cross-ADR ordering for the foundational pillars: **0006 → 0004/0005 → 0002(stage 1) → 0001 → 0003 → 0002(stage 2)** (see §3). The second-pass deep-dives **0007/0008/0009** mostly *ride* the existing increments rather than adding their own — see the mapping note under §3 — with the genuinely-new multi-room + strategy work collected in **Increment 7**. (Companion analysis: [`../design/competitive-analysis-overmind.md`](../design/competitive-analysis-overmind.md); small-bug backlog: [`proposed-fixes.md`](proposed-fixes.md).)

| Pillar | Current → Pain | Target (decision) | ADR |
|---|---|---|---|
| Entity model | specs/ECS recyclable Entity indices used as durable cross-refs → repair tax (E) + raw-u32 squad aliasing (D/A) | Stable-game-ID arena/store (RoomName / ObjectId / minted SquadId), id→Entity rebuilt per tick; **delete `repair_entity_integrity`**. Interim: generation-carrying handle. Squads migrate first (smallest, most broken). | [0001](../design/0001-entity-model.md) |
| Serialization / persistence | bincode positional + no version header → silent breakage on schema drift (D); seg-55 collision wipes the cost matrix | Segment safety (disjoint `COMPONENT_SEGMENTS`/`COST_MATRIX_SEGMENT` — **done, Phase 0**) + loud-reset-on-decode-failure early; **the versioned/tagged format lands ONCE, holistically, at the Inc-7 entry** after the data structures stabilize (§0 reset-anytime policy, 2026-06-10 — supersedes the original two-stage staging to avoid migration debt). | [0002](../design/0002-serialization.md) |
| Behavior modeling | `screeps-machine` FSM → split-pass side effects, unguarded combat intents, opaque `Option` control flow (F); one `Err` tears down a mission | Data-driven FSM behind the unchanged Job/Mission trait seam; **all** intents (incl. combat) through one guarded sink; reservations computed once/tick; transient-error tolerance. Utility AI for SELECTION only. Pilot on `HaulJob` with replay intent-diff parity. | [0003](../design/0003-behavior-modeling.md) |
| Squad cohesion | N independent `move_to(range 0)` toward a soft-gated virtual anchor; defense unwired; rover `Follow` unused (A) | **First** wire `SquadDefenseMission` onto `SquadContext` (the dominant break). Then lead-follower + **hard in-range wait-gates** (rover `Follow`/`desired_offset`); cohesion as an invariant (non-cohesive >N ticks → force-abort, also closes the lifecycle hang); retire orphaned squad missions. Behavioral-only. | [0003](../design/0003-behavior-modeling.md) |
| CPU governance / load-shedding | none (Field Report C) → death-spiral; leaks bypass local budgets; ephemeral caches wiped on reset | **Global `CpuGovernor`** (Normal/Conserve/Critical from bucket + trend) read at the top of every expensive system + **one budgeted pathfinding facade** sharing a per-tick ops pool (generalize, don't replace, the rover budget). Persist/warm `RoomRouteCache`; tiered shedding (war recompute, expansion, visuals shed first; pathfinding floor kept). **Land early — None-breaking, survival-first.** | [0004](../design/0004-cpu-governance-and-load-shedding.md) |
| Runtime / scheduling | specs dispatch (fixed order); zero panic isolation under `panic="abort"` | Explicit **ordered/priority scheduler** (NOT a cooperative async executor) + **tick-level `catch_unwind` containment** so `serialize_world` always runs + narrow risky boundaries to `Result`/log-and-continue (transfer target enums, visual flush). Resumable work only where a concrete need exists. A runtime-model change off specs rides on 0001 and is deferred. | [0005](../design/0005-runtime-and-scheduling-model.md) |
| Eval & iteration | manual iteration, zero tests (IBEX-023), observability coupled to the debug flag | Rust harness around a Dockerized screeps-launcher private server (**delivered in Phase 0**: server-kit/ibex-eval/rest-api; deploy via `screeps-pack` — npm-free, parity-gated, the `js_tools/deploy.js` interim is superseded) + an **always-on, versioned metrics segment** decoupled from visualization + pure-logic kernel tests; the colony-health score (§1) + pre-deploy gates. **Land first — the verification substrate for every later increment.** | [0006](../design/0006-eval-and-iteration-harness.md) |
| Hauling & logistics | greedy per-hauler matcher, ungated re-match, commitment churn (IBEX-011/030); Manhattan hauler sizing | **Keep** greedy+reservation (do NOT adopt CPU-spiky stable matching); two-phase snapshot-then-assign behind a frozen `select_pickup_and_delivery(snapshot, creep)` seam; bucket-gated re-match cadence; committed-delivery guard (confirm-then-consume); route-distance hauler sizing + shared predicted capacity. | [0007](../design/0007-hauling-logistics.md) |
| Combat & squad architecture | lifecycle smeared across 4 missions (1 ok, 1 broken, 2 dead); renew-as-glue prevents teardown (IBEX-002) | Objective-driven missions + a **generic Squad Manager** owning lifecycle (compose→roster, spawn-request generation, **pre-spawn-over-renew**, cohesion-invariant force-abort, retirement); spawn stays a dumb fulfiller; DELETE SquadAssault/Harass, MERGE Attack+SquadDefense onto one model. | [0008](../design/0008-combat-and-squad-architecture.md) |
| Room planning & multi-room | strong per-room foreman; restart-thrash (IBEX-037); **no multi-room layout**; route-blind expansion (IBEX-032) | Harden (restart backoff) + bench-tune scoring weights; add a `RoomName`-keyed `RoomGraph` (generalize the gather BFS, fixing IBEX-036/032) + an `InterRoomRoadLayer` for base↔remote roads; offline foreman bench as a pre-deploy plan-quality gate. **Already landed ahead of plan:** the D2 upgrade-area work-slot optimization (foreman `4107412`) and the shared `room_scoring` module (P0.P7) that Inc 7's `claim.rs` rework consumes. | [0009](../design/0009-room-planning-and-multiroom-layout.md) |

## 3. Increment Plan (strangler-fig)
Each increment: replaces one thing, behind a **stable seam**, harness-validated before the next. Order is exactly the review §8 *Sequencing* paragraph: substrate → survival-critical governor/containment → robustness (versioned format) → identity → cohesion/lifecycle → format swap + repair-pass deletion → behavior model.

| # | Increment | Replaces / adds | Stable seam | Validation | Breaking? | Status |
|---|---|---|---|---|---|---|
| 0 | **Test, telemetry & eval substrate** (IBEX-023) | Rust eval harness (Dockerized screeps-launcher; deploy via `screeps-pack`), always-on versioned metrics segment (seg 57 — reserved in `segments.rs`, not yet emitted), first pure-logic kernel tests (create_body, classify_threat, formation geometry, encode/decode round-trip, spawn-ordering descending invariant) | game-API boundary / `GameView` trait; `serialize_world`/`deserialize_world` | pure-logic unit tests green + **local-server smoke-run** (deploy → run K ticks → metrics captured) | **None** | **DONE (Phase 0 + Phase 1)** — Phase 0: harness/deploy/capture/smoke/BASELINE-0+1/host lane. Phase 1 (exit audit [`phase-1.md`](../execution/phase-1.md) §2.7): seg-57 versioned metrics every tick, scenario configs + fault injection (CPU burner / state-preserving reset / panic probe), colony-health score + `compare` differ, `GameView` skeleton, F3 arbiter double, kernel count 33→51 (grows by the kernel-vs-shell rule; formation geometry awaits its Inc-4 extraction). Seeded N=9 gate aggregation → Phase 2 |
| 1 | **CPU governor + containment + scheduler seam** (IBEX-003/016, 025, 029, 021) | global `CpuGovernor` (Normal/Conserve/Critical); single budgeted pathfinding facade (findnearest/structure_data/RoomRouteCache/find_route share the rover ops pool); tick-level containment so serialize always runs (JS boundary vs `panic=unwind` — decided in-phase); **intent differ + one guarded intent sink incl. combat's bare sites (IBEX-029** — reconciled here from 0003's "alongside Inc 6": the differ is built at Inc 1 per 0015 and the sink is its first consumer**)**; raise war cadences off 1; **priority-scheduler seam at parity** (shadow-dispatch diff); riders: 0007 two-phase snapshot + governor-gated re-match, 0009 D1 restart backoff + gather budget (IBEX-037/036), 0011 executor quick-wins, 0013 P0 bank-feasibility remainder, IBEX-008 visual guard, IBEX-015 stuck-recovery wiring | scheduler hook + pathfinding facade + intent sink; tick-level abort boundary | harness induces CPU pressure + a forced reset with active war: **progress continues, no restart-loop, serialize never skipped**; shadow-dispatch parity vs old ordering | **None** (survival-first; a `panic=unwind` profile, if chosen, is a build-config change not a format break) | **CORE DONE (Phase 1 — M1 reached; exit audit [`phase-1.md`](../execution/phase-1.md) §2.7)** — governor + tier-scaled ops pool (`find_route` admission-controlled), JS-boundary containment (decision recorded; `env.tick` after serialize; live panic acceptance: vm_starts 1→2, counters loud, colony alive), guarded sink incl. all 23 combat sites (IBEX-029) + order-sensitive intent digest, `StageClass` scheduler seam (parity by construction), war cadences 2/10/50 (IBEX-021), riders D3 (backoff IBEX-037 + gather budget IBEX-036) / D4 (renew) / D5 (bank window) / D6-as-telemetry / C6 visual guard (IBEX-008). Quick-win subset was Phase 0 (IBEX-010/043/044/046; 009/019/020; 045/050 obsolete). **Deferred → Phase 2:** 0007 two-phase snapshot + re-match (D1/D2), B5 cache-warm/TTL remainder; IBEX-015 behavioral recovery → Inc 6 |
| 2 | **Loud-failure persistence** (IBEX-004/014; **RESCOPED 2026-06-10** by the §0 reset-anytime policy) | ~~version header + reject-and-reset + round-trip/old-snapshot/fuzz~~ → **deferred to the Inc-7-entry holistic serialization pass** (versioning a still-churning format = migration debt). Remaining scope, small: **any decode failure → loud logged reset** (replacing the silent decode→empty and deser-error→continue paths); overflow watermark routed to seg-57 + fail-loud; ~~`CreepRoverData.path` `#[serde(skip)]` (IBEX-049)~~ **declined 2026-06-12** (post-reset repath storm; see [proposed-fixes](proposed-fixes.md)) | frozen `serialize_world`/`deserialize_world` seam | forced reset reloads a non-empty cost matrix; induced decode failure produces ONE loud reset event, never a silent empty world; overflow emits a loud error not a silent drop | **State reset** (free under the reset-anytime policy) | **interim DONE (Phase 0 D1)** — `segments.rs` registry with whole-table compile-time uniqueness, `COMPONENT_SEGMENTS` shrunk to 50–54, seg-55 wipe fixed and live-proven, watermark log. Remainder is small enough to ride Phase 1/2 as tasks |
| 3 | **Stable-ID SquadStore** (IBEX-002b, 005-prep) | A1 generation-carrying handle (interim) → A2 `SquadStore` keyed by minted `SquadId`; dangling-ref counter emitting | typed handle / `SquadStore` lookup (id→Entity rebuilt per tick) | round-trip: serialize squad+members, deserialize, assert **same logical squad**; recycle an index, assert **no foreign attach**; dangling-ref counter at zero | **Memory/format** (interim handle is Behavioral) | unstarted (MEDIUM confidence — design settled) |
| 4 | **Squad cohesion + lifecycle** (IBEX-001, 002) | wire `SquadDefenseMission` onto `SquadContext` (dominant fix first) → lead-follower **hard in-range wait-gates** → cohesion-as-invariant force-abort → **retire orphaned squad missions** (SquadAssault/Harass dead code) | `SquadContext`/`new_with_squad`; rover `Follow`/`desired_offset` | scenario engagement: **cohesion metric** (member spread, ticks-in-Loose) above threshold; non-cohesive squad force-aborts within N ticks; no idle defense missions linger after threat clears | **Behavioral** | **orphan deletion DONE early** (Phase 0 C1: SquadAssault/Harass ~950 lines removed — was slotted here/Inc 5); rest unstarted (MEDIUM) |
| 5 | **Stable-ID ownership + repair-pass deletion** (0001 A3, 005, 002b finalize; **RESCOPED 2026-06-10** — the format swap moved to the Inc-7 entry) | migrate **mission/operation ownership to stable ids**; **delete `repair_entity_integrity`** (A3); shape changes reset state freely (§0 policy) | unchanged `serialize_world`/`deserialize_world` seam; 0001 store landed | a missing ref is a handled lookup-miss (no `ConvertSaveload` panic); repair pass removed with the integrity invariant still holding | **State reset** (free) | unstarted (MEDIUM) |
| 6 | **Data-driven FSM** (IBEX-006/042) | data-driven FSM behind the Job trait, **piloted on `HaulJob`** then rolled out job-by-job (the guarded intent sink itself lands at Inc 1 with the differ; Inc 6 consumes it for parity); **transient-error tolerance** (`MissionResult::Wait` vs abort) + 0007 committed-delivery guard on the HaulJob pilot | unchanged Job/Mission trait | **replay intent-diff parity** on HaulJob (old vs new produce the same intents); transient room/visibility loss no longer tears down a long-running mission | **Behavioral** | unstarted (MEDIUM; IBEX-006 friction live-corroborated in BASELINE-0) |
| 7 | **Multi-room & capability layer** (IBEX-032/018/027; gap G3) | **ENTRY GATE — holistic serialization pass** (absorbs the deferred Inc-2 Stage-1 + old Inc-5 Stage-2 scope, per the §0 reset-anytime policy): the data structures are now stable (post 0001/0003/0008/FSM), so design and land the final versioned/tagged format ONCE — version header, round-trip + snapshot corpus + fuzz suite — so `RoomGraph` and all later persistent state land on the final format with no migration debt. Then: `RoomName`-keyed `RoomGraph` (generalizes the gather BFS) + route-distance expansion gating; `InterRoomRoadLayer` (base↔remote roads); hauling route-distance sizing + shared predicted capacity; combat Squad-Manager polish (boost pipeline wire per 0010); market trailing-window trust (0012 M2); **third-pass additions per gap-analysis §3:** SK-farming gate lift (G-8), defense-posture extensions (G-10 RampartStation + anti-quad tower kernel), stronghold loot economics (G-3), per-source franchise P&L (G-17), config whitelist (G-11), portal graph-flag (G-16), terminal empire-balancer slice (G-6), CPU-per-room attribution (0014) | `RoomGraph` resource; `InterRoomRoadLayer` above the per-room seam; the frozen matcher snapshot seam | harness: multi-room planning sheds first under pressure with no headroom regression; route-distance rejects unreachable claims; bench plan-quality gate | **Memory/format** (RoomGraph) + Behavioral | **LOW confidence — re-plan at gate.** Substrate already landed early: 0009 D2 work-slot optimization (foreman `4107412`) + shared `room_scoring` module (P0.P7) with the `claim.rs` consumption spec'd |
| 8 | **End-game engines** (gap-analysis §3; converting a stable bot into a compounding one) | **Power creeps + operator powers** (ADR 0013 — the anchor: OperatorSystem, ops economy, enableRoom policy); **GCL consumer wire** (boosted upgraders consuming 0010 Tier B — highest ROI per line in the corpus) + energy-routing policy; **empire intel & player profiling** (G-1) + observer sweep; **offensive nuke doctrine** (G-4); **Recovery posture** (G-12); full `EmpireAllocator` + intel consumption (ADR 0014) | `OperatorSystem`; `PlayerProfile` store; the 0014 posture resource (read-only since Inc 4–5) | gate: M7 reached + colony-health stable across ≥2 colonies; spawn-utilization % proves the OPERATE_SPAWN multiplier empirically; GPL state seeding in the harness (0013/0006) | Behavioral + additive Memory/format (profiles, operator state) | unstarted — **LOW confidence, directional only; re-plan at gate** |
| 9 | **Dominance extensions** (evidence-gated menu, not a queue) | GCL temple + unclaim/reclaim cycling (G-7c); highway deposits + leveled-commodity revisit once 0013 matures (G-9); automated campaign planner (G-18 capstone); pixels/CPU treasury (G-15, MMO-only, flag-gated) | existing seams (0008 objectives, 0010 factory, 0014 allocator) | each item enters only if seg-57/score evidence shows the preceding engines saturated | Behavioral, flag-gated | unstarted — **LOW confidence by design: a menu, not a queue; no planning investment until evidence gates open** |

> Ordering rationale: **substrate first** (Inc 0 — so every later step is verifiable), then the **extinction-class governor + containment** (Inc 1 — Field Report C/IBEX-025, the only failure that *permanently* kills the colony), then **make persistence loud-not-silent** (Inc 2, rescoped small: loud-reset semantics only), then **identity** (Inc 3 — unblocks both cohesion and the repair-pass deletion), then **cohesion/lifecycle** (Inc 4 — Field Reports A/B, gated on the stable store + cohesion metric), then **stable-ID ownership + repair-pass deletion** (Inc 5 — gated on the 0001 store), then the **behavior model** (Inc 6 — gated on replay-parity infra), and only then — with the data structures finally stable — the **one holistic serialization pass** at the Inc-7 entry (§0 reset-anytime policy). The renderer (Field Report H) is a debug-only quick-win, slotted opportunistically once tick-level containment (Inc 1) is in.

> **Second-pass ADR mapping (0007/0008/0009).** These mostly ride existing increments rather than adding their own. **0007 (hauling):** quick-wins (IBEX-010/050) + two-phase snapshot + bucket-gated re-match → **Inc 1**; committed-delivery guard → **Inc 6** (HaulJob FSM pilot); route-distance sizing + predicted capacity → **Inc 7**. **0009 (room planning):** restart backoff (IBEX-037) + gather-BFS budget (IBEX-036) → **Inc 1**; offline bench weight-calibration is host-only, anytime after **Inc 0**; `RoomGraph` + inter-room roads → **Inc 7**. **0008 (combat/squad):** generic Squad Manager + SquadDefense merge → **Inc 4** (gated on 0001's SquadStore at Inc 3 and 0003's cohesion movement at Inc 4); `WarOperation`-as-supervisor + orphan-variant deletion (SquadAssault/Harass) → **Inc 5**. **Increment 7** collects the genuinely-new multi-room + strategy capability work after the structural rewrite is stable. The [proposed-fixes backlog](proposed-fixes.md) enumerated the Inc-1 quick-wins concretely — **its Group A (everything except IBEX-029, which needs the Inc-1 intent differ) landed in Phase 0**; IBEX-029 and IBEX-021 landed in Phase 1; **the backlog CLOSED 2026-06-12**: IBEX-018 interim (ADR 0012 M0+M1 — FairValue oracle + exposure caps + cost ceilings on the existing ordersystem; the full TradePlanner/risk-ledger stays Inc 7), IBEX-040 (rover shove-walkability via the cached structure cost layer), and IBEX-048 (depleted-mineral idle-suspend/recreate) all landed; IBEX-049 was **declined by operator decision** (dropping serialized paths concentrates fleet-wide repathing onto the post-reset tick — `needs_path` repathing is deliberately budget-exempt; revisit only with staggered post-reset repathing at the Inc-7 serialization pass).
>
> **Third-pass ADR mapping (0010–0014 + world-class gap analysis).** **0010 (boost/lab/factory):** L0 kernels → **Inc 0+**; defense-boost slice L1 → **Inc 4** (with the Squad Manager steps); empire ReagentPlanner + offense gating L2, market integration L3, FactoryMission L4 → **Inc 7**. **0011 (spawn orchestration):** executor quick-wins → **Inc 1**; demand seam + group spawning + combat pre-spawn → **Inc 4**; economic producers → **Inc 5–6**; full placement/assist/incubation → **Inc 7**. **0012 (market/risk):** kernels → **Inc 0**; IBEX-018 interim M1 → **Inc 1–2**; full planner M2 + risk ledger M3 (seg 58) → **Inc 7** (buy flags gated on the M4 adversary scenario). **0013 (power):** accrual fixes (IBEX-043, duo math, decay window, surplus-gated processing) ride **Inc 1–7**; the OperatorSystem anchors **Inc 8**. **0014 (empire strategy):** allocator kernels → **Inc 0+**; thin read-only posture resource → **Inc 4–5**; `war.rs` declarer→executor split → **Inc 5** (rides 0008 Step 3); CPU attribution → **Inc 7**; full allocator + intel → **Inc 8**. Increments **8/9** below are seeded from [world-class-gap-analysis §3](../design/world-class-gap-analysis.md); its §5 residual-gaps register lists the still-open items (harness scenarios/state-seeding, MMO ops runbook, nuke evacuation, link/queen doctrine, body-layout kernel).

### 3.1 Standing per-increment testing obligations (folded from [component-test-plans.md](component-test-plans.md) per ADR 0015)

Every increment, in addition to its row's validation column: (1) a new seam lands **with its contract file** (draft until the owning increment freezes it) — S-registry in 0015; (2) host (`cargo test`) **and** wasm (`cargo clippy-wasm`) both green; (3) kernel tests accompany extracted pure logic (kernel-vs-shell rule: strategy tweaks need NO new tests); (4) increment gates run in gate mode (N=9 seeded runs, may gate immediately); (5) new metrics fields version the seg-57 schema. Per-increment testing infrastructure (from the consolidated roadmap table): Inc 0 → F1 host lane ✅/F8 harness ✅/F3 MemoryArbiter double/F9 scenario format/F13 seg-57/F14 scorer+differ + tranche-1 kernels; Inc 1 → F4 intent differ + shadow-dispatch + F10 CPU-pressure inducer; Inc 2 → loud-reset event assertions (F5 corpus runner + F6 decode fuzzer move with the holistic serialization pass to the Inc-7 entry, still consuming F3); Inc 3 → F7 snapshot fixtures + identity round-trips; Inc 4–5 → S7/S9/S11/S14 contracts + F4 recorder proper; Inc 6 → HaulJob byte-parity replay; Inc 7+ → S10/S15 contracts, S6 extension.

## 4. Milestones & Sequencing
- **M0 — Verifiable (end of Inc 0):** harness can deploy + advance ticks + capture the metrics segment; colony-health score computable; baseline recorded under `runs/` keyed by (scenario, git SHA). **Status 2026-06-11: REACHED** (Phase 1) — seg-57 captured every run, score computed + `compare` differ automated, **BASELINE-2** recorded (`runs/smoke-8086a68-…`, 2016 ticks, health 0.5640 — the first scored baseline). Seeded gate aggregation carried to Phase 2.
- **M1 — Survivable (end of Inc 1):** under induced CPU pressure + forced reset the bot makes progress and never restart-loops; no tick ever skips `serialize_world`; the extinction-class risk is closed. *This is the table-stakes milestone — the bot is competitively viable only after it.* **Phase 1 exits here.** **Status 2026-06-11: REACHED** ([`phase-1.md`](../execution/phase-1.md) §2.7) — pressure, reset-under-pressure (reset mid-burn, vm_starts 1→2, growth continued) and panic-containment acceptance runs green; the "with active war" variant honestly awaits the Inc-4 combat scenarios.
- **M2 — Loud (end of Inc 2, rescoped):** decode failures and overflow are loud, deterministic events — every reset has a logged cause; no spontaneous empty-world resets. (Full durability — the versioned format that *survives* schema drift — arrives with the holistic serialization pass at the Inc-7 entry, per the §0 reset-anytime policy.)
- **M3 — Identifiable (end of Inc 3):** every squad ref resolves the same logical squad across reset/recycle; dangling-ref counter at zero.
- **M4 — Coordinated (end of Inc 4):** defense and offense squads hold formation (cohesion rate above threshold); stuck/finished campaigns tear down on a deadline. Field Reports A/B closed.
- **M5 — Clean (end of Inc 5):** id-keyed ownership; `repair_entity_integrity` deleted. (The tagged format moved to the Inc-7 entry.)
- **M6 — Evolvable (end of Inc 6):** data-driven FSM proven at parity on HaulJob and rolling out; transient errors tolerated. Field Report F closed.
- **M7 — Expansive (end of Inc 7):** opened by the **holistic serialization pass** (versioned tagged format + corpus + fuzz on the now-stable structures — durability finally complete); then route-aware expansion + planned base↔remote roads (multi-room layout); haulers sized by route distance; boost pipeline + market hardening landed; (optionally) incubation. The bot can convert an economic lead into territory — the competitive verdict's stated weakness closed.
- **M8 — Compounding (end of Inc 8):** operators running (`OPERATE_SPAWN` multiplier empirically proven via spawn-utilization %); boosted upgraders doubling GCL at the cap; player-level intel driving war/peace through the 0014 posture engine; nuke doctrine + Recovery posture live. The dominance flywheel (squads → power → throughput → economy → squads) is spinning.
- **M9 — Dominant (Inc 9 menu, evidence-gated):** temple GCL cycling, deposits/commodities, the automated campaign planner — entered only as seg-57 evidence shows the preceding engines saturated.

## 5. Per-Increment Validation Strategy
The full testing program now lives in **[ADR 0015](../design/0015-testing-and-validation-strategy.md)** (taxonomy L0–L6, seam-contract registry, fast-iteration policy) and **[component-test-plans.md](component-test-plans.md)** (per-component plans + the consolidated testing-roadmap table — fold that table into §3 at roadmap time). Increment gates run in **"increment gate mode"** (N=9 on-demand seeded runs that may gate immediately); the earned-gating soak rule applies only to the nightly lane. The three checks below remain the spine; 0015 absorbs and extends them.

Every increment is gated by the **same three harness checks** before the next begins (review §9 pre-deploy gates + §8 sequencing):
- **Harness smoke-run** — deploy → run K ticks on the economy-bringup scenario with **zero deser failures, zero caught panics, zero segment-overflow drops, CPU under `tick_limit`, no death-spiral alarm**; colony-health score not regressed below the (scenario, git SHA) baseline beyond threshold.
- **Serialization round-trip** — encode→decode identity on the touched components (the encode/decode helpers are pure and a `MemoryArbiter` double makes the pipeline testable today). The old-snapshot corpus and fuzz suite are built with the Inc-7-entry holistic serialization pass (§0 reset-anytime policy), not before.
- **Replay parity** — record real `GameView` reads, replay old-vs-new behind a flag, diff the emitted intents (the gate for the Inc 6 FSM pilot before cutover). **The Inc-1 scheduler-seam gate does NOT ride record/replay** — it is in-process **shadow-dispatch** (old + new schedulers over the same live tick, diffed through shadow intent sinks) per ADR 0015, since `GameView` is only a skeleton at Inc 1.

**Per-increment focus:** Inc 0 stands the substrate up (smoke-run is the deliverable). Inc 1 adds CPU-pressure + forced-reset replay. Inc 2 adds the loud-reset event assertion. Inc 3 adds the squad-identity round-trip + index-recycle test. Inc 4 adds the cohesion-metric scenario assertion. Inc 6 adds HaulJob intent-diff parity. The Inc-7 entry adds the full round-trip/snapshot-corpus/fuzz suite with the holistic serialization pass.

**Resets** (§0 reset-anytime policy, 2026-06-10): any increment may drop serialized state — the old two-sanctioned-resets choreography is retired. Practical courtesy stands: prefer a quiet window when an active colony matters, and every reset must be **loud** (logged cause). Migration/back-compat machinery is deliberately absent until the Inc-7-entry holistic serialization pass.

## 6. Risks & Rollback
Lead with the two survival-class risks; each step is independently reversible because it sits behind a stable seam (revert the seam, keep the running bot).

| Risk | Increment | Mitigation | Rollback trigger |
|---|---|---|---|
| **CPU pathfinding death-spiral → colony collapse (extinction-class)** (IBEX-003/016) | 1 | Global `CpuGovernor` + single budgeted pathfinding facade + graceful degradation + MIN_PATHFIND_OPS floor; persist/warm route cache; raise war cadences; always-on bucket-trend telemetry | Harness death-spiral alarm fires (negative bucket trend over the window) or CPU exceeds `tick_limit` on the pressure scenario → revert the governor/facade behind its scheduler-hook seam; keep the existing per-system local budgets |
| **Cost-matrix seg-55 wiped every tick → full rebuild on the worst (post-reset) tick** (IBEX-013) | 2 | Move cost matrix to a dedicated segment; **compile-time disjointness assert**; interim non-breaking shrink of `COMPONENT_SEGMENTS` to 50–54 | Forced-reset test reloads an empty seg-55, or the disjointness assert can't be satisfied → fall back to the interim shrink (no format change) and re-stage |
| Deserialization failure → silent full state loss | 2 (rescoped) | **Loud log-and-reset on any decode failure** (resets are free under the §0 policy — silence is the only enemy); overflow watermark telemetry; the versioned format + corpus/fuzz arrive at the Inc-7 entry | Any *silent* empty-world event in a smoke-run (reset without a logged cause) → treat as a release blocker; the loud-reset path is tiny and stays |
| Reachable hot-path panic aborts tick + skips serialize (IBEX-010/025) | 1 | Nuker-withdraw `panic!`→`Err` + one-shot log; **tick-level `catch_unwind` boundary** so serialize always runs; panic-counter telemetry | Nonzero caught-panic count persists after the boundary lands → narrow the offending boundary to `Result`; the `catch_unwind` itself is the backstop and stays |
| Raw-u32 squad ref aliasing → wrong/None squad state (IBEX-002b) | 3 | Generation-carrying handle (interim) → stable `SquadId` `SquadStore`; round-trip + index-recycle tests | Round-trip resolves a foreign squad, or dangling-ref counter nonzero → stay on the generation-carrying interim handle (Behavioral, no format commitment) until the store is proven |
| War/squad cohesion broken → war system unusable (IBEX-001) | 4 | Wire `SquadDefense` onto `SquadContext` first; lead-follower hard wait-gates; cohesion-as-invariant; cohesion telemetry | Cohesion rate below threshold on the engagement scenario → keep the SquadDefense wiring (already a net win) and revert the lead-follower model to the existing virtual-anchor mover |
| Operation/mission lifecycle hang (IBEX-002) | 4 | Per-state wall-clock deadlines; stop renewing non-cohesive squads; `WarOperation` supervisor; `Defending→Cleanup` when no hostiles | Harness stuck-operation watchdog fires (campaign neither progresses nor tears down) → ship the interim per-attack created-tick age abort while the supervisor is re-staged |
| Renderer per-target visual-limit throw → skipped serialize (IBEX-008) | 1 (containment) / opportunistic | `get_visual_size` watermark + drop-with-telemetry; finite-coord clamp; global overlay once to None; tick-level containment (Inc 1) | Renderer-on smoke-run aborts a tick → disable the renderer feature flag; containment from Inc 1 already prevents the serialize skip |
| Format swap regresses persistence (IBEX-004 stage 2) | 7 entry (was 5; §0 rescope) | The holistic pass rides the frozen seam after ALL structure churn (0001/0003/0008/FSM) is done; full round-trip/snapshot-corpus/fuzz built with it | Round-trip/fuzz red on the new body → stay on loud-reset bincode (safe, just not durable across schema drift); defer the swap further — nothing downstream of Inc 7 hard-requires it except long-lived persistent additions like `RoomGraph` |

## 7. Delivered-since-corpus changelog

What landed between the corpus commit (`c9fea19`, 2026-06-09) and this update — the ledger behind the §3 Status cells:

| When | Commits | Delivered |
|---|---|---|
| Phase 0 main | `5125f21`…`9631866` (plan, A1–A14, B/C/D waves, baselines) | Harness stack (server-kit + ibex-eval split, rest-api shared client, prospector, screeps-pack npm-free deploy superseding `js_tools/deploy.js`); host test lane + ~33 kernel/pin tests; orphan squad missions deleted (~950 L); proposed-fixes **Group A** (IBEX-010/043/044/045/050/009/019/020/046 — 045/050 resolved-as-obsolete after engine-source verification); `segments.rs` registry — **seg-55 cost-matrix wipe (Critical IBEX-013) fixed and live-proven**; multi-bot identity; BASELINE-0 → BASELINE-1 (CPU max 55.01→28.79, zero panics/deser both) |
| Phase 0 close | `eea5956`, `4cc3a5c` | P0.P7 shared `screeps_foreman::room_scoring` (foreman `3e4a937`) — the Inc-7 `claim.rs` substrate; pack sibling-convention defaults |
| Post-phase live validation | `b005afb` | Controller-container classification range 2→3 + anchored upgrader pickup (two pre-existing live bugs; +6 pin tests; live-verified) |
| MMO tooling | `cf9e8b6`, `0452c66`, `4a68520` | Per-endpoint quota modeling → evidence-based pacing + `--no-rate-limit`; prospector `--shard` enforcement/validation + name-claimability gating + novice policy (operator respawn flow unblocked) |
| ADR 0009 D2 | foreman `4107412` + superproject `8a3ae39`, `99603dd` | **Upgrade-area work-slot optimization implemented** (joint container+link scoring, ranked search-tree candidates, slot exclusion, road penalty); ibex consumes the foreman placement-contract constants; bench-verified zero plan regressions (the hidden-E11N25 lesson produced the standing plan-loss-accounting gate) |
| Phase 1 main (2026-06-10/11) | `93a4a2d`…`8086a68` (P1.A1–A7, B1–B6, C1–C6, D3–D6; per-task commits) | **M0+M1 reached.** Seg-57 versioned metrics (`screeps-ibex-metrics` schema crate, writer/reader shared); scenario configs v1 + fault injection (CPU burner, state-preserving reset, panic probe → kit `ConsoleInjection`s); colony-health score (survival-gated) + `score.json` + `compare` differ; `CpuGovernor` (Normal/Conserve/Critical on bucket+trend, pure kernel) + tier-scaled mission ops pool (`pathbudget.rs`; findnearest/spawn-distances/`find_route` charged — IBEX-035/039) + route-cache stale-serve; **JS-boundary containment** (loader catch → `Memory._metrics.aborted_ticks`; `env.tick` advances only after serialize; live panic acceptance) ; guarded intent sink — all 23 bare combat sites (IBEX-029) — + order-sensitive intent digest (the C5 parity instrument); `StageClass` scheduler seam with governor-driven Critical shedding; war cadences 2/10/50 (IBEX-021); planner backoff (IBEX-037) + gather budget (IBEX-036) + loud seg-60; engine-true renew math + renew-behind-priority; power-bank feasibility window + 20×ATTACK cap; visual finite-clamp + flush cap (IBEX-008); `memory_helper::path_set` intermediate-creation fix (real bug found live); kernel count 33→51; **all builds warning-free** (standing bar); **BASELINE-2** (health 0.5640) |
| Statics→Resources migration (2026-06-11) | `93a0e35`…`bbe86e0` (statics-review M0–M6; per-slice commits) | **Every mutable static in the bot crate migrated to specs Resources** per [`../reviews/statics-and-pathfinding-review.md`](../reviews/statics-and-pathfinding-review.md) (operator pulled the Phase-2 slices forward): `GovernorSnapshot` + `PathfinderService` (the single pathfinding instance owning the tier-scaled ops pool, nearest-by-path selection, and the absorbed route cache — `pathbudget.rs` and the pathfinding half of `findnearest.rs` deleted), `IntentRecorder` (shadow-dispatch now diffs two instances directly), fault counters onto `MetricsState`, `Features`, `BotIdentity`; dead surface deleted (~280 LOC: 10 findnearest fns, 2 visualize methods, `RoomDisposition::name`, 3 recorder fns, `bucket_trend`). Remaining statics = the EP-1.1 sanctioned set only. Encodes EP-1.1–1.7 (`docs/guides/engineering-practices.md`). M7 (PathEngine replay seam + cache persistence) stays Inc-6/7 gated |
