# Phase 0 Execution Plan — Baseline Tooling, Cleanup & Critical Fixes

- **Status:** Active (drives implementation)
- **Date:** 2026-06-09
- **Precedes:** ADR-driven rewrite increments ([rewrite plan](../plans/rewrite-plan.md) Inc 0–9). Phase 0 ≈ a concrete slice of Increment 0 (harness + test substrate) + the safe Increment-1 quick-wins, executed **before** any ADR pillar work begins.
- **Related:** [ADR 0006](../design/0006-eval-and-iteration-harness.md) (harness design — authoritative for components/endpoints), [ADR 0015](../design/0015-testing-and-validation-strategy.md) (test taxonomy; Phase 0 stands up the L0 lane), [component-test-plans](../plans/component-test-plans.md) (F1–F19 fixture registry), [proposed-fixes](../plans/proposed-fixes.md) (Group A — the fix specs live there; this plan sequences them), [ADR 0008](../design/0008-combat-and-squad-architecture.md) (the verified dead-code inventory).

> **Purpose.** Establish a baseline of *capability* (run/deploy/observe the bot on a local private server with one command), *measurement* (a recorded baseline run + the first pinned kernel tests), and *hygiene* (supplanted code removed, the obviously-incorrect bugs fixed) — so every subsequent change is made against a measurable, reproducible baseline. **No ADR pillar implementation happens in Phase 0.**

---

## 0. Ground rules

- **Read-only for design:** ADRs are not re-decided here. Where a Phase-0 task touches an ADR's territory, the ADR governs and is cited.
- **Breaking changes:** Phase 0 contains exactly **one** sanctioned Memory/format break (P0.C1 — orphan mission-variant removal), absorbed by the next eval-server `bootstrap --reset`. Everything else is None/Behavioral. **MMO guard:** no MMO deploy between C1 landing and an explicitly accepted MMO state-drop window — on MMO, C1's discriminant shift would decode the whole mission/operation world to empty.
- **Secrets:** `.screeps.yaml` is already gitignored (`.gitignore:50`) with a tracked `.example-screeps.yaml` template. Phase 0 extends that discipline to the harness (§A7) — credentials never appear in logs, run artifacts, or committed files.
- **Verification baseline:** every fix lands with its pin test (§B3); the phase ends with a recorded before/after baseline comparison (§5).
- **No usage-specific bleed onto generic abstractions (operator directive, AGENTS.md §8):** bot-crate changes must not add caller-specific predicates/helpers to the `Job`/`Mission`/`Operation` traits or dispatch enums (anti-example: `is_power_bank_attack()` on the generic surface). Keep the data with its owner, use concrete-type accessors + variant matches at the strategy-owning caller, or a small opt-in capability trait. The D3 (IBEX-043) fix is the live test case — review it against this rule before commit.

---

## 1. Workstream A — `screeps-eval`: private-server execution & deployment harness

> **Post-A14 note:** the crate described below was split by P0.A14 into `screeps-server-kit` (generic mechanism) + `screeps-ibex-eval` (ibex policy). Task rows A1–A12 describe the work as it happened under the original name — accurate history; the living surfaces are the two new crates.

A Rust crate providing a **library** (consumed later by integration tests / `screeps-testkit`) and a **CLI** (`cargo run -- <cmd>`). **The CLI serves two users with equal priority: the automation harness AND the operator's manual iterative testing** — every automation primitive (server lifecycle, bootstrap, deploy, tick control) is exposed as a human-friendly command, so speeding up manual iteration is a first-class deliverable, not a by-product (P0.A8). Implements ADR 0006's components 1–7 in their Phase-0 form.

**Lifecycle decision (D-1, RESOLVED):** start **in-repo** (workspace-excluded), **extract to a submodule with its own remote once the crate stabilizes**. Design for that extraction from day one: self-contained README + example config, no dependence on workspace-internal crates or repo-relative paths beyond the documented config-discovery rule, independent dependency tree. The operator creates the remote at extraction time.

**Stack (standard, well-regarded crates):** `tokio` (async runtime), `bollard` (Docker API — connects to Docker Desktop via named pipe on Windows, `Docker::connect_with_local_defaults()`), `reqwest` (rustls; HTTP API), `tokio-tungstenite` (console websocket), `clap` (derive CLI), `serde`/`serde_yaml`/`serde_json`, `anyhow`/`thiserror`, `tracing` + `tracing-subscriber`, `secrecy` (secret wrapping, §A7). The Rust `screeps-api` crate is **evaluated** in P0.A4 (auth + console stream); if unmaintained/awkward, the thin `reqwest` client per ADR 0006's endpoint list is the fallback — the task is written to be resilient to either outcome.

| ID | Task | Detail | Done when |
|---|---|---|---|
| **P0.A1** | Crate scaffold | `screeps-eval/` at repo root, **workspace-excluded** (add to root `Cargo.toml` `exclude`, the `screeps-foreman-bench` precedent). Own **`screeps-eval/.cargo/config.toml`** pinning the host triple (the parent workspace config sets `wasm32-unknown-unknown`, and config discovery walks up from CWD — without an override, `cd screeps-eval && cargo run` would cross-compile; foreman-bench survives only via explicit `--target`). `src/lib.rs` (library: config, docker, server, api, capture modules) + `src/main.rs` (clap CLI). Depends on **no workspace wasm crate**. | `cd screeps-eval && cargo run -- --help` works host-native with no `--target` flag |
| **P0.A2** | Server lifecycle (bollard) | Manage the launcher stack **natively via bollard** (no compose dependency): create network + `mongo:8` + `redis:7` + `screepers/screeps-launcher` containers (volumes per `C:\code\screeps-launcher\docker-compose.yml`, which stays the reference). **Config is file-driven, no host env vars (operator directive):** the shared config module reads an **`eval:` section in `.screeps.yaml`** (gitignored; keyless example in `.example-screeps.yaml`) which either (a) points `serverConfig:` at an **existing launcher config** — e.g. the local `C:\code\screeps-launcher\config.yml` clone (local-only, not pushed), used as the base verbatim including its steamKey — or (b) supplies `steamKey:` directly to combine with the **vendored keyless template** (`screeps-eval/server/config.yml`, committed — **never copy the reference config verbatim; its first line is the live key**). Either way the harness **merges in-memory** (ensuring the **CLI bind host `0.0.0.0`** — verify the exact key name against the screeps-launcher repo; in-container localhost bind is the default failure) and writes the merged runtime config under `screeps-eval/target/runtime/` (gitignored via the global `target` rule; noted in A7's sweep) for the bind-mount. Publish **21025 (game/API)** and **21026 (server CLI)** per config, defaults overridable in `eval.ports`. **Image policy: pull `screepers/screeps-launcher` from the registry by default; building from the local clone is a recorded investigation item, not Phase-0 scope.** `status` **introspects running containers via bollard inspect** to discover the *actual* published ports/config (operator suggestion — works even against a manually-started stack). Documented fallback if the bind fights us: `docker exec <launcher> screeps-launcher cli`. Health-wait on `http://127.0.0.1:<game-port>/` with a **generous first-boot timeout (10 min+)** — the launcher runs an in-container npm install on first start. Commands: `server up\|down\|destroy\|status\|logs`. | `server up` from a cold Docker Desktop reaches healthy; the **host-side CLI connection succeeds**; `server destroy && server up` is repeatable; `status` reports discovered ports from a running stack |
| **P0.A3** | World bootstrap | Drive the **server CLI (21026)**: `system.resetAllData()`, `setPassword(user, pass)` (from config — §A7), `system.setTickDuration(ms)` (config; fast eval default), spawn placement (config coords or first-valid auto-pick). Command: `bootstrap [--reset]`. Opponent bots: **out of Phase 0** (harness scenario work, Inc 0 proper). | `bootstrap --reset` yields a running world with our spawn placed and creds matching `.screeps.yaml` |
| **P0.A4** | Deploy | **Shell out to `node js_tools/deploy.js --server private-server` — the permanent Phase-0 path (operator directive).** The deploy script and the wasm build pipeline (wasm-pack/rollup/npm toolchain) are **not modified**: a native upload would still require the JS build step, so going native buys nothing while risking the toolchain. deploy.js reads `.screeps.yaml` itself (verified: no credentials on argv/env). The harness wraps it: working-dir, error surfacing, build-mode flag. Command: `deploy [--debug]`. *(P0.A11's investigation returned **GO**; the rust-native `screeps-pack` prototype is **P0.A13, sequenced into the gauntlet wave** — `deploy` switches to the library path once parity is proven; deploy.js serves as the fallback ONLY during the parity window and the wrapper is deleted at cutover per A13.)* | `deploy` pushes the current build and the server console shows the new code tick over |
| **P0.A5** | Data capture | Console websocket subscription → `runs/<scenario>-<git-sha>-<stamp>/console.jsonl` (structured; severity-tagged lines pass through; **directory keyed by (scenario, git SHA) per the F14 fixture convention** so baselines and the later differ share one scheme); memory/segment reads via HTTP (`/api/user/memory-segment`) — **seg 99** (live stats) now, **seg 57** when ADR 0006's metrics segment lands → `metrics.jsonl`. Command: `run --ticks N` (collect for N ticks, then summarize `summary.json`: scenario, git SHA, ticks observed, CPU stats from seg-99, creep counts, errors seen). Add **`runs/` to `.gitignore`**. | `run --ticks 200` produces console+metrics+summary artifacts with zero manual steps |
| **P0.A6** | Smoke loop + baselines | `smoke` = `server up` → `bootstrap --reset` → `deploy` → `run --ticks K` → summary + nonzero-exit on: deploy failure, zero ticks observed, or console panic/deser-failure lines. Record **BASELINE-0** (current `master` code, pre-Phase-0 changes) and later **BASELINE-1** (§5). | `smoke` is one command, green, repeatable; BASELINE-0 artifact exists |
| **P0.A7** | Secrets policy (enforced, not aspirational) | (a) Config loading: **file-driven only — host env vars are NOT used** (operator directive: painful/brittle on Windows; the `SCREEPS_EVAL_*` overrides are **deleted** by P0.A9 — unused code is removed, not kept); creds stay in `.screeps.yaml`, eval settings/secrets in `screeps-eval/config/` per A9(b); password/token/steamKey held in `secrecy::SecretString` — `Debug`/`Display` redact by construction. (b) **No secret ever enters `runs/` artifacts or tracing output** — redaction at the config boundary, not at call sites. The **merged runtime server config** (A2) *does* contain the steamKey by necessity — it lives only under `screeps-eval/target/runtime/` (gitignored) and is part of the sweep. (c) **The CLI send path is a named leak risk**: A3's composed `setPassword("<user>", "<pass>")` payload exists post-`expose_secret()` — never log raw CLI payloads; mask `setPassword` args in any command echo/transcript; the manual sweep explicitly covers the CLI send path (the deploy.js subprocess is verified clean — it reads `.screeps.yaml` itself; no credentials on argv/env). (d) `.example-screeps.yaml` updated if the harness adds keys (the `eval:` section ships keyless); A2's vendored template diff is part of the sweep. (e) A **pin test** asserts `format!("{:?}", config)` contains no secret material (known limit: it cannot catch the CLI path — hence (c)). (f) `runs/` gitignored (A5). | The pin test exists and passes; manual sweep of a smoke run's artifacts, **the CLI transcript, and `target/runtime/`** finds no credential outside the sanctioned runtime config |

| **P0.A8** | Operator mode (manual iteration) | First-class commands for the operator's hands-on loop, beyond what automation needs: `server up\|down\|destroy\|status\|logs` (A2) with human-readable `status` (container health, tick rate, API reachability); **`cli`** — interactive passthrough/REPL to the server CLI (send arbitrary commands, e.g. `system.pauseSimulation()`), with `setPassword`-class payload masking per A7(c); **`tick set <ms>\|pause\|resume`** — tick-rate control without remembering CLI incantations (floor: see D-2); **`open`** — print/launch the web-client URL (`http://127.0.0.1:21025/`). All built on the same library functions the harness uses — no parallel code path. | The operator can cold-start, watch, pause, retune, and tear down the server with single commands and zero references to docs |
| **P0.A9** | Config & build simplification (operator directives, post-A3/A8) | Five simplifications, landing as one refactor after A3/A8 (same files): **(a) delete the `SCREEPS_EVAL_*` env overrides** — unused code is removed, not kept (`apply_env_overrides` + README mentions go); **(b) relocate eval config into the crate** with a clean in-container/out-of-container split: `screeps-eval/config/server.yml` (committed, keyless in-container launcher template — relocated from `server/config.yml`), `screeps-eval/config/local.yml` (**gitignored via a crate-local `.gitignore`** — steamKey + local overrides like ports/tickMs/spawn; committed `local.example.yml` documents it), and the **`eval:` section leaves `.screeps.yaml` entirely** (that file returns to creds-only, shared with deploy.js; the launcher-clone `serverConfig:` indirection is dropped — copy the key once into `local.yml`); **(c) remove directory-walking discovery** — the crate is always invoked from its own directory, so paths are fixed: `config/*.yml` crate-relative, creds at `../.screeps.yaml`, `--config` flag stays as the only override; **(d) image-build capability (hermetic option):** config `image:` block supporting `build:` from a Dockerfile context (bollard `build_image`; vendored or operator-cloned launcher-repo context — investigate what context actually builds) alongside the default registry pull; **(e) simplify the wasm/host target forcing** — evaluate **removing the root `[build].target = wasm32` default** (wasm-pack forces its own target; root gains `clippy`/`check` aliases for the bot crates; plain `cargo test` then works everywhere and the child `.cargo/config.toml` triple-hardcode disappears) vs. keeping the child-config pattern; decide on evidence, update `.cargo/config.toml` comments + AGENTS.md §8 + the B1 `test-host` task to match whichever lands. | env-override code gone; `cd screeps-eval && cargo run/test` works with no walking and no stale paths; a documented `server build-image` (or `image.build`) path produces a usable launcher image; the target-forcing story is one sentence in the README; `.screeps.yaml` is creds-only again |

| **P0.A10** | Identity & multi-bot deployments (operator directive, post-A9) | The current flow registered the **bot** under the operator's name ("Azaril") — wrong identity model. Target: **the operator is themself** (Steam-auth via the steamKey — the Steam client/web login against the local server; screepsmod-auth can also give that user a password), and **bot deployments are their own users**. Mechanism: bot deployments = additional `servers:` entries in `.screeps.yaml` (same host, distinct username/password — deploy.js then targets any of them with `--server <entry>` unchanged); `eval.bots: [<entry-name>, …]` lists them; `bootstrap` registers + places a spawn for **each** listed bot (distinct rooms — prospector-ranked once P4 integrates); `deploy --user <entry>` selects the target. Supports ibex-vs-ibex matches and human-alongside-bot play. Recommend renaming the current bot entry's username to `ibex` so "Azaril" stays the operator. | two bot users + the operator's own identity coexist on one world; each bot deploys independently; documented in README |
| **P0.A11** | Rust-native deploy investigation (operator directive; community-share candidate) | Investigate an npm-free, rust-native end-to-end deploy: **prior art first** — rustyscreeps' `cargo-screeps` (exactly this idea, possibly stale), **trunk-rs** (clone to a peer folder; the modern reference for orchestrating cargo build → `wasm-bindgen-cli-support` (a real library) → `wasm-opt`), the current pipeline's actual steps (js_src/ rollup glue + deploy.js upload format + the `.screeps.yaml` `wasm-pack-options` build-std flags), and the Screeps code-upload API's binary-module support. Deliverable NOW: an investigation/design doc (viability, architecture, parity-validation plan, and the **community-share path** — a tool that lets anyone link `screeps-game-api` into their own crate and deploy without cloning the starter). Prototype is a follow-up gated on the doc's verdict; **deploy.js stays the working path throughout**. | `docs/design/rust-native-deploy-investigation.md` exists with a clear go/no-go + architecture; no pipeline changes |

| **P0.A12.1** | Use the `base64` crate (operator directive; verification-pass item) | `screeps-rest-api/src/code.rs:61` hand-rolled a `base64_encode` + alphabet table for the code-upload binary modules. Replace with the **`base64` crate (0.22)** — the bot crate already depends on it (`screeps-ibex/Cargo.toml:31`, `use base64::prelude::*` in serialize.rs), so this is the established convention, not a new dependency in spirit. Standing rule: **never hand-roll well-solved encodings** (base64/hex/json) in any crate — use the standard, widely-audited crate. Delete the hand-rolled function + its tests in favor of `BASE64_STANDARD.encode(...)` (keep one fixture test asserting the uploaded module shape, not the encoding internals). | hand-rolled encoder gone; `base64 = "0.22"` in screeps-rest-api; module-shape fixture test still green |
| **P0.A12** | Shared Screeps REST client crate (operator directive) | **One API client, not N** — `screeps-eval` and `screeps-prospector` already independently implement signin/world-status/place-spawn. Extract into a shared host-side crate (working name `screeps-rest-api`; workspace-excluded, D-1 lifecycle, strong community-share candidate alongside A11): auth (token + user/pass + rolling X-Token adoption), the pinned endpoint set from both crates (game API, register, place-spawn, map-stats/room-status/terrain/objects, world-size/status, respawn, memory-segments, console websocket) **plus code upload** (`POST /api/user/code` — the A11 deploy tool's needs land here), rate-limit courtesy, secrecy discipline, fixture-tested endpoint shapes with source citations — "keeps us honest about API interaction": one place where shapes are pinned. Consumers: eval, prospector, the A11 tool, all future tooling. Uses `screeps-game-api` pure types in its surface where P6's verdict allows. | both crates depend on the shared client; zero endpoint code duplicated; endpoint shapes pinned once with citations |

| **P0.A13** | `screeps-pack` — THE deployment tool (operator directive; gauntlet wave) | **Full replacement of the npm deployment path for USER and TEST workflows alike** — not a harness-internal helper. Build the [investigation](../design/rust-native-deploy-investigation.md)'s GO design as a workspace-excluded **lib + standalone CLI**: orchestrate `cargo build --target wasm32` honoring the `.screeps.yaml` `configs` **per-server** (the `wasm-pack-options` build-std/rustflags AND per-server feature flags like `mmo: ["--features","mmo"]` — a silently-dropped flag is risk #2; minification is dropped by design: the glue is ~88 lines, document the size delta), wasm-bindgen via **lockfile-resolved prebuilt CLI download** (trunk's mechanism — never static-pin `cli-support`), the CJS glue template replacing `js_src/main.js`'s role (bucket gate, staged init, halt trap preserved), optional wasm-opt, upload via the **shared `screeps-rest-api` client** — **every `servers:` entry works: private-server (user/pass) and mmo/ptr/season (token auth)**, so the operator's manual deploys are npm-free too (`screeps-pack deploy --server mmo`). Sequenced into the gauntlet wave (needs A12, a stable bot crate, the freed eval server). server-kit deploy calls it as a library; the parity window uses deploy.js as fallback. **Parity gate before cutover:** module-map diff + pinned-version wasm byte-compare vs the deploy.js pipeline + a green 600-tick smoke; BASELINE-1 records via screeps-pack only if green. **At cutover (operator directives — clean surface):** the `--legacy` flag, its code path, and the deploy.js shell-out are **deleted from screeps-eval**; screeps-pack becomes the **documented primary deploy path for the repo** (AGENTS.md §8 + README updates land with it); `js_tools/deploy.js` + the npm toolchain remain in-repo ONLY as an optional user-customization escape hatch — **npm/node are no longer required for any standard user or test workflow**. MMO deploys still respect the §0 C1 format-break guard (process, not tool). | parity report committed; `screeps-pack deploy --server <any entry>` works standalone incl. token-auth servers; `screeps-eval deploy` is library-driven; no `--legacy`/shell-out remains; repo docs name screeps-pack as the deploy path; npm absent from standard workflows |

| **P0.A14** | Split `screeps-eval` into server-kit + screeps-ibex-eval (operator directive; post-consolidation, pre-gauntlet) | **Two crates with a clean mechanism/policy line, so others can use the private-server scaffolding for their own bots:** (1) **`screeps-server-kit`** (name open to veto) — the GENERIC local private-server toolkit: bollard lifecycle (`docker.rs`), launcher-config merge (`server_config.rs`), server-CLI client + bootstrap primitives + tick control (`server.rs`), generic deploy orchestration (`deploy.rs` — wraps deploy.js today, calls `screeps-pack` as a library after A13; deploying *some crate* is bot-agnostic), console/metrics **capture mechanism** (`capture.rs` — JSONL streams + sampling are generic), the operator CLI (`server`/`bootstrap`/`cli`/`tick`/`open`), config loading for all of the above. Depends on `screeps-rest-api`. Community-share candidate alongside rest-api and pack — README written for external users. (2) **`screeps-ibex-eval`** — the IBEX evaluation policy: smoke **gates** (the panic/deser markers pinned to ibex's log strings), `run`/baseline scenarios + summary schema where ibex-specific, baseline comparison, and later the ADR-0006 colony-health score + regression diffing. Depends on server-kit; thin CLI (`smoke`, `run`, baselines). The line: **server-kit = mechanism, screeps-ibex-eval = what "correct/healthy" means for ibex.** Both stay workspace-excluded, D-1 extraction-ready. **Sequenced AFTER the consolidation wave lands (it is editing screeps-eval now) and BEFORE the gauntlet** — A13's screeps-pack integration then targets the final layout (pack slots into server-kit's deploy), avoiding rework. | two crates, each with Usage-then-Design README; server-kit contains zero ibex-specific strings/policy; `screeps-ibex-eval smoke` behaves identically to today's `screeps-eval smoke`; A13 integrates against server-kit; **and a grep-driven `screeps-eval`/`screeps_eval` reference sweep (operator directive) comes back clean**: clap command names, tracing env-filter defaults, error-message paths, Cargo metadata, crate cross-references, and the LIVING doc references (exit criteria, gauntlet/A13 rows, ADR 0006 layout note, testing-doc forward references) all renamed — historical records (baseline-0 report, completed-task descriptions, commit messages) keep the old name as accurate history, annotated only where confusing |

**Decision point D-1 — RESOLVED (operator):** in-repo, workspace-excluded now; **extract to a submodule with its own remote once stable** (see the lifecycle decision in the workstream intro). The operator creates the remote when extraction time comes.

---

## 1b. Workstream P — spawn-site selection tool (`screeps-prospector`)

A **separate utility crate** (operator directive) for choosing WHERE to spawn — used by the private-server setup flow (upgrading A3's interim placement) **and for spawning onto MMO remote worlds** (operator-assist; this supersedes the gap-analysis G-16 "operator does it better" verdict with tooling — full MMO automation remains out of scope). Library-first + CLI, same lifecycle as `screeps-eval` (in-repo, workspace-excluded, extraction-ready; README **for other users** is part of done). Runs **in parallel** with Workstream A (new crate, no file overlap).

The flow it implements: query the server for rooms **open for spawning** → fetch room terrain + objects **with a file-backed cache** (API limits, especially on MMO) → score rooms (cheap heuristics filter → **foreman plan scoring** for finalists — the spawn lands where the foreman layout wants a spawn, so the eventual base is optimal) → recommend, and place via REST on explicit confirmation. Reference REST usage: [Qionglu735/screeps_tool `screeps_api.py`](https://github.com/Qionglu735/screeps_tool/blob/master/screeps_api.py).

| ID | Task | Detail | Done when |
|---|---|---|---|
| **P0.P1** | Crate + REST client | `screeps-prospector/` (workspace-excluded, own `.cargo` per the A1 pattern until A9(e) settles target forcing; path-dep `screeps-foreman` with `default-features = false`). REST client: token + username/password auth, shard-aware; endpoints pinned from the python reference + live behavior, not guesswork (room-status/map-stats, room-terrain, room-objects, world-size, world-status, **place-spawn**, respawn). | client compiles; endpoint shapes unit-tested against recorded fixtures |
| **P0.P2** | Room cache, bench-compatible | File-backed cache **in the foreman-bench map-JSON shape** (`{description, rooms:[{room, terrain, …}]}` — the bench's MMO shard maps + private-server default at `screeps-foreman-bench/resources/` are both seed data and the join target). **Upsert** semantics; extended per-room fields (objects: source/controller/mineral positions; status: open/novice/respawn; fetchedAt) as *optional* keys the bench loader ignores (verify its serde tolerates unknown fields). Terrain cached forever (immutable); status with TTL. | `fetch` populates/upserts a cache file the bench can still load; re-runs hit cache, not API |
| **P0.P3** | Scoring | Two-stage: cheap heuristics over the cache (source count, mineral, swamp %, exit count, controller presence) filter to top-N → **offline foreman plan score** per finalist (a `PlannerRoomDataSource` over cached terrain+objects, the bench's `FastRoomTerrain` pattern) → ranked recommendations incl. the plan's spawn tile. | `score`/`recommend` rank a fixture map deterministically; finalists carry a plan-derived spawn position |
| **P0.P4** | Recommend/place CLI + integration | CLI: `scan` (open rooms), `fetch [--rooms…]`, `score`, `recommend`, `place --room --x --y --yes`, `auto --yes` (private flow). **MMO safety: recommend-first always; `place` requires explicit `--yes`; no auto-place against screeps.com ever.** `screeps-eval bootstrap` upgrades from its interim placement to the prospector library (a follow-on edit in `screeps-eval` once both crates are stable — not part of the parallel build). | private-server `auto --yes` places an optimally-sited spawn end-to-end (verified live after A3/A8 lands); MMO `recommend` documented |
| **P0.P5** | Docs for other users | README (detailed **Usage**, then **Design**: API client + caching strategy + scoring pipeline + the bench-format contract + MMO safety + extraction plan), per the operator directive that this tool serves more than this repo. | README current with shipped behavior |
| **P0.P6** | Adopt `screeps-game-api` types (operator directive) | Use the local fork's **pure types** instead of re-implementing game parsing wherever feasible host-side: `RoomName` (parsing/arithmetic), `RoomXY`/coordinate types, terrain enums/`LocalRoomTerrain`, and its serialization methods. **Investigate first**: whether the crate (path `../screeps-game-api`) compiles on the host target for types-only use (its js-sys/wasm-bindgen externs usually compile host-side but cannot run — types-only consumption is the question); if a small fork change (e.g. a feature gating the JS-bound surface) makes it clean, that change is **upstream-quality** per AGENTS.md §7. Applies to prospector (terrain/rooms) and eval capture parsing alike; document the verdict either way. | prospector/eval use game-api types for room names/coords/terrain, or a written verdict explains why not |
| **P0.P8** | Seeding model fix (operator directive; post-consolidation pass) | The `cache seed` default `../screeps-foreman-bench/resources/default-private-server.json` is a repo-relative cross-crate path that breaks for any external user and at D-1 extraction — and **seeding buys little on a private server anyway** (a small owned world: `scan`+`fetch` fills the cache live in seconds; on MMO the bench maps are stale snapshots — the model is fetch-on-demand + the permanent terrain cache). Fix: (a) **no default path** — `cache seed --from <path>` requires an explicit argument and is repositioned in the README as an *optional import* for niche cases (sharing scans between machines, importing bench/offline exports), not a setup step; (b) the documented happy path for BOTH server types is `scan` → `fetch` (already implemented); (c) tests keep using the bench fixture **as test data** via their own fixture-copy mechanism (no CLI involvement); (d) sweep the crate for any other repo-relative defaults that violate extraction-readiness (the `../.screeps.yaml` creds path is the ONE documented exception). | no cross-crate default paths in the CLI; README shows scan→fetch as the happy path; seed requires --from |
| **P0.P7** | Shared composable room scoring (operator directive; post-P3) | **One room-candidate scoring implementation, not three** (foreman plan-quality layers, `claim.rs` live expansion scoring, prospector stage-1 are currently separate). Extract prospector's stage-1 scorers into a **composable scoring module hosted in `screeps-foreman`** (recommended home: its terrain DTOs (`FastRoomTerrain`) + the bench calibration tooling already live there, and it compiles both wasm + host — submodule sequencing per AGENTS.md §9; the alternative, a new pure `screeps-room-scoring` workspace crate, is acceptable if foreman scope is a concern). Shape: a `RoomScorer` trait + weighted pipeline over pure room-fact DTOs, with **appendable context scorers** supplied by the caller — e.g. *proximity to already-selected rooms*, *route/path distance to owned rooms*, threat — so prospector composes it with cached/static context while live Ibex (claim/expansion at Inc 7, [ADR 0009](../design/0009-room-planning-and-multiroom-layout.md) D3.5 / IBEX-032) composes the same scorers with live `RoomRouteCache`/game-state context. Stage-2 (full foreman plan scoring) stays foreman-native; bench weight-calibration (0009 D2) then covers BOTH stages in one place. Prospector P3 becomes a consumer, not an owner. | prospector stage-1 runs on the shared module; `claim.rs`'s Inc-7 route-aware rework is spec'd against the same trait; zero scoring logic duplicated between prospector and foreman |

**Live-test constraint:** while Workstream A agents hold the Docker stack, P1–P3 verify **offline** (bench fixtures + recorded API responses); the live `auto` end-to-end (P4) runs once the A3/A8 work settles.

---

## 2. Workstream B — host test lane + baseline pin tests

Stands up ADR 0015's **L0 lane** in its minimal Phase-0 form. (Full Tranche-1 ~60 kernel tests are Increment-0 work and may proceed in parallel; Phase 0 requires only the pins below.)

| ID | Task | Detail | Done when |
|---|---|---|---|
| **P0.B1** | `test-host` lane | Root `.cargo/config.toml` gains the alias (per ADR 0015 §4): `test-host = ["test", "--target", "x86_64-pc-windows-msvc"]` (CI later passes its own triple explicitly). Verified working on this machine by the testing pass (`cargo test -p screeps-ibex --target x86_64-pc-windows-msvc --no-run` succeeds). | `cargo test-host -p screeps-ibex` compiles & runs (zero tests initially) |
| **P0.B2** | `screeps-testkit` | **Deferred unless needed** — Phase-0 pins are plain in-crate `#[cfg(test)]` against pure functions; create the testkit crate only when the first fixture double (MemoryArbiter/GameView) is actually required. Do not scaffold speculatively. | n/a (explicit non-goal unless triggered) |
| **P0.B3** | Baseline pin tests (~12) | In-crate tests pinning (i) **known-correct behavior Phase 0 must not break** and (ii) **each Workstream-D fix**: spawn-queue **descending-priority invariant** (the review's false-positive guard — lock it so it is never "fixed" into an inversion; `spawnsystem.rs:85–94/236`), `create_body` min-cost clamp (IBEX-022 refuted — lock the clamp), serialize encode/decode round-trip (pure helpers, `serialize.rs:310–344`), `RepairQueue` `max_hits=0` guard, segment-map disjointness (**the D1 `const` assert IS the regression test** — per component-test-plans §1(c); no runtime twin), capacity helper boundary cases (D-IBEX-050), `saturating_sub` sites (D-IBEX-044/045), transfer `(TransferTarget, mode)` matrix returns `Err` not panic (D-IBEX-010), power-bank counter counts only PowerBank attacks (D-IBEX-043), config-redaction pin (A7), threat-classify smoke (current behavior snapshot, informational). | `cargo test-host` green with the pin set; each D-row below names its pin |

---

## 3. Workstream C — dead-code removal (supplanted only)

**The rule (operator-stated):** remove only what is dead because **supplanted**; keep what is dead because **not yet implemented**. Concretely: a construct is removable iff (a) **zero call sites** (verified by crate-wide search at removal time, not from memory) AND (b) an ADR marks it **superseded**. Everything else stays.

| ID | Task | Detail | Breaking |
|---|---|---|---|
| **P0.C1** | Remove orphaned squad missions | `SquadAssaultMission` (`missions/squad_assault.rs`, 592 L) + `SquadHarassMission` (`missions/squad_harass.rs`, 357 L) + their `MissionData::SquadAssault`/`SquadHarass` variants (`missions/data.rs:33–34`, match arms `:66–67`/`:104–105`, `mission_type!` registrations `:260–261`) + `missions/mod.rs:22/:24` mod decls. Zero `::build` call sites (re-verified against the working tree by the plan review); no per-variant serialization registration exists (`game_loop.rs` serializes `MissionData` storage whole), so data.rs + mod.rs is the complete edit — non-exhaustive-match errors make the arms unforgettable. Superseded by `AttackMission` force plans (ADR 0008 DELETE rows; ~950 L gone). **Variant removal shifts positional-bincode discriminants (7 later variants) → Memory/format**: lands **after BASELINE-0 is recorded**; the break is absorbed by the next `bootstrap --reset` (the BASELINE-1 bring-up). See the §0 MMO guard. | **Memory/format** (absorbed by the BASELINE-1 reset; MMO guard applies) |
| **P0.C2** | Bounded supplanted-sweep | One pass applying the rule above to: remaining commented-out code blocks (keep those documenting intent, e.g. `raid.rs`'s deliberately-disabled registrations), unused imports/`#[allow(dead_code)]` items whose owner was deleted by C1, and `todo.md` grooming (check off the refuted IBEX-022 body-sizing item and the DONE-marked planner items). Anything ambiguous → **keep + note**, do not expand scope. | None |

**Explicit KEEP list** (dead-but-planned; each has an ADR owner — do NOT remove):
`BoostQueue` + `Boosted*` compositions ([0010](../design/0010-boost-lab-factory-pipeline.md) wires them) · `total_energy_invested` economy-abort ([0008](../design/0008-combat-and-squad-architecture.md)/[0014](../design/0014-empire-strategy-and-posture.md) wire it) · rover `Follow`/`desired_offset` ([0003](../design/0003-behavior-modeling.md) §B.2 builds on it) · `duo_sk_farmer` (gap G-8 SK farming) · power-bank attack machinery ([0013](../design/0013-power-economy-and-power-creeps.md)) · `TransferTarget::Nuker` deposit plumbing (gap G-4 nukes) · `repair_entity_integrity` (deleted only at Increment 5, [0001](../design/0001-entity-model.md) A3) · `DefendMission`'s observer role (KEEP per 0008 inventory).

---

## 4. Workstream D — critical & obviously-incorrect fixes

Exactly the **Group A** set from [proposed-fixes.md](../plans/proposed-fixes.md) (the fix specs — before/after sketches, exact lines — live there; this table sequences and binds each to its pin test). Group B (needs-design: IBEX-018/021/040/048/049) is **explicitly out of Phase 0** — those ride their ADR increments.

| Order | Fix | One-liner | Severity | Pin test (B3) |
|---|---|---|---|---|
| D1 | **IBEX-013 (interim)** | `const` disjointness enforcement + shrink `COMPONENT_SEGMENTS` to 50–54 + chunk-count watermark log — stops `serialize_world` wiping the cost-matrix segment **every tick**. **Shape (operator directive — no convention-enforced scatter, no system details bleeding into the game loop):** a dedicated **`segments` module** is the single code-form of ADR 0002's segment-allocation registry — ALL segment ids live there as named constants (component range, cost-matrix, stats-history 56, planner 60, live-stats 99, + reserved ids), with the compile-time **uniqueness check over the whole table** (not a pairwise assert in the game loop); `game_loop.rs`, `costmatrixsystem.rs`, `statssystem`/`stats_history`, `roomplansystem` all **import their ids from it** and define none locally. The core loop never references another subsystem's segment constant. | **Critical** | the whole-table `const` uniqueness check (compile-time = the regression test); validation run: **re-deploy identical code** (a global/heap reset that *preserves* segments — `bootstrap --reset` wipes them, so it cannot be the probe) then assert `load_cost_matrix_cache` non-empty. This is the embryo of component-test-plans §1's single-owner `forced_reset_reloads_nonempty_cost_matrix` artifact |
| D2 | **IBEX-010** | Nuker-withdraw `panic!` → `Err(InvalidArgs)` + one-shot log (reachable whole-tick abort during raids) | **Critical-path panic** | `(TransferTarget, mode)` matrix test |
| D3 | **IBEX-043** | Power-bank concurrency `.filter(\|_\| true)` no-op → count only `AttackReason::PowerBank` | High-value one-liner | counter test |
| D4 | **IBEX-029 — DEFERRED to Increment 1** | Routing the 23 bare `squad_combat` intents through `action_flags` is behavior-preserving *only if* the pipeline pairing is exactly right — the verified site list has same-state adjacent pairs (`:248/:250`, `:478/:480`, `:693/:695`) where same-pipeline gating changes *which* call wins, and the only honest validation is the **intent differ** (shadow-dispatch), which lands Inc 1 per ADR 0015. Pipeline authority is **`jobs/actions.rs:22–42`** (`RANGED_HEAL` shares pipeline B with `RANGED_ATTACK`/`RANGED_MASS_ATTACK`; `HEAL` is its own pipeline C) — NOT the proposed-fixes pairing note, whose refinement flag is mandatory. | (rides Inc 1 with the differ) |
| D5 | **IBEX-044/045** | `saturating_sub` on `game::time()-t` cadence/timeout sites + `store.rs` free-capacity | Latent underflow→abort | boundary tests |
| D6 | **IBEX-050** | ~~Extract single capacity helper preserving the per-resource summation~~ **RESOLVED BY SOURCE VERIFICATION (operator-prompted):** the "double-count" premise is FALSE in the current engine — `getUsedCapacity()` no-arg is a memoized single `_.sum` over the raw store (`engine store.js:36-50`; engine-mechanics folklore row 26). **Delete `HasExpensiveStore` + the per-resource summation entirely**; call sites (all general stores: creeps/containers) use `store().get_used_capacity(None)` / `get_free_capacity(None)` directly (mind the binding's `i32` on free + `.max(0)`). The IBEX-045 Rust-side subtraction concern evaporates with it. **Caveat kept on record:** `storeCapacityResource` structures (lab/nuker/powerSpawn) return `null→0` through the binding for the no-arg call — if a future call site totals a special store, per-resource summation is required THERE, locally, with a comment citing folklore row 26. | Tech-debt w/ correctness edge | call sites compile + behave identically on general stores; one live haul smoke confirms (a non-empty-store probe rides the gauntlet) |
| D7 | **IBEX-009/019/020** | Latent unwrap/expect hardening: staticmine `if let`, attack.rs:615 explicit bind, `get_room()` sentinel | Latent (all currently guarded) | compile + existing-behavior smoke |
| D8 | **IBEX-046** | `debug_assert!(is_finite)` at priority sources + guard transfer divisors | Latent NaN | finiteness asserts under `test-host` |

Each fix: implement → pin test green (`cargo test-host`) → `cargo clippy -p screeps-ibex` clean (wasm target). **Smoke runs twice, not per-fix** — once after D1 (the Critical) and once after the final fix — because a full smoke is a nightly-LTO wasm build + a server run (tens of minutes at realistic tick rates), and per-fix smokes are exactly the per-change drag ADR 0015 §3 forbids. Fixes land as individual commits with their tests.

---

## 5. Sequencing, baselines & exit criteria

```
A1 ──→ A2 ──→ A3 ──→ A4 ──→ A5 ──→ A6 ─→ BASELINE-0 (current master, fresh world)
 B1 (parallel) ──→ B3 pins for pre-existing invariants
                                    then: C1 → C2  (cleanup; world reset absorbed)
                                          D1 → D8  (each with its pin test)
                                    A6 again ─→ BASELINE-1 (Phase-0 code, fresh world)
                                    compare summaries → docs/execution/baseline-0-report.md
```

Both baselines are **fresh-bootstrap runs of K ticks at the same tick duration**, so the comparison is apples-to-apples; the comparison report (committed) is the first artifact of the regression-diffing discipline ADR 0006 §7 builds out.

**Exit criteria (all must hold):**
1. `cd screeps-eval && cargo run -- smoke` is **one green command** from a cold Docker Desktop.
2. `cargo test-host` green, including the spawn-ordering and redaction pins.
3. Secrets: redaction pin passes; a manual sweep of `runs/` artifacts and harness logs finds no credential material; `.gitignore` covers `runs/`.
4. Supplanted code removed (C1/C2); KEEP list intact and documented above.
5. All eight D-rows landed, each with its pin test, clippy-clean on the wasm target.
6. BASELINE-0 and BASELINE-1 recorded; comparison report committed. **Gate only on hard zeros** (zero panics, zero deser failures, deploy succeeded, ticks observed > 0) — the metric comparison (CPU, creep counts) is **informational** in `baseline-0-report.md`, not a gate: a single-run exact metric gate is precisely the flake generator ADR 0015 rejects, and no N-seed machinery exists yet.
7. The one Memory/format break (C1) was absorbed by the sanctioned BASELINE-1 bring-up reset — **no unsanctioned drop of live/MMO state occurred** (eval-server `bootstrap --reset` is a state drop by design and doesn't count; the §0 MMO guard held).
8. **Operator mode works end-to-end (P0.A8):** cold-start → watch → `tick set` → pause/resume → tear down, each a single command, verified by the operator personally.
9. **Documentation delivered (operator directive):** `screeps-eval/README.md` contains detailed **Usage** (every command, the config shape, the secrets rules, troubleshooting the known failure modes) followed by a **Design** section (module map, the config/merge flow, port discovery, why workspace-excluded, extraction plan) — current with the shipped behavior, not the plan.
10. **Prospector delivers (Workstream P):** `recommend` ranks rooms offline against the bench fixtures with plan-derived spawn tiles; a live private-server `auto --yes` placed an optimally-sited spawn at least once; MMO mode is recommend-first with explicit-confirm placement, documented in its own README (Usage, then Design).
11. **Rust-native deploy lands (P0.A13):** `screeps-pack` builds + uploads the bot with the parity gate green (module-map diff, wasm byte-compare, 600-tick smoke); it is the **standalone deployment tool** — `screeps-pack deploy --server <entry>` works for every `.screeps.yaml` entry including token-auth MMO/PTR/season — and server-kit deploy runs it as a library; **npm/node are required by NO standard user or test workflow**; the `--legacy` flag + shell-out are deleted from screeps-eval; repo docs (AGENTS.md §8, READMEs) name screeps-pack as the primary deploy path; `js_tools/deploy.js` + npm remain only as an optional user-customization escape hatch; the parity report is committed.

**Out of scope (deferred to their increments):** CpuGovernor/pathfinding facade (Inc 1), serialization version header + dedicated cost-matrix segment (Inc 2 — D1 is the *interim* only), all Group B fixes, scenario library/opponent bots, `screeps-testkit` fixtures beyond need, any ADR pillar implementation.

**Operator decision points:** **D-1 — RESOLVED:** in-repo now, extract to a submodule once stable (§1). **D-2 — RESOLVED (operator):** tick-rate **floor is 50 ms** — at/below that the server and UI start failing to keep up (operator experience). Defaults: smoke runs **100 ms** (safely above the floor), manual watching **1000 ms**; `tick set` (P0.A8) accepts anything ≥ 50 ms, warns below 100 ms. Wall-clock expectations still per the honesty note: the server may not sustain the configured rate once creeps exist, and first boot includes a multi-minute in-container npm install (A2's health-wait tolerates it). **D-3 (default unless vetoed):** K = 2,000 ticks for baselines — reaches **RCL2 + unreserved remote activity** on a fresh world ("first remotes" with reservers is *not* reachable at RCL2, whose 550 max spawn energy is below a CLAIM+MOVE reserver's 650 cost).
