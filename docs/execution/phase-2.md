# Phase 2 Execution Plan — Combat-Effective: Harness, Identity, Movement, Goals, War

> **⚠ SUPERSEDED for forward-looking status (2026-06-18).** For *what's done and what remains*, read the single master doc: [`../plans/combat-overhaul-plan.md`](../plans/combat-overhaul-plan.md). **This document is now the cold-resume per-task historical log** — §2.0 (newest-first status log with commit SHAs), §2.2–2.7a (per-task validation cells), §2.8 (sequencing graph), §2.9 (checkpoints), §2.1 (operator-settled constraints). Its Status columns may lag; the master doc §3 is authoritative for current status.

> **Scope:** The combat-squad overhaul — harness-first, then behavior — spanning the combat micro-sim ([ADR 0006](../design/0006-eval-and-iteration-harness.md) Part B), squad identity ([ADR 0001](../design/0001-entity-model.md) Inc 3), group movement ([ADR 0003](../design/0003-behavior-modeling.md) §B), the objective-queue + Squad Manager + tactics ([ADR 0008](../design/0008-combat-and-squad-architecture.md)), war supervision + escort + a thin posture hook ([ADR 0014](../design/0014-empire-strategy-and-posture.md)), and synchronized spawning ([ADR 0011](../design/0011-spawn-orchestration.md) D3/D4/D9). The higher-level backlog/overview lives in [`../plans/combat-overhaul-plan.md`](../plans/combat-overhaul-plan.md); **this document is the authoritative execution tracker.** Rewrite-plan increment mapping: this phase covers the **combat-sim addition to Inc 0** + **Inc 3** (SquadStore) + **Inc 4** (cohesion/manager/tactics) + **Inc 5** (war supervisor). Exit at **M4 (Combat-Effective)**.
>
> **Why this phase, in one paragraph.** Combat squads are "very ineffective": null tactics ("just stand and ranged-mass-attack"), **orphaned → idle** when an objective completes, and **scatter** (cohesion/group-pathfinding). The fix copies the scouting pull model (a global objective queue), adds a lifecycle/tactics owner (the Squad Manager), and a proper anchor-based group mover — but **none of it is measurable today** (the harness scores `military: None`, no cohesion metrics), so the harness lands first.
>
> **Living document.** §1 (Design) is the end state and changes only when a design decision changes (ADRs are authoritative for rationale). §2 (Execution) is the work tracker: statuses, decisions (the *how/why*), baselines, and resume notes are updated **as work lands** — task IDs (`P2.*`) appear in commit messages exactly like Phase 0/1. **If you are resuming cold:** read §2.0 status log (newest first), then the checkpoint table §2.7 to see which checkpoint is next, then that checkpoint's workstream task table.
>
> **Status: NOT STARTED** (authored 2026-06-17). Design corpus settled (ADRs 0006/0008/0003/0011/0014/0015/0001 revised through 2026-06-17, incl. the anchor-primary movement correction). No production code yet.

---

## 1. Design — the end state

Each subsystem sits behind a seam that outlives this phase. ADR references are authoritative for rationale; this section pins what Phase 2 builds toward so the executor never re-derives intent. The **operator-settled decisions** (2026-06-16/17) that constrain everything below: harness = hybrid combat micro-sim running the bot's *own* code; movement = **anchor-primary** (NOT lead-follower); corridors handled by **relaxing** the same mover (no separate follower mode); `Follow`/`pull` reserved for no-MOVE/under-MOVE bodies; squad-on-objective-complete = **retask-if-viable-else-recycle**; boosts = **wire** behind the kill-switch; common movement code goes in the **pathfinding/movement system**.

### 1.1 Combat micro-sim harness (the harness-first prerequisite) — [ADR 0006](../design/0006-eval-and-iteration-harness.md) Part B

A deterministic, in-process Rust combat simulator that drives the bot's **own** combat decision code, so iteration is fast (`cargo test-host`, no Docker), introspectable (per-tick state + reason tags + replay), and self-play-capable (the same code on both sides — no tactics fork to overfit). Three host-only, workspace-EXCLUDED crates: `screeps-combat-engine` (mechanism: the combat-tick port), `screeps-combat-agent` (the `CombatView`/`CombatIntent` trait seam + `IbexAgent` + scripted opponents), `screeps-combat-eval` (policy: scenarios, cohesion metrics, score, parity, replay). The Dockerized private server stays as the **fidelity oracle + acceptance gate**, not the per-change loop. Fidelity bounded three ways: per-change **byte-exact conformance vectors** captured from the live engine, a **nightly sim-vs-server parity budget**, and the **live seg-57 cohesion canary**. The combat mechanic surface is bounded (verified against `C:\code\screeps-engine`): two-phase accumulate-then-apply, the intent priority/exclusion table, per-part 100-hit pools + front-to-back destruction + `calcBodyEffectiveness`, all damage/heal/dismantle/tower-falloff formulas, TOUGH/boost rounding, fatigue + same-tile movement conflict, ramparts/safe-mode, single 50×50 room. Omits economy/market/power/spawn-logistics/inter-room/NPC-AI; pathfinding is reused from `screeps-rover`, not reimplemented.

### 1.2 The tactical seam (trait-first) — [ADR 0006](../design/0006-eval-and-iteration-harness.md) §B.2, [ADR 0003](../design/0003-behavior-modeling.md), [ADR 0015](../design/0015-testing-and-validation-strategy.md) S17

The combat decision is **generic over a JS-free `CombatView` trait** emitting `CombatIntent`s — no `game::*` below the seam. Two implementors: a live adapter (reads `game::*`, isolated in a leaf module — the `screeps-rover` `screeps_impl.rs` template) and a sim adapter (reads `CombatWorld`). **No cargo feature** (operator preference): the decision fn is pure and `screeps-ibex` already host-links ([ADR 0015](../design/0015-testing-and-validation-strategy.md) verified), so the sim depends on the bot at the host target and calls `decide()` directly. A `tactical` feature is the fallback only if host-compiling the bot proves too heavy. There is then exactly one implementation of target-selection/formation/kite/focus-fire; self-play is `IbexAgent` vs `IbexAgent`. Extraction is **parity-first**: the live shim and the extracted fn must emit byte-identical intents (the `IntentRecorder` digest) before any sim result is trusted.

### 1.3 Squad identity — [ADR 0001](../design/0001-entity-model.md) Inc 3, [ADR 0015](../design/0015-testing-and-validation-strategy.md) S8

`SquadStore` + `SquadId` (a stable id minted from a monotonic counter persisted with the store — **never** a recycled index) replaces the raw `squad_entity: Option<u32>` on `SquadCombatJob`. `resolve(id)` returns the same-logical-squad-or-`None`, **never a foreign squad** after a recycle/reset (the IBEX-002b aliasing). A **dangling-ref counter** emits to seg-57 on every miss. This is the hard gate for the Squad Manager (a lifecycle owner keyed on a recyclable index would resurrect the aliasing across the whole roster). `WORLD_FORMAT_VERSION` bumps when the `SquadId` field lands (one loud reset, sanctioned by the reset-anytime policy). **Why a minted id and not a `specs::Entity`/saveload marker** — an `Entity` is a runtime handle (index+generation reassigned every world-build), not a serialization-stable key; storing one keeps the `ConvertSaveload`/`repair_entity_integrity` machinery this rewrite deletes, and `JobData` is plain serde so it can't use markers anyway. Full rationale + comparison table: [ADR 0001 §"Why a minted `SquadId`…"](../design/0001-entity-model.md). (ADR 0001 A1 is an interim *generation-carrying handle*; A2's `SquadId` is the end state — don't conflate them.)

### 1.4 Group movement: the anchor mover — [ADR 0003](../design/0003-behavior-modeling.md) §B.2 (corrected 2026-06-17), [ADR 0008](../design/0008-combat-and-squad-architecture.md) §5

**One anchor mover on a continuum of tightness** (not two modes with a handoff). The anchor is the squad's shared coordinate frame (`virtual_pos` + orientation); each member targets `anchor + rotate(base_offset, orientation)`.
- **Open terrain (N≤4):** rigid body — exact offsets, hard cohesion gate, a **cached footprint-aware tile-path** the anchor follows (pathfound once via the reusable "moving-maximum" cost transform `applyMovingMaximum(w,h)` — in the pathfinding/movement system, parameterized by W×H — cached on `SquadPath`, followed step-by-step, **re-pathed only on invalidation/stuck**, the rover `CreepPathData.path` discipline; NOT the current straight-line `signum` `advance_virtual_pos`), plus tower-avoidance pricing; **lockstep block advance** (one cached pathfind + N cheap direction-moves). Full **orientation/rotation** (the dormant `FormationLayout::{orient_toward,mirror_y,rotate_cw}` + `threat_direction` + `reassign_slots`/`threat_facing_slots`): face the threat, keep tanks/high-HP front + healers back, `mirror_y` on retreat to kite as a block, rotate-in-place (engine-confirmed 4-cycle) to present a fresh creep.
- **Corridor / room-edge:** **relax the same mover** — width-1 footprint pathfind + travel-oriented `line`/loose tolerance; self-mobile members file through (terrain enforces single-file), the hard gate re-forms the box on the open side. No separate follower mode, no `pull`.
- **Blob (N>4):** loose-centroid cohesion ("within K tiles of the centroid") or split into anchored sub-squads.
- **`Follow`/`pull`** reserved for no-MOVE/under-MOVE'd compositions (pulled attacker / dedicated puller), an optional rover capability — not the corridor mechanism. Fatigue cohesion for normal squads = MOVE-balanced bodies + the aggregate-fatigue gate.

The *current* anchor is broken only because `advance_virtual_pos` steps in a straight line (no pathfind) while members path independently and the mode ratchets into Loose — all fixable; ~90% of the proper machinery already exists, dormant.

### 1.5 Global goal layer + lifecycle owner + tactics — [ADR 0008](../design/0008-combat-and-squad-architecture.md), [ADR 0015](../design/0015-testing-and-validation-strategy.md) S9/S14

- **`CombatObjectiveQueue`** — modeled directly on the scout `VisibilityQueue` (`room/visibilitysystem.rs`): a global, persistent, priority/TTL request queue of *objectives* (`Secure`/`Defend`/`Dismantle`/`Harass`/`Farm`/`Escort`, per-room/target). Two-layer: persistent durable facts + `UnwinnableTarget` give-up backoff; **ephemeral assignment** (`claimed_by` — never serialized, self-heals on reset, cannot dangle). Idempotent upsert (priority max-merge, TTL extend). `SuccessPredicate` is an **observable world-state** predicate, decoupling objective lifetime from creep lifetime. Producers (war/defense-scan/claim/attack) upsert; the manager pulls.
- **`SquadManager`** — a single perpetual ECS system (like `ScoutOperation`): claim/retask objectives for `SquadId`s; composition→roster via the threat-matched `damage.rs`/`sized_*_body` helpers; **pre-spawn replacement, never renew**; compute the per-tick tactical orders; enforce cohesion-as-invariant + per-state deadlines; retire/force-abort. On objective-complete-while-healthy: **retask** to the next viable objective (≥40% residual TTL), else recall-and-recycle. Orphan/idle GC at two levels (manager owns all squads; `SquadCombatJob` gains a `Recall` terminal state).
- **Tactics** (computed once by the manager, executed by the FSM through the one guarded sink): authoritative focus-fire from the whole-squad centroid + a manager-supplied ranked fallback (out-DPS the *aggregate* enemy heal); kiting orders (`Engage`/`Kite`/`Hold`) in the ordered path; centralized boost-aware heal; engage/disengage with **coupled hysteresis** (no yo-yo). **The concrete behaviors live in [ADR 0008a](../design/0008a-combat-tactics.md)** (the tactics catalog + tunable-params table + experiment register, EXP-*). These are the experimental SHELL: we **iterate on the sim (per-change) and the private server (acceptance) to find effective tactics** — 0008a's experiment register IS the validation loop for workstream G3, run foundations-first (EXP-FOUND/KITE/FOCUS before TOWER/BREACH/COMP/DEF). G3's "done" = the relevant EXP-* gates pass, not a fixed parameter set.

### 1.6 Defense, war supervision, escort, posture hook — [ADR 0008](../design/0008-combat-and-squad-architecture.md) Step 5, [ADR 0014](../design/0014-empire-strategy-and-posture.md), [ADR 0017](../design/0017-threat-aware-expansion-lifecycle.md)

- **Defense converges onto the manager:** `SquadDefenseMission` becomes a `Defend` objective (the dominant cohesion fix — defense is squad-LESS by construction today). Keep the 2026-06-16 `hostile_warrants_defender` body-parts trigger + owned-room-invader rule; preserve the ADR 0017 §13 ownership-subordinate invariant (a `Defend` objective for a room we no longer own is withdrawn immediately).
- **`WarOperation` becomes a supervisor:** withdraw low-value objectives when `max_concurrent_attacks` shrinks (IBEX-028); feed real per-squad spend so the economy-abort fires (IBEX-026); `UnwinnableTarget` backoff.
- **Escort/pre-clear** (ADR 0017 deferred): the claim pipeline declares an `Escort{room}` objective for marginal targets (sizing via `DefenseEscalation::from_threat`).
- **Thin posture hook** (ADR 0014, minimal): player-offense objectives are produced only under a `WarDecl`; NPC policing and reactive defense are posture-independent. The full posture engine is a later phase.

### 1.7 Synchronized spawning — [ADR 0011](../design/0011-spawn-orchestration.md) D3/D4/D9

The `SquadManager` is the **sole combat demand producer** (`GroupId = SquadId`). Align-finish group admission (all members emerge within window W at full TTL; until admitted the group consumes zero lanes — no half-spawned quads idling at rally) + pre-spawn replacement (no renew). Boost-on-spawn handoff (IBEX-027 → **wire** behind the kill-switch): declare boosts from `required_boosts()` → reserve at schedule time → gate deploy on `is_ready`.

### 1.8 Explicitly OUT of Phase 2

- The **full posture/allocator engine** (ADR 0014 §2–3 — CPU capacity model, marginal-surplus arbitration): only the thin `WarDecl` gate (§1.6) lands here.
- The **data-driven FSM rewrite** (ADR 0003 §A, Inc 6) — the combat job stays `screeps-machine`; only the *tactical decision extraction* (§1.2) and the `Recall` state land. The combat sim recorder/replay is the combat-scoped early realization of the F4 recorder; the full GameView-real recorder stays on its Inc-6 schedule for the HaulJob pilot.
- The **holistic serialization pass** (version headers / migration / fuzz, Inc 7): combat shape changes ride the reset-anytime policy (loud reset, `WORLD_FORMAT_VERSION` bump).
- **Carried-but-separable from Phase 1** (non-combat; may ride opportunistically or slip without harm): D1/D2 (0007 `TransferSnapshot` + governor-gated re-match), B5 remainder (route-cache warm / cost-matrix TTL), Critical-tier shed validation (needs a harder burn calibration). The Inc-2 serialization-tail remnant (loud decode-reset, watermark→seg-57, IBEX-049) is small and already substantially live; IBEX-049 (`#[serde(skip)]` on regenerable `CreepPathData.path`) rides P2.I2.
- **`pull`-based under-MOVE compositions** (P2.M4) are optional and only built if/when such bodies are fielded.

---

## 2. Execution — work tracking (living)

> **▶ RESUME POINT (2026-06-18, after P2.G4-O6) — SNAPSHOT.** The live resume point + ordered remaining-work plan now live in the master doc ([`../plans/combat-overhaul-plan.md`](../plans/combat-overhaul-plan.md) §4–5); this is the dated snapshot at the current point, kept with the §2.0 detail below.
>
> **Where we are.** Combat overhaul slice on `master` (Docker WFV 12, reset clean, ticking, no thrash): **G1/G2/G3/G3-tail** + **K2c/K3/K5** + **G4-defense (legacy `SquadDefenseMission` removed; all defense → `Defend` objectives; W7N1 thrash fixed)** + **G4-O1 (anchor mover wired for cohesive formation travel)** + **G4-O2 (formation orientation)** + **G4-O3 (layered dismantle: breach the rampart shielding the target first; `room_grid_dijkstra` moved into rover)** + **G4-O4 (manager wave-retry + `mark_unwinnable` backoff; Defend exempt)** + **G4-O6 (offense producers — single-squad migration DONE: `InvaderCore`→`Dismantle`, `AttackFlag`→`Secure`, `ResourceDenial`→`Harass`; `InvaderCreeps` reconciled into remote-defense `Defend`; unified offense cap; deployed to Docker)**. **Host tests green (bot 160, rover 27, decision 41), wasm + clippy clean.** **Offense legacy (`AttackMission`/`AttackOperation`) NOT removed yet** — sequenced in the [master plan doc](../plans/combat-overhaul-plan.md) §5; O1–O4 + **O6 done**, **O5 is next** (power-bank as `Farm{PowerBank}`), then **O7** (delete the legacy, soak-gated incl. the deferred heavy multi-squad assault + power-bank). O1 travel + O2 orient + O3 breach are now **wired to live consumers** via O6 (a manager siege/secure/harass squad fields against an invader core / attack flag / hostile player remote) — behavior validates on the soak the next time such a target appears (world peaceful right after the WFV-12 reset).
>
> **Do next, in order:**
> 1. **Docker soak (running now).** Validate under a real threat: (a) defense — owned-room/defend-flag/remote-invader threats each field a manager `Defend` squad with G3 tactics, retiring when clear, W7N1 thrash gone; (b) **O1 — squads travel to the target room as a cohesive formation** (the anchor mover) instead of trickling in, then kite when engaged; (c) **K5** — stronghold in a farmed SK room → duo recalled, mining halts, resumes after clear. (Right after the WFV-12 reset the world is peaceful, so threats must arise/be introduced to exercise combat.) Combat flags are structurally on (`manager_defense` removed; `source_keeper.farming` default true). MMO also runs combat ON (earlier deploy) — monitor it too.
> 2. **G4 offense — continue the [master plan doc](../plans/combat-overhaul-plan.md) §5: O5 next (reordered after O6 — operator 2026-06-18).** ~~O1 anchor~~ ~~O2 orientation~~ ~~O3 breach~~ ~~O4 wave-retry/unwinnable~~ ~~O6 offense producers (single-squad)~~ (done) → **O5** = power-bank as `Farm{PowerBank}` + bespoke dropped-power haul → **O7** = delete `AttackMission`/`AttackOperation`/`AttackReason` (soak-gated, incl. the deferred heavy multi-squad assault). O6 overall = `WarOperation::run_offense_evaluation` upserts `Secure`/`Dismantle`/`Harass` objectives (single-squad cases first: `InvaderCore`/`InvaderCreeps`/`Flag`/`ResourceDenial`/`ProactiveDefense`; reconcile `InvaderCreeps` with the remote-defense `Defend`; **defer the heavy multi-squad drain+quad assault** — it doesn't fit one-squad-per-objective). **The live consumer + Docker validation gate for O1/O2/O3** (inert until now). → **O5 power-bank** (DEFERRED to the O6/O7 window: its haul needs a bespoke dropped-power collector that lives only in `AttackMission::Exploiting` (deleted O7); niche + hard to soak-test; crack-alone would regress) → **O7 delete `AttackMission`/`AttackOperation`/`AttackReason` + variants (WFV bump), soak-gated** (also gated on the deferred heavy-assault + power-bank paths). This is the rest of "no legacy combat mission code remains."
> 4. **L1 cross-room evacuation follow-up** (ADR 0008 / master plan doc §5 L1): in-flight creeps can't flee *across* a room boundary; K5 made it a concrete trigger. (Overlaps O1 — a squad-level multi-room mover helps both.)
> 5. **W2/W3/W4** — `WarOperation`→supervisor (withdraw on `max_concurrent_attacks` shrink, real per-squad spend → economy-abort), `Escort` objective (claim pre-clear), thin `WarDecl` posture hook.
> 6. **T-POS attack-positioning** — reuse the scored search for an engage-anchor (master plan doc §5 T-POS).
> 7. **K4 — SK mineral** — deferred (remote extractor/container construction + mineral-glut predicate; §2.0 (ai)).

### 2.0 Status log (newest first)

- **2026-06-23 (cf)** — **MMO-deploy roadmap locked + P1 healer heal-coverage positioning (ADR 0019 §8) LANDED (host-green).** Resumed toward the MMO combat deploy. An `ultracode` mapping workflow (5 readers over force-composition / target-selection / EV-Lanchester engine / healer-positioning / multi-squad-identity) grounded a phased roadmap; operator picked **force-intelligence-first** + **sizing-solver depth**. **Sequence: P1 healer §8 → P2 EV force-sizing solver (winnability oracle replacing the tower-count proxy + composition sizing + tower-drain EV; defender-profile-driven so it scales to players) → P3 watch a winnable clear (W7N5 L1) → P4 WFV re-proof + hygiene + go-ahead → MMO deploy v1; P5 fast-follow = identity I1/I2 + multi-squad G4-HEAVY + S5 blob auction + S6 adaptivity.** **⚠ STALE-WFV FOUND:** `WORLD_FORMAT_VERSION` is **14** (bumped 13→14 by `b29224f`, the ADR 0009c road planner — non-combat), so ADR 0020 §10/§11 + plan §7 WFV-13 claims + the `3290c86..HEAD` proof are stale → corrected in all three (re-prove from `b29224f..HEAD` at P4). **P1 (ADR 0019 §8, see §8.1):** the squad healer now picks its own tile by **heal-coverage vs danger** (the principled replacement for the stopgap escort) — `kite.rs` gains `w_heal` + `KiteScoreParams::healer()` + `AllyNeed` + a HEAL-COVERAGE term in `score_tile` (Σ ally need × heal-efficacy(range), subtracted like `w_dmg`); `lib.rs` computes each pure-support healer's goal via `plan_kite_anchor` (terrain-aware flood + shared threat field + survival veto — NOT the terrain-blind `decide_movement`) into the new transient `SquadDecision.member_goals`; `squad_manager` stamps it as that member's `Advance{range:0}` directive (anchorless path only → SK/skirmish duo; siege keeps healers-back slots). EXP-HEAL-POS = two `kite.rs` term tests (covers-2-vs-0 + backs-off-danger-on-tie). decision **71** / eval+agent **18** / ibex **157**; check-wasm + clippy-wasm clean; **no WFV bump** (all transient). Live verification folds into the P3 soak (needs an engaged squad). **Next: P2 EV force-sizing solver — full design + roadmap captured in [ADR 0020 §12](../design/0020-ev-adaptive-blob-combat.md) (narrows 0020-S5 to the sizing oracle; WFV 14→15 folds into the deploy reset; rampart breach-cost counts ONLY breach-corridor blockers + tower-guarding ramparts via `breach_path_blockers`, never a room-wide sum).**
- **2026-06-23 (ce)** — **Combat live soak = 6-bug hunt (squad combat now runs end-to-end on the private server).** Continuing the offense soak, root-caused + fixed (all deployed, host 157 green, clippy+wasm clean, no WFV bump): **`aaac0f7`** squad move-to-room border thrash (`tick_move_to_room` used a cross-room `in_range_to(center)` false-arrival → MoveToRoom↔Engaged 20-transition loop, never moved); **`e6daacb`** squad Retreating↔Engaged ping-pong vs an unwinnable target (Retreating re-engaged on HP>80% ignoring the squad retreat signal); **`8295ea9`** winnability gate (offense produced a siege-quad Dismantle for ANY invader core ignoring towers → squad melted on entry to a level-5 stronghold; now skips towers > `MAX_SINGLE_SQUAD_STRONGHOLD_TOWERS=1`, the deferred G4-HEAVY case); **`9a9c259`** healer escorts the attacker proactively (was: only chased *damaged* allies → lagged until the attacker took fire). Earlier same arc: `1aee1ac` auth-mod pin, `97f4e91`+`22398a4`+`70013cd` serialization dangling-ref + backstop + `-g` backtraces (entry cd). **Environment unblock (docs):** private-server neutral rooms default `active=false` → frozen (keepers/cores/ghost creeps); fix = set all active + RESTART (`docs/guides/private-server-commands.md`). **The offense pipeline now works end-to-end: rooms process → cores activate → Dismantle objectives → correct skip of unwinnable strongholds → no FSM loops → healer travels with its attacker. RESUME CHECKPOINT + open items + the healer heal-coverage increment (§8) = [ADR 0019](../design/0019-combat-position-selection.md) §8/§9.** Open: implement §8 (healer heal-coverage objective; stopgap escort shipped), watch a winnable clear (point at W7N5 L1), extend EV/Lanchester to target selection + G4-HEAVY.
- **2026-06-22 (cd)** — **Serialization dangling-ref panic: root-caused + fixed + backstop hardened + named wasm backtraces (found via the offense soak).** Driving a live offense soak, the bot hit a serialize-time panic LOOP: `specs ConvertSaveload for Entity` → `func(entity).unwrap()` on a missing marker (saveload/mod.rs:182), halt→recover→re-panic (~21 CPU/tick). **Root cause (`97f4e91`):** `WaitForSpawnSystem` deleted a failed spawning creep via a direct `entities.delete()`, bypassing `EntityCleanupSystem` (the only path that strips a creep's `Entity` from missions/`SquadContext` before deletion) → orphaned ref panics at serialize. Fix routes it through `EntityCleanupQueue` (cleanup prepass runs before serialize); it was the only `entities.delete()` outside cleanup.rs and **self-healed the bricked world on deploy**. **Backstop (`22398a4`):** `repair_entity_integrity` didn't scrub mission CREEP lists (`Mission::repair_entity_refs` defaults to no-op; only `claim` overrode it). Added `Mission::get_creeps()` + a generic scrub-and-log pass + `get_creeps` in the 10 creep-tracking missions → opaque specs panic becomes a logged auto-repair naming entity+mission. **Diagnostics (`70013cd`):** `panic=abort` on wasm32 (no std backtrace) + release `wasm-opt` stripped the name section → no usable stack. Enabled `wasm-opt -g`: +0.70 MiB (3.38→4.08 MiB, < 5 MiB limit) → FULLY NAMED Rust backtraces (verified live); wasm gzips 4:1 → compression is the headroom follow-up. Host 157 green, clippy+check-wasm clean, no WFV bump, deployed. **Live repro+fix of the operator-observed "brittle serialization / ECS dangling refs" class (validates P2.I1/I2).** Caveat: abrupt multi-level RCL DB-bumps (soak fast-forward) triggered the spawn cancellation that exposed it + wedged the colony spawn (see `docs/guides/private-server-commands.md`); bump RCL once/early.
- **2026-06-19 (cc)** — **Combat deployability assessment → ADR 0020 §10 (path to MMO deploy) + §11 (remaining-work/resume inventory).** An `ultracode` workflow (5 surveys → synthesis → fact-verifying critique) assessed what gets us to a live MMO deploy. **VERDICT: GO, soon, defense-only-first** — the current combat (ADR 0019 Stages 0–3 + ADR 0020 steps 1–4) is the minimum viable state (strictly better than the pre-overhaul baseline that was already live, host+sim green), and the hard-blocker set has **no combat-redesign items, only deploy hygiene**: (1) WFV — **VERIFIED SAFE** (`git diff 3290c86..HEAD -- screeps-ibex/src` = only `squad_combat.rs`+`squad_manager.rs`, zero serde shape changes → no bump; MMO server <13 = one intended loud reset); (2) keep `MAX_CONCURRENT_SQUADS` static 4 (don't ship the dynamic cap); (3) operator go-ahead + seg-57 canary emitting. Path: pre-flight green → Docker soak A–D (force combat — the tactics have NEVER fired live; every capture hit a peaceful window) → `screeps-pack check`/`--dryrun` → operator go-ahead → **defense-only single MMO surface, watch the cohesion canary** → widen. Off-ramp = `military.*` posture + `raid`/`dismantle`/`farming` kill-switches (no redeploy needed). Critique fixes folded in: use the git-diff WFV proof (not grep); the "dangling-ref counter" telemetry doesn't exist yet (I2 unstarted) → watch aliasing *symptoms*. Full 21-row resume inventory in ADR 0020 §11 (next build entry point = step 5, gated on defining the cross-goal EV currency first). **ADR 0020 steps 1–4 done; remaining work captured for resume.**
- **2026-06-19 (cb)** — **ADR 0020 step 4 FINISHED — bed basket + meta-Nash + EV-per-CPU gate (`6ea835b`).** Gave the tournament teeth: matches now play over a **bed basket** (open field, wall corridor, mutual tower crossfire), which makes strategies actually diverge (the lone open bed was low-decisiveness). Added the **meta-Nash mixed strategy** (fictitious play — the robust mix, bridge to step-6 adaptivity) and an **EV-per-CPU-at-large-N gate** (10v10 managed self-play bounded ~1.7 ms/squad-tick — guards the blob regime). **FINDING (tournament-backed):** the basket discriminates (default exploitability 0→90), and the **"cautious" engage preset (`w_taken 1.5, w_dmg 1.0`) DOMINATES** the field (mean +66, Nash 1.00) while the shipped default (`w_taken 0.5, w_dmg 2.0`) sits mid-pack (−20, beaten by 90 net HP) — the default over-weights damage vs safety. The robustness gate still PASSES (90 ≪ gross 1500: no hard counter), so this is a tuning LEAD, not a forced retune — adopting cautious globally off a 3-bed all-ranged basket risks overfit; broaden the basket (melee/mixed/asymmetric objective) or let the adaptivity layer pick the mix. eval **18**; clippy clean; no WFV bump. **ADR 0020 steps 1–4 now done; remaining: step 5 blob auction + soft cohesion, step 6 archetype + mixed-strategy adaptivity (consumes the Nash mix), step 7 adversarial room-gen; + the cautious-engage retune lead and per-archetype n / boosted-TOUGH.**
- **2026-06-19 (ca)** — **ADR 0020 step 4 — self-play tournament + exploitability ship-gate, v1 (`b73e344`).** `screeps-combat-eval/tournament.rs`: generalizes the single-bed sweep into a population round-robin — every strategy (a `SquadTacticParams`: default + aggressive/cautious/kite-heavy/advance-heavy) plays every other in symmetric self-play (both sides, side-bias cancelled), scored by net-HP, into an antisymmetric `PayoffMatrix` + zero-sum mean-payoff ranking. The headline is **exploitability** (largest margin any strategy beats a candidate by) as the ship-gate: the shipped default is currently exploitability 0 (no hard counter in the field) — a far more robust signal than single-bed dominance, targeting "adaptive, not counterable". `TournamentBudget` Quick/Thorough tier (operator §8.4). `run_managed`/`ranged_file` → `pub(crate)`. +2 tests (matrix antisymmetric + zero-sum; default not grossly exploitable). **HONEST LIMIT:** the v1 open-field ranged bed is low-decisiveness (payoffs ±3), so the gate only bites once a discriminating **bed basket** (corridor / tower-pressure / asymmetric attacker-vs-defender) is added — that + Elo/meta-Nash + scripted archetypes vs the managed squad + EV-per-CPU-at-large-N are the step-4 enrichment. eval **17**; clippy clean; no WFV bump.
- **2026-06-19 (bz)** — **ADR 0020 EV combat — steps 1–3 LANDED (rampart fidelity, EV target/kill calc + spill, Lanchester engage gate).** The research-driven EV combat overhaul (ADR 0020) is being implemented after the operator interview resolved its open questions. **Keystone (`0d262c8`):** fixed the combat-engine sim's rampart single-target damage REDIRECT (it matched neither the engine nor our own `engine-mechanics.md` §2.6 — siege tactics were tuned against a fantasy where rampart defenders die to direct fire). **EV target selection + damage spill (`2256c9f`/`fa2620d`/`2d095b0`):** `select_focus_target` is now `argmax threat/ttk` over KILLABLE hostiles (discard out-healed + rampart-shielded → fixes the dogpile-a-sheltered-healer bait); kill budget = `hits + maximum same-tick heal` (creep healers **and energized hostile towers** — the engine nets damage→heal→death, `tick.js:120-136`); `assign_focus_fire` spills overkill across targets so combined fire doesn't over-damage one creep (symmetric to deficit-capped `assign_heals`); `CombatStructureDto.energy` gates tower heal AND threat-field tower damage (a drained tower does neither). **Lanchester engage/retreat gate (`eabaed6`):** replaced flat-HP retreat with `assess_engage` — fighting-strength μ over killable enemies (integer n=2; parity-safe) + the operator's **retreat-is-LOSS** rule (unwinnable iff damage we can't kill-or-out-heal > our heal/tick, so a tank+heal siege out-healing towers DISMANTLES instead of retreating; an outgunned squad retreats at full HP) + a **safeMode engage-veto** (`SquadView.enemy_safe_mode`, both adapters). The Lanchester gate also gave the weight-sweep real signal (no longer flat). decision **69** / engine 40 / rover 31 / agent 35 / eval 15; bot check + check-wasm + clippy clean; no WFV bump; host-validated, not deployed. **Remaining ADR 0020: tournament/exploiter gate (step 4), blob auction + soft cohesion (step 5), archetype + mixed-strategy adaptivity (step 6), adversarial room-gen (step 7); plus per-archetype n + boosted-TOUGH tank reduction.**
- **2026-06-19 (bx)** — **ADR 0019 Stage 4 STARTED — tunable seam + managed-squad EXP scenarios + weight-sweep tuning loop (`08c2331`, `f3b8ca2`, `6f59b82`).** Measure-first tuning of the now-landed position utility. **Seam:** `decide_squad_with_pathing` gained `SquadTacticParams { kite, engage }` (`Default` = shipped presets) so the EXP loop sweeps weights without touching the live presets (live + tests pass `Default`; `ManagedSimSquad::with_tactics` injects for the sweep). **Scenarios:** a `run_managed` runner (steps ≥2 managed squads head-to-head, merging intents, + drives hostile towers via the now-`pub` scripted controller) + two register experiments on the *managed* (decide_squad_with_pathing) path — **EXP-POS-SELFPLAY-1** (two managed ranged squads close + fight cohesively, spread ≤6) and **EXP-POS-KITE-1** (a managed ranged squad kites + kills a melee squad). Register now 10 experiments. **Sweep:** a grid over the kite preset's `w_future`/`w_prox` scored by the continuous net-HP exchange, run as a standing regression guard (trips if a scorer change leaves the default dominated). **Findings:** (a) the sweep is **FLAT** on the melee beds (open/slow/healing-bruiser) — the outcome is invariant to these weights (the utility isn't brittle in a melee standoff); the tuning *gain* lives in a terrain-/tower-pressure bed (damage-taken continuous in position) — Stage-4 follow-up; (b) EXP-POS-SELFPLAY is low-casualty (ranged squads reposition rather than sustaining range-3 trade — an **engage-stickiness** tuning target). decision 59 / agent 35 / eval **15**; bot check + check-wasm + clippy clean; no WFV bump; host-validated, not deployed. **Remaining Stage 4 (opportunistic): boosted-TOUGH net-damage conversion in `ThreatField` (needs a boost field on the body DTO; v1 is safe-conservative), a weight-discriminating sweep bed, the engage-stickiness fix.**
- **2026-06-19 (bw)** — **ADR 0019 Stage 3b COMPLETE — focus_damage richness + default-on gate + advance-to-damage layer + EXP-COHESION; #6b goal-latch removed (`717dd2a`, `93c2063`, `c4a5266`).** Closed out the last four Stage-3b items. **focus_damage richness:** the engage DMG reward is now the *net hits actually landed* on the focus — a convex blend of EFFECTIVENESS (net/max-output, pulls a melee block to range 1) and KILL-PRIORITY (net/focus_hits, commit to a near-dead focus), 0 when out-healed (→ safety disengages); `SquadMemberView` gains melee/ranged power, `FocusDamage` carries the focus's hits + nearby enemy heal (`None` ⇒ flat in-`r*` reward, tests/sim byte-identical). **default-on gate:** the unified utility is the live default (no kill-switch); the CPU bench is the standing regression gate — a generous 5 ms/block-tick budget (non-flaky across debug/release) for BOTH the per-squad and build-once-per-room paths + a behaviour-preserving plan-count check (sharing speedup ~1.47×). **EXP-COHESION-1 (full-sim):** a managed ranged squad threads a wall-gap corridor toward a melee keeper, focus-fires it dead, and survives (worst pairwise spread 2 / 3 survivors) — registered in the EXP-* harness (8 experiments). It surfaced that the engage search, a *bounded* flood, stalled short of a far focus → replaced the would-be approach/position branch with a **dominant proximity layer** (engage `w_prox` 0.4→1.5; proximity *is* the advance-to-damage layer and is 0 inside `r*`, so ONE search marches AND positions), and broke the proximity drift with a **perpendicular-deviation tie-break** off the centroid→focus line (`5e0fc68`): proximity distance stays **Chebyshev** — Screeps charges equal diagonal/cardinal cost, so that's the true distance; an interim euclidean magnitude was wrong (it mis-priced diagonals and only beelined for an axis-aligned focus), replaced by the capped, low-weighted perpendicular tie-break that beelines along the real approach line at any angle. **#6b goal-latch was REMOVED, not added** (operator challenge: "do we not want to compute the optimal each tick?"): disabling it left every kiting/cohesion scenario passing unchanged (no observed oscillation), it never saved CPU (the full search runs each tick regardless), and it caused a bug (stickiness pinned the advance two tiles short) — the **deterministic tie-break (#6a)** is the real, kept oscillation guard (the search heap is a total order on `(g,x,y)` → identical inputs yield the identical goal). decision **59** / engine 40 / rover 31 / agent **35** / eval **14**; bot check + check-wasm + clippy clean; no WFV bump; host-validated, not deployed. **Stage 3b is complete; ADR 0019 fully landed (host-validated).**
- **2026-06-19 (bv)** — **ADR 0019 Stage 3b — build PositionLayers once per room, share across squads (`4185aef`).** The layer architecture's perf payoff: the integer `ThreatField` + the reachability floods depend only on a room's enemies, not on which squad is asking — so build them ONCE per target room and share across every squad engaging there. `plan_kite_anchor` / `decide_squad_with_pathing` gained `shared: Option<&PositionLayers>` (`Some` → reuse the shared layers; `None` → build inline = the unchanged single-squad path); new `build_room_layers()` pub helper. The live `SquadManager` (`compute_squad_orders`) now holds a per-tick `HashMap<RoomName, (LocalCostMatrix, PositionLayers)>` cache keyed on `target_room` (get-or-build); `build_target_matrix()` extracted so the layer build + the kite search share ONE terrain-walled matrix. Bench + agent sim pass `None` (single-squad — built inline as before). This recovers the per-squad field-build regression from (bt) for the multi-squad co-located case the bench models. All crates green: engine **40** / rover **31** / decision **55** / agent **35** / eval **14**; `cargo check` + `check-wasm` clean; no WFV bump; host-validated, not deployed. **Remaining Stage 3b: `focus_damage` actual-hits richness (RMA/kill-priority), #6b goal-latching, default-on gating, full sim EXP-COHESION.**
- **2026-06-19 (bu)** — **ADR 0019 Stage 3b — the FINAL normalized position utility (`992b191`); must-fixes #1/#2/#3 complete.** `score_tile` is now the complete ADR §1.2 unified utility: every term normalized to `[0, SCALE=1000]` (weights are dimensionless mixing ratios), so flee (kite preset) vs stand (engage preset) emerge from ONE function by reweighting. Terms: **TAKEN** = the integer `ThreatField`'s net incoming hits per most-fragile-member HP (**#2 fragile-θ + #3 normalized + #1 integer**) — so RANGED threats + TOWERS now shape positioning (the reach-depth proxy gave ranged `reach 0` → it ignored ranged fire entirely); **FUTURE** (reachability); **COHESION** (wall-aware `g`, steepened); **PROXIMITY** (focus beyond `r*`); **OPENNESS**; **EDGE** (anti-corner for the scored path, complementing the per-creep flee repulsors); **DMG** reward (in `r*` of the focus). **Weapon-range `r*`** parameterized on `SquadKiteView` (3 ranged / 1 melee, threaded from the squad) → melee/siege blocks close to 1, ranged hold 3. `KiteScoreParams` renamed to the term set + presets re-tuned to the normalized scale. **`FUTURE_HORIZON` 5→3** so a kiter HOLDS weapon range (durable standoff at `r*`) rather than fleeing past shooting range (the bug the rewrite surfaced + fixed). No-field path keeps a normalized reach-depth+tower proxy for term-isolation tests. **All six must-fixes now satisfied: #1 integer / #2 per-block-θ / #3 normalized (this) + #4 survival veto + #5 seed filter + #6a deterministic tie-break.** CPU bench ~868 µs/block-tick (100 plans). decision **55** / engine 40 / rover 31 / agent 35 / eval 14; clippy + bot wasm clean; no WFV bump; host-validated, not deployed. **Remaining Stage 3b: focus_damage actual-hits richness (RMA/kill-priority), build-once-per-room sharing (perf), #6b goal-latching, default-on gating, full sim EXP-COHESION.**
- **2026-06-19 (bt)** — **ADR 0019 Stage 3b richer layer 1 — integer ThreatField + survival veto #4 (`f96748b`).** First richer layer: **`ThreatField`** = raw incoming hits/tile (`i32`), stamped from enemy creeps (melee range 1 / ranged range 3, via new `KiteThreat.attack_power`/`ranged_power` computed in `kite_threats` from the body) + towers (range falloff, per-range LUT). A cached layer in `PositionLayers` (built once, read by the veto now + the normalized safety term next). v1 stamps **unboosted** output (boosted-TOUGH reduction is a follow-up — correct for the common case, conservative for a boosted TOUGH defender). **Survival veto (#4 / Guard 4):** `plan_kite_anchor` prices a tile by `LETHAL_TILE_PENALTY` if its net incoming (`raw − squad_heal`) would kill the most-fragile member in `< SURVIVAL_HORIZON=3` ticks → the squad never positions onto a suicide tile (even ENGAGE) unless every reachable tile is lethal (best-effort least-bad). `SquadKiteView` gains `fragile_hits`+`squad_heal` (#2, from the squad's members). +3 tests (field stamps creep footprints + tower falloff; veto flees a lethal *ranged* zone where the reach-0 safety term wouldn't — isolating the veto's value). decision **55** / agent 35 / eval 14; clippy + bot wasm clean; no WFV bump. **CPU bench 950 → 1042 µs/block-tick** (the per-squad field build; the **build-once-per-room sharing** is the perf follow-up that recovers it for multi-squad — the layer architecture's intent). **Remaining richer layers: normalized integer safety term reading the field (#1/#3 + #2 full, replaces reach-depth), `focus_damage` layer (actual hits), build-once-per-room sharing, weapon-range r*, #6b goal-latching, default-on gating, full sim EXP-COHESION.**
- **2026-06-19 (bs)** — **ADR 0019 Stage 3b must-fixes assessed — #5 + #6 already satisfied (verified + tested); #1–4 fold into the richer layers (`ab3e3de`).** Checked the 6 default-on must-fixes against the code rather than assuming work: **#5 seed filter DONE** (`kite_threats` skips harmless creeps + `threat_step_ticks` = `ceil(weight/move)`/`None`-for-no-MOVE filters immobile → only mobile attack-capable chasers seed the reachability flood; +test for the cadence/immobile model). **#6 deterministic tie-break DONE** (rover's heap is a total order on `(g,x,y)` + the best-tile update is on strict `<` → ties resolve to the closest-to-origin tile, also anti-oscillation, no RNG; +determinism test: identical input → identical goal). **#1 integer field / #2 per-block θ / #3 normalized terms / #4 survival veto are COUPLED to the richer ACTUAL-HITS ThreatField** — they ARE the signed-normalized-integer-utility-over-actual-hits, so they land WITH the richer layers (on the reach-depth proxy they'd use arbitrary normalization refs + a body-agnostic veto, both redone). **#6 goal-latching** (commit N ticks) is a focused live follow-up (ephemeral `#[serde(skip)]` on `SquadContext`, no WFV bump); the deterministic tie-break already covers core determinism. decision **53**; clippy clean. **Next (operator-sequenced): the richer layers — integer `ThreatField` + `focus_damage` (actual hits) — with must-fixes #1–4 baked in, then weapon-range r* + goal-latching + default-on gating.**
- **2026-06-19 (br)** — **ADR 0019 Stage 3b — live engage positioning wired (`01823d6`).** `decide_squad_with_pathing` (THE live `SquadManager` decision) gained an **ENGAGE branch**: a ranged squad engaged with a creep focus and not kiting now runs the scored search with `KiteScoreParams::engage()` → picks the EV-optimal engagement tile (in weapon range, minimal threat, cohesive) → `Advance{range:0}` onto it (or `Hold` if the centroid is already optimal), instead of a naive straight-line `Advance` to the focus. **Mutually exclusive with the kite branch** (one scored search either way → per-tick cost unchanged for a fighting squad); reuses the same machinery so flee (kite preset) vs stand (engage preset) differ only by weights. Ranged-only + creep-focus scoped (melee/siege keep the range-1 Advance; structure/breach keeps the breach Advance; weapon-range `r*` for melee is a follow-up). **This is the live wiring — the manager already calls this fn, so engaged squads get EV positioning now.** +1 test (engaged ranged squad → engage `Advance{range:0}` onto an in-weapon-range tile). decision **51** / agent 35 / eval 14; clippy + bot wasm-check clean; no WFV bump. Not deployed; the default-on MMO gate (CPU bench pass + 6 must-fixes) remains. **Remaining Stage 3b: richer integer ThreatField + focus_damage layers (actual hits), weapon-range r* for melee engage, the 6 must-fixes, default-on gating, full sim EXP-COHESION, optional perf (snapshot-once / matrix dedup).**
- **2026-06-19 (bq)** — **ADR 0019 Stage 3b — flee vs stand from ONE scorer (the unification thesis, demonstrated) (`e7a7ad9`).** `score_tile` gained a **focus-engagement reward** (negative cost for a tile within weapon range of the focus) + `KiteScoreParams::engage()` (the same scorer reweighted: safety no longer dominates, focus-damage does, kite-away future term off). So **flee** (kite preset → safety dominates → flee to safety) and **stand/close** (engage preset → focus-damage dominates → commit to the fight) emerge from one `score_tile` purely by the objective weights — directly answering the operator's "generalize flee/stand/close into one objective-aware decision." `w_focus_dmg` defaults 0 ⇒ kite/retreat **byte-identical** (term ×0). +1 test (objective weights flip flee↔stand: kite prefers the safe out-of-range tile, engage prefers the in-range threatened tile). decision **50** / agent 35 / eval 14; clippy + bot wasm-check clean; no WFV bump. **Remaining Stage 3b (default-ON, gated): live-wire an Engage objective (SquadManager picks engage params for offensive objectives + a plan_engage path reusing PositionLayers), the richer integer ThreatField + focus_damage layers (actual hits not just in-range), the 6 must-fixes, default-on gating on the CPU bench, full sim EXP-COHESION; optional perf (snapshot-once / matrix-clone dedup).**
- **2026-06-19 (bp)** — **ADR 0019 Stage 3b started — CPU bench (the default-on gate) + flood-dedup (`8121b81`, rover `f83b339`, `0b38863`).** **CPU bench (`screeps-combat-eval::bench`):** the compound worst case the ADR demands before wiring the utility — open 50×50 room (floods settle full max_ops), 6 towers, 5 melee + 5 ranged mobile chasers, 4 converging blocks each running `plan_kite_anchor`. Baseline **~1121 µs/block-tick** (4 blocks × 25 ticks); at ≤4 squads ≈ 4.4ms native → ~9–22ms wasm worst case, confirming the perf-MF work must precede adding layers. Structural assert (all blocks plan every tick) + loose death-spiral time guard. **Flood-dedup:** `LocalPathfinder::run`/`search_scored` now thread the tile's settled path-cost `g` into the cost fn (rover); cohesion reads the kite search's own `g` (= wall-aware distance-from-centroid) instead of a **separate centroid flood** — `PositionLayers` keeps only the shared threat flood, `plan_kite_anchor` threads per-tile `g` as `KiteFields.cohesion_dist`. Byte-identical (search g == the old centroid-flood g): all plan/kite tests pass; resolves the Stage-2-tail redundancy. **Bench: 1121 → 950 µs/block-tick (~15%, 3 floods → 2).** Gates: rover **31** / decision **49** / agent 35 / eval **14**; clippy + bot wasm-check clean; no WFV bump. **Remaining Stage 3b (default-ON, gated): integer `ThreatField` + `focus_damage` layers, signed normalized `position_utility`, `plan_engage_anchor` (attack-positioning) reusing `PositionLayers`, the 6 must-fixes, full sim EXP-COHESION; optional further perf (snapshot-once / matrix-clone dedup, the next bench targets).**
- **2026-06-19 (bo)** — **ADR 0019 Stage 3a DONE — `PositionLayers` cache (operator's layer architecture) (`28d6540`).** Each score term is now a per-(room,tick) cached LAYER; the expensive shared floods (threat-chaser reachability + centroid-distance) live in a `PositionLayers` struct built **once** via `PositionLayers::build` (use-agnostic: centroid + threats + matrix, not a kite-specific view) and reused by every position consumer this tick via `.fields()` — so a kite scorer and a future attack-positioning scorer borrow the SAME instance, floods never run twice (the operator's point: build once, reweight per use). `plan_kite_anchor` refactored to build it once instead of two ad-hoc floods → **byte-identical** (same maps; all plan/kite tests pass). +1 reuse test (build once, two weightings off the same cached layers → different scores). Resolves the Stage-2-tail centroid-flood redundancy. decision **49** / agent 35 / eval 13; clippy + bot wasm-check clean; no WFV bump. **Stage 3b remaining (default-ON, gated, separate approval):** build the compound-worst-case CPU sim bench FIRST, then add the integer `ThreatField` + `focus_damage` as further layers, the signed normalized `position_utility`, `plan_engage_anchor` (attack-positioning, T-POS) reusing the same `PositionLayers`, the 6 must-fixes, and a full sim EXP-COHESION.
- **2026-06-19 (bn)** — **ADR 0019 Stage 2-tail behavior — wall-aware (g-cost) cohesion (`9d8d1f6`).** Cohesion now uses true path-distance from the centroid (a second `reachability_from` flood, `step_ticks=1` so g = tiles) instead of Chebyshev: a tile reachable only by routing around a wall is correctly "far" and won't split the block. Strict improvement — identical to Chebyshev in open terrain (8-dir uniform steps), differs only near walls; unreachable tiles get a room-scale penalty. Bundled the shared maps into `KiteFields { threat_reach, centroid_reach }`; `score_tile` takes `&KiteFields` (all-None ⇒ byte-identical pre-Stage-2). +1 test (behind-wall tile penalized; open == Chebyshev) → decision **48**. agent 35 / eval 13; clippy + bot wasm-check clean; no WFV bump. **Stage 2-tail remainder FOLDED INTO STAGE 3 (judgment):** the integer `ThreatField` precompute, `boost`+TOUGH/heal net, perf scratch-reuse + matrix-dedup, and a full sim EXP-COHESION are structural/CPU (not behavior) — the precompute is a CPU *regression* for the current single-block case, the net conversion needs the field to be actual-hits, the perf work is premature while not deploying; they land with Stage 3's unified utility + CPU bench where they're exercised + gated together. (Known: the centroid cohesion flood duplicates the search_scored flood from the same origin — a Stage-3 dedup via threading the search g-cost.) **Behavior content of Stage 2 + 2-tail is COMPLETE: future-aware kiting + wall-aware cohesion. Next: Stage 3 (the unified utility — build the compound-worst-case CPU sim bench first, then the integer ThreatField + signed utility + the 6 must-fixes; default-ON gated on the bench), separate approval.**
- **2026-06-19 (bm)** — **ADR 0019 Stage 2 reachability core LANDED — the operator's flood-fill, end-to-end future-aware kiting (`1670723` rover + `1838e1a`).** **2a (rover submodule `1670723`, superproject `ecdb28b`):** `LocalPathfinder::reachability_from` — a multi-source weighted Dijkstra: for every tile, the soonest (in ticks) ANY source can reach it. One heap carries each entry's owning source index so a tile expands at the cadence of the wave that reached it soonest; first-pop settles the per-tile min over all sources. One flood → the whole `ReachabilityMap` regardless of source count, `max_ops`-bounded. v1 uses one cadence over all walkable terrain (terrain-weighted ticks = documented follow-up); captures source-speed + wall-routing. +4 rover tests (single-source cadence, multi-source min, wall detour, off-room no-op) → rover 31. **2b/2c (`1838e1a`):** folded into kite scoring — `KiteThreat.step_ticks` (plain cadence `ceil(weight/move)`, `None`=immobile, computed in `kite_threats` from the body — no adapter change), `plan_kite_anchor` builds the shared reachability map once (mobile-chaser seeds only, Guard 5), and `score_tile` gains a **future-threat term** penalizing low ticks-to-reach (linear within `FUTURE_HORIZON=5`, `w_future=5`) so the kiter favors **durable standoff** over safe-this-tick — fixing 'safe now, caught next tick' and complementing U8-1's anti-cornering. `Option<&ReachabilityMap>`; `None` → byte-identical four-term score. +2 decision tests → 47. **Re-sequenced (Stage 2-tail / Stage 3, non-blocking):** integer `ThreatField` precompute (multi-block CPU win), cohesion→g-cost, `boost` + TOUGH/heal net conversion, perf scratch-reuse + matrix-dedup, EXP-COHESION scenario. **No kill-switch** (operator: not deploying until combat is better → harness-validation suffices). Gates: engine 40 / rover 31 / decision 47 / agent 35 / eval 13; clippy + bot host + wasm-check clean; no WFV bump. **Next: Stage 2-tail (integer ThreatField + g-cost cohesion) or Stage 3 (unified utility) — both return for separate approval.**
- **2026-06-19 (bl)** — **ADR 0019 Stage 0 + Stage 1 core LANDED — unified position-selection foundation (`54cee73`).** Operator approved the deep-dive's recommended first increment (Stage 0+1, the risk-free foundation). **Stage 0:** proved `kite::tower_dps_at_range` bit-identical to `engine::tower_attack_damage_at_range` over all in-room ranges 0..=49 (kite used f32+round, engine f64+floor — but every intermediate value lands on an exact multiple of 30, so they agree; gate passed). **Stage 1 core:** added the `combat-decision → combat-engine` dependency (engine is a pure leaf → no cycle; **bot host + wasm-check both still build** — the engine enters the live bot's wasm graph for the first time and compiles clean), **deleted** the duplicated `tower_dps_at_range`, delegated kite's tower-safety term to the engine curve (the single source of truth). **Byte-identical** — all agent + eval U7 scenarios (EXP-KITE/BREACH/NEST) + kite tests pass unchanged. **Re-sequenced into Stage 2 (with rationale, operator-visible):** the integer `ThreatField` *precompute* + f32→i32 conversion + `boost` on `CombatBodyPart` + trivial-geometry guard — eagerly precomputing 2500 tiles for one ~400-op search is a CPU *regression* until reachability + multi-block sharing amortize it, and the f32→i32 rewrite risks byte-identity for zero Stage-1 gain; the genuine Stage-1 foundation (delete the drift-prone dup + land the engine edge, ADR 0019 §7) shipped cleanly. Gates: engine 40 / decision 46 / agent 35 / eval 13, decision clippy + bot wasm-check clean. No WFV bump; not deployed (byte-identical, host-validated). **Next: ADR 0019 Stage 2 (rover `reachability_from` multi-source flood + integer `ThreatField` precompute + scratch reuse; doubles as U9 cohesion via g-cost) — returns for separate approval behind `features.combat.reachability_cohesion` + a CPU gate.**
- **2026-06-19 (bk)** — **ADR 0019 authored — unified combat position-selection (11-agent ultracode deep-dive).** Operator Q: should flee/stand/close be generalized into one objective-aware optimal-position decision (composition+health, goal, pathfinding cost, threat field, future flood-fill positions)? Workflow `wf_cb3f1a59-240` (6 explorations → synthesis → 3 adversarial reviews → finalized spec, ~744k tokens, every claim verified file:line). **Decision = HYBRID:** one signed INTEGER per-tile utility `U` (flee/stand/close emerge as argmax under objective-selected weights; θ=composition+health parameterizes) + **5 hard guards** (critical-HP flee, cohesion clamp, trivial-geometry short-circuit, survival-horizon veto, reachability non-chaser seed filter). Two shared `[i32;2500]` maps once-per-room: threat field T (stamped, tower curve = engine) + reachability R (multi-source Dijkstra, NEW rover `reachability_from`). Per-block scoring (fragile-member θ for safety). Integer everywhere kills the live/sim parity flake + tie oscillation (the two blocking review findings). SEARCH in rover, pure SCORE in combat-decision, math delegated to engine. **Subsumes U8 kiting + T-POS attack-positioning + remaining U9 position work.** No WFV bump. Full spec `docs/design/0019-combat-position-selection.md`; staged Stage 0→4 each flagged + CPU-gated (Stage 3 default-ON gated on a compound-worst-case sim bench + 6 must-fixes + a tick-global B·max_ops op budget — honoring the recorded CPU-death-spiral failure).
- **2026-06-19 (bj)** — **P2.U8 DONE — single-creep behavior fixes across kiting/focus/heal-self (`d9fc3f8`).** Finished the U8 single-creep set the operator asked for before U9. **U8-2 (heal-self/survivability):** a pure support creep (HEAL + MOVE, no offense) had **no melee evasion** — only ranged creeps kited, so a solo healer walked up to a wounded ally and got cut down (harness-verified: survivors=0, 1260 dmg, dead by t31). The fallback healer branch now evades any **melee-capable** hostile (`has_working(Attack)`) closing within range 2 — edge-aware (reuses U8-1 `kite_repulsors`) — and self-heals as it backs off, only following a wounded ally when unthreatened. Result: survives the full run (survivors=1, ~half the damage, melee-exposure 0.19→0.07). **U8-3 (focus):** the solo no-orders fallback (`fallback_attack`) targeted the lowest-hits hostile, **not** the healer-first priority the coordinated path (`select_focus_target`) uses — so an unmanaged creep chipped the nearest weakling while an enemy healer topped the pack up. New `priority_hostile_within(view, range)` = enemy **healer first** (lowest-hits), else lowest-hits, used in both fallback attack pipelines (melee + ranged single-target; RMA-when-≥3 unchanged). The ordered path's min-hits fallback is intentionally left (its focus already IS the healer; healer-first there would chase a far healer past an adjacent weakling). **Engage-range** reviewed — the existing ranged-hold-at-3 / melee-close-to-1 + the melee-evade buffer is adequate (no change). **U8-1 + the armed-only metric** landed in (bi). Gates: engine 40 / agent 35 / decision **46** (+2: healer-evades, solo-focuses-healer) / eval **13** (+1: healer kites instead of dying), clippy clean; host-only crates. **U8 COMPLETE. Next: U9 (squad behavior — SQUAD-1 = collapse the triple 50/40/80% retreat hardcodes in `squad_combat.rs:184,366,676` onto `decide_squad`'s SquadOrderState; SQUAD-2/3/4 group-up/target-reconcile/heal-assign).** U9 + U-TOWER both touch high-conflict shared files (`combat-decision/lib.rs`, `military/squad_combat.rs`) — serialize.
- **2026-06-19 (bi)** — **P2.U8 STARTED (operator chose single-creep first) — U8-1 edge-aware kiting + the armed-only melee metric (`db37e1f`, `1ba009d`).** **U8-1:** the U5 metric surfaced that a straight-line kiter flees into the room CORNER (flee = maximize distance from the threat → the farthest point is the corner), where at MOVE parity it loses a step against the wall and the chaser closes to melee. Fix = `kite_repulsors(pos, threats)` adds a synthetic repulsor on each room edge the kiter is within `EDGE_AVOID_THRESHOLD=6` of, so the `Flee` goal pushes away from BOTH the threat and the walls → the kiter rounds the corner along the interior. Applied to both kite-flee sites in `decide_movement` (immediate melee-evade + fallback); **byte-identical in open space** (no near edge → threats only); reuses the rover Flee (no rover change, no one-off algorithm — honors [[no-one-off-pathfinding-algorithms]]). Validated: a kiter pinned in the SE corner with a slower armed chaser rounds out to range 3-4 in open space and survives (eval test), vs being pinned at range 1. **Metric refinement (operator: "only track adjacency when the other creep can actually damage it"):** added `CreepFrame.attack_power`/`ranged_power` (computed in `record_tick` from the SimBody working-part power; 0 = disarmed); `metrics::positioning` now counts melee exposure only within range 1 of an enemy with `attack_power>0`. Fixes the inflated reading that motivated U8 (a kiter next to a chaser whose ATTACK parts were shot off read ~0.45 despite 0 damage → now <0.1; regression test pins it). Gates: engine 40 / agent 35 / decision 44 (+3) / eval 12 (+2), clippy clean. **Next U8:** rest of SINGLE-* (focus/heal-self/engage-range), then U9 (squad, SQUAD-1 retreat collapse) / U-TOWER — confirm order with operator (both touch high-conflict shared files).
- **2026-06-19 (bh)** — **P2.U3–U7 DONE — the harness-expansion FOUNDATION is complete (`8353afa`, `24bfe7c`, `1da8d4f`, `31a6673`).** Built the ultracode-designed scenario+metrics+scoring layer on top of U1/U2: **U3** structure-targeted apply layer (`SimView.pos_to_struct` + `to_engine_action` → `AttackStructure`/`RangedAttackStructure`/`Dismantle`; shield rampart>wall wins on a shared tile since the engine applies single-target structure damage with no auto-redirect — the breach hits the shield first); **U4** tower intents re-keyed `usize` index→`StructureId` (engine `Intents.towers` + `TickFrame.tower_intents` + scripted controller) — fixes stale index-keying once `world.towers.retain` drops a dead tower in a nest; **U5** `screeps-combat-eval::metrics::SideMetrics` — the five families (healing/DPS/positioning/survivability/efficiency) with **both review must-fixes baked in** (tower damage attributed separately from creep DPS via tower-fire-intent+falloff reconstruction; cohesion via the shared `cohesion::measure`), positioning body-free (recording has no bodies), wired into EXP-KITE-1; **U6** symmetric `EngagementOutcome` + `self_play` + `eval::scoring` adjudicating SideA/SideB/**Draw** on residual military advantage = recent **HP slope** (the derivative, not HP level — the review's inverted-incentive fix: HP-level rewards turtling, HP-slope rewards winning the attrition now, a true grind reads ~0 → correct Draw; un-gameable by a passive turtle); **U7** room-variety register 5→7 (EXP-BREACH-1 ranged siege breaks the rampart shield first via `destroyed_kinds`; EXP-NEST-1 tower nest deals attributed tower damage with creep DPS==0 + bleeds attackers) + a `run_engagement` "side gone" fix to count owned structures (a breach defender with only spawn+rampart was read as eliminated at t0). **Finding surfaced by the metric:** a straight-line kiter corners itself at the wall (~45% late-fight melee exposure) but still wins as the chaser's front-loaded ATTACK parts get stripped — a real **U8** positioning weakness now quantified. **Tower↔creep fire cohesion (U-TOWER, operator 2026-06-19) recorded + now unblocked** (a shared pure `decide_towers(view, squad_focus: Option)` serving squad-present *and* passive-base-defense, live multi-room *and* single-room sim; after U5/U6 — both now done). Gates: engine **40** / agent **35** / eval **10** (7 register + 3 metric/scoring suites), all clippy clean; host-only crates, no bot rebuild. **Next: behavior fixes — U8 (single-creep) / U9 (squad, incl. SQUAD-1 retreat-logic collapse) — these touch high-conflict shared decision files (`combat-decision/lib.rs`, `military/squad_combat.rs`); confirm sequencing + the U-TOWER-vs-U8/U9 order with the operator before starting.**
- **2026-06-19 (bg)** — **P2.U1 + U2 DONE — harness-expansion foundation (`ScenarioBuilder` + recording fidelity) (`08d22ab`).** Kicked off the ultracode-designed **U-series** (the operator goal: *expand scenarios — room variety w/ walls/ramparts/towers — stronger metrics, bots-play-each-other incl. stalemate scoring, dramatically improve single+squad combat, maybe multi-room*). A design workflow (`wf_c4ad0572`, 11 agents) ground-truthed the engine/harness and decomposed it into **U1–U12** (sequence: scenario+metrics foundation U1–U7 → behavior fixes U8–U10; multi-room U11/U12 = XL engine rewrite, **deferred** — every observed symptom is within-room). Its adversarial review caught: the original `residual_EV` stalemate score is an *inverted* incentive (redesign in U6), tower damage must be attributed separately from creep DPS (U5 must-fix), cohesion must reuse `combat-decision::cohesion::measure` not a re-impl (U5 must-fix), and `ScenarioBuilder` edge-coordinate panic risk (fixed in U1 by clamping/bounds-checking all synthesized tiles to 0..=49). **U1 (`screeps-combat-agent/src/scenario.rs`):** fluent `ScenarioBuilder` over `CombatWorld` — terrain (`wall`/`wall_column`/`wall_row` w/ choke gaps / `swamp_rect`), passive structures (`cwall`/`rampart`/`spawn`/`perimeter`), firing `tower`/`tower_nest`, `safe_mode`, `from_units`/`empty`/`world_mut`/`build`; mints structure ids high (`STRUCT_ID_BASE=1_000_000`) so they never collide with creep ids. 4 tests (gap-leaving wall column, rampart bunker, edge tower-nest no-panic, from_units+tower). **U2 (`screeps-combat-engine/src/record.rs`):** recording fidelity the new metrics need — `CreepResult.raw_damage` (pre-rampart/TOUGH, for effective-vs-raw DPS), `TowerFrame` + `TickFrame.towers` snapshot (tower energy/hits per tick), additive `TickFrame.destroyed_kinds` (structure-kind on death for breach metrics). **Additive only** — `destroyed_structures` kept; no behavior change. **Host-only crates** (the bot doesn't compile engine/agent), so no wasm/bot rebuild; engine **40** / agent **32** / eval **2** green, clippy clean. Roadmap persisted to master plan §5(b) (U-series) + §5(g) (multi-room deferral decision pending operator). **Next: U3** (structure-targeted apply layer in `to_engine_action` + SimView structure ids — unblocks breach/dismantle scoring) → U4 (tower re-key by id) → U5 (richer metrics module) → U6 (symmetric outcome + self-play/stalemate scoring) → U7 (scenario suite) → U8/U9 behavior fixes. **Operator decision pending: multi-room (U11/U12) now vs. after within-room behavior is solid.**
- **2026-06-19 (bf)** — **P2.H5 first increment — the EXP-* register as a metric-producing suite (`bc7cc62`).** New workspace member **`screeps-combat-eval`** (the combat *policy* layer, ADR 0006 Part B): the ADR 0008a experiment register as a runnable suite over the H4 sim. Each experiment sets up a scenario, runs it through `run_engagement` (real `IbexAgent` vs a scripted opponent, engine-resolved), extracts **measured metrics**, and gates each to pass/fail; `register()` runs them, `report()` renders the dashboard (`examples/run_register`). Ported the sim-runnable foundational experiments, **5/5 passing:** EXP-FOUND-1 (two-phase kill inequality, both directions), EXP-KITE-1 (range-3 kiter takes 0 melee at MOVE parity), EXP-FOCUS-1 (3×ranged clear a 5-HEAL turtle in 3t, no losses), EXP-TOWER-1 (edge drain self-heals + bleeds the tower to 0 energy), EXP-COMP-1 (composition comparison: quad 5t < duo 15t vs a heal-wall — front-to-back HEAL degradation makes DPS matter). **This is the tactics-tuning loop:** change a tunable in `screeps-combat-decision`, run the register, watch the metrics move; gates flag regressions. Member like agent (deps engine+decision+agent+game-api; inherits workspace patch/lock). eval 2 tests, clippy clean (also fixed a `div_ceil` nit in `replay.rs`). **Remaining H5:** sim-vs-server **parity oracle** + byte-exact golden vectors + nightly seeded gate (need Docker-capture integration); the harder register items (BREACH/DEF-2/CTRL/NPC/PARITY + the full COMP sweep) as the sim grows ramparts/controllers. ADR 0008a register-status + master plan §3/§5 updated. **Next: H5 tail (parity oracle / more EXP) or I1 (SquadStore/SquadId).**
- **2026-06-19 (be)** — **P2.H4 DONE — opponent roster completed (drain) + tower scenarios + SVG replay scrubber (`d5a8747`).** Added `DrainAgent` (4th archetype: pure tower-bait tank — self-heal + hold, no attack), completing the rush/kite/turtle/drain roster. Added a scripted **tower controller** to `run_engagement` (each tower fires at the nearest enemy creep) + tower-only-side termination + `side_b_tower_energy` reporting, and a **drain-vs-tower** scenario (13-HEAL tank survives falloff fire, bleeds the tower to 0 — mirrors the engine drain conformance). `run_engagement` now captures a per-tick `CombatRecording` (`record_tick` drop-in) in `EngagementOutcome.recording`. New **`replay.rs`** `to_svg` renders a recording as an SVG filmstrip (one mini-map/tick; creeps = owner-coloured circles ∝ HP); `examples/replay_demo` emits one SVG per adversarial scenario (delivered the 4 to the operator). Extracted a shared `self_or_ally_heal` helper (turtle + drain). Agent crate **28 tests** (was 18 pre-H4), clippy clean (incl. examples). **H4 complete; H5 remaining** = the `screeps-combat-eval` crate (aggregated EXP-* scoring + sim-vs-server parity oracle + nightly seeded gate + byte-exact golden vectors). **Next: H5/eval crate (unlocks the 0008a EXP-* tactics-tuning loop) or I1 (SquadStore/SquadId).**
- **2026-06-19 (bd)** — **P2.H4 first increment (scripted opponents + adversarial harness) + LIVE combat-path validation on Docker.** **H4 (`screeps-combat-agent`, `a1e517d`):** added the scripted opponent roster — `RushAgent` (melee bruiser), `KiteAgent` (range-3 skirmisher), `TurtleAgent` (stand+heal) — each a `TacticalAgent` emitting `MoveTo`/`Flee` goals that route through the SAME rover pathfinder (`pathing::resolve_move_direction` → `LocalPathfinder` A*) the bot uses, so opponents pathfind (not raw directional — a wall-routing test proves an opponent routes through a gap). Added `run_engagement` (head-to-head IbexAgent-vs-opponent through the engine → outcome + side-A cohesion) and `world_from_units`/`Unit` (composition builder, so adversarial tests field our AI in arbitrary compositions — e.g. a 4×ranged quad out-DPSes a strong turtle). 25 agent tests (was 18), clippy clean. Remaining H4: `DrainAgent` (tower scenario), SVG replay scrubber, aggregated EXP-* scoring (→ H5/`screeps-combat-eval`). **LIVE VALIDATION (Docker, WFV 13, debug_log on):** introduced a level-1 invader stronghold in W7N5 via `strongholds.spawn` (server CLI). Found the world already has natural invader cores + remote invaders, and confirmed the **objective→manager→squad pipeline fires end-to-end**: the offense scan produces `Dismantle` objectives for cores (W5N7, W7N3) + `Defend` for remote invaders (W8N4); the manager fields combat squads (census: 8 combat creeps across W8N4/SK rooms). Correctly does NOT assault the player room (W5N8, towers+safemode — `attack_players` off). **Gaps:** W7N5 unscouted (non-reserved → opportunistic visibility slow → O3 breach + tower fight not yet exercised); confirm a siege-quad actually clears a core (offense MEDIUM competes with defense/SK under `MAX_CONCURRENT_SQUADS`). This substantially closes the CP-H/CP-M behavioral-validation gap (the combat path provably fires live). **Next: H4 tail (drain/replay/eval crate) or I1 (SquadStore/SquadId).**
- **2026-06-18 (bc)** — **Docker soak check after O7 — healthy + stable, combat-dormant (peaceful).** Post-O7 (WFV 13) soak: server up/healthy; 45s console capture for `panic|error|deserial|fault|War|Squad|objective|Defend|Dismantle|Secure|Harass|Farm|stronghold|invader|CPU|spiral` → **zero matches**. Live health (`exec`): `t≈454683, bucket=10000 (FULL — no CPU spiral), creeps=101, myrooms=6, offense=true, defense=true`. So: clean WFV-12→13 loud reset, full economy rebuilt (101 creeps / 6 rooms), CPU healthy, combat flags survived the reset. **GAP (unchanged): the world is peaceful → the live objective/manager combat path (Dismantle-core / Defend / Secure) is not behaviorally exercised** — the one remaining pre-M4 validation; needs a threat (wait/introduce, or drive via the H4 harness). ADR 0008 updated with current state + roadmap-to-M4; master plan §3/§4 + resume point refreshed. **Recommended next: H4 (harness — also unblocks behavioral validation + the EXP-\* tuning loop) or I1 (SquadStore/SquadId — foundational for S1 + a future multi-squad assault).**
- **2026-06-18 (bb)** — **P2.G4-O7 DONE + DEPLOYED — the legacy offense path is GONE; the G workstream (objective-driven combat migration) is complete.** Deleted `missions/attack_mission.rs` + `operations/attack.rs` (`AttackMission`/`AttackOperation`/`AttackReason`/`AttackPhase`/`PlannedSquad`/`DeployCondition`/`ManagedSquad` + the heavy-assault planner `plan_by_detected_threat`/`build_force_plan`), the `MissionData::AttackMission` + `OperationData::Attack` variants + dispatch arms + `mission_type!` + mod exports (`missions/mod.rs`, `operations/mod.rs`), the `OperationData::Attack` arms in `managersystem.rs`, the `MissionData::AttackMission` viz arm + its `squad_contexts`/`creep_owner` SystemData fields (`visualization.rs`), and **all of war.rs's active-attack machinery** (`ActiveAttack` struct + `active_attacks` field, `reassign_home_rooms`, the heavy-recompute threat-propagation loop + active-attack visibility subloop + proactive-detection, `is_attacking_room`/`get_attack_for_room`/`add_active_attack`/`remove_active_attack`/`cleanup_dead_attacks`, the `child_complete`/`repair_entity_refs` overrides → trait no-op defaults, the `TargetSource→AttackReason` From impl, the dead `_ => None` launch branch). `run_offense_evaluation` is now purely objective-driven (every candidate → `Secure`/`Dismantle`/`Harass` upsert; no fallback); `run_heavy_recompute` shrank to the offense-objective cap calc + border-visibility refresh; the offense summary reports cap+on/off only. **Pure dead-code deletion** (the legacy was already runtime-unreachable post-O5/O6 — verified the sole `AttackOperation` constructor was never reached). **`SquadCombatJob` is now the only combat-driving job — "no legacy combat-driving mission code remains."** **WFV 12→13** (removed serialized `MissionData`/`OperationData` variants → one loud reset). Compiler-guided deletion: clean build after one unused-import fix. Bot host **157**, wasm + clippy clean. **Deployed to Docker** (WFV 12→13 loud reset). The heavy multi-squad player assault is the deferred §5(g) future capability (design preserved in ADR 0008a). **Next: independent tracks — H4/H5, I1/I2, W2–W4, S1/S2, K2c-2** (no more offense legacy to remove).
- **2026-06-18 (ba)** — **P2.G4-SK-DEADCODE DONE + legacy found RUNTIME-UNREACHABLE → G4-HEAVY deferred, O7 un-gated (only soak-gated now).** Removed the dead `AttackReason::SourceKeeper` variant + its `build_force_plan` arm + status label (`operations/attack.rs`); `duo_sk_farmer` composition KEPT (the SK farm uses it). **Then verified the bigger picture:** the only `AttackOperation` constructor is `build_with_context` at `war.rs:1000` (the launch loop's `_ => None` branch), but after O5+O6 every offense candidate (InvaderCore→Dismantle, AttackFlag→Secure, ResourceDenial→Harass) maps to an objective and `continue`s — so **no `AttackOperation` is ever constructed; the entire `AttackMission`/`AttackOperation`/`AttackReason` + heavy-assault planner is already runtime-dead.** Consequence: **G4-HEAVY (the towers≥4 drain+quad multi-squad assault) is NOT an O7 blocker** — there's no live heavy assault to preserve (O6 dropped it when AttackFlag→single Secure quad); it's reframed as a deferred future capability (master §5(g)) needing a multi-squad/sequenced-objective mechanism + I1/I2 + operator demand for player offense (`attack_players` default OFF). **So the only remaining G4 work is O7 = delete the dead legacy, gated ONLY on the Docker soak validating the new objective/manager path** (clear a real invader core cohesively). **SK-DEADCODE NOT deployed standalone** — removing a serialized `AttackReason` variant rides O7's WFV-bump reset rather than wiping the running soak for a dead-code removal. Bot host **157**, wasm + clippy clean. Master plan doc §3/§4/§5/§6 reframed. **Next: let the Docker soak run (offense ON); when it shows the Dismantle-core path working, do O7.**
- **2026-06-18 (az)** — **P2.G4-O5 RESOLVED BY DROPPING — power-bank farming was non-functional; removed the no-op producer, deferred real farming to an ADR-gated workstream (operator-decided).** Investigating O5 (the planned "port the power-bank haul out of `AttackMission::Exploiting`") revealed there was **nothing functional to port**: (1) the crack never worked — the squad only sees `get_hostile_structures` (filters to owned-and-not-mine, `squad_combat.rs:1011`) and `select_focus_target` only targets `Ownership::Hostile` structures (`decision/lib.rs:216`), but a power bank is **neutral/unowned** → never in the squad's view, never targeted (legacy `AttackMission` used the same focus logic); (2) the haul never worked — `SquadTarget::CollectResources` has **no executor** (the "haulers" spawn as `SquadCombatJob`, which has no pickup behavior; the `transfer_queue` haul path can't read ground resources in a highway room regardless). So the live behavior was: launch a duo + haulers that travel to a highway room and idle, wasting energy. **Operator decision (AskUserQuestion): drop power-banks, unblock O7.** Removed the `PowerBank` candidate from `run_offense_evaluation` + the now-dead `power_bank_min_ticks_needed` / `count_power_bank_attacks` / `max_concurrent_power_banks` (field + init + heavy-recompute assignment) + their 3 tests. `TargetSource::PowerBank` (pub enum variant, no warning) + the legacy `AttackReason::PowerBank` arm are left for O7's sweep. **O7 is no longer gated on a power-bank port** — only on the heavy multi-squad assault (G4-HEAVY). **Engine truth recorded for the future workstream** (`_damage.js:35-36`): attacking a bank applies `damage × POWER_BANK_HIT_BACK (0.5)` back to the attacker → real farming needs a DEDICATED healed squad (objective-scoped targeting; do NOT make the generic decision target neutral structures) + a PREDICTIVE dropped-power collector timed to the crack. **An ADR for power-bank farming is now a tracked TODO** (master plan doc §5(g)). No WFV bump. Bot host **157** (160 − 3 power-bank tests), wasm + clippy clean. Committed (this), deploying to Docker (offense is ON now, so this also stops a real bank from triggering the wasted spawns).
- **2026-06-18 (ay)** — **P2.G4-O6.2 DONE + DEPLOYED — the rest of the single-squad offense producers; O6 (single-squad migration) complete.** Routed the remaining single-squad `AttackReason`s through `run_offense_evaluation`'s launch loop as objective upserts: **`AttackFlag` → `Secure { room }`** (`quad_ranged`, `OBJECTIVE_PRIORITY_HIGH` — explicit operator intent), **`ResourceDenial` → `Harass { room }`** (`solo_harasser`, `OBJECTIVE_PRIORITY_LOW` — opportunistic income denial). **`InvaderCreeps` RECONCILED → dropped:** the reserved-remote-invader `Defend` context in `run_defense_scan` already fields a squad on the identical trigger (`reservation().mine() && visible() && hostile invaders`), so a `Secure` objective here would **double-field the same room** — the offense path is removed (the `all_npc` computation is kept; the resource-denial gate needs it). **Unification (cleanup):** O6.1's `InvaderCore`→`Dismantle` upsert was refactored OUT of the in-scan block into the **launch loop** too — an `AttackCandidate.target_pos: Option<Position>` now carries the core tile, so **all** migrated reasons flow through one `source → (ObjectiveKind, priority, composition)` branch, respecting the dedup (highest-scored candidate per room) and a single combined cap. **War-cap reconcile (the O6.2 cap work):** the launch-loop budget is `active_attacks.len() + (Attack-owned objectives currently in the queue)`, capped at `max_concurrent_attacks` — an **existing** objective is always re-asserted (TTL refresh so the manager keeps fielding it), only **new** offense is gated (candidates are score-sorted desc, so a skipped new objective is the lowest-value one); legacy `AttackOperation`s share the same budget. Un-migrated reasons stay coexisting: **`PowerBank`** (→ O5) and the **deferred heavy multi-squad player assault** (`plan_by_detected_threat` towers≥4 drain-duo + quad sequencing — doesn't fit one-squad-per-objective; O7's delete is gated on it). `ProactiveDefense`/`ThreatResponse`/`Expansion` are enum variants the offense scan never produces → nothing to migrate. **No WFV bump** (logic-only; `target_pos` is on the non-serialized `AttackCandidate`, not a persisted struct). Bot host **160**, wasm + clippy clean. **Deployed to Docker** (hot swap, no reset; console clean — no panic/error/deser, normal economic ticking [construction, terminal transfers], no stray War/offense output). **Behavior validates when a target appears** (attack flag / invader core / hostile player remote — world peaceful now). **Next: O5** (power-bank as `Farm{PowerBank}` + bespoke dropped-power haul) → **O7** (delete `AttackMission`/`AttackOperation`/`AttackReason`, soak-gated, incl. the deferred heavy-assault + power-bank paths).
- **2026-06-18 (ax)** — **P2.G4-O6.1 DONE + DEPLOYED — first offense producer migration: `InvaderCore` → `Dismantle` objective (the live consumer + validation gate for O1/O2/O3).** `WarOperation::run_offense_evaluation`'s invader-core block now captures the highest-level core's **level *and* position** (`invader_cores().iter().max_by_key(level).map(|c| (c.level(), c.pos()))`) and, when `invader_core_attack_score` still says it's worth attacking (affordability/interest gate preserved), **upserts a `Dismantle { room, pos: core_pos }` objective** (`ForceRequirement::single(SquadComposition::siege_quad())`, `OBJECTIVE_PRIORITY_MEDIUM` — above SK-farm LOW, below all defense; `owner(ObjectiveOwner::Attack)`; `OFFENSE_OBJECTIVE_TTL=100`) **instead of** pushing an `AttackCandidate` / launching an `AttackOperation`. **All OTHER `AttackReason`s stay on `AttackOperation` (coexistence).** The consumer side was already complete from O1–O3: the manager's `objective_target` maps `Dismantle`→`SquadTarget::AttackStructure { position: pos }`, `is_formation_objective(Dismantle)`=true ⇒ Formation/siege style (keep the anchor, advance to the focus, orient toward it), and `compute_squad_orders`/`decide_squad_with_pathing` do the breach redirect — so a manager siege squad now **travels in formation (O1), orients toward the core (O2), and breaches the shielding rampart of a stronghold core (O3)** end-to-end. The room is re-evaluated each scan (no `active_attacks` entry for it now), so the upsert is **idempotent + re-asserts the TTL**; once the core is dead the room drops out, the objective lapses, and the manager retires the squad — mirroring the W1 defense `Defend`-upsert lifecycle. **Composition is standardized `siege_quad` for all levels** (level-aware sizing — e.g. `solo_core_attacker` for a bare level-0 reserver — is a future refinement, consistent with the W1 accepted trade-off; the economy gate in `invader_core_attack_score` already limits *when* a core is sieged). **Cap:** no war-side cap on this increment — the manager's `MAX_CONCURRENT_SQUADS=4` gates fielding; a dedicated active-offense-objective count (replacing `count_power_bank_attacks`/`active_attacks`) reconciles the war cap when the remaining single-squad reasons migrate (O6.2). **No WFV bump** (logic-only producer change; objective-queue shape unchanged). Bot host **160** (no new unit test — the producer is integration-level, Docker-gated like W1), wasm + clippy clean. **Deployed to Docker** (hot swap, no reset; console clean — no panic/error/deser, normal economic ticking, no stray War/Squad/objective output). **Behavior validates when an invader core appears** in a remote we mine (world peaceful right after the WFV-12 reset → path dormant for now). **Next: O6.2** (`InvaderCreeps`→`Secure` reconciled with remote-defense `Defend`; `Flag`→`Secure`; `ResourceDenial`→`Harass`; `ProactiveDefense`; + the active-offense-objective war-cap count) → then O5/O7.
- **2026-06-18 (aw)** — **P2.G4-O4 DONE — wave-retry + unwinnable backoff in the manager (ADR 0008 §5/§6 + g4-offense-plan updated first, per operator).** Generalized `AttackMission::handle_wave_wipe` into the `SquadManager` Phase A reconcile: a squad is *wiped* when it had spawned members but all are now dead (`squad_is_wiped`: `total_members_added > 0 && members.is_empty()` — gradual losses are refilled by Phase B's unfilled-slot spawns and never reach all-empty, so this only fires on an overwhelmed squad). On a wipe the squad is retired; for a **non-`Defend`** objective the room is also `mark_unwinnable`'d — the queue's exponential backoff then makes `best_unclaimed_near` skip it until `retry_after` (the manager stops feeding squads into an unwinnable siege; the producer's re-assert is ignored meanwhile; a fresh squad is fielded once the backoff lapses = automatic retry). **`Defend` is exempt** (never abandon an owned room — a wiped defense squad is simply re-fielded). `request_renew` audit: the manager never renews (replacement via Phase B; "never renew" already holds) — the only combat usage is the legacy `AttackMission` (deleted in O7). No WFV bump. +2 host tests (`squad_is_wiped`, `is_formation_objective`); bot **160**, wasm + clippy clean. Improves live robustness now (a wiped SK-farm duo backs off); the offense path it most benefits lands with O6. **Next: O5** (power-bank as a `Farm{PowerBank}` objective + coordinated hauler deploy) → O6 producers → O7 delete `AttackMission`.
- **2026-06-18 (av)** — **P2.G4-O3 DONE — layered dismantle target selection (break the shield first); `room_grid_dijkstra` moved into rover (operator: "we can move it into rover if it's generally useful for pathfinding").** When a siege squad's focus is a hostile structure behind a rampart/wall, the squad now targets the breach blocker on the path first (a rampart shields its tile — you can't damage the structure until the rampart falls). **Pathfinding move (rover submodule `448c2d4`):** `room_grid_dijkstra` (weighted 8-dir Dijkstra) + `reaches_room_edge` (reachability flood-fill) relocated from the bot's `pathing::gridsearch` into `screeps-rover` — both are general pure-`std` primitives, so the decision crate can reuse them (no-one-off-pathfinding rule: the algorithm lives in the pathfinding system; the bot's derelict breach planner + the combat breach selector both layer their *pricing* over it). Bot callers (`dismantle.rs` breach planner, `dismantlebehavior.rs` reachability) → `screeps_rover::…`; 8 gridsearch tests moved with it. **Pure combat breach (`screeps-combat-decision`):** `breach_redirect` in `decide_squad_with_pathing` builds the breach pricing from the view's structures (constructed walls + hostile ramparts = dismantlable by hits; other hostile structures + terrain = impassable, route around), runs `room_grid_dijkstra` centroid→target, and redirects the focus + Advance goal to the first dismantlable blocker on the cheapest corridor. Bounded to the structure-siege phase (a structure focus means no hostile creeps remain → the per-tick grid search only runs then). Separation upheld (search in the pathfinding system, pricing + decision pure, manager applies via the existing `decide_squad_with_pathing` call, job executes). **No WFV bump.** Inert live until O6 (no `Dismantle` objectives yet); unit-validated now. Gates: rover **27** (+8 moved), decision **41** (+1 breach), bot host **158** (166−8 gridsearch moved to rover), wasm + clippy clean. Commit `9780df9`. **Next: O4** (wave-retry + unwinnable backoff in the manager).
- **2026-06-18 (au)** — **P2.G4-O2 DONE — formation orientation (face the threat), separation upheld (operator: "uphold operation/mission/job/squad separation").** A box-fighting (siege) squad now orients toward the threat — tanks/high-HP front, healers back. **Pure decision** (`screeps-combat-decision`): `decide_squad` outputs `orientation: Option<Direction>` (centroid → focus, Engaged-only; `decide_squad_with_pathing` propagates it) — +1 unit test (east→Right, north→Top). **Direction-aware application** (`military/squad.rs`): new `slots_front_to_back` projects each formation-slot offset onto `threat_direction` (front = toward the threat), and `reassign_slots` lands the most front-worthy members (HP fraction + tank/melee role) in the leading slots — **replaces** the old min-Y placeholder `threat_facing_slots`/`safe_slots` and **drops the per-death `orient_toward`** (orientation now lives in slot assignment → no double-rotation, layout stays base so footprint + travel stay stable) — +1 unit test. **Manager** (`squad_manager.rs`): combat style derived from the objective kind (`is_formation_objective`: `Dismantle` → Formation/siege, else Skirmish/kite). On arrival a Formation squad **keeps** the anchor, advances it toward the focus, sets `threat_direction = decision.orientation`, and `reassign_slots` — the job's `squad_has_anchor` branch then formation-follows; a Skirmish squad drops the anchor + kites (O1). **Separation upheld:** pure decision in the crate → manager applies → job executes movement. **No WFV bump** (combat style derived per-tick, not serialized; reuses `SquadContext.threat_direction`). **Inert live until O6** (no `Dismantle` objectives yet → all current squads Skirmish) — unit-tested now; full formation-orient behavior validates in the sim + soak when O6 fields siege squads. Gates: decision 40, bot host **166**, wasm + clippy clean. Commit `0c79fa7`. **Next: O3** (layered dismantle target selection).
- **2026-06-18 (at)** — **P2.G4-O1 DONE + DEPLOYED — the anchor mover is wired into the manager for cohesive formation travel (operator: "continue with existing plan"; "uphold operation/mission/job/squad separation").** Scoping found the anchor mover **mechanism already existed + tested** (rover `AnchorPath` footprint-aware cached pathfind via `moving_maximum`, holds-on-blocked; `formation.rs` `advance_squad_virtual_position` cohesion gate + corridor collapse/re-form; the job's `MoveToRoom` formation-travel + `squad_has_anchor`→`execute_formation_movement` branch) — the gap was solely that the `SquadManager` never set up/advanced the squad anchor, so its squads trickled into the target room per-creep (the "scatter" symptom). **Fix (`a0e4284`):** `compute_squad_orders` advances the squad's footprint anchor toward the target-room centre while the squad is still converging; the job reads `virtual_pos` and issues each member's `move_to` — **upholding §5 separation: the manager decides the squad frame, the job owns movement issuance** (no `move_to` in the manager). On arrival the manager **drops the anchor** (`squad_path = None`) so the `Engaged` state kites via the pure `decide_movement` rather than formation-follow — keeping G3 kiting (SK duo / ranged defense) intact; engaged formation-follow is the separate **O2** (it needs a combat-style regime — fight-as-box vs kite — gated on composition, not just `squad_has_anchor`). During the trickle-in window arrived members hold formation near the advancing anchor instead of charging in alone (the intended "gather then engage"). **No WFV bump** (reuses `SquadContext.squad_path`). The travel anchor uses the live multi-room `ScreepsPathfinder`, so it's **Docker-validated, not combat-sim-validated** (the sim is single-room; the AnchorPath mechanism is rover-tested). Gates: host **165**, wasm + clippy clean. **Deployed to Docker** (WFV 11→12 loud reset clean — no panics/deser failures, bot ticking, no `SquadDefense` lines, no W7N1 thrash). Multi-room cohesion + trickle-in formation-hold validate on the soak under a real threat. **Next: O2** (`g4-offense-plan.md` (deleted 2026-06-18; → master plan doc)).
- **2026-06-18 (as)** — **P2.G4 defense-half DONE — legacy `SquadDefenseMission` fully removed; all defense is now `Defend` objectives (operator: "do the offense migration; make sure no legacy attack/defend mission code remains"; scope: "defense now, offense sequenced").** An understanding workflow (`wf_1d0ec829`, 4 mappers) found the offense half (`AttackMission`) needs 4–5 manager capabilities it lacks (multi-room anchor mover / formation orientation / layered dismantle / wave-retry / power-bank exploit) — deleting it now would regress live offense, and offense is the hardest path to validate headless — so the operator chose **defense-now / offense-sequenced**. **Defense half (this entry):** deleted `missions/squad_defense.rs`; removed `features.military.manager_defense`; removed `MissionData::SquadDefense` variant + both dispatch arms + the `mission_type!` line + the `mod` export; migrated **all three** war.rs defense contexts — owned-room reactive, operator `defend`-flag, and reserved-remote-invader — to **unconditional `Defend` objective upserts** (escalation→composition `quad_ranged`/`duo_attack_heal`/`solo_ranged`, `OBJECTIVE_PRIORITY_HIGH`, `owner(Defense)`, `DEFEND_OBJECTIVE_TTL`) fielded by the `SquadManager` with full G3 tactics. The ADR 0017 §13 ownership-subordinate invariant is preserved by **producer-scoping** (context 1's gather filters `owner().mine() && visible()`, so a lost room drops out → TTL lapse → retire). The `hostile_warrants_defender` trigger + threat sizing stay in the producer. **Bonus fix:** the migration **resolves the W7N1 remote-defense thrash** — the squad-less mission self-terminated every tick on a reserved (not owned) remote via the `!owner().mine()` guard, so the producer re-created it endlessly; an objective has no mission-internal ownership self-termination, so the producer re-asserts while invaders are present and the manager retires the squad when they're gone. **WFV 11→12** (removed enum variant = serialized-shape change, loud reset). Also committed the previously-uncommitted server-kit `exec` subcommand. **Adversarially reviewed.** **Offense half = NOT done, sequenced in `g4-offense-plan.md` (deleted 2026-06-18; → master plan doc)** (O1 anchor-mover → … → O7 delete `AttackMission`, soak-gated). **Adversarial-review follow-ups applied (same change):** the review flagged that funneling all 3 defense contexts through the manager's `MAX_CONCURRENT_SQUADS=4` cap at equal `HIGH` priority could starve a far owned room — so the contexts are now **priority-tiered** (owned reactive = `CRITICAL`, defend-flag = `HIGH`, remote-invader = `MEDIUM`, all above SK-farm `LOW`); `MAX_SPAWN_DISTANCE` restored **6→10** (legacy `MAX_DEFENSE_SOURCE_DISTANCE` parity, so a far defend-flag/owned room can still source a defender); and Phase-C claiming now **skips objectives with no in-range home** (via `best_unclaimed_near_excluding`) so an unfieldable objective never leaks a concurrency slot to a never-spawned `SquadContext` (also fixes the prior malformed-no-force placeholder-claim). **Accepted trade-off (noted, not fixed):** the manager fields standardized `SquadComposition`s (which scale to home energy capacity) rather than the legacy per-body *threat*-sized `sized_defender_body`/`sized_healer_body` + `defender_spawn_readiness` gate — threat-aware roster sizing is a future manager refinement, tracked alongside G4-offense. `DefendMission` (defend.rs, a room-safety *signal*) was correctly KEPT. Gates: full bot host suite **165**, wasm + clippy clean.
- **2026-06-18 (ar)** — **P2.K5 IMPLEMENTED — the SK farm now stands down for invader strongholds (operator: "the SK mission needs to account for invader cores/bases in the room").** SK rooms periodically spawn an **invader stronghold** (a `StructureInvaderCore` `level()≥1`, ringed by ramparts + 1–6 towers that reach the *whole* room at ≥150 dmg/tick, plus defender creeps at L3+); the old farm would field the suppression duo + march miners straight into tower fire (the keeper gate sees creeps, not towers; `contested` only checked player owner/reservation, and a stronghold reserves nothing). Engine + bot ground-truth established by a research workflow (`wf_66183c3f-879`): level 0 = harmless highway reserver, 1–5 = self-defending stronghold (level set at creation, so the deploy window is caught); `invader_cores()` is live-only (`#[serde(skip)]`) but `hostile_structures` is **persisted** and, in an unclaimable SK room, is set *only* by a stronghold (keeper lairs are excluded; no player structures possible). New **`sk_room_has_stronghold(room_data)`** (live `invader_cores().level()≥1` when visible, persisted `hostile_structures` fallback when blind) drives two layers: **(1) operation veto** — `SkRoiInputs.stronghold_present` is a hard gate in `score_sk_farm` (won't *start* a farm into a stronghold, even an already-committed one); **(2) mission pause** — `run_mission` **`withdraw`s the `Farm{sk}` objective immediately** (so the `SquadManager` retires the duo *this tick* rather than commanding it to suppress keepers for the 200t TTL → deeper into towers), forces **all** source-mining + haul children off (`gate_mining_children(force_off=true)`), and re-probes visibility once per `STRONGHOLD_RESCOUT_INTERVAL=1500`t (an in-range observer services it free; else one cheap scout peeks the room — observers are RCL 8 but SK farming starts at RCL 6, so an opportunistic-only request would never resume an observer-less colony). The farm stays `Running` (a stronghold is transient — auto-collapses in ~75k ticks). **Adversarially reviewed** (the resume-deadlock was the review's catch → the throttled non-opportunistic probe; the every-tick-opportunistic + throttled-non-opp pairing was rejected because the queue upsert makes the non-opp flag sticky). ⚠ **Residual gap (L1 single-room flee, ADR 0008):** stops the bot *feeding* the room but in-flight creeps can't flee *across* the boundary and the K0 reflex ignores towers — prompt cross-room evacuation is the L1 follow-up. ADR 0018 §3.5 updated. Gates: SK scorer tests 6 (+2: stronghold-vetoes-fresh + stronghold-vetoes-committed), full bot host suite **165**, wasm + clippy clean; **no WFV bump** (reuses persisted `hostile_structures`; the new input + `force_off` param aren't serialized).
- **2026-06-18 (aq)** — **P2.G3-tail COMPLETE (Steps 4–8) — cohesive, pathfinding-scored kiting, wired live + self-play-validated.** **Step 4/5** (pure): `SquadMovement{Advance|Kite|Hold}` directive; `decide_squad_with_pathing` runs ONE `plan_kite_anchor` (Retreating, or Engaged + ranged + a melee threat ≤3 of the centroid) → a `Kite` goal, else `Advance` to weapon range; `decide_movement` rewritten with the precedence **critical-HP flee → ranged melee-evade (SK-duo guard, byte-identical) → squad Kite/Advance goal → out-of-cohesion rejoin → prior fallback** (only when `cohesion_radius==0`). The block moves to ONE shared goal so cohesion is structural; the lone individual break is the critical-HP flee. **Step 6** (live, **NO WFV bump**): the `SquadManager` builds the room's terrain-baked cost matrix + calls `decide_squad_with_pathing`, stamping the directive onto each member's `TickOrders` via `#[serde(skip)]` fields (ephemeral → no serialized-shape change, so the plan's 11→12 bump was unneeded); the job's anchorless Engaged `Formation` branch now routes through `decide_movement` (`execute_decide_movement`). **Step 7** (pure): `assign_heals` ports `compute_heal_assignments` over member indices → `SquadDecision.heal_assignments`; the manager resolves idx→entity→`ObjectId`. **Step 8** (sim): `ManagedSimSquad` drives the anchorless path through the engine; **EXP-SQUAD-KITE-1** self-play scenario (ranged duo+healer vs a high-HP melee keeper) asserts focus-fire + `max_pairwise ≤ 4` cohesion + survival. **The search lives in rover (`LocalPathfinder::search_scored`, inherent — no server-PF analog); combat supplies only the pricing; live==sim (same `LocalPathfinder`, only the matrix source differs); kiting is single-room (cross-room travel is the separate `MoveToRoom` phase).** Gates: decision 39, agent 18, rover 19, bot 164, wasm + clippy clean; no WFV bump. Commits b4d59c1→(this). **CP-G tactics (G3) now substantively done** (pure + simulatable + live + the EXP-SQUAD-KITE gate); remaining G-workstream = G4 (offense migration, G3-gated → now unblocked). **Operator future direction:** the same scored search → attack-positioning (`plan_engage_anchor`), to explore next (`g3-tail-plan.md` (deleted 2026-06-18; → master plan doc) Future).
- **2026-06-18 (ap)** — **P2.G3-tail Steps 1 + 3 — the pure cohesive-pathfinding-kite foundation is complete.** **Step 1** (rover submodule `e6d996f`): `PathfindingProvider::search_scored` — a bounded scored single-room search (`run` gained a `dijkstra` flag so an arbitrary positive cost isn't mistaken for an admissible distance heuristic; floods by path cost, returns the min-`cost` reachable tile). `ScreepsPathfinder` **delegates to the pure `LocalPathfinder`** — a bounded local search has no server-PF analog, and delegating makes live kite positioning byte-identical to the sim. 3 rover tests (rover 19). Superproject ptr bumped (`1fe48a3`). **Step 3** (decision crate): `plan_kite_anchor(view, pf, room_callback, max_ops) -> Option<KitePlan>` runs ONE `search_scored` from the centroid pricing tiles with `score_tile`, `None` = holding optimal (`MAX_KITE_OPS=400`); added `screeps-rover` (pure default) as a decision-crate dep. 2 tests; decision crate **31**. Together with Step 2 (`score_tile`), this is the complete **pure** "find a safe + cohesive + higher-EV kite tile via one bounded search, combat supplies only the pricing" capability — fully simulatable, no fork. bot wasm + agent crate (17) still green (rover feature unification OK). **It is wired into nothing yet** (inert) — that's Steps 4–8: **Step 4** `SquadMovement` directive + `decide_squad_with_pathing` (consume `plan_kite_anchor`), **Step 5** rewrite `decide_movement` for the squad goal + cohesion + critical-HP override, **Step 6** live wiring + WFV 11→12, **Step 7** heal-port, **Step 8** sim scenario EXP-SQUAD-KITE-1. See `g3-tail-plan.md` (deleted 2026-06-18; → master plan doc).
- **2026-06-18 (ao)** — **P2.G3-tail STARTED — design synthesized + Step 2 (pure kite scoring) landed.** Operator: kiting/flee must keep **squad cohesion** (don't sacrifice the squad for individual tactics unless beneficial), and **use pathfinding to pick a safe, higher-EV flee/kite position**. A design workflow (`wpk8wt7bo`, 2 cohesion angles + pathfinding-position + sim-wiring/heal-port maps → synthesis) chose **ANGLE B (squad owns ONE directive) fused with a pathfinding-scored goal tile**: cohesion is a **term in the per-tile score**, not a per-creep clamp (the clamp is the disorganized pattern); **one bounded search per squad** via rover's pure `LocalPathfinder`, combat supplies only the pricing (no-one-off-pathfinding rule); critical-HP is the lone sanctioned cohesion break; SK-duo non-regression is structural. **The full 8-step checklist is `g3-tail-plan.md` (deleted 2026-06-18; → master plan doc)** (only Step 6 bumps WFV 11→12). **Step 2 DONE:** `cohesion::centroid()` extracted (shared live+sim) + new pure `kite.rs` (`score_tile` = lower-is-better cost over SAFETY ≫ COHESION > VALUE > openness; `tower_dps_at_range`; tunable `KiteScoreParams`); decision crate **29 (+6)**, clippy clean. **Operator future direction recorded:** the same scored-search generalizes to **attack-positioning** (maximize expected damage/heal + optimal range − damage-taken for the focus) — a T-POS experiment to explore **after** flee/kiting completes (g3-tail-plan.md "Future"). **Next: Step 1** (rover `search_scored`) → Step 3 (`plan_kite_anchor`) → 4/5 (`SquadMovement` + `decide_movement` rewrite) → 6 (live wire + WFV) → 7 (heal-port) → 8 (sim wiring + EXP-SQUAD-KITE-1).
- **2026-06-18 (an)** — **P2.G3 v1 IMPLEMENTED — squad tactics moved into the PURE decision engine (operator: "what goes in the combat decision engine so it can be simulated"; "treat code as changeable for clean separation").** Caught a mistake-in-progress: the first G3 pass was about to compute focus-fire/heal/retreat **inside the game-coupled `SquadManagerSystem`**, where the sim can never run them — violating the harness's reason for being (ADR 0006 §B.2: tactics are pure + self-play-validated, no fork). Corrected: a new **pure `decide_squad(SquadView) → SquadDecision`** in `screeps-combat-decision` (the squad analog of `decide_combat`/`decide_movement`) — picks the squad's shared focus (`select_focus_target`) and resolves **engage vs retreat with coupled hysteresis** (once Retreating, re-engage only above a band `retreat_threshold+0.3`, never while a member is critical — the "no yo-yo" fix), returning the combat state + shared focus. The **`SquadManager` is now the thin live adapter**: builds the JS-free `SquadView` from `SquadContext` + the room's hostile/structure DTOs (the shared `squad_combat` adapters), calls `decide_squad`, and writes the result back as `tick_orders` + state — **zero tactics math in the manager**. The job's `Engaged` **`Formation`-without-anchor path now falls to its own kiting** (`fallback_movement`) so a manager focus order never charges a ranged (SK duo) squad into the formation range-1 fallback — the §5 ⚑ "job owns movement" fix, minimal form. **Clean responsibilities recorded (ADR 0008 §4.1, new table):** per-squad + per-creep decisions = PURE `screeps-combat-decision`; `SquadManager` = lifecycle + adapter; `SquadCombatJob` = execution + movement issuance; `SquadContext` = data carrier (its `compute_heal_assignments`/`should_retreat`/`reassign_slots` migrate into `decide_squad` next). **v1 scope:** shared focus + hysteresis (3 kernel tests in the decision crate). **Deferred to G3-tail (recorded):** heal-**assignment** + slot/orientation migration into `decide_squad` (still called as `SquadContext` methods from the adapter for now); the **sim wiring** (`screeps-combat-agent` `SimSquad` calling `decide_squad` → the EXP-* validation loop). Gates: **decision crate 23 (+3)**, bot 164, **sim agent crate 17 (still builds)**, wasm build + clippy clean; no WFV bump. **Next: G3-tail** (heal-assignment→pure + sim wiring), then validate; G4 offense migration after.
- **2026-06-18 (am)** — **SK-mining orchestration review (operator: how does the SK-specific op/mission compose vs reusing the mining outpost?).** **It exists, in code + design.** Design: [ADR 0018](../design/0018-source-keeper-room-exploitation.md) §3.6 decided a **NEW economy-family `SourceKeeperOperation`** owning a **NEW `SourceKeeperFarmMission`**, *reusing the leaf missions* (`SourceMiningMission` + `HaulMission`) + the squad machinery — explicitly **NOT** `MiningOutpostOperation`/`MiningOutpostMission`. Code: `operations/sourcekeeper.rs` (op + pure ROI scorer) + `missions/sourcekeeperfarm.rs` (coordinator). **Composition, side-by-side:** *Remote mining* = `MiningOutpostOperation → MiningOutpostMission → { LocalSupplyMission [→ per-source `SourceMiningMission`×N, per-mineral `MineralMiningMission`, `RoomTransferMission`], `HaulMission`, `ReserveMission`, `DefendMission` }`, gated **room-level** on `DefendMission::is_room_safe`. *SK* = `SourceKeeperOperation → SourceKeeperFarmMission → { Farm{sk} objective (→ SquadManager → duo), per-source `SourceMiningMission`×N (direct), `HaulMission` }`, gated **per-source** on keeper liveness. **Why SK does NOT reuse `MiningOutpostMission`/`LocalSupplyMission`:** (1) the outpost op *excludes* SK rooms by design (`miningoutpost.rs:54/140`); (2) SK is controller-less → no `ReserveMission`, and "defense" = the suppression duo (a combat objective), not a `DefendMission`; (3) SK needs **per-source** suppression gating (mine the clear sources while a keeper guards another), but `LocalSupplyMission::allow_spawning` is **room-level** (propagates uniformly) — so the SK coordinator owns `SourceMiningMission` **directly**; (4) the SK mineral (K4) is separately blocked (no extractor in an unownable room). **Overlap to reconcile when we revisit mission orchestration:** the SK coordinator's `ensure_mining_children` (per-source `SourceMiningMission` creation) **duplicates** `LocalSupplyMission::ensure_children`'s source loop. **Options:** *(a, recommended)* extract a shared `ensure_source_mining(room, homes, gate: Fn(source)->bool)` helper used by both — dedups the leaf-creation while preserving each op's distinct strategy (outpost = reserve+defend; SK = suppress-farm); *(b)* give `LocalSupplyMission` a per-source gate + skip mineral/transfer for SK (more coupling); *(c)* generalize `MiningOutpostMission` to handle SK (drop the exclusion, `ReserveMission`→none, `DefendMission`→combat objective, room→per-source gate) — most ambitious, the outpost lifecycle fits SK poorly. **Also converges with W1:** the outpost's `DefendMission` child should likewise become a `Defend` objective on the queue, so both paths' defense lands on one model. Recorded for the orchestration revisit; no code change.
- **2026-06-18 (al)** — **Combat-overhaul behaviors flipped ON by default (operator: "turn them on by default, we'll test in Docker/sim before MMO").** `features.source_keeper.farming` **false→true** and `features.military.manager_defense` **false→true** (both via their struct-level `#[serde(default)]` → the new struct defaults; removed a stray per-field `#[serde(default)]` on `manager_defense` that would have forced `bool::default()`=false for a missing key). So a fresh world now runs SK farming + manager-fielded defense by default. **Memory-persistence gotcha (flagged for deploy):** `features::prepare/load` write the full merged `_features` back each tick, so a key **already persisted as false** in an existing world's Memory is NOT overridden by the new Rust default — on the **existing Docker world**, set `_features.source_keeper.farming = true` and `_features.military.manager_defense = true` in Memory (or clear `_features` to regenerate). `manager_defense` is a brand-new key (absent in Memory) so it picks up `true` automatically; `farming` may be persisted-false. **MMO note:** defaults-on means the behaviors are live on the next MMO deploy too — the operator's plan is to validate in Docker/sim first (no MMO deploy without go-ahead). bot 164, wasm + clippy clean; no WFV bump (Memory flag only).
- **2026-06-18 (ak)** — **P2.W1 IMPLEMENTED (feature-flagged, default OFF) — defense migrates onto the objective+manager.** Operator chose to do the migration now; shipped behind **`features.military.manager_defense` (default false)** so the live defense path is **unchanged until validated on a private server** (combat is the one path unverifiable headless — the flag is also the clean A/B for the parity check). When ON, `WarOperation::run_defense_scan` **upserts a `Defend{room}` objective** (escalation→composition: `DefenseEscalation::{Solo→solo_ranged, Duo→duo_attack_heal, Quad→quad_ranged}`, `OBJECTIVE_PRIORITY_HIGH`, `owner(Defense)`, short `DEFEND_OBJECTIVE_TTL=60`) instead of building the legacy squad-less `SquadDefenseMission`; the producer re-asserts every defense scan while the room warrants a defender (idempotent), and when it stops (room safe/lost) the 60t TTL lapses → the manager retires the squad. Threaded the `CombatObjectiveQueue` into the **operation** execution data (`OperationSystemData` + `OperationExecutionSystemData` + both construction sites in operationsystem.rs — mirrors the mission threading) so operation producers can upsert. The manager now **maps objective priority → spawn priority** (`spawn_priority_for`: `≥HIGH → SPAWN_PRIORITY_HIGH`) so a `Defend` squad spawns HIGH, not starved below economy. The defended (owned) room is itself an in-range home → it spawns its own defenders. **No WFV bump** (the flag is a Memory `_features` field, serde-default; no serialized component shape changed). **Known minimal-manager gaps (carry to G3):** the manager has no per-tick formation/kite/coordinated-focus orders yet, so a flag-ON defense squad fights via each creep's `decide_combat` (a *lifecycle* win — objective-managed, no orphan/scatter-of-lifecycle — not yet the full *cohesion* win); and `MAX_CONCURRENT_SQUADS=4` has no preemption, so 4 active farms could delay a 5th defense (a non-issue while farming is OFF). **Offense (G4) is the larger, regressive-without-G3 half — NOT done this session** (AttackMission already performs coordinated focus/heal the minimal manager lacks). Gates: bot 164, wasm build + clippy clean. **Recommend: validate W1 on the Docker server (flip `manager_defense` on, confirm a defended room forms + retires a squad, vs the legacy baseline), then build G3 before G4.**
- **2026-06-18 (aj)** — **Attack/defend reconciliation REVIEW (operator-added scope): the migration is real but G3-gated; coexistence is already safe.** Operator: *"review the existing attack and defend missions to reconcile with the objective system… one cohesive set… the mission may be driving creep behavior (movement) that should live in the squad or the job."* **Findings (behavior map):** **`AttackMission`** drives creep behavior the squad/job should own — it writes `member.tick_orders = TickMovement::Formation` (Spawning ~481), calls `advance_squad_virtual_position` (Rallying ~651, Engaging ~978), computes the focus target + writes `TickOrders{attack_target}` to every member (Engaging ~920–964), sets `threat_direction`+`reassign_slots` (~945), and `compute/apply_heal_assignments` (~968) — the §5 ⚑ inversion. **`SquadDefenseMission`** is **squad-LESS** (`squad_entity: None` → scatter by construction): defenders/healers tracked in bare `EntityVec`s, no `SquadContext`, no focus/cohesion. **The target end-state** (ADR 0008 §1 MERGE rows, migration steps 4–5): `AttackMission` → a `Secure`/`Dismantle` **objective producer**; `SquadDefenseMission` → a `Defend` **objective**; both fielded by the `SquadManager`; the mission-driven movement/focus/heal moves into the manager's per-tick **orders** (G3) which the job *executes* — and per the §5 ⚑ / principle 8, the *movement-request issuance* drops to the squad/job. **WHY NOT NOW:** the G2 manager is **minimal — it has no tactics/orders (G3 unstarted)**. Migrating offense/defense onto it **today would regress combat** (lose focus-fire, kiting, heal assignment, formation orders that `AttackMission` performs). So the migration is correctly **sequenced after P2.G3** (tactics in the manager) — exactly the ADR's step-4/5 dependency. **Safe today:** the two models **coexist by construction** — the manager only touches squads whose `SquadContext.objective_id` is `Some` (it mints those); `AttackMission`/`SquadDefenseMission` squads leave it `None` and run unchanged. **Recorded as the G4/W1 work** (rows updated with the behavior-map line refs); no code this session (a premature migration is a regression). **Recommended next sequence:** G3 (tactics/orders in the manager, the ADR 0008a EXP-* loop) → G4 (migrate `AttackMission`) → W1 (migrate `SquadDefenseMission`), validated on the combat sim per-change + the private server.
- **2026-06-18 (ai)** — **P2.K4 DEFERRED with prerequisite analysis (not stubbed) — SK mineral mining is blocked on infra the bot lacks.** The *reuse* is ready (`MineralMiningMission`, which `LocalSupplyMission` already creates per extractor), **but** it requires a `(mineral, extractor, container)` triple: `create_structure_data` only forms a `mineral_extractors_to_containers` pair when an **extractor structure AND an adjacent container already exist** (`structure_data.rs:123–129`). SK rooms are **controller-less / unownable**, and **the bot has no mechanism to build a remote extractor or container in an unowned room** (the room planner plans only owned rooms; `structure_data` only *reads* existing structures). Second, **"the ADR 0012 market gate" is the market-*trading* risk system** (FairValue oracle + exposure ledger, `transfer/fairvalue.rs`), **not** a "should we mine this mineral given glut" predicate — that predicate does not exist. So K4 needs **two new pieces**, each substantial and outside the combat overhaul's scope: **(1) remote extractor + container construction** in an unownable room (the real blocker — without it the mineral path can never fire), and **(2) a mineral-glut mining-decision gate** (stored-amount / market-exposure aware). Shipping inert, never-firing mineral code would be misleading, so K4 is **deferred-with-analysis** rather than stubbed. The energy half of SK exploitation (the prize — K2c clearing + K3 source mining) is complete. ADR 0018 §3 + the K4 row updated. **Recommend K4 as a separate future workstream** (gate it behind a `source_keeper.mine_minerals` flag + a glut predicate, built on a new remote-construction capability).
- **2026-06-18 (ah)** — **P2.K3 IMPLEMENTED — suppression-gated SK source mining + hauling.** `SourceKeeperFarmMission` is now a full coordinator: alongside the `Farm{sk}` objective upsert (K2c) it **owns mining children** — one `SourceMiningMission` **per source** in the SK room (enumerated from `static_visibility_data.sources()`, created with `Some(mission_entity)` owner + `add_mission`, mirroring `LocalSupplyMission::ensure_children`) + one long-haul `HaulMission` (SK room → homes). Each tick `gate_mining_children` computes a **per-source suppression signal** — a source is minable iff the room is visible AND **no live Source Keeper** (a `is_source_keeper_owner` hostile from `room_data.get_creeps()`) is within `KEEPER_DANGER_RANGE=5` of it — and sets `child.allow_spawning(suppressed)` + `set_home_rooms`; the haul child runs when any source is minable. The duo (K2c) creates the dead-keeper windows; mining exploits them **per source**, and the **K0 `Flee` reflex** (the SK miners are `HarvestJob`s, which carry the `Flee` state) makes a re-appearing keeper cost a *flee, not a death* — closing the energy-sink the operator flagged. Added `source_mining_missions: EntityVec` + `haul_mission: EntityOption` fields + `get_children`/`child_complete` (so cleanup cascades on self-cancel). The suppression signal is **data the child reads** — the miner job still owns its movement (ADR 0008 §5 / principle 8). The `MiningOutpostOperation` SK exclusion is untouched (the coordinator, not the outpost op, mines SK rooms). `SourceMiningMission` drop-mines via `HarvestJob` when container-less, so it works in a fresh (unbuildable) SK room. **`WORLD_FORMAT_VERSION` 10→11** (the mission struct gained child fields → `MissionData::SourceKeeperFarm` reshaped). **Feature still OFF** → inert on deploy. Gates: bot 164, wasm build + clippy clean. **Next: P2.K4** (SK mineral, behind the ADR 0012 market gate) — then the operator's added scope: reconcile `AttackMission`/`SquadDefenseMission` onto the objective+manager (G4/W1).
- **2026-06-18 (ag)** — **P2.K2c COMPLETE — the SK coordinator produces a `Farm{sk}` objective; the manager fields the duo (the reconciled model, end-to-end).** Threaded the `CombatObjectiveQueue` into `MissionExecutionSystemData` (`combat_objective_queue: &mut CombatObjectiveQueue`, added to `MissionSystemData` + both `PreRun`/`Run` construction sites in missionsystem.rs — available to every mission producer). `SourceKeeperFarmMission::run_mission` now, after its self-cancel checks (feature-off / contested → `Success`), **upserts each tick a low-priority preemptible `Farm{SourceKeeper, room}` objective** — `ObjectiveRequest::new(... , OBJECTIVE_PRIORITY_LOW, ForceRequirement::single(SquadComposition::duo_sk_farmer())).owner(SourceKeeper)` → `combat_objective_queue.request(req, game::time())` — and returns `Running`. The `SquadManager` (G2) claims it, fields the `duo_sk_farmer`, and **retires the squad when the upsert stops** (feature-off / contested / lost-homes self-cancel → the objective TTL-lapses → manager Phase-A retire). The mission **owns no `SquadContext`** — the reconciliation (§2.0 (ad)) realized: missions REQUEST + supply context, the manager owns squad state, the `SquadCombatJob` self-drives the kite/engage (job-owns-movement). **K2c-2 (mission-owns-squad) is formally superseded.** Remaining K2 refinement (per-source suppression signal + pure-ROI-decline withdrawal) rides **K3**. Gates: bot 164, wasm build + clippy clean; no WFV bump (the producer change is logic-only). **Feature still OFF** (`source_keeper.farming` default false) → inert on deploy. **Next: P2.K3** — suppression-gated `SourceMiningMission` + `HaulMission` children of the coordinator.
- **2026-06-18 (af)** — **P2.G2 IMPLEMENTED (minimal) — the `SquadManager` (the single combat squad lifecycle owner).** New `military/squad_manager.rs` = `SquadManagerSystem`, a perpetual ECS system wired into `for_each_system!` **after `RunMissionSystem`** (so it sees objectives the producers pushed this tick) and **before `SpawnQueueSystem`** (so its spawn demand is consumed this tick). Each tick, three phases against the `CombatObjectiveQueue`: **(A) reconcile** — gather manager-owned squads (a `SquadContext` whose new `objective_id` is set), **retire** (delete the squad entity via `updater.exec_mut` + `release_entity`) any whose objective is gone (producer stopped re-asserting → TTL lapse) or duplicates, else **re-establish the ephemeral claim** (idempotent — self-heals the claim map after a reset); **(B) field rosters** — for each live squad read the composition off its objective's `ForceRequirement` and queue a spawn for every unfilled slot, broadcasting one shared `spawn_queue.token()` to in-range owned home rooms (`MAX_SPAWN_DISTANCE=6`), each `SpawnRequest` carrying a `create_spawn_callback` that mints a `SquadCombatJob::new_with_squad(target_room, squad_entity)` + `add_member` (the exact `AttackMission` pattern); **(C) claim new objectives** up to `MAX_CONCURRENT_SQUADS=4` via `best_unclaimed_near`, minting a `SquadContext` (`objective_id` + `target` set) bound to the objective. **`SquadContext` gains `objective_id: Option<ObjectiveId>`** (the manager↔objective binding that survives a reset; legacy `AttackMission`/`SquadDefenseMission` squads leave it `None` → the manager ignores them, so the two ownership models **coexist during migration**). **`WORLD_FORMAT_VERSION` 9→10** (SquadContext reshaped). **Scope = minimal "field a `Farm{sk}` squad"**, faithful to ADR 0008 §3: **replacement, not pre-spawn** (a dead member's slot unfills → re-spawned; **no `request_renew`** — the ADR's "never renew" already holds); members **self-drive** (the `SquadCombatJob` MoveToRoom + engage-via-fallback of status-log (ac)), so the manager pushes **no per-tick movement** (job-owns-movement, ADR 0008 §5 ⚑). **DEFERRED (follow-ons, noted in the module doc):** pre-spawn-before-death, per-tick tactical orders (G3), retask-on-complete (≥40% TTL), `SquadId`/`SquadStore` keying (P2.I1 — the squad is keyed by its `SquadContext` `Entity` for now, consistent with G1's claim key), and the general `Recall` terminal state (P2.M0 — until it lands, a retired squad's orphaned members fall to the existing `SquadCombatJob` fallback: **no dangling `SquadContext`, no leak**, they fight/idle to TTL). The manager is **inert without objectives** (only the feature-gated producers create them) → safe on deploy. **S9/S14 contracts now exercised end-to-end** (queue→manager→SquadContext→job). Gates: bot host **164 (+2** manager mapping/distance tests**), wasm build + clippy clean**. **Next: P2.K2c** — the `SourceKeeperFarmMission` coordinator upserts a `Farm{sk}` objective (`ForceRequirement::single(duo_sk_farmer)`) so the manager fields it; then K3 suppression-gated mining.
- **2026-06-18 (ae)** — **P2.G1 IMPLEMENTED — the `CombatObjectiveQueue` (the combat goal layer).** New `military/objective_queue.rs`, modeled directly on the scout `VisibilityQueue` (the operator reference): the two-layer split — persistent `CombatObjectiveData` (a serialized singleton **component**: `objectives` + `UnwinnableTarget` give-up backoff + the minted-`ObjectiveId` counter `next_id`) and the ephemeral `CombatObjectiveQueue` **resource** (working copy + the per-tick `claimed_by` assignment, **never serialized** → self-heals on reset, cannot dangle). Mechanics: idempotent `request(req, now)` upsert keyed by `ObjectiveKind` target-equality (priority max-merge, TTL extend, force/deadline/owner refreshed to the re-asserting producer's current ask) minting a stable `ObjectiveId`; `claim`/`release_entity`/`release_dead` single-owner; `best_unclaimed_near(home, now)` (priority desc, proximity tiebreak, skips claimed + give-up-backoff rooms); `expire(now)`; `mark_unwinnable`/`is_unwinnable_now`/`clear_unwinnable` (exp backoff base 2000 / cap 20000, mirrors the scout reachability backoff); `withdraw`. `ObjectiveKind` = `Secure|Defend|Dismantle|Harass|Farm{PowerBank|SourceKeeper|Core}|Escort` (each carries a room); `ForceRequirement` = `Vec<SquadComposition>` (the manager fields these). Wired the cleanup system (Main-pass: Cleanup, after `VisibilityQueueCleanupSystem`: load→expire→release_dead, creates the singleton) + sync system (Main-pass: Persistence, after `VisibilityQueueSyncSystem`) into the `for_each_system!` macro, and `CombatObjectiveData` into the serialize/deserialize tuples + failure-clear (game_loop.rs). **`WORLD_FORMAT_VERSION` 8→9** (a component joins the positional bincode stream — one loud reset on deploy). **Deferral notes (faithful to the scout model):** (1) **no stored `SuccessPredicate`** — success is observed (a producer stops re-asserting → TTL lapses; the manager observes world-state), exactly like the predicate-free `VisibilityQueue`; (2) **claims are keyed by the squad's ECS `Entity`** (the `SquadContext` entity) for now — the `SquadId` key arrives with P2.I1; ephemeral claims being `Entity`-keyed is correct (runtime handle, never serialized). **The core takes an explicit `now`** so it's pure/kernel-testable without `game::*`. **Register S9 draft contract: TODO with G2** (the seam isn't fully exercised until the manager consumes it). Gates: **bot host 162 (+10 kernel tests** — upsert/dedup, max-merge+TTL, expire+runtime-prune, claim/release single-owner, proximity tiebreak, exp-backoff cap+clear, skip-unwinnable, load/save round-trip drops claims, withdraw**), wasm build + wasm clippy clean** (host clippy: only the 10 pre-existing `bodies.rs` `contains()` warnings). **Next: P2.G2** — the `SquadManager` (consume the queue: claim `best_unclaimed` → mint a `SquadId` → field a roster reusing `SquadContext::from_composition` + the `create_spawn_callback` pattern → retire on withdrawal), ≥ enough to field a `Farm{sk}` squad. **Operator also added scope this session:** reconcile `AttackMission`/`SquadDefenseMission` onto the objective system (one cohesive set — missions REQUEST objectives + supply context, never drive creep movement) — tracked as G4/W1 (the §2.0 (ad) reconciliation), sequenced after G2 tactics.
- **2026-06-18 (ad)** — **SQUAD-MANAGEMENT RECONCILIATION (operator): all combat squads are managed by the `SquadManager` via `CombatObjectiveQueue` requests — missions/operations never own squad state.** Operator: the SK farm (and everything) should use a **request command** (scout-style, like the war objectives) — request an objective, the manager assigns + runs a squad — *not* a mission managing squad state. This is the ADR 0008 G-workstream model, now confirmed as the **single** squad-management layer for ALL combat (SK, attack, defense). **Consequences:** (1) the SK clearing = a **`Farm{sk}` objective** on the `CombatObjectiveQueue` fielded by the `SquadManager` (ADR 0018 §3.3 rewritten); **`SourceKeeperFarmMission` is repurposed to a thin per-room COORDINATOR** (requests the objective, owns the K3 mining, holds the suppression signal) — it does **not** own a `SquadContext`. (2) **K2c-2 (mission-owns-squad) is SUPERSEDED** — the spawn/renew/teardown lives in the `SquadManager`, not the mission, which also resolves the (ac) teardown problem (the manager retires the squad on objective withdrawal). (3) **K2c-squad now depends on the G-workstream** (G1 `CombatObjectiveQueue` + G2 `SquadManager`) — a re-sequence: the SK squad can't run until a (minimal) queue+manager exists. (4) `AttackMission`/`SquadDefenseMission` likewise migrate to objective-producers (G4/W1) — the same reconciliation. **"Full set of work" (operator) now = G1 (queue) → G2 (manager, ≥ enough to field a `Farm` squad) → K2c (produce the objective + coordinator) → K3 (suppression-gated mining) → K4 (mineral)** — i.e. the combat-rewrite squad core, a large multi-increment build, deploy-gated. Build sequentially, commit green increments, then validate the whole set + deploy.
- **2026-06-18 (ac)** — **K2c-2 investigation (squad spawn/renew) — scoped + key simplifier found; held for a focused pass.** **Finding:** `SquadCombatJob` **self-drives** — it has its own `MoveToRoom` state (→ `target_room`) + a `fallback_movement`/`execute_combat_via_seam` that engages hostiles in the target room when the mission supplies NO orders (`squad_combat.rs:32,109,403,509,520`). So K2c-2 does **not** need to replicate `AttackMission`'s Engaged focus/advance logic — the mission just spawns the `duo_sk_farmer` into a `SquadContext` (target = the SK room) and the job moves there + kites/engages keepers itself (the movement overhaul + `decide_combat`). **Approach decided:** *replicate* a minimal spawn loop (reuse `SquadContext::from_composition`, `SquadCombatJob::new_with_squad`, `SpawnRequest` + the `create_spawn_callback` pattern @ `attack_mission.rs:539-565`, `duo_sk_farmer`); defer *factoring out* AttackMission's spawn machinery to the G-workstream `SquadManager` (operator's repetition-OK guidance). **Open questions to resolve first (the teardown is load-bearing):** (1) **squad-entity teardown on cancel** — `AttackMission` has no `get_children`/`remove_creep` override; squad entities are deleted explicitly (e.g. the wave-wipe loop `attack_mission.rs:842-850`), so the SK farm must release its `SquadContext` entity + recall its creeps on self-cancel or it leaks a dangling squad (the exact dangling-ref class the rewrite fights — find the squad-GC/cleanup path before writing the spawn); (2) the member-maintenance system that prunes dead members from `SquadContext.members` (dead→unfilled slot→re-spawn, vs explicit pre-spawn renewal); (3) the per-source suppression-signal representation K3 reads. Held: introducing a half-built squad lifecycle that leaks on cancel is worse than a clean pause. **Next: resolve (1)–(3), then implement spawn + renew + teardown + suppression signal.**
- **2026-06-18 (ab)** — **SK farm stop/cancel (operator-flagged gap): the mission self-cancels when no longer viable, not just when disabled.** The ADR's §3.5 wrongly implied the *operation* withdraws — but the operation's `mission_data` is read-only, so it cannot retire a mission. Corrected: the **`SourceKeeperFarmMission` self-cancels** (returns `MissionResult::Success` → cleanup releases the squad), like `MiningOutpostMission` self-completing on observed state. Implemented in `run_mission` the cheap **permanent** checks: feature `source_keeper.farming` OFF (stops EXISTING farms, not only new ones) and the room being **contested** (player owns/reserves it); lost-homes already self-cancels via `pre_run_mission`. **Transient** pressure (CPU-critical, war preempting the bodies) deliberately does NOT cancel — it must pause the squad, not tear it down on a blip. ADR 0018 §3.5 rewritten (permanent→cancel vs transient→pause; symmetric with the ROI commit via `score_sk_farm` recomputed `already_committed=true`). The ROI/out-of-range withdrawal + squad teardown ride K2c-2. Gates: bot 152, wasm + clippy clean.
- **2026-06-18 (aa)** — **P2.K2c-1 IMPLEMENTED — the persistent `SourceKeeperFarmMission` type + the operation's create hook.** New `MissionData::SourceKeeperFarm` enum arm + both dispatch arms + `mission_type!` (missions/data.rs) + `missions/mod.rs` decl; `missions/sourcekeeperfarm.rs` = `SourceKeeperFarmMission{owner, sk_room_data, home_room_datas}` (plain, non-machine) with the `Mission` boilerplate — `run_mission` returns **Running** (persistent placeholder; never Success), `pre_run_mission` drops dead home rooms (errors→cleanup if none left). The `SourceKeeperOperation` loop now computes per-room `already_committed` (a `SourceKeeperFarmMission` on the room) + a within-scan concurrency count, feeds both to `score_sk_farm`, and on **Commit && !already_committed** builds the mission + `room_data.add_mission` (idempotent, mirrors `MiningOutpostOperation`). **The squad spawn/renew is the next increment (K2c-2)** — the mission owns no creeps yet; `run_mission` is a TODO for the `duo_sk_farmer` `SquadContext`/`squad_combat` lifecycle (reusing AttackMission machinery), the T-NPC-2 kite, the per-source suppression signal, and Withhold/Veto→withdraw. Gates: bot 152, wasm + clippy clean; no WFV bump (additive enum variant). **Next: K2c-2** — decide factor-out-vs-replicate of AttackMission's squad spawn/rally/renew, then implement.
- **2026-06-18 (z)** — **P2.K2b IMPLEMENTED — the `SourceKeeperOperation` (economy family).** New `OperationData::SourceKeeper` enum arm + both dispatch arms (`operations/data.rs`); `OperationManagerSystem` always creates it (like the other ops — behavior is gated inside `run_operation`, not at creation); `SourceKeeperOperation` in `operations/sourcekeeper.rs` mirrors `MiningOutpostOperation`: a `gather_candidate_room_data` that marks a scouted SK room (K1's static `is_source_keeper()` + sources) **viable** and never expands the BFS *through* an SK/hostile-owned room, and a throttled (`time%50==35`) `run_operation` that gathers home rooms (`SK_HOME_MIN_RCL=6`) + `SkFrontier` candidates (within `features.source_keeper.max_range`), requests visibility for unscouted ring rooms, builds `SkRoiInputs` per candidate (live sources, `distance×50` haul tiles, est. duo cost, home-capacity affordability, contested = hostile owner/reservation, CPU tier, cap), runs `score_sk_farm`, and logs the verdict behind `features.source_keeper.diagnostics`. New `SourceKeeperFeatures` (`farming` default **OFF**, `max_concurrent_farms=1`, `max_range=2`, `diagnostics`). Mission spawn/withdraw is a **TODO(K2c)** (the op scores + logs but creates nothing yet). Gates: bot 152, wasm + clippy clean; no WFV bump (additive enum variant + serde-default feature struct). **Next: K2c** — the persistent `SourceKeeperFarmMission` + wire Commit→spawn / Withhold→withdraw.
- **2026-06-18 (y)** — **P2.K2a IMPLEMENTED — pure SK-farm ROI scorer (first K2 sub-leaf).** New `operations/sourcekeeper.rs` with `score_sk_farm(&SkRoiInputs) -> SkRoiScore{gross, net, decision}` — **pure arithmetic, no `game::*`**: hard-gate veto (contested / CPU-critical / military-wanted / unaffordable / over `max_concurrent_sk_farms` when not already committed), then the energy/tick model `net = gross(n×4000/300) − suppression(duo_cost/1500) − mining(WORK to saturate) − haul(CARRY for 2×dist round-trip) − cpu_penalty(dist)`, then `Commit` iff `net ≥ MIN_SK_ROI` (5.0) with a `SK_ROI_HYSTERESIS` (3.0) looser floor for an already-committed farm (no flapping). Engine values mirrored locally (provenance-named) so it's a clean kernel. 5 unit tests on hand-computed e/t (close room commits @ ~29 net; distance kills it; each hard gate vetoes; an existing farm keeps its slot over the cap; hysteresis band keeps a marginal farm but won't start one). Gates: bot 152 (+5), wasm + clippy clean. **Next: K2b** (`SourceKeeperOperation` enum arm + scan `SkFrontier` via K1's `is_source_keeper()` + run the scorer + commit/withdraw the farm mission, feature-flagged) then **K2c** (`SourceKeeperFarmMission`).
- **2026-06-18 (x)** — **P2.K0 reworked from a top-level early-return into a first-class `Flee` STATE (operator critique).** Operator: bolting `try_flee_from_local_threats()` onto every `run_job` top is ugly + bypasses the state machine, and the flee `action_flags.consume(MOVE)` was a "competes-for-actions" smell — flee should be a state, transitioned to. Reworked: `movebehavior` now exposes `is_threatened()` (detection, the transition guard) + `issue_flee()` (the move, no `SimultaneousActionFlags` guard needed) + private `compute_flee_targets()`; the pure `nearby_threat_flee_targets` core + 3 tests are unchanged. Each remote-economy job (`staticmine`/`harvest`/`linkmine`/`haul`) gains a fieldless **`Flee` state**: a `if is_threatened(tick_context) { return Some(XState::flee()); }` guard at the top of each state's `tick` (the canonical transition), and `Flee::tick` issues the flee and returns to the entry state (`Idle`/`MoveToContainer`) once clear — so the **job owns the move** and it competes with no other action. **Operator notes folded in:** (1) the per-state transition is repetitive by design — de-dup deferred to the **state-machine-rework ADR**; (2) `screeps-machine`'s `*` emits a body on *every* variant + a dispatcher (so it only works for methods no state implements manually — not a safe shared hook here), which is why the transition is per-state not `*`. Gates: bot 147, wasm + clippy clean (no new warnings); no WFV bump (additive fieldless enum variant). Supersedes the (v) early-return design.
- **2026-06-18 (w)** — **P2.K1 IMPLEMENTED — scout-driven static SK detection.** `RoomStaticVisibilityData` gains `keeper_lairs: Vec<Position>` (`#[serde(default, rename = "kl")]` — mirrors the existing `exits` field, so additive / **no WFV bump**), populated from keeper-lair structures in `create_static_visibility_data` (`room.find(STRUCTURES)` → filter `KeeperLair` → `pos()`), plus `is_source_keeper()` / `keeper_lairs()` accessors. SK identity is now persisted scout-once/know-forever (lairs are permanent), independent of the transient dynamic `source_keeper` flag — exploitation triggers off this. **Coordinate sector-ring predicate dropped:** room x/y coords are private in this game-API version (the long-standing `room/data.rs` TODO that drove structure-scanning detection), and the operator chose scouting over convention — so the scouted static flag is the sole source of truth. **`SkFrontier` candidacy** (static-flag + range-to-home + not-owned/contested) stays a *computed* candidate and rides **K2**'s operation scan (it needs the home set). **No behavior change** (pure intel). Gates: bot host 147, wasm + clippy clean. **Next: P2.K2** (the `SourceKeeperOperation` + ROI scorer + persistent `SourceKeeperFarmMission`).
- **2026-06-18 (v)** — **P2.K0 IMPLEMENTED — general remote-economy flee reflex (first K leaf).** `try_flee_from_local_threats()` in `jobs/utility/movebehavior.rs`: reads cached `RoomData` hostiles (skips **our own** rooms — defense/towers handle those), flees every **dangerous** hostile (a live `ATTACK`/`RANGED_ATTACK` part — keepers + invaders, not harmless scouts) within `THREAT_FLEE_RANGE=5` via the existing rover `movement.flee(...).range(N)` (the `squad_combat::flee_from_hostiles` primitive — pure reuse). Wired at the **top of `run_job`** for the four remote-economy jobs (`staticmine`/`harvest`/`linkmine`/`haul`): if it fleed, the state machine is skipped that tick — the **job owns the move** (principle 8). Pure core `nearby_threat_flee_targets` (range + danger filter) is host-unit-tested (3 tests). Fixes the stand-and-die energy sink for ALL remote mining (was: room safety gated only new spawns; existing creeps had zero threat awareness). **Gates:** bot host 147 (+3), wasm build clean, clippy clean (only the 10 pre-existing `bodies.rs` `contains()` warnings, none mine). No serialized-shape change → no WFV bump. **Behavioral validation = a deploy soak** (a remote creep flees a keeper/invader instead of dying). **Next: P2.K1** (scout-driven static SK detection + `SkFrontier`).
- **2026-06-18 (u)** — **Flagged the mission/squad/job movement-split tension in [ADR 0008](../design/0008-combat-and-squad-architecture.md) (operator: "fix later").** ADR 0008 §4/§5 as drafted has the `SquadManager`/mission compute per-tick movement orders (`TickMovement` Engage/Kite/Hold, the anchor advance) with the job *"executing, never re-deciding"* + mission-side `advance_virtual_pos`/`issue_virtual_anchor_flee` — the same inversion of the bot's normal job-owns-movement pattern. Added a prominent **⚑ DEFERRED flag** callout in §5 + a pointer at §4: missions/manager set **goals/objectives/context** (objective, shared anchor `virtual_pos`+orientation, focus, suppression signal); the **squad/job performs the actual movement request to the pathfinder**. Shared *context* (anchor frame, cohesion gate, focus) stays squad/manager-level; only *movement-request issuance* moves down. To fix later with Migration step-2 anchor-mover + `SquadManager` (M/G). Consistent with ADR 0018 principle 8 + the §2.1 convention. No new task (it rides the existing M2/G2 work); flag only.
- **2026-06-18 (t)** — **SK design refinements (operator): job-owns-intent principle + ROI-scorer specifics + `SkFrontier` storage model (ADR 0018 §2 principle 8, §3.1, §3.2; §2.1 new convention).** Three operator steers folded in: (1) **Missions provide context; jobs own creep intent (incl. movement)** — added as principle 8 + a standing §2.1 convention. The squad system currently inverts it (`AttackMission` `TickMovement`, mission-side `issue_virtual_anchor_flee`) = a known M/G tension; K must not extend it (K0 flee + K3 hold/retreat live in the job; K2's mission only *publishes* the per-source suppression signal as data). (2) **ROI scorer made concrete** (§3.2): inputs (yield `n×4000/300` e/t, haul distance, home capability, contention/threat veto, CPU/posture) → amortized e/t cost model (suppression/mining/haul) → `net ≥ min_sk_roi` floor + hysteresis + hard gates (`max_concurrent_sk_farms`, economy-scaled) → output `{score, Commit|Withhold|Veto}`; **pure/kernel-testable**, no `game::*`. (3) **`SkFrontier` = computed candidate, NOT stored** (§3.1): derived each scan from {static SK flag, range-to-home, not-owned/contested}, like `gather_candidate_room_data`; only the **static SK intel** (lairs/sources — the one new `RoomData` field) + the operation's **committed-farm set** persist. K1/K2 rows updated. **Resume unchanged: P2.K0/K1.**
- **2026-06-18 (s)** — **Miner/hauler survivability decided — a general flee reflex (P2.K0), not SK-specific ([ADR 0018](../design/0018-source-keeper-room-exploitation.md) §3.4 + alternatives/migration; audit `webx9je2v`).** Operator: miners/haulers must flee / stay out of range of invaders + keepers so they don't become an energy sink. Audit confirmed **they stand and die today** — room safety (`is_room_safe`/`allow_spawning`) only gates *new spawns*; existing miners (`staticmine`/`harvest`/`linkmine`) and haulers (`haul.rs`) have **zero threat awareness** and work until killed, and haulers additionally get *stuck* (a hostile room turns unpathable under `HostileBehavior::Deny`) burning pathfinding ops. **Decision:** a **general** job-level reflex `try_flee_from_local_threats()` in `jobs/utility/movebehavior.rs` (sibling to `mark_working`) that reads `RoomThreatData.hostile_creeps` and calls the **existing** `movement.flee(...).range(N)` primitive (already used by `squad_combat::flee_from_hostiles` / `issue_virtual_anchor_flee` — pure reuse), wired into remote miners + haulers before work intents. It is **general** (hardens ALL remote mining, not just SK) → tracked as **P2.K0**, lands first/independent, and is the safety net beneath the K2 per-source suppression. Survivability is now a **3-layer model**: spawn-gate · per-source suppression (K2) · flee reflex (K0). *(Side-finding: `check_movement_failure` is wired for telemetry — the "dead/IBEX-015" framing is about job-level RECOVERY, which is still pending per the metrics-crate note; left as-is.)* **Resume point unchanged: P2.K0/K1.**
- **2026-06-18 (r)** — **SK structure decided — new mission type + war-system/economy fit ([ADR 0018](../design/0018-source-keeper-room-exploitation.md) revised; audit `wg8jqmdk1`).** Operator asked whether SK needs a new mission type and how it fits the war system + overall structure; a 3-reader audit mapped the op/mission/job contract, the war pipeline, and the mining pipeline. **Decisions:** (1) **NEW persistent mission `SourceKeeperFarmMission`** is required — `AttackMission` is one-shot (completes → **deletes itself** at `attack_mission.rs:1991`; wave-wipe ≠ standing renewal), incompatible with a 300t-respawn farm that must run forever with per-creep TTL renewal and no economy-abort. The dead `AttackReason::SourceKeeper`/`AttackMission` path is **superseded** (optional throwaway one-shot kiting smoke test only). (2) **Owned by a NEW economy-family `SourceKeeperOperation`, NOT `WarOperation`** — SK farming is a remote-mining ROI decision, not a posture/`WarDecl` campaign; mirrors `MiningOutpostOperation`. (3) **Reuse, don't fork:** the farm owns the `duo_sk_farmer` squad via existing `SquadContext`/`squad_combat`/the new movement, plus per-source `SourceMiningMission` (already per-source, neutral-capable, `allow_spawning`-gated — `missions/localsupply/source_mining.rs`) + `HaulMission` children; the lone new code is the coordinator. (4) **Combat capacity arbitrated/preemptible:** near-term a posture/capacity/CPU gate on the operation; target = a low-priority `Suppress` objective on the ADR 0008 `CombatObjectiveQueue` (preempted by defense/active war). (5) **Detection = scout-driven, persisted in STATIC room data** (lairs are permanent — scout-once/know-forever; dynamic = keeper liveness/respawn), per operator. Adding op/mission **enum variants** is serialization-transparent (no WFV bump); a new STATIC room-data **field** needs `#[serde(default)]` or a WFV bump. ADR 0018 §3.1/§3.2/§3.3/§3.4 rewritten + new §3.6 (structure & war-system fit) + alternatives/migration updated; workstream K rows (K1/K2/K3) revised to match. **Resume point unchanged: P2.K1** (scout-driven static detection + `SkFrontier`).
- **2026-06-18 (q)** — **Source Keeper exploitation scoped — [ADR 0018](../design/0018-source-keeper-room-exploitation.md) authored (Proposed); new workstream K added.** Operator: SK-room handling is "a good place to start" (it produces the live combat the peaceful P2.H3 capture couldn't, and SK sources are the highest-yield remote — 4000/cycle vs 1500 neutral). A 3-reader capability audit (workflow `wx6ef4kpi`) found **both halves are missing**: (1) **clearing is dead code** — `AttackReason::SourceKeeper`/`duo_sk_farmer` exist (`attack.rs:177`) but nothing creates the target (no `TargetSource::SourceKeeper` variant + no `From` mapping at `war.rs:47-61`; the offense scan never emits it and explicitly filters keepers out of the invader path at `war.rs:873`); (2) **mining explicitly excludes SK rooms** at two checkpoints (`miningoutpost.rs:54,66,140`), no SK mineral harvest, no clear→mine handoff; (3) **detection works only to EXCLUDE** — lair scan → `source_keeper` flag (`room/data.rs:547`) bars claim/mining/pathing, but there is NO coordinate predicate (can't know a room is SK pre-scout) and NO adjacency/frontier classification. **The clearing TACTICS already exist on paper** ([0008a](../design/0008a-combat-tactics.md) **T-NPC-2** SK range-3 kite-kill + lair-rotation pre-staging); ADR 0018 is the missing **strategic/economic/intel layer** that turns them on + pairs them with mining. Decision: build SK exploitation in 4 phases (intel→clearing→source-mining→mineral-mining), feature-flagged (`source_keeper_farming`, default OFF), exploited-never-owned (request→fulfill→release on the CombatObjectiveQueue), suppression-gates-mining. **Tracked as workstream P2.K** (§2.7a); design README updated (also indexed the previously-missing 0017). **NEXT / resume point: P2.K1** (coordinate `is_source_keeper_room` predicate + `SkFrontier` adjacency tag — pure, no behavior change) then **P2.K2** clearing. K2 can activate the dead `AttackReason::SourceKeeper` on the **current** AttackOperation/AttackMission path for early live-combat validation (the movement-overhaul payoff) WITHOUT waiting for the G-workstream SquadManager; migrate to the manager with P2.G4.
- **2026-06-18 (p)** — **P2.H3 DONE + movement polish (A1 orientation, A2 self-healing corridor) + DEPLOY VALIDATED on the Docker private server (commits `472624b`, `f5966ef`).** **H3:** `screeps-ibex-metrics` gains `CohesionMetrics` (squad_count / engaged_squads / avg_in_formation_rate / avg_centroid_spread / max_pairwise) as an **additive optional** seg-57 field (schema stays v1 — `skip_serializing_if` + `serde(default)`); `metrics.rs` aggregates `cohesion::measure` over live `SquadContext`s each tick (member positions paired with layout slot offsets); `score.rs` replaces the placeholder `military: None` with the **mean in-formation rate across the engagement window** (blocks with `engaged_squads > 0`; absent ⇒ blend renormalizes over CPU+econ), repairs the broken test `block` helper, adds a military-term test. **Movement A1:** each Engaged tick records the anchor→focus bearing as `threat_direction` + calls `reassign_slots()` (front-worthy members → threat-facing slots; member↔slot only, no geometry rotation ⇒ stable). **Movement A2:** `advance_virtual_pos` probes the **tight (2×2) footprint every tick** independent of the current member layout — a quad whose box can't fit collapses to single-file (`Line`), and **re-forms to the box the instant it fits again**; decision extracted to a pure `corridor_layout_transition` (unit-tested: collapse / stay / re-form / duos-left-alone). Scoped to quads (every intended `Line` is a 2-member duo ⇒ a 4-member `Line` is always a collapsed quad, safe to re-form). No `WORLD_FORMAT_VERSION` bump (no serialized-shape change; box/line already existed). **Tests:** metrics 4, eval/score 5 (incl. military term), bot 144 (+2 corridor-transition), engine 40 / rover 16 / decision 20 / agent 17 unchanged; host+wasm+clippy clean. **DEPLOY:** built + uploaded the WFV-8 bot to `private-server` (ibex) via the server-kit (`deploy` → 33 s, 3.36 MiB); kept the existing world (no `--reset`). **500-tick capture (`runs/movement-validate-f5966ef-…`) is GREEN:** clean one-shot WFV-8 reset (`faults.deser_failures` pinned at **1** across all samples, never climbs; `vm_starts` constant; console window 0 panics / 0 deser-failures / 0 error lines); **no CPU death-spiral** (used avg 4.03 / max 6.19 of 100, bucket full at 10000); **cohesion block present + well-formed in every seg-57 sample** (schema v1); 44 creeps stable. **HONEST GAP:** the capture window was peaceful (`squad_count: 0` throughout — the bot was in pure economic expansion: construction / scouting / mining outposts, not in conflict with ibex-2), so the cohesion **behavior** (squad re-form, non-zero military term) was **not exercised live** — it is covered by deterministic tests (sim `re_forms_a_tight_box_after_a_corridor` + the new `corridor_layout_transition` + the kiting sim test). The quad-box re-form only triggers on a `≥2-tower` attack (`quad_ranged`), which this map doesn't produce on demand; live behavioral confirmation awaits a combat scenario (P2.H4 opponents, or a forced engagement). **CP-H/M telemetry + deploy-health gate met; the live combat-move gate carries to H4.**
- **2026-06-17 (o)** — **MOVEMENT WORKSTREAM COMPLETE — M2 live swap + M3 + re-form (commits `a17027f`, `c5265ab`, `be63eed`; rover `87db130`).** **M2 live swap:** `SquadPath` embeds `rover::AnchorPath`; `advance_virtual_pos` drives it through the live server `PathFinder` (`ScreepsPathfinder`) over the room cost matrix with terrain baked in so the `moving_maximum` footprint transform covers walls; the squad box routes as a unit, on `Blocked` it holds (never a blind step). `WORLD_FORMAT_VERSION` 7→8 (one loud reset). The cohesion gate (`advance_squad_virtual_position`) is unchanged. **M3:** sim squad mover gains a loose mode unifying corridors + blobs — a blob (N>4) is always loose; a small squad always tries the box and, on `Blocked` (corridor), relaxes to single-file, then **re-forms to a tight box the instant the box can path again** (the AnchorPath footprint-invalidation makes the box re-test real). Live `advance_virtual_pos` retries width-1 on `Blocked`. **Sim squad support** (`screeps-combat-agent::squad::SimSquad`) is the validation vehicle — anchor mover + members + cohesion gate, all in-process. **Tests:** rover 13, decision 20, agent 18 (squad: box-crossing, 3-wide-gap detour, 1-wide-corridor single-file thread, **re-form to tight box after a corridor**, 5-blob loose advance, fully-sealed→Blocked); bot 142; host+wasm+clippy clean. **DEPLOY-VALIDATION PENDING (operator gates):** the M2 live swap is compile + full-suite green but NOT behaviorally validated live (`SquadContext` is specs-coupled so the live cohesion gate can't run headless); validate on the private server (squad cohesion on seg-57, no scatter, clean WFV-8 reset, no path CPU death-spiral) before mmo. **CP-M / M3 reached pending that soak. Next: P2.H3** (cohesion metrics → seg-57 + military score; its cohesion *core* already exists from INC 3).
- **2026-06-17 (n)** — **Movement: INC 0b/2/1/3 + the M2 mechanism landed (rover `69300fc` ptr; commits `0940825`/`111fa13`/`c71b266`/`86cfbad`/`6ca0610`).** Bottom-up, all sim-validated: **INC 0b** (`0940825`) — the sim plans paths through rover: a `CombatWorld` `CostMatrixDataSource` + `pathing::resolve_move_direction` (rover's headless `LocalPathfinder` → next-step `Direction` for the engine to resolve); **`screeps-combat-agent` became a workspace MEMBER** (operator note — it depends only on members now, so it inherits the workspace `[patch]`+lock; dropped the replicated patches + seeded lock; builds host+wasm). **INC 2** (`111fa13`) — `decide_movement` on the seam (kite/engage/flee/heal-follow goals) + `agent_intents` applies them; **end-to-end kiting works (EXP-KITE-1 via the seam): a ranged kiter takes 0 melee while chipping a melee chaser, self-play through the engine.** **INC 1 / P2.M1** (rover `c71b266`) — `moving_maximum(w,h)` footprint cost transform (generalizes the dead `apply_quad_cost_overlay`); seals gaps narrower than the footprint. **INC 3** (`86cfbad`) — cohesion-metric **core** (`combat-decision::cohesion`: spread / max-pairwise / in-formation-rate) — the M2/M3 validation instrument + H3's basis, placed in the shared member crate (NOT eval/seg-57). **INC 4 / P2.M2 MECHANISM** (rover `6ca0610`) — `AnchorPath`: a virtual anchor that follows a **cached footprint-aware path** (pathfind once over a `moving_maximum`-transformed matrix, follow, re-path on invalidation/stuck, signum fallback) replacing straight-line `advance_virtual_pos`; generic over `PathfindingProvider` (live=ScreepsPathfinder, sim=LocalPathfinder), serde for persisted squad state; sim-tested (routes a 2×2 footprint around a wall). Tests: rover 13, decision 20, agent 11, all clean host+wasm+clippy. **REMAINING for M2 = the LIVE swap** (deploy-gated, not done): thread the bot's `PathfinderService` + a footprint cost matrix through `advance_squad_virtual_position` (attack_mission 3 sites) into `advance_virtual_pos` so `SquadPath` embeds `AnchorPath`; bump `WORLD_FORMAT_VERSION`; activate dormant `threat_direction`→`orient_toward` + `reassign_slots`. It can't be behaviorally validated without a deploy (no mmo deploy w/o go-ahead) and `SquadContext` is specs/Entity-coupled (the cohesion gate can't run headless), so it's checkpointed as a deliberate integration. **Then P2.M3** (relax-for-corridors width-1 line + N>4 loose-centroid). THEN H3.
- **2026-06-17 (m)** — **Movement workstream STARTED — INC 0a: headless `LocalPathfinder` in rover (rover `1109360`, superproject `a3dd8ab`, ADR `a8eb224`).** Operator reprioritized: do **all of movement before H3** ("creeps aren't useful without it"), and movement decisions need real **multi-step room-aware pathfinding**, not a greedy stepper (a kiter must path around obstacles, not corner itself). Two architecture decisions (operator, 2026-06-17, recorded in ADR 0006 §B.2): **(1) rover is the one movement/intent system everywhere** — rover drives intent (planning, caching, stuck/repath, Follow/pull, visualization), and the **"server" validates** (live = real server; sim = the `screeps-combat-engine` `resolve_moves` = the `movement.js` port, authoritative). So the seam emits movement **goals** (`MoveTo`/`Flee`), pathing lives below it (shared, cached), conflict resolution is engine-authoritative in sim. **(2) the shared pathing goes IN rover** (the pathfinding-system home; rover already has the `screeps`-gated/pure-default split — the foreman pattern). Rover shipped only the JS `ScreepsPathfinder`, so **INC 0a adds a pure headless `LocalPathfinder`** (weighted-grid A* / flee-Dijkstra over a caller-supplied `LocalCostMatrix`, single room, paths exclude origin) usable without the `screeps` feature; gated the `SOURCE_KEEPER_*` consts behind `screeps` so the default build (the harness consumes it) is warning-free. 6 rover host tests; rover clean both feature configs; bot wasm unaffected. **Increment roadmap (bottom-up):** ✅INC 0a rover `LocalPathfinder` → INC 0b agent: `CombatWorld` `CostMatrixDataSource` + drive rover headless → `Direction` → engine `resolve_moves` → INC 2 `decide_movement` on the seam (kite/engage/flee/heal-follow) + sim apply → INC 1 (**P2.M1**) `moving_maximum` W×H footprint transform **in rover** → INC 3 cohesion-metric core (eval, pulled forward — NOT seg-57) → INC 4 (**P2.M2**) anchor mover: cached footprint path replacing straight-line `advance_virtual_pos` + activate dormant orient/rotate/reassign + cohesion gate (the live swap; `WORLD_FORMAT_VERSION` bump; no mmo deploy w/o go-ahead) → INC 5 (**P2.M3**) relax-for-corridors + N>4 loose-centroid. Then H3.
- **2026-06-17 (k)** — **Combat decision lifted into its own member crate `screeps-combat-decision` (commit `cfd0966`; operator-prompted — "foreman pattern").** A 4-investigator + synthesis workflow confirmed `screeps_ibex::combat` was already pure (only *names* screeps-game-api value types, zero `crate::`/`super::`/game::* refs, no serde), so it lifts verbatim. New **workspace MEMBER** crate `screeps-combat-decision` (the `screeps-foreman`/`screeps-combat-engine` profile: lone dep `screeps-game-api`, builds host+wasm, inherits the workspace patch, **no cargo feature**) now holds the seam + `select_focus_target`/`decide_combat`. The bot **re-exports** it as `crate::combat` (`pub use screeps_combat_decision as combat;`) so the live adapters (`squad_combat`, `attack_mission` — the only `game::*` users) are byte-unchanged. **Payoff:** `screeps-combat-agent` dropped its whole-bot dependency — it now depends only on `screeps-combat-decision` + `screeps-combat-engine` (two tiny members); its host build went from compiling the entire wasm bot (~26s) to three small crates (~6.5s) and its `[patch]` table from 7 entries to 1. The crate boundary now *mechanically* enforces "no `game::*` below the seam". No serialized shape changed → no `WORLD_FORMAT_VERSION` bump. Gates: decision 9 + agent 5 (incl. end-to-end engine kill) + full bot suite green; bot host/wasm/clippy + full `build-wasm` clean. **Combat crate trio is now: engine (mechanism) · decision (tactics/seam) · agent (sim glue).**
- **2026-06-17 (j)** — **P2.H2 combat decision extracted + live-swapped onto the seam — ONE implementation now drives live + sim (commits `1dccd10`, `11d648c`).** Extended the seam to the **per-creep combat decision**: `CombatView` gains `me` + per-creep `orders` (`CreepOrders`); `CombatIntent` variants carry the target **position** (what the `IntentRecorder` digest folds) + the creep id (`None` for structures). New pure `combat::decide_combat` — a faithful port of `squad_combat`'s `execute_attack_with_orders`/`execute_heal_with_orders` (orders path) + `fallback_attack`/`fallback_heal` (no-orders path), emitting in the live pipeline order (melee A → ranged B → heal) so the digest matches. **Live swap:** `squad_combat::Engaged` now runs attack+heal through `execute_combat_via_seam` (builds the `CombatView` from `game::*` — the live adapter leaf) → `decide_combat` → `translate_intents` (re-emits through the guarded sink in order); the four inline functions are **deleted** (one implementation, no drift). DTO building shared via `creep_to_dto`/`structure_to_dto` (squad_combat + attack_mission). `screeps-combat-agent`: `SimView::view_for` yields per-creep views with the shared focus; `IbexAgent::decide = decide_combat`; `to_engine_action` translates intents to engine `CombatAction`s — **incl. an end-to-end test where the bot's REAL decision focus-fires a hostile healer to death through `resolve_tick`**. **Parity = by construction** (the live path *is* `decide_combat`), pinned by `decide_combat` unit tests fixing the exact (category, target-pos) intent sequence; the live-server *digest* comparison is **P2.H5** (with the golden vectors). 9 bot combat tests + 5 agent tests + full bot suite (151) green; host+wasm+clippy clean. **Movement (formation + kiting) is NOT extracted — it rides P2.M2** (the anchor-mover rework) and is outside the digest gate. **P2.H2 done; next P2.H3** (cohesion metrics + military score).
- **2026-06-17 (i)** — **P2.H2 seam core landed: `CombatView`/`CombatIntent`/`TacticalAgent` + first decision extracted at parity + the sim adapter (commits `78533d2`, `bc642bc`).** The JS-free seam (ADR 0006 §B.2, S17) is now `screeps_ibex::combat` — the bot's only intentional public surface beyond the wasm exports. **First decision extracted:** `select_focus_target` (was `attack_mission::compute_focus_target`) as a pure fn over `CombatView`; `attack_mission` is now the **live adapter** (reads `game::*` into JS-free DTOs, preserving hostile/structure order) and delegates — **parity by construction** (identical algorithm + `min_by_key` tie-breaks, so the downstream attack-target intents — and the `IntentRecorder` digest — are unchanged). New **`screeps-combat-agent`** crate (workspace-EXCLUDED, host-only, depends on the full bot at the host target): `SimView::from_world` builds a `CombatView` from a `screeps-combat-engine` `CombatWorld` (minting a synthetic `RawObjectId` per sim creep + reverse map), and **`IbexAgent` runs the bot's REAL `select_focus_target` over the sim** — self-play with no fork (a `HoldAgent` proves the trait is swappable). **7 bot host tests + 3 agent host tests**; bot host+wasm+clippy clean, agent host+clippy clean. Setup notes for resumers: the agent crate replicates the workspace `[patch]` graph (game-api at `../../screeps-game-api`, two levels up); its `Cargo.lock` is seeded from the workspace lock (gitignored project-wide) so it reuses the game-api-compatible js-sys 0.3.85 / wasm-bindgen 0.2.108 pins (a fresh resolve grabs 0.3.102/0.2.125 which `screeps-game-api 0.23.1` does not compile against). **H2 remaining:** grow `decide` to per-creep orders (extract the attack/heal *execution* from `squad_combat.rs` behind a `me`-carrying view) + an explicit recorded-tick intent-diff parity test; **formation-advance extraction is deferred to ride P2.M2** (the anchor-mover rework replaces straight-line `advance_virtual_pos` with the cached-path follower — extracting the current behavior first would be throwaway).
- **2026-06-17 (h)** — **P2.H1 engine CLOSED — pull + tower-as-target landed; golden vectors → H5 (commits `2d26cd5`, `b316834`).** Two final mechanism slices: (1) **pull** (rate2/rate3) — `resolve_moves_with_pulls` + `Intents.pulls`/`set_pull`: a creep dragged by an adjacent, *moving* puller follows into its vacated tile and is eligible with **no MOVE / nonzero fatigue** (engine `canMove` `_pulled`); contention tiebreak extended `rate1 → rate2 (pulled) → rate3 (pulling) → rate4`. (2) **tower-as-target** — `SimTower` gains `id`/`hits_max`/`is_alive`, joins the targetable-structure snapshot as kind `Tower`, shares the structure damage/repair pools (dismantle/attack/RMA/repair), and still fires the tick it dies (two-phase). **40 host tests** (added 4 pull + 4 tower-target); host+wasm clean, clippy clean; README + AGENTS source map + changelog updated. **Operator decision (2026-06-17): defer the live-server byte-exact golden vectors to P2.H5** (they need new per-tick object-snapshot infra closer to the server-parity harness than the per-change loop) — **P2.H1's engine port is therefore DONE**, validated by the 40 hand-computed conformance tests (each pinned to its engine `file:lines`); the byte-exact-vs-live check moves under H5. **Next: P2.H2 — the `CombatView`/`CombatIntent` trait seam → self-play.**
- **2026-06-17 (g)** — **P2.H1 cont.: `CombatRecording` replay/introspection (commit `c34b6bb`).** `record.rs` (`CombatRecording` / `record_tick`): per-tick capture of pre-tick state + intents + optional **reason tags** + outcomes, with a deterministic id-sorted text scrubber — the "see WHY a squad did X on tick N" introspection (ADR 0006 §B.5). Added an `Intents.reasons` metadata field (resolver-ignored; the agent populates it). **32 host tests** (added recording of a kiting engagement w/ reasons + a breach w/ destruction). README + AGENTS source map extended; host+wasm clean. **P2.H1 remaining for _done_:** pull (rate2/rate3), tower-as-target, server-captured golden vectors — then P2.H2 (the `CombatView` trait seam → self-play).
- **2026-06-17 (f)** — **P2.H1 cont.: structures + dismantle + tower heal/repair (commit `f61db2e`).** `SimStructure` (Spawn/Rampart/Wall) on `CombatWorld`; new actions Dismantle / AttackStructure / RangedAttackStructure; RMA extended to structures with the **rampart RMA-shield** (skips a non-rampart target on a rampart tile; single-target still hits a creep on a rampart per the engine) + melee **attack-back rampart-exemption**; tower **Heal/Repair**. **30 host tests** (added EXP-BREACH: wall-breach + spawn-kill; rampart-shield; attack-back-suppression; EXP-DEF: tower-heal-sustains-a-defender + tower-repair-outpaces-dismantle). README + AGENTS source map extended; host+wasm clean, clippy clean. P2.H1 remaining for *done*: `CombatRecording` (replay/introspection), pull (rate2/rate3), tower-as-target, server-captured golden vectors.
- **2026-06-17 (e)** — **P2.H1 cont.: movement-conflict resolution (commit `0d78cd9`).** `movement.rs` ports the engine `movement.js check`: eligibility/fatigue, same-tile contention (swap `rate1` + moves/weight `rate4`), obstacle + **recursive chain-block** (a blocked front stops the whole column — the cohesion mechanic). Wired into `resolve_tick` as **phase C** + phase-D move/fatigue application; added terrain (walls/swamp) to `CombatWorld` and per-creep move intents to `Intents`. Attacks resolve on tick-**START** positions (no dodge-by-moving). **24 host tests** (added EXP-KITE-1 — range-3 kiting at MOVE parity takes 0 melee + chips the chaser — plus 7 movement-resolution: simple move / wall block / fatigue-or-moveless / contention / swap / blocked-front-stops-column / column-advances). README + AGENTS source map + reconciliation extended; host+wasm clean, clippy clean. Remaining for P2.H1 *done*: structures-as-targets (ramparts/walls/spawn) + dismantle + tower heal/repair, pull (rate2/rate3), `CombatRecording`, server-captured golden vectors.
- **2026-06-17 (d)** — **P2.H1 cont.: stationary two-phase resolver + crate provenance docs.** `resolve.rs` — the two-phase accumulate-then-apply tick: creep actions (Attack/RangedAttack/RangedMassAttack/Heal/RangedHeal) + tower fire accumulate into per-target damage/heal pools (with the `creeps/intents.js` priority/exclusion subset — RMA supersedes single rangedAttack, melee attack dropped when healing), then **damage-then-heal netting** (signed, so a same-tick heal can rescue) → deaths → fatigue regen. Melee **attack-back**, **safe-mode** gating, and tower **energy drain** modeled. **16 host tests** now (added: kill inequality both directions, focus-fire 1-vs-2-attackers, tower-drain self-heal + energy burn, safe-mode zeroing, melee attack-back) — these *are* EXP-FOUND-1 / EXP-FOCUS-1 against hand-computed engine values. Warning-free, host + wasm. Added **`README.md`** (users) + **`AGENTS.md`** (AI): the latter carries the **engine→code provenance map** (every formula → its engine `file:lines`), the **pinned engine commits** (engine `8097782`/v4.3.2, common `2fb779b`, game-api `0.23.1`), and the **reconciliation procedure** for engine updates (diff exactly the mapped files, update + re-test). Still **not committed** (operator hasn't asked). Next: same-tile movement-conflict resolution (`rate1..rate4`/pull) + structures/ramparts/dismantle/tower-heal + `CombatRecording` + server-captured golden vectors.
- **2026-06-17 (c)** — **P2.H1 first slice landed: the combat-engine math kernel.** New workspace crate `screeps-combat-engine` — a workspace **MEMBER** (not EXCLUDED as ADR 0006 §B.1 wrote): it's pure logic over `screeps-game-api` value types (`Part`/`Position`), exactly the `screeps-rover`/`screeps-foreman` profile, with no tokio/bollard, so the exclusion rationale doesn't apply and it inherits the workspace `screeps-game-api` patch. Modules: `constants` (engine combat numbers), `body` (per-part 100-hit pools + back-to-front degradation from `_recalc-body`'s fill, boost-aware `calcBodyEffectiveness` power, and the TOUGH/boost `_applyDamage` reduction with the single end-round), `damage` (rangedMassAttack + tower falloff — tower formula kept identical to the bot's `military/damage.rs`), `state` (`CombatWorld`/`SimCreep`/`SimTower` value types). **10 host conformance tests pass** (5×ATTACK=150, boosted ×4, heal 12/48, part-hits fill back-to-front so `body[0]` dies first, power degrades as front parts die, TOUGH ×0.3 within-capacity + capacity-exceeded spill + dead-TOUGH-no-mitigation, RMA {1,0.4,0.1}, tower 600→450→150). **Host + wasm32 both compile** (dual-target rule); clippy clean. Created at the wrong path first (sibling of the repo) → moved into `screeps-ibex/screeps-combat-engine`. Engine re-read for fidelity: `creeps/tick.js` `_applyDamage`, `_recalc-body.js`, `utils.js:623` `calcBodyEffectiveness`. **Next:** `resolve.rs` two-phase tick (intent priority/exclusion + per-target damage/heal pooling + damage-then-heal netting + deaths + fatigue regen) for the *stationary* engagement (drives EXP-FOUND-1/EXP-FOCUS-1), then movement-conflict resolution, then `CombatRecording` + server-captured conformance vectors (P2.H1 done = byte-exact on those).
- **2026-06-17 (b)** — **Tactics catalog authored + operator decisions.** [ADR 0008a](../design/0008a-combat-tactics.md) (Combat Tactics & Behavior Catalog + 17-step experiment register) landed — G3's definition of done is now "the EXP-* gates pass," not a fixed parameter set. **Operator decisions (2026-06-17):** (1) **quad composition** — *let EXP-COMP-1 decide* (build both the uniform RA+HEAL brick AND the 2+2 split, self-play, adopt the winner); (2) **marginal-claim escort (P2.W3)** — *build it* (confirmed in scope, no longer deferred); (3) **reserve denial (T-CTRL-3)** — build the *proactive* de-reservation capability but **gate it behind a feature flag defaulting OFF** (reactive denial stays always-on; proactive is operator-enabled, `features.rs` pattern); (4) **boost-commit** — *conservative floor* (gate boosted assaults behind a stored-mineral/lab-throughput floor; downgrade to unboosted or wait when short — never drain the economy). Other 5 catalog open-questions are sim-tunable (resolved by experiment sweeps).
- **2026-06-17 (a)** — Document authored. Design corpus settled: ADR 0006 (combat micro-sim, Part B), 0008 (objective queue + manager + tactics + anchor movement), 0003 §B.2 (anchor-primary movement correction), 0011 (synchronized spawn — already aligned), 0014 (thin WarDecl hook), 0015 (S17 seam), 0001 (SquadStore). Operator decisions baked in (harness hybrid/trait-first; anchor-primary, relax-don't-switch for corridors; retask-else-recycle; wire boosts; common movement code in the pathfinding system). Prereqs in place: Phase-1 substrate complete (seg-57, colony-health score, governor, containment, intent sink/differ — M0+M1, sign-off 2026-06-12); the eval harness crates (`screeps-server-kit`/`screeps-ibex-eval`/`screeps-ibex-metrics`) exist and are economy-only (`military: None`). **First push (operator): P2.M0 quick-wins + P2.H1 in parallel.**

### 2.1 Conventions

- Task IDs `P2.<workstream><n>` (workstreams: **H** harness, **I** identity, **M** movement, **G** goals/manager, **W** war/defense, **S** spawn, **K** source-keeper exploitation) in every commit touching the task; flip the Status column in the same commit (`unstarted → in-progress → done (commit)`).
- **Leaf-first commits** for submodule work: the footprint-pathfind primitive and any `Follow`/`pull` fix land in `screeps-rover` / the pathfinding system **first**, then the consumer in `screeps-ibex`. The combat-sim crates are new workspace-EXCLUDED members (own `.cargo/config.toml`, host triple — the foreman-bench/server-kit precedent).
- **Validate on the sim per-change; on the private server at checkpoint exits.** Sim conformance vectors gate as hard-exact; combat *outcomes* gate as N-seed paired-seed diffs (sim seeds are reproducible — the N=9 gate ADR 0015 mandates is buildable here). Runs land under `runs/` keyed (scenario, git SHA).
- Every seam lands with its **draft contract** (0015 registry: S8 SquadStore, S9 objective, S14 SquadContext, S17 tactical). Host + wasm lanes both green before `done`.
- **`WORLD_FORMAT_VERSION` (game_loop.rs) MUST bump** on any serialized-shape change (SquadId field, `CombatObjectiveData` persistent layer) — a loud reset per the reset-anytime policy.
- **Missions provide context; jobs own creep intent (incl. movement)** (operator, 2026-06-18). A mission supplies coordination *data* (assignment, squad anchor/slot, the SK per-source suppression signal, focus/objective); the creep's **job** state machine reads it and issues the per-tick move/action — the bot's normal pattern (`jobs/utility/movebehavior.rs`). The squad system currently **inverts** this (`AttackMission` writes `TickMovement` directives the job executes; `issue_virtual_anchor_flee` flees mission-side) — a known tension to resolve toward job-owned movement in **M/G**; new work (K) must not extend the inversion (K0 flee + the K3 miner hold/retreat live in the job; K2's mission only publishes the suppression signal).
- **Anti-overfitting is a standing rule** (operator): no opponent-specific constants; threat measured at runtime; the sim runs the bot's real code; seed diversity + opponent roster (scripted/self-play/recorded); the live seg-57 cohesion canary is the final arbiter and tightens the parity budget when sim and MMO disagree.

### 2.2 Workstream H — combat micro-sim harness (ADR 0006 Part B) — **lands first**

| ID | Task | Depends on | Validation | Status |
|---|---|---|---|---|
| P2.H1 | `screeps-combat-engine` crate: `CombatWorld` (JS-free), two-phase resolve, per-part 100-hit pools + front-to-back destruction, all damage/heal/dismantle/tower formulas, TOUGH/boost rounding, fatigue + same-tile movement conflict (incl. **pull** rate2/rate3), ramparts/**towers-as-targets**/safe-mode, single 50×50 room | — | every formula pinned by a hand-computed conformance test (each cites its engine `file:lines`) under `cargo test-host`; byte-exact-vs-live capture → **P2.H5** | **done** — constants/body/damage/state/resolve/movement/record, **40 host tests** (EXP-FOUND/FOCUS/KITE/BREACH/DEF + pull + tower-as-target + replay capture), host+wasm clean, clippy clean, README+AGENTS provenance docs; §2.0 (c)→(h); commits `06134cc`/`0d78cd9`/`f61db2e`/`c34b6bb`/`2d26cd5`/`b316834`. **Server-captured golden vectors moved to P2.H5** (operator decision 2026-06-17) |
| P2.H2 | Tactical seam: JS-free `CombatView` + `CombatIntent` + `TacticalAgent` trait; extract the first decision (target-selection + combat execution) generic over the trait (no cargo feature); live adapter (leaf) + sim adapter; `screeps-combat-agent::IbexAgent` wraps it. Register **S17** | P2.H1 | live-vs-extracted **intent byte-diff parity** (`IntentRecorder` digest) — parity-by-construction (live path *is* the extracted fn) + unit-pinned intent sequence; the live-server digest comparison lands with H5's golden vectors | **done (combat decision)** — `combat::{select_focus_target, decide_combat}` extracted; `squad_combat`/`attack_mission` are the live adapters (inline combat fns deleted → one implementation); `screeps-combat-agent` `SimView`/`IbexAgent`/`HoldAgent` drive the bot's real code over a `CombatWorld` (end-to-end kill test); 9 bot + 5 agent + 151 full-suite tests; §2.0 (i)/(j); commits `78533d2`/`bc642bc`/`1dccd10`/`11d648c`. **Movement (formation advance + kiting) deferred to P2.M2** (anchor-mover rework; outside the digest gate) |
| P2.H3 | Cohesion metrics (`cohesion.rs`) → seg-57 `CohesionMetrics` block (additive, version bump); unblock the colony-health **military term** (replace `score.rs` `military: None`) | P2.H1, P2.H2 | metrics round-trip pin; military-term kernel tests; a combat change moves `colony_health` | **done** — `CohesionMetrics` additive on seg-57 (schema stays v1); `metrics.rs` aggregates `cohesion::measure` over live `SquadContext`s; `score.rs` military term = mean in-formation rate over the engagement window (`None` ⇒ renormalize); metrics 4 + eval/score 5 (incl. military-term test). **Deploy-validated** (§2.0 (p)): cohesion block live + well-formed in every seg-57 sample on the private server. **A combat change moving `colony_health` is covered by the military-term unit test; the LIVE combat-move check carries to P2.H4** (no combat in the peaceful capture window). Commit `472624b` |
| P2.H4 | Scenarios + opponents + self-play + replay viz: `CombatScenario` data, scripted opponents (rush/kite/turtle/drain), self-play runner, SVG/ASCII replay scrubber with reason tags | P2.H2, P2.H3 | full loop runs; scenario scores report-only, earning gate status per the flake policy | unstarted |
| P2.H5 | Server parity harness + nightly acceptance + seeded **N=9** combat gates (folds the carried F5/F6 determinism for combat): `parity.rs` sim-vs-server divergence; wire named combat scenarios into the server acceptance gate. **Includes the byte-exact golden-vector capture moved out of H1** (operator decision 2026-06-17): combat-scenario seeder + per-tick object snapshotter on the Docker server (new infra, `screeps-server-kit` consumer) → ~12 frozen `tests/conformance/` vectors the sim must reproduce byte-exact | P2.H4 | parity within the tracked budget; nightly N-seed server gate live; **sim reproduces every captured golden vector byte-exact** | unstarted |

### 2.3 Workstream I — squad identity (ADR 0001 Inc 3)

| ID | Task | Depends on | Validation | Status |
|---|---|---|---|---|
| P2.I1 | `SquadStore` + `SquadId` (minted, generation-carrying); kernel-level store with `resolve(id) → same-squad-or-None`. Register **S8** | — | host fixtures: recycle/stale-ref worlds resolve to `None`, **never a foreign squad**; ids stable across serialize/reset | unstarted |
| P2.I2 | Re-key `SquadContext` + `SquadCombatJob` by `SquadId` (replace `squad_entity: Option<u32>`); dangling-ref counter → seg-57; `#[serde(skip)]` on `CreepPathData.path` (IBEX-049); bump `WORLD_FORMAT_VERSION` | P2.I1 | soak window: dangling-ref counter zero; loud one-time reset on deploy; combat still functions post-reset | unstarted |

### 2.4 Workstream M — group movement: the anchor mover (ADR 0003 §B.2, 0008 §5)

| ID | Task | Depends on | Validation | Status |
|---|---|---|---|---|
| P2.M0 | **Step-0 quick-wins (no new infra — start immediately):** whole-squad-centroid focus target; recompute `heal_power` each tick + boost-aware heal math; wire dead `check_movement_failure` (IBEX-015); add `Recall` terminal state to `SquadCombatJob` (orphaned creeps recover now) | — | sim cohesion-rate ↑ once P2.H3 lands; live console sanity; orphaned creep returns home instead of idling to TTL | unstarted |
| P2.M1 | **Footprint-pathfind primitive in `screeps-rover`** (the pathfinding-system home, operator decision 2026-06-17 — NOT the bot's gridsearch): the "moving-maximum" cost transform `moving_maximum(w,h)` (generalize/retire the dead `apply_quad_cost_overlay`), reusable + parameterized by W×H. Built on the **INC 0a `LocalPathfinder`** — the headless pure search both live and sim consume | — | kernel tests: footprint correctness for 1×2 / 2×2 / 3×N; a search never routes the block through a sub-footprint gap | **done** — rover `moving_maximum` (commit `c71b266`/rover `ceb3c14`); 3 footprint tests + the `LocalPathfinder` bridge (rover `1109360`); §2.0 (m)/(n). (tower-avoidance pricing deferred to the M2 consumer) |
| P2.M2 | **The anchor mover:** replace straight-line `advance_virtual_pos` with a **cached footprint-aware anchor path** (pathfound once, cached, followed step-by-step, re-pathed only on invalidation/stuck) + lockstep block advance + hard cohesion gate; wire dormant orientation (`threat_direction`/`orient_toward`/`reassign_slots`/`mirror_y`/rotate). Register **S14** | P2.M1, P2.H3, (P2.I2 if keyed by SquadId) | sim: cohesion-rate ↑, member-spread ↓, no permanent-Loose ratchet; squad rotates to face the threat; tanks front / healers back | **code done; live deploy-validation + orientation wiring pending** — `rover::AnchorPath` (cached footprint path, Blocked-on-failure, footprint-invalidation, serde) sim-validated; **live-swapped**: `SquadPath` embeds `AnchorPath`, `advance_virtual_pos` drives `ScreepsPathfinder` over a terrain-baked matrix, `WORLD_FORMAT_VERSION` 7→8 (commits `a17027f`/`be63eed`, rover `87db130`); §2.0 (o). **Remaining:** behavioral validation on the private server (no headless validation — `SquadContext` is specs-coupled); the dormant `threat_direction`→`orient_toward`/`reassign_slots` orientation wiring (a follow-on; the cohesion + footprint path are the squad-scatter fix) |
| P2.M3 | **Relax-for-corridors + blobs:** width-1 footprint pathfind + loose tolerance through chokes, re-form on open terrain; loose-centroid cohesion for N>4 | P2.M2 | sim: squad threads a 1-wide corridor single-file and re-forms the box on the far side; a 5+ blob stays within centroid tolerance | **done (sim)** — `SimSquad` loose mode (corridor relax + re-form ASAP + N>4 centroid); live `advance_virtual_pos` retries width-1 on Blocked; §2.0 (o); commits `c5265ab`/`be63eed`. (Live member box↔line layout switch = remaining live polish) |
| P2.M4 | *(optional)* `pull` for under-MOVE compositions — fix the rover line-510 fatigue short-circuit; pull-pair duo path | — (only if such bodies fielded) | a pulled under-MOVE attacker keeps formation across plains + room edges | unstarted (conditional) |

### 2.5 Workstream G — goals + lifecycle owner + tactics (ADR 0008)

| ID | Task | Depends on | Validation | Status |
|---|---|---|---|---|
| P2.G1 | `CombatObjectiveQueue` (scout `VisibilityQueue` pattern): persistent `CombatObjectiveData` + `UnwinnableTarget` backoff; ephemeral `claimed_by`; idempotent upsert; `best_unclaimed` selection; TTL / release / release_dead. Register **S9** | — | kernel tests: upsert max-merge, TTL expire, claim/release single-owner, exponential backoff + clear-on-winnable | **done** — `military/objective_queue.rs` (two-layer persistent component + ephemeral resource; explicit-`now` pure core; `ObjectiveKind` incl. `Farm{sk}`; cleanup/sync systems wired; WFV 8→9); **10 kernel tests**, bot 162, wasm build + clippy clean; §2.0 (ae). Claims `Entity`-keyed pending P2.I1 `SquadId`; no stored `SuccessPredicate` (scout-model observed success); **S9 contract finalizes with G2** |
| P2.G2 | `SquadManager` skeleton behind the live offense path (parity mode): claim/retask, composition→roster (threat-matched `damage.rs`/`sized_*_body`), `SquadId` minting, retire/force-abort + per-state deadlines | P2.G1, ~~P2.I2~~ (SquadId deferred — keyed by `SquadContext` Entity), P2.H2 | replay intent-diff parity vs legacy `AttackMission`; kill a member → successor pre-spawned; unreachable room → torn down within deadline | **done (minimal)** — `military/squad_manager.rs` `SquadManagerSystem`: reconcile (retire-on-withdrawal) → field rosters (replacement, AttackMission spawn pattern) → claim up to `MAX_CONCURRENT_SQUADS`; `SquadContext.objective_id` binding; WFV 9→10; bot 164; §2.0 (af). **Replacement not pre-spawn; tactics(G3)/retask/SquadId(I1)/Recall(M0) deferred.** Threat-matched sizing rides the existing per-`BodyType` defs (composition supplies the slots) |
| P2.G3 | **Tactics in the PURE decision engine** (corrected from "in the manager" — operator 2026-06-18: tactics must be simulatable, ADR 0006 §B.2). Implement the [ADR 0008a](../design/0008a-combat-tactics.md) catalog as pure `decide_squad`/`decide_combat`/`decide_movement` in `screeps-combat-decision`; the manager/job are adapters (ADR 0008 §4.1). Tune via the experiment register | P2.G2, P2.M2 | **the EXP-* gates** — EXP-FOUND-1/2, EXP-KITE-1/2, EXP-FOCUS-1/2, EXP-ENGAGE-1 pass on the sim; foundations before composites | **v1 done** — pure `decide_squad` (shared focus + engage/retreat coupled hysteresis; 3 kernel tests); `SquadManager` = thin live adapter (builds `SquadView`, applies the decision — no tactics math); job `Formation`-no-anchor → own kiting (§5 ⚑); responsibilities table in ADR 0008 §4.1; §2.0 (an). **G3-tail:** heal-assignment + slot/orientation → `decide_squad`; `SimSquad` sim wiring → the EXP-* loop |
| P2.G4 | Migrate `AttackMission` → objective producer; generalize `handle_wave_wipe` into the manager; **delete all combat `request_renew`** (pre-spawn replaces). **Push the mission-driven creep behavior down** (the §5 ⚑ inversion, behavior-mapped §2.0 (aj)): `tick_orders=Formation` (Spawning ~481), `advance_squad_virtual_position` (Rallying ~651 / Engaging ~978), focus compute + `TickOrders{attack_target}` (Engaging ~920–964), `threat_direction`+`reassign_slots` (~945), `compute/apply_heal_assignments` (~968) → become the manager's G3 orders the job executes | P2.G2, **P2.G3 (hard — migrating before tactics exist regresses combat, §2.0 (aj))** | sim parity on a recorded engagement; **zero** combat renew intents; objective-complete-while-healthy → retasks, never idles | unstarted (G3-gated; coexistence with the manager is already safe — legacy squads carry `objective_id=None`) |

### 2.6 Workstream W — defense, war supervision, escort, posture hook (ADR 0008 Step 5, 0014, 0017)

| ID | Task | Depends on | Validation | Status |
|---|---|---|---|---|
| P2.W1 | Migrate `SquadDefenseMission` → `Defend` objective on the queue/manager (the dominant cohesion fix — today it is **squad-LESS**, `squad_entity: None`, defenders/healers in bare vecs → scatter by construction, behavior-mapped §2.0 (aj)); keep `hostile_warrants_defender` + owned-room-invader rule; preserve the ADR 0017 §13 ownership-subordinate withdrawal | P2.G2, **P2.G3 (manager must field a focus-firing, cohesive defense squad first)** | sim/server: defense quad **forms up** (cohesion-rate ↑ vs the squad-LESS baseline); threat clears → squad **retired within the deadline**; room lost → withdrawn immediately | **done behind `features.military.manager_defense` (now default ON, §2.0 (al)), §2.0 (ak)** — `run_defense_scan` upserts a `Defend{room}` objective (escalation→composition, HIGH, TTL 60) → manager fields/retires; queue threaded into the operation data; manager maps priority→spawn-priority. Legacy path used only when explicitly set false. **Full cohesion (formation/focus) waits on G3** (today a lifecycle win, fights via per-creep `decide_combat`); deploy-validate vs the legacy baseline |
| P2.W2 | `WarOperation` as supervisor: withdraw low-value objectives on `max_concurrent_attacks` shrink (IBEX-028); feed real per-squad spend → economy-abort (IBEX-026); `UnwinnableTarget` backoff wired | P2.G2 | drop room_count → active attacks trimmed; launch at an unreachable/over-towered room → torn down + backed off | unstarted |
| P2.W3 | Escort/pre-clear objective (ADR 0017 deferred): claim pipeline declares `Escort{room}` for marginal targets; sizing via `DefenseEscalation::from_threat` | P2.G2 | a marginal claim target is pre-cleared and held while the `[Claim,Move]` claimer commits | unstarted |
| P2.W4 | Thin posture hook (ADR 0014): player-offense objectives produced only under a `WarDecl`; NPC policing + reactive defense posture-independent; manual attack/defend flags reinterpreted as operator declarations. Register **S11** (read-only draft). **Reserve denial (T-CTRL-3):** reactive (enemy reserves our target) always-on; build **proactive** de-reservation but **feature-flag it OFF by default** (operator decision 2026-06-17, `features.rs` pattern). | P2.G1 | no player-room offense without a `WarDecl`; flag-declared war launches; peace tears it down within the deadline; policing unchanged vs baseline; proactive de-reservation fires only when its flag is enabled | unstarted |

### 2.7 Workstream S — synchronized spawning (ADR 0011 D3/D4/D9)

| ID | Task | Depends on | Validation | Status |
|---|---|---|---|---|
| P2.S1 | `SquadManager` as sole combat demand producer (`GroupId = SquadId`); align-finish group admission (emerge within window W; zero lanes until admitted) + pre-spawn replacement | P2.G2 | sim/server: quad members emerge within W and rally cohesively (emergence-tick spread bounded); successor emerges before incumbent death − margin | unstarted |
| P2.S2 | Boost-on-spawn handoff (IBEX-027 → **wire**, behind the kill-switch): declare boosts from `required_boosts()` → reserve at schedule time → gate deploy on `is_ready` | P2.S1, ADR 0010 lab pipeline | boosted compositions spawn boosted; kill-switch disables cleanly with no spawn starvation | unstarted (gated on 0010) |

### 2.7a Workstream K — Source Keeper room exploitation ([ADR 0018](../design/0018-source-keeper-room-exploitation.md))

SK rooms are unclaimable, controller-less rooms (the 8-room ring around each sector center) holding 3 sources (4000/cycle) + 1 mineral, each guarded by a 5000-HP keeper that respawns 300t after damage. **Clearing tactics already exist** ([0008a](../design/0008a-combat-tactics.md) T-NPC-2); this workstream is the strategic/economic/intel layer that activates them + adds mining. Feature-flagged `source_keeper_farming` (default OFF); exploited-never-owned. **Structure decided ([0018](../design/0018-source-keeper-room-exploitation.md) §3.6, audit `wg8jqmdk1`):** a NEW economy-family `SourceKeeperOperation` owns a NEW **persistent** `SourceKeeperFarmMission` per SK room (the lone new mission type) — NOT `WarOperation`/`AttackMission` (one-shot: clears→completes→**deletes itself**→keeper respawns→thrash). The farm reuses the squad machinery (`SquadContext`/`squad_combat`/the new movement + the `duo_sk_farmer` composition) and the per-source `SourceMiningMission` + `HaulMission` (just `allow_spawning`-gated on a **per-source** suppression signal). Combat capacity is arbitrated/preemptible: near-term a posture/capacity gate on the operation; target = a low-priority `Suppress` objective on the ADR 0008 `CombatObjectiveQueue` (preempted by defense/war). Detection is **scout-driven, persisted in STATIC room data** (lairs are permanent — scout-once/know-forever; dynamic layer = keeper liveness/respawn). The dead `AttackReason::SourceKeeper`/`AttackMission` path is superseded (optionally kept as a throwaway one-shot kiting smoke test). **Miner/hauler survivability is a three-layer model** (audit `webx9je2v`): **spawn-gate** (`is_room_safe`/`allow_spawning` — already exists, gates new spawns) · **per-source suppression signal** (K2 — SK: is this source safe to work) · **general per-creep flee reflex** (K0 — a hostile got close anyway → flee; today miners/haulers *stand and die*, a net energy sink). K0 is general (all remote mining, not just SK) and lands first.

| ID | Task | Depends on | Validation | Status |
|---|---|---|---|---|
| P2.K0 | **General remote-economy flee reflex (NOT SK-specific; lands first, independent):** a first-class **`Flee` state** per remote-economy job (`staticmine`/`harvest`/`linkmine`/`haul`). `movebehavior::is_threatened()` (detection over cached `RoomData` hostiles — **non-owned rooms only**; a live `ATTACK`/`RANGED_ATTACK` part within `THREAT_FLEE_RANGE=5` ⇒ keepers + invaders, not harmless scouts) is the per-state transition guard at the top of each state's `tick`; `Flee::tick` calls `issue_flee()` (the existing `movement.flee(...).range(N)` primitive) and returns to the entry state once clear. The **job owns the move**, and because it is its own state it competes with no other action. Fixes the **stand-and-die energy sink** for ALL remote mining (existing `is_room_safe`/`allow_spawning` only gates *new spawns*); the safety net beneath the K2 per-source suppression signal. *(Per-state transition is repetitive by design — de-dup deferred to the state-machine-rework ADR; operator 2026-06-18.)* | — | pure core `nearby_threat_flee_targets` unit-tested; a remote miner/hauler with a dangerous hostile in range flees + survives | **done** — `Flee` state + 3 tests; host+wasm+clippy clean, bot 147 (+3); live soak pending |
| P2.K1 | **Intel (scout-driven, static):** record SK identity in **static** room data — `RoomStaticVisibilityData.keeper_lairs: Vec<Position>` + `is_source_keeper()`/`keeper_lairs()` accessors, populated from keeper-lair structures in `create_static_visibility_data`. `#[serde(default)]` (mirrors `exits`) ⇒ additive, **no WFV bump**. Coordinate sector-ring predicate **dropped** (room x/y coords are private in the API — `room/data.rs` TODO — and the operator chose scouting over convention). `SkFrontier` candidacy stays a **computed candidate** (range-to-home + not-owned/contested) → rides K2's operation scan ([0018](../design/0018-source-keeper-room-exploitation.md) §3.1) | — | **no behavior change**; host+wasm+clippy clean, bot 147 | **done** — static field + accessors + population (`room/data.rs`); SkFrontier candidacy rides K2 |
| P2.K2 | **Clearing (the live-combat payoff):** NEW `SourceKeeperOperation` (economy family) + **pure ROI scorer** (inputs→amortized e/t: gross `n×4000/300`; suppression/mining/haul costs; `net ≥ min_sk_roi` floor + hysteresis + hard gates incl. `max_concurrent_sk_farms`; output `{score, Commit|Withhold|Veto}` — kernel-testable, [0018](../design/0018-source-keeper-room-exploitation.md) §3.2). NEW **persistent** `SourceKeeperFarmMission` owning the `duo_sk_farmer` squad (reuse `SquadContext`/`squad_combat`/the new movement + the composition) — never returns Success; per-creep TTL renewal; T-NPC-2 micro (range-3 kite, never r1, pre-stage the next lair before its 300t respawn); **publishes a per-source suppression signal as DATA the miner job reads** (mission provides context, the job owns movement — §3.3/principle 8). NOT `AttackMission` (one-shot lifecycle is wrong, §3.3). Capacity gate/preemption per §3.6 | P2.K1, P2.M2 (kiting) | ROI scorer kernel tests (commit/withhold/veto on hand-computed e/t); ROI scorer kernel tests (commit/withhold/veto on hand-computed e/t); T-NPC-2 sim metrics (keeper dead ~50t, 0 melee, duo survives a 3-lair rotation); private-server soak: the duo suppresses a real SK room, seg-57 cohesion + military term move, no path CPU spiral | **done (clearing on the objective+manager model)** — K2a pure `score_sk_farm` (5 kernel tests); K2b `SourceKeeperOperation` (scans `SkFrontier`, scores, Commit→creates the coordinator, feature-gated OFF); **K2c-1** the persistent `SourceKeeperFarmMission` + self-cancel; **K2c (reconciled, §2.0 (ag))** the coordinator **upserts a low-priority `Farm{sk}` objective** (`ForceRequirement::single(duo_sk_farmer)`) each viable tick → the **`SquadManager` (G2) fields + retires the duo**; the mission owns NO squad (K2c-2 mission-owns-squad superseded). T-NPC-2 kite quality = the movement overhaul + `decide_combat` (the self-driving `SquadCombatJob`); per-source suppression signal + pure-ROI-decline withdrawal ride **K3**. bot 164, wasm + clippy clean |
| P2.K3 | **Source mining:** per-source `SourceMiningMission` (REUSE `missions/localsupply/source_mining.rs` — already per-source/neutral-capable/`allow_spawning`-gated) as children of the farm mission, `allow_spawning` ← the K2 per-source suppressed signal; long-haul home via `HaulMission` (ADR 0007). Keep the `MiningOutpostOperation` SK guard (`miningoutpost.rs:54/140`) intact | P2.K0, P2.K2, ADR 0007 | a 3-source SK room sustains 0 miner deaths over a long soak (0008a metric); net energy ROI positive after suppression cost | **done** — `SourceKeeperFarmMission` owns per-source `SourceMiningMission` + a `HaulMission`; `gate_mining_children` sets `allow_spawning` per source on a keeper-liveness signal (no live SK creep within `KEEPER_DANGER_RANGE=5`); K0 `Flee` is the per-creep survival backstop; `get_children`/`child_complete` added; outpost-op SK guard untouched; WFV 10→11; bot 164, wasm+clippy clean; §2.0 (ah). 0-deaths soak = deploy-gated |
| P2.K4 | **Mineral mining:** harvest the SK mineral node, behind the ADR 0012 market-glut risk gate | P2.K3, ADR 0012, **+ remote extractor/container construction (missing)** | mineral flows home; the market risk gate is respected | **deferred (blocked on missing infra, §2.0 (ai))** — `MineralMiningMission` reuse is ready but needs a `(mineral, extractor, container)` triple; the bot **cannot build a remote extractor/container in an unownable SK room** (no remote-construction capability; planner is owned-rooms-only), and **ADR 0012 is market-*trading* risk, not a mining-glut gate** (no such predicate exists). Needs two new pieces (remote construction + a glut predicate) — a separate future workstream. Energy half (K2c+K3) is the prize and is done |
| P2.K5 | **Invader-stronghold safety:** the SK farm must stand down when an invader stronghold (`StructureInvaderCore` `level()≥1` + ramparts/towers/defenders) is in the room — its towers reach the whole room (≥150 dmg/tick) and would shred the duo + miners | P2.K2, P2.K3 | scorer veto tests; no creep fed into a stronghold room on a soak; farm resumes after the stronghold decays/clears | **done (§2.0 (ar))** — `sk_room_has_stronghold` (live `invader_cores().level()≥1` / persisted `hostile_structures` fallback); **operation veto** (`SkRoiInputs.stronghold_present` hard-gates `score_sk_farm`, even an already-committed farm); **mission pause** (`run_mission` `withdraw`s the `Farm{sk}` objective immediately → duo retired this tick, `gate_mining_children(force_off)` halts all mining+haul, throttled re-probe `STRONGHOLD_RESCOUT_INTERVAL=1500`t for resume). Transient → pause not cancel. Adversarially reviewed. ⚠ in-flight cross-room evacuation is the L1 follow-up. SK scorer tests 6 (+2), bot 165, wasm+clippy clean; no WFV bump |

### 2.8 Sequencing

```
H1 → H2 → H3 → H4 → H5            (harness: trust → seam → metrics/score → scenarios → parity)
M0 (quick-wins, immediate) ───────┐  M1 (footprint primitive, immediate) ─┐
                                  │                                        │
I1 → I2 (SquadStore)              │                                        │
                                  ▼                                        ▼
                         (CP-H + CP-I + M1 reached)            M2 (anchor mover) → M3 (relax)
                                  │                                        │
                                  └──────────────► G1 → G2 → G3 → G4 ◄─────┘   (queue → manager → tactics → migrate)
                                                            │
                                                   ┌────────┼────────┐
                                                   ▼        ▼        ▼
                                                 W1/W2/W3/W4        S1 → S2    (defense+war ; synchronized spawn)
```

Workstreams interleave except where Depends-on says otherwise. **P2.M0 and P2.M1 start immediately** (no harness/identity dependency); they're validated on the sim once P2.H3 lands. **H is the critical path** — everything downstream validates on it. **I (SquadStore) is the hard gate for G (manager)** and runs in parallel with H. Suggested first commit chain: **P2.M0** (visible improvement now) ∥ **P2.H1** (the trust foundation) → P2.H2 → P2.H3 (military score live) → then M2/G in earnest. **K (Source Keeper)** rides M2 (kiting): **K1** intel (scout-driven static detection) is independent and immediate; **K2** builds a NEW `SourceKeeperOperation` + persistent `SourceKeeperFarmMission` (the first real live exercise of the movement overhaul) — NOT the one-shot `AttackMission` path; its suppression squad is a low-priority preemptible combat consumer that folds into the ADR 0008 `CombatObjectiveQueue`/`SquadManager` when G lands — then **K3/K4** mining (reusing `SourceMiningMission`).

### 2.9 Checkpoints (resume points)

Each checkpoint is a **resumable boundary**: it has an entry gate (its dependencies) and an exit gate (a recorded validation). Resuming cold = find the last checkpoint marked reached in §2.0, then start the next checkpoint's first unstarted task. Milestones (M2–M4) are recorded against the rewrite-plan §4 sequence.

| CP | Name | Entry gate | Exit gate (recorded under `runs/` or as kernel pins) | Milestone | Status |
|---|---|---|---|---|---|
| **CP-H** | Measurable combat | — | H1–H3 done: sim byte-exact on conformance vectors; tactical seam at parity; cohesion metrics in seg-57; **military score moves on a combat change** | **M2 (Measurable)** | **H1+H2+H3 code done; cohesion metrics live + well-formed on seg-57 (deploy-validated §2.0 (p)); military-score-moves-on-combat covered by the kernel test. NOT YET reached: the LIVE combat-move (no combat in the peaceful capture window) — carries to a P2.H4 scenario** |
| **CP-I** | Stable identity | — | I1–I2 done: recycle/stale-ref fixtures never resolve foreign; dangling-ref counter zero across a soak; loud reset clean | — | not reached |
| **CP-M** | Cohesive movement | CP-H (H3), M1 | M2–M3 done: cohesion-rate ↑ + spread ↓ in the sim; squad rotates to face threat; threads a corridor and re-forms; no Loose ratchet | **M3 (Cohesive)** | **code done (M1/M2/M3 + sim squad mover + A1 orientation/reassign + A2 self-healing corridor box↔line, §2.0 (o)/(p)); deploy-health validated (clean WFV-8, no CPU spiral, cohesion telemetry live). Behavioral soak (live cohesion-rate ↑ / re-form) PENDING a combat scenario — the peaceful private-server window formed zero squads** |
| **CP-G** | Goal-managed + tactical | CP-H, CP-I, CP-M | G1–G4 done: offense on the queue+manager; focus-fire/kite/hysteresis effective in the sim; objective-complete → retask (never orphan/idle); unreachable → torn down; zero combat renew | — | not reached |
| **CP-W** | Unified defense + bounded war | CP-G | W1–W4 done: defense forms up + retires on the manager; war trims/economy-aborts/backs-off; offense gated by `WarDecl`; escort available | — | not reached |
| **CP-S** | Synchronized spawn | CP-G | S1 done (S2 if 0010 ready): quad emerges within W, no trickle scatter; pre-spawn before death; boosts (if wired) | **M4 (Combat-Effective)** | not reached |
| **CP-K** | Source Keeper exploitation | CP-M (M2 kiting), K1 | K1–K3 done: a real SK room **suppressed** (the `duo_sk_farmer` holds the keepers — seg-57 cohesion + military term move on the engagement) and **mined** with positive net ROI; no path CPU spiral; feature-flag clean | — | not reached |

### 2.10 Exit criteria (the M4 audit — to be recorded at phase close)

| # | Criterion | Source | Status |
|---|---|---|---|
| 1 | Combat is **measurable**: cohesion/orphan metrics in seg-57; colony-health military term live; a combat change moves the score and the differ catches it | M2 / 0006 | pending |
| 2 | Combat is **introspectable**: deterministic sim replay + reason-tagged per-tick log answers "why did this creep do X"; self-play runs | M2 / 0006 | pending |
| 3 | Sim **fidelity bounded**: conformance vectors byte-exact per-change; nightly sim-vs-server parity within budget; MMO seg-57 canary agrees | 0006 | pending |
| 4 | Squad **identity** stable: no foreign-squad resolution across recycle/reset; dangling-ref counter zero across a soak | 0001 | pending |
| 5 | Squads are **cohesive**: cohesion-rate materially up vs baseline; rotate-to-face-threat works; thread a corridor + re-form; defense (was squad-LESS) forms up | M3 / 0003/0008 | pending |
| 6 | Squads are **goal-managed**: objective-complete → retask-or-retire (no orphan/idle); every objective reaches a terminator; unreachable/over-towered → torn down within deadline + backed off | M4 / 0008 | pending |
| 7 | Tactics are **effective**: focus-fire kills through aggregate enemy heal; ranged squads kite instead of standing; retreat has no yo-yo | M4 / 0008 | pending |
| 8 | **Synchronized spawn**: a squad emerges within window W (no trickle); pre-spawn-not-renew (zero combat renew intents); boosts wired-or-killswitched | M4 / 0011 | pending |
| 9 | War is **bounded**: player-offense only under a `WarDecl`; cap-shrink trims; economy collapse aborts; peace tears down | 0008/0014 | pending |
| 10 | New seams (S8/S9/S14/S17) have draft contracts; host + wasm lanes green; combat kernel count recorded; rewrite-plan §3/§4/§7 updated | 0015 / process | pending |
| 11 | Operator sign-off | operator | pending |
