{
  "critiques": [
    {
      "design": "per-tick-demand-priority (PTRP)",
      "attacks": [
        "The two-constant allowance (stored>=10k OR deficit<=10%) makes low-RCL rooms effectively permanent CriticalOnly: pre-storage rooms rarely hold 10k liquid (RoomEconomyData.stored_energy = containers only, ~2k each) and extensions cycle below 90% during every spawn wave, so civilian repair below Critical is suppressed for most of the early game, not just under collapse. That may even be correct policy, but it is a much broader behavior change than 'stress gate' advertises, and the constants are room-stage-blind — the sim guard family MUST include low-RCL healthy rooms or this ships a silent early-game road/container-upkeep freeze. (Containers are partially protected: <50% maps to Critical via map_high_value_priority, so the floor is 50% container health, ~22.5k ticks of owned decay — probably tolerable, but it's an unexamined consequence.)",
        "Change B is per-hauler greedy across tiers with no distance cap: a hauler at the far corner commits to a 50-energy extension across the room while the adjacent Medium lab starves and a second, closer hauler finds the extension already pending-booked and diverts to Medium anyway. Bounded and per-tick-correct as claimed, but the movement-efficiency cost is unquantified and the design's own sim hook must A/B it against a range-limited variant, which it doesn't propose.",
        "It deliberately leaves the withdraw side open: builders/upgraders still register pickup tickets at ALL priorities against the same containers/storage haulers need for refill supply (build.rs:84-93, upgrade.rs:112-122), and Pickup-state top-off plus get_additional_deliveries still pledge creep-carried energy to None-priority sinks at mask ALL. Post-wipe with no storage, secondary chaining to controller containers IS a refill loss (upgraders consume it). So 'energy saved for refills' is only enforced on the repair and delivery-selection lanes; consumption competition — one of the recon's five leak classes — survives intact. PTRP fixes the reported symptom but not the full stated goal.",
        "Room-scoping is unspecified for the multi-room case: a remote hauler repairing in an outpost room has no RoomEconomyData for that room. Default-Unrestricted means remote road repair keeps leaking during a home collapse (haul.rs:247 doesn't even deduct from tickets); keying on the home/delivery room needs plumbing the design doesn't describe. Small hole, but it is precisely the remote-hauler leak class the recon enumerated.",
        "The binary allowance is a two-state posture in per-tick clothing. It satisfies the directive's letter (pure function of this tick's snapshot, no latch) and repair genuinely has no committed work to tear down, so flapping is harmless — but the design should say explicitly that the flap-rate metric goes in the sim so the 'no damping unless observed' directive has its observation channel.",
        "Neither PTRP nor either competitor touches the capacity-sized replacement-body head-of-line stall (source_mining.rs:397 class, todo.md:18). PTRP makes energy ARRIVE faster, then a 1700-cost replacement miner banks it for hundreds of trickle ticks. Correctly scoped out, but it means no design here can claim to fix bootstrap recovery time alone — the sim's K4 kernel is where that gets settled, and the ADR must say so."
      ],
      "misreads": [
        "No material misreads found. Its codebase claims verified: select_deliveries flatness and nearest-wins (transfersystem.rs:1674-1710, haulbehavior.rs:164-167), the tier interleave already existing on the pickup path, the TransferPriority-in-serialized-tickets WFV trap, EconomySnapshot ephemerality, and the JobSystemData plumbing pattern all check out.",
        "One overstatement: 'graceful degradation is structural' is true for change B (empty High tier when full) but NOT for change C — the allowance kernel is a genuine behavioral threshold, and in poor-but-healthy rooms it does not converge to today's behavior (see attack 1). Parity holds only via the kill-switch, not structurally."
      ],
      "verdict": "WINNER. Closest to a bug fix (B makes the already-registered High priority binding, restoring internal consistency with the pickup path's existing tier discipline), one primitive seam closes all five drive-by repair roles, ~6 files, zero serialized state, fully per-tick, honest about its gaps. Its weaknesses (withdraw-side competition, two policy constants, low-RCL over-suppression) are exactly the things the sim exists to measure. Ship it flag-gated as v1 and let M4 decide whether the ledger's extra machinery is warranted."
    },
    {
      "design": "energy-ledger-allocation (RoomEnergyLedger)",
      "attacks": [
        "The advertised '~6 choke points' undersells the surface: the map_structure_repair_priority signature change fans to every repair call site in one commit; per-generator demotion touches lab/terminal/tower/controller-container/powerspawn deposit registration (N sites to police forever); try_draw introduces within-tick shared-mutable budget debits in specs iteration order as a NEW convention every future sink author must remember. Against the operator's SIMPLER ask, this is the highest concept-count design that still quantizes down to 'stress = fund refill first': Sink ladder, grants, draws, LedgerView, flow-scaling, ALLOC_WINDOW, SPAWN_WAVE_RESERVE, HORIZON_TICKS. It deletes 3 scattered heuristics (real EP-2.6 credit) but adds a cross-cutting arbiter the symptom doesn't require.",
        "The 'funded' predicate is ambiguous under partial grants: granted[i] = min(demand, remaining) yields partial funding, but every consumer gate described is boolean (surplus_for, repair_funded, demote-to-None). A lab sink granted 40% of its window demand is... funded? Unfunded? Each answer creates a different mid-stress behavior, and the design never defines it. This is exactly the kind of hidden semantic that surfaces as a live oscillation report.",
        "Flow-scaled S2-S5 demands are computed from LIVE fielded WORK counts — the allocation follows the fleet instead of steering it (a room that shed all builders shows zero build demand, surplus flows to upgrade, which spawns upgraders, which entrenches the shed). Since the design leaves spawn priorities untouched except the repairer gate, the waterfall is a leak-stopper, not a rebootstrap driver — same effective role as PTRP's gates, with strictly more machinery. Its genuine marginal value over PTRP is ONE thing: withdraw-side gating (builders/upgraders can't even register pickup tickets when unfunded). That's real — it's the leak class PTRP concedes — but it also carries the biggest tuning risk: post-wipe, BUILDING extensions is part of the compounding investment, and S0's full-wave reserve outranks S2 Build. The reserve self-scales with capacity so it can't deadlock, but whether reserve-then-build beats build-and-refill-interleaved is an empirical question the shipped default answers by fiat.",
        "It concedes the intra-mask nearest-wins bug stays: under stress the mask is emptied by demotion (symptom fixed), but the root selection inconsistency PTRP repairs remains in the codebase, healthy-room diversion to nearer Medium sinks persists, and a later fix of it lands on top of the demotion logic — two mechanisms where one would do. Compensator, not cure.",
        "The 'healthy rooms fund everything, bit-identical' parity claim is overstated for flag-ON: flow-scaled window estimates can transiently under-fund a burst (large construction push exceeding burn_rate × ALLOC_WINDOW against a mid-sized storage), producing behavior diffs in rooms that are healthy by any sensible definition. Kill-switch parity is real; flag-on healthy parity is behavioral-in-expectation and must be sim-gated, not asserted."
      ],
      "misreads": [
        "No hard misreads; its citations check out (stored_energy composition, EP-4.3 tower demand split, RepairQueue lifecycle, jobs' Write access precedent via transfer_queue). Two soft ones: (1) 'bit-identical to today' for flag-on healthy rooms is not supported by its own flow-scaling mechanism (see attack 5); (2) 'S0 self-liquidating' glosses that the SPAWN_WAVE_RESERVE withholds a full capacity-worth of liquid energy from S2-S5 even at full extensions — correct behavior arguably, but it is a standing tax on lower sinks, not a transient."
      ],
      "verdict": "SECOND. Architecturally the most complete answer — it is the only design that arbitrates the withdraw side, and the ledger-by-sink accounting is independently valuable (it IS the sim's leak metric). But it is the most machinery for the least verified need: ship it only if the sim's per-sink ledger metric shows withdraw competition is a material fraction of T_recover inflation after PTRP lands. Do not ship the waterfall as v1."
    },
    {
      "design": "unified-ev-bidding (Energy Sink Market)",
      "attacks": [
        "Quantization into the frozen 4-bucket TransferPriority enum destroys the market's resolution exactly where it claims to add value: a 15-bid post-wipe extension and a 6-bid hostile-tower refill both land High and nearest-still-wins inside the bucket (its own con admits this). Trace what actually survives quantization: read-site 4 is PTRP's change B verbatim; the rho_floor repair gate is PTRP's change C with a continuous threshold; the build/upgrade bids mostly reproduce today's constants. So as a LIVE design, the market is PTRP plus ~8 policy constants plus a pricing model whose precision is thrown away at the serialization boundary — the payoff (raw rho on tickets) is explicitly deferred behind a WFV bump. Highest cost, near-identical shipped behavior: dominated for v1.",
        "The repair_bid imminence formula is wrong against engine mechanics: ticks_to_death = hits/decay_per_tick ignores ROAD_WEAROUT traffic wear (per-creep-step damage scaling with body size — the sim architecture itself models it as REQUIRED). On trafficked corridors, real time-to-death is far shorter than the decay-only estimate, so 'roads deliberately ride near ~18% health' becomes 'trafficked roads die', silently, room by room. The formula needs a traffic term or a safety margin, and the design's own Family S sim check is the only thing standing between this and live road-network loss.",
        "'Degrades to current behavior by construction when healthy' is false for repair: today, any passing WORK creep repairs roads below 75% (Low) opportunistically; under the market, the floor never drops below ~1.0 (storage numeraire / rho_upgrade), and a 50% road bids ~0.36 — so healthy-room road repair above ~18% health stops PERMANENTLY, flag-on. That is a large steady-state behavior change presented as graceful degradation. Kill-switch parity is real; flag-on healthy parity is not.",
        "Mispricing blast radius: rho_refill = 1 + 14·U makes refill outbid upgrade whenever any harvester is missing (U > 0 pops on every miner death), and V_UPGRADE ~1.0 vs storage-numeraire 1.0 puts steady-state upgrading at a knife-edge tie decided by a constant nobody has measured. The downgrade override catches catastrophe, but chronic upgrade starvation in mid rooms is exactly the slow failure a coarse sim corpus misses. Eight interacting constants is a genuine calibration program, not a patch.",
        "Operational opacity: 'why did the hauler skip the lab' now requires per-room bid archaeology and a new HUD readout (self-admitted). The operator asked for SIMPLER state management under stress; an emergent market is simpler math but harder operations than a deficit gate you can read in one console line."
      ],
      "misreads": [
        "The 'healthy = today's priorities from the same formulas' claim is a misread of its own repair pricing — current opportunistic repair fires at <75% health; the market's floor never permits that (see attack 3).",
        "repair_bid's decay-only ticks_to_death contradicts the engine's traffic-wearout mechanic that the companion sim architecture correctly lists as mandatory (see attack 2).",
        "Otherwise its codebase claims verify: the five read sites, the ticket/WFV firewall, EconomySnapshot field additions being ephemeral, and the tick_opportunistic_repair single-primitive coverage are all consistent with the tree."
      ],
      "verdict": "THIRD as a live design, FIRST as the sim-era valuation language. The e/t bid kernel is the ratified ADR 0033 §D5.4 idiom and is exactly the right currency for the sim's objective function and for pricing repair_leak_e — build sink_economics.rs for the SIM. But shipping it as live policy now buys almost nothing over PTRP after quantization, carries the largest mispricing surface, and smuggles in a permanent healthy-room road-upkeep change. Revisit as live policy only if the M4 tournament shows it beating PTRP+ledger materially AND the operator approves the ticket-reshaping WFV reset that unlocks its actual resolution."
    }
  ],
  "ranking": [
    "per-tick-demand-priority",
    "energy-ledger-allocation",
    "unified-ev-bidding"
  ],
  "hybrid_recommendation": "Yes — a staged hybrid beats the winner alone, and the three designs are more composable than competing: PTRP's B is literally read-site 4 of the market, and the ledger's per-sink accounting is the sim's leak metric. The hybrid: (1) NOW: ship PTRP flag-gated (repair stress gate C+D first, tier-faithful delivery B as a second slice) — simple, per-tick, zero WFV, fixes the observed symptom. (2) SIM ERA: build the K1–K4 kernel seam and express all three designs as alternative kernel implementations in the M4 tournament; adopt the e/t bid functions (sink_economics.rs, with the traffic-wearout term fixed) as the sim's OBJECTIVE/valuation layer regardless of which policy wins — that is the §D5.4-consistent currency for H_recover, repair_leak_e, and constant calibration. (3) CONDITIONALLY: fold the ledger's one unique capability — withdraw-side gating of builder/upgrader pickup tickets — into PTRP as a third slice IF AND ONLY IF the sim's per-sink energy ledger metric shows withdraw competition is a material residual after PTRP (the metric directly answers whether design 2's machinery pays for itself; don't ship the waterfall, ship the gate it justifies). (4) END-STATE: the full market becomes live policy only on a demonstrated M4 win plus an operator-approved WFV reset for ticket-resolution — never as a default drift. This gives the operator: simple and shippable today, one sim that adjudicates the rest, and the EV currency as the sim-validated end-state rather than a leap of faith.",
  "sim_critique": "The real-logic-vs-kernel call is RIGHT and honestly argued: recon proves the economy jobs/missions are hard-coupled to live Creep handles and the specs ECS (jobsystem.rs:44-55), so combat-style extraction-first is the only path to testing real logic, and routing all four DECISIONS through K1–K4 makes the A/B evaluate the code the bot will actually adopt. The M1 repro-the-disease gate before scoring any cure is the strongest single element. Weaknesses: (1) M0 is near-minimal but not minimal — road/container decay + traffic wearout + dropped decay belong in M1 with the repro family that needs them; more importantly there is no fast movement tier: every long-horizon run pays full rover per-tick cost, which is the direct cause of the flagged 10^5-tick RCL-rush cost risk. Add an analytic-movement mode (path-length latency, no contention) as a fast tier and reserve rover_driver for families where congestion matters (refill latency, Family S), or M2's cost measurement will quietly shrink the corpus. (2) Metric crispness gaps: T_recover's recovered-state clause (ii) '>=0.9 x potential over trailing 300' is a cliff with two unjustified constants — a policy sustaining 0.89 scores eta=0 forever; 'sustained' in clause (i) is undefined; and W = T*_recover weighting means one pathological layout hitting the cap dominates pooled H (0-score x max-weight). Define sustain windows, justify or sweep the 0.9, and consider capping any single scenario's weight share. The oracle is a loose bound, not an oracle — saved only by EP-6.7 paired diffs, which the doc correctly leans on, but then absolute eta should be labeled decorative. (3) NO live-fidelity gate exists anywhere before M5: M3's A/A parity is sim-vs-sim (seam vs inline), which never validates the transcribed baseline against the LIVE bot. K2 is the hardest transcription (select_best_delivery scoring + tier interleave + additional-delivery chaining) and a fidelity miss there invalidates M4 rankings. Add a cheap live instrumentation counter (energy spent on repair while refill deficit exists, via metrics.rs) shipped with the stopgap — it validates the stopgap AND gives M1 a live-vs-sim repro comparison. (4) Missing metrics: intent count per tick (the CPU directive prices intents; a policy that wins H by emitting more intents must be visible) and a flap-rate/oscillation metric (the operator's no-hysteresis directive requires an observation channel to ever justify damping). (5) Labs/minerals (explicit operator asks) are M6 stubs with no metric of their own — defensible since neither headline touches compounds, but the ADR must record operator sign-off on the deferral, and M6 as specced is decorative: it models reactions but not boost EFFECTS on the economy loop (boosted upgrader WORK materially changes T_RCL(6+)), nor mineral density re-roll on regen, nor terminal mineral-sale-to-energy as a recovery lever. Either give M6 a real metric (compound time-to-X, boost e/t-equivalent feeding the 0033 military-w arm) or shrink it to the stub and say so. (6) spawn_throughput reused from screeps-combat-decision makes a combat crate a dependency of the economy decision crate — promote it to a shared home (one reviewed move, the stats.rs REUSE-AFTER-MOVE pattern) before two domains co-own a mirror of live spawnsystem.rs. (7) The corpus has no hostile-pressure collapse variant (energy crash under harassment is a common real cause); fine to defer, but list it in Remaining. Everything else — conservation audit as the IntentAudit analog, determinism fence from M0, required_rcl capture slice, Family S guard rail, the null-result honesty — is sound and correctly follows the 0033 pattern.",
  "stopgap": "Ship PTRP's repair stress gate immediately, before any sim exists — it is the verified direct cause of the operator's observed symptom (harvest.rs:264 deducts drive-by road repair from spawn-bound delivery tickets; haul.rs:229/246 confirmed in-tree) and it is one small, flag-gated, zero-WFV slice: (1) pure kernel energy_stress.rs (deficit per-mille + stored floor -> Unrestricted/CriticalOnly, integer math, host-unit-tested); (2) plumb Read<EconomySnapshot> into JobSystemData per the existing repair_queue pattern; (3) raise effective min priority to Critical inside tick_opportunistic_repair and the two shared repair selectors — one primitive covers harvester/hauler/static-miner/builder; (4) gate get_repairer_priority (kills the perverse decayed-roads-spawn-a-HIGH-builder loop) and peaceful tower repair; (5) features.energy.repair_stress_gate kill-switch, default on after a Docker collapse sanity check (console-kill all creeps, watch recovery). Explicitly define the remote-room posture (recommend: key on the hauler's home/delivery room, not the room the creep stands in). Ship WITH it the live telemetry counter (repair energy spent while spawn deficit exists, exported via metrics.rs) — it verifies the fix live and later anchors the sim's M1 repro gate. HOLD BACK from the stopgap: change B (tier-faithful delivery) lands as slice 2 a few days later — it is a genuine bug fix but touches transfersystem selection internals and deserves its own host-test pass; the ledger and the market wait for the sim. Do not attempt the capacity-sized-body fix in the stopgap; it is K4/sim territory.",
  "synthesis": "Recommended end-state: energy prioritization under stress is owned by ONE small pure-kernel layer (the K1–K4 seam of the sim architecture) consumed identically by the live bot and the offline economy sim — the combat-decision pattern replicated for economy, which is simultaneously the operator's ask (a) and the dissolution of mirror-drift risk. The shipped v1 policy inside that layer is PTRP: tier-faithful delivery selection (restoring consistency the pickup path already has) plus a per-tick repair allowance computed from the existing EconomySnapshot — no modes, no latching, no new serialized state, no WFV bump. The sim (econ-engine/econ-decision/econ-eval over sim-core, per the architecture with the fixes above: fast movement tier, crisper recovered-state definition, live-fidelity counter, intent-count and flap-rate metrics, spawn_throughput ownership move) then adjudicates the two open questions PTRP deliberately leaves: whether withdraw-side gating of builder/upgrader pickups is worth the ledger's machinery (answered by the per-sink ledger METRIC, adopted as a PTRP slice 3 only if material), and whether the e/t sink market beats simple gates by enough to justify the operator-approved WFV reset its ticket resolution requires.\n\nThe e/t currency wins as the measurement language even where it loses as the shipped policy: H_recover, T_RCL(N), and repair_leak_e are all priced in the same energy-equivalent unit ADR 0032/0038/0033 already ratified, so the sim's objective function, PTRP's two calibrated constants, and any future market migration share one accounting. Sequence: stopgap (repair gate + telemetry) this week -> PTRP slice 2 (tiered delivery) -> sim M0–M4 with all three designs as kernel candidates -> M5 live adoption of the tournament winner behind the existing kill-switch, old inline behavior deleted per EP-2.6. Every step is independently shippable, per-tick-optimal, reset-free, and sim-evaluable — which is the ranking criterion the operator actually stated."
}