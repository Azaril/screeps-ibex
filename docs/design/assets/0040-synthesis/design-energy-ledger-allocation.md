{
  "name": "RoomEnergyLedger — per-tick waterfall budget arbiter (ADR 0040 Angle 2)",
  "overview": "A per-tick, never-serialized specs Resource that allocates each owned room's liquid energy down a strict sink ladder (spawn/extension refill + wave reserve → defense/survival-critical → build → repair → upgrade → industry) and exposes two O(1) queries — `surplus_for(room, Sink)` and `try_draw(room, Sink, amount)` — that existing missions and jobs consult before registering demand or burning carried energy. The allocation math is a pure kernel (`energy_ledger.rs`, the `room_economics.rs` pattern) shared verbatim by the live bot and the planned offline economy sim, so the exact shipped policy is what the sim A/Bs. In a healthy room the waterfall fully funds every sink and every gate passes — behavior is bit-identical to today; under collapse the on-hand balance is exhausted by the refill sink and all lower-sink leaks (road repair, builder/upgrader withdraw competition, peaceful tower repair, repairer-builder spawns, lab/terminal loading) shut off at their single pricing choke points.",
  "mechanism": "**1. The pure kernel** (`screeps-ibex/src/energy_ledger.rs`, new — sibling of `room_economics.rs`, same \"functionally PURE, no game::* reads, integer energy, bit-deterministic\" contract). `pub fn allocate(inputs: &LedgerInputs) -> RoomLedger`. `LedgerInputs` (all u32/DTO, built adapter-side): `spawn_energy`, `spawn_energy_capacity`, `liquid_energy` (storage+containers+terminal — exactly `RoomEconomyData.stored_energy`), `hostiles_active: bool`, `controller_downgrade_emergency: bool` (ttd < max/2, the existing `upgrade.rs:209` test), per-sink burn-rate demands (below), and `income_e_t: u32` × `HORIZON_TICKS` (a named constant, **shipped default 0** — see failure modes). Waterfall: `budget = liquid_energy + income_e_t * HORIZON_TICKS`; walk the fixed sink array in order, `granted[i] = min(demand[i], remaining)`, `remaining -= granted[i]`. Ladder (fixed array, the deterministic tie-break):\n- **S0 Refill**: `(spawn_energy_capacity - spawn_energy) + SPAWN_WAVE_RESERVE` where the reserve = `spawn_energy_capacity` (bank one full spawn wave of liquid energy — this is the operator's \"save energy for refills\" made explicit). Stock-based and exact — read from live stores, never estimated.\n- **S1 Survival-critical**: tower refill demand when `hostiles_active` (militarily_active, per the defend-gate lesson), emergency wall repair (the existing `wall_repair.rs` siege generator), `RepairPriority::Critical` structure repair (spawn/tower/container <50% via `map_high_value_priority`, `jobs/utility/repair.rs:39-53`), and the downgrade-emergency upgrade floor. This is the EP-4.3 never-shed set; roads can never reach S1.\n- **S2 Build, S3 Repair (normal), S4 Upgrade, S5 Industry** (labs/terminal/factory/nuker/powerspawn): demands are **flow-scaled, not stock-scaled** — `min(remaining_stock_need, burn_rate × ALLOC_WINDOW)` where burn_rate = live WORK-part counts × 5 e/t (build) / 1 e/t (repair, upgrade) from the engine constants already transcribed in `screeps-rover-eval/src/value.rs:48-51`. Flow-scaling is what makes a healthy 200k-storage room fund everything (parity) while a 300-energy post-wipe room funds only S0. S4-vs-S5 ordering is a declared operator-decidable constant (the ADR 0033 §D5.4 numbered-decision convention).\n\n**2. The ECS adapter** — `EnergyLedgerSystem`, registered at `game_loop.rs:83`, one line after `EconomyAssessmentSystem` (:82), `StageClass::Always` (it feeds the never-shed spawn/haul set). Reads `EconomySnapshot` (`military/economy.rs:27-51`) + cached `RoomData::get_structures()`; writes `Write<RoomEnergyLedger>` — sole writer (EP-2.7). `RoomEnergyLedger { rooms: HashMap<Entity, RoomLedger> }`, registered via explicit `world.insert(RoomEnergyLedger::default())` at `game_loop.rs:~904` (the `RepairQueue` pattern). Queries are point lookups only — no iteration reaches a decision (EP-6.13). Jobs resolve room Entity via `EntityMappingData::get_room` exactly as `tick_opportunistic_repair` already does (`repairbehavior.rs:123-124`).\n\n**3. Consumption seams — minimal churn, one choke point per leak class** (each is 1–5 lines at an existing pricing-policy point; the ledger supplies pricing input, per the \"feature modules supply pricing policy only\" directive):\n- **Repair pricing (kills leaks §3c + builder-repair + tower-repair + the fallback scan in ONE signature)**: `map_structure_repair_priority` (`jobs/utility/repair.rs:93-110`) already takes `available_energy: Option<u32>` for walls; replace that param with a `LedgerView { repair_funded: bool, critical_funded: bool }`. When `repair_funded == false`, `map_normal_priority` and the sub-Critical arms of `map_high_value_priority` return `None`. Because this one function prices (a) `LocalBuildMission::pre_run_mission` RepairQueue population (`localbuild.rs:181-216`), (b) the opportunistic in-range fallback scan (`repair.rs:208-232`, which today bypasses the queue with `available_energy: None`), and (c) tower repair target selection (`tower.rs:426-453`), gating it starves every repair consumer of road targets simultaneously — no per-role special-casing.\n- **Drive-by repair (leak §3c, the operator's headline symptom)**: `tick_opportunistic_repair` (`repairbehavior.rs:111`) gains one guard: `if !ledger.surplus_for(room, Sink::Repair) { return false }` (Critical targets exempt via `critical_funded`). Plumbed by adding `energy_ledger: Read<RoomEnergyLedger>` to `JobSystemData` (`jobs/jobsystem.rs:17-34`) and a `&RoomEnergyLedger` field to `JobExecutionSystemData` (:36-42) — byte-for-byte the existing `repair_queue` plumbing pattern (:29→:41). Covers harvest.rs:225/264, haul.rs:229/246, staticmine.rs:144 without touching the serialized `allow_repair` flag.\n- **Withdraw-side competition (leak §5 — the deepest class)**: `BuildJob::Idle` (`jobs/build.rs:56-103`) and `UpgradeJob::Idle` (`jobs/upgrade.rs:112-122`) gate state entry on their sink: no build/repair state and — critically — **no energy-pickup ticket registered** when `granted == 0`; pickup ticket size = `min(free_capacity, try_draw(room, sink, free_capacity))` when funded. `try_draw` debits the sink's remaining grant within the tick (Write access in RunJobSystem; same first-come semantics as the existing pending-ticket bookkeeping at `transfersystem.rs:587-617`, deterministic per run). Energy that S0 needs never leaves the supply node toward a starved sink. `HarvestJob::Idle`'s upgrade/build/repair arms (`harvest.rs:165-211`) get the same gates; its High-delivery arm is untouched. Downgrade-emergency upgraders bypass via S1.\n- **Transfer-lane demand demotion (leak §3a — nearest-wins inside the flat ACTIVE mask)**: deposit generators for lab (`labs.rs:431-439`), terminal (`terminal.rs:336-401`), peaceful tower (`tower.rs:176-180`), controller container (`room_transfer.rs:352-366`), powerspawn/nuker demote to `TransferPriority::None` when their sink is unfunded — so `get_new_delivery_current_resources_state`'s flattened ACTIVE selection (`haulbehavior.rs:129-179`) simply no longer contains competitors to the High spawn/extension deposits. Deliberately NOT adding a tier above High or reordering `TransferPriority` — variants ride serialized `HaulState` tickets and would force a WFV bump.\n- **Spawn-side echo (the perverse repairer spawn)**: `get_repairer_priority` (`localbuild.rs:112-122`) returns no request when `granted(Sink::Repair) == 0`; `LocalBuildMission`/`UpgradeMission` desired-count logic consults `surplus_for` instead of their ad-hoc `has_sufficient_energy`/`has_excess_energy` storage thresholds (`localbuild.rs:225-244`, `upgrade.rs:183-200`) — deleting two of the scattered duplicate stress heuristics (EP-2.6). Storage deposits stay `None`-priority (leaks §3b/§3d are non-leaks: storage is liquid and counted).\n- **In-flight work parks, never tears down** (EP-3.8): gates apply at state ENTRY and at the opportunistic call; already-registered tickets complete. Bounded residual leak ≤ one carry per creep at the stress transition.\n\n**4. Kill-switch**: `features.rs` additive `EconomyFeatures { ledger: bool }` default true, serde-default (no Memory migration). When false, `EnergyLedgerSystem` writes a fully-funded ledger (every `surplus_for` true, `try_draw` grants everything) — consumers need no flag awareness and behavior is bit-identical to today. Removal point per EP-10.5: after the sim A/B + one live soak validate.\n\n**5. Failure modes, designed-in answers**: (a) *Income estimation error* — eliminated at v1: `HORIZON_TICKS = 0` ships, so the waterfall runs on measured on-hand stock only; income enters only as a sim-tuned constant later (EP-4.6), and even then over-estimation misallocates at most `income_err × HORIZON` of flow per tick and self-corrects next tick because grants are recomputed from real balances. (b) *Budget deadlock* (\"S0 demand forever unmet → room frozen at zero build/upgrade\") — S0 demand is finite (≤ capacity + reserve, e.g. ≤ 2×12,900 at RCL8) and self-liquidating: refilling extensions is precisely what re-opens the waterfall; a room where S0 stays unmet has ~zero income, where starving build/repair IS the optimum, and the S1 downgrade floor + never-shed spawn lane still run. No latch, no ratchet — per-tick recompute with the fixed ladder as tie-break satisfies the per-tick-optimal directive; if funded/unfunded flapping is ever actually observed live, the sanctioned fix is a trend input to the kernel, not hysteresis. (c) *Double-reserve interaction* — the spawn queue's head-of-line banking (`spawnsystem.rs:428-436`) reserves in-structure energy for the top spawn; the ledger's S0 reserve banks liquid energy to refill those structures. Complementary layers, no cycle: upgraders draw storage/containers (counted, gated), never extensions.",
  "state_changes": "NEW, all ephemeral (recomputed every tick, never serialized): `RoomEnergyLedger` specs Resource (`HashMap<Entity, RoomLedger>` + within-tick `try_draw` debit counters), inserted in `create_environment` (`game_loop.rs:~904`, the RepairQueue pattern); pure module `energy_ledger.rs` (`LedgerInputs`/`RoomLedger`/`Sink` — plain integer DTOs); `EnergyLedgerSystem` in the `for_each_system!` list at `game_loop.rs:83`. CHANGED (read-side plumbing only): `JobSystemData`/`JobExecutionSystemData` gain the ledger (`jobs/jobsystem.rs:17-42`); `MissionSystemData`/`MissionExecutionSystemData` likewise (`missions/missionsystem.rs:45→75` pattern); `map_structure_repair_priority`'s `available_energy: Option<u32>` param becomes a 2-bool `LedgerView` (compile-time-visible signature change, all ~5 call sites updated in the same commit); `features.rs` gains `EconomyFeatures { ledger: bool }` (serde-default, additive, no migration). DELETED (EP-2.6): `has_sufficient_energy` (`localbuild.rs:225-239`), `has_excess_energy` (`upgrade.rs:183-200`), the wall-repair `available_energy > 10_000` storage-sum special case (`repair.rs:84`, `repair.rs:180-184`) — all subsumed by ledger queries. EXPLICITLY NOT TOUCHED (serialization firewall): `TransferPriority` enum shape, `TransferWithdrawTicket`/`TransferDepositTicket`, `HaulJobContext`/`HaulState` (incl. `allow_repair`), `RoomData`, any `ConvertSaveload` type in the `game_loop.rs:494-512` persistence tuple. No new persisted state anywhere; income stays stateless (no EWMA) specifically to avoid persistence pressure.",
  "pros": [
    "Closes ALL recon-enumerated stress leak classes (drive-by road repair, builder/upgrader withdraw competition, peaceful tower repair, repairer-builder HIGH spawns, lab/terminal/controller-container haul diversion) through ~6 choke points that already exist as pricing-policy sites — no per-role rewrite, no changes to hauler matching internals",
    "Pure-kernel allocation (room_economics.rs precedent) means the EXACT shipped policy runs in the offline economy sim — no mirror/fork drift, satisfying EP-6.2/EP-6.14 and the operator's testability ask directly",
    "Zero serialized-state impact by construction: no WFV bump, deploys without a reset, and the kill-switch degrades to bit-identical current behavior (fully-funded ledger) so parity is verifiable per EP-2.2",
    "Honors the per-tick-optimal directive: no mode latch, no hysteresis — a stateless waterfall over measured on-hand stock with a fixed ladder as the deterministic tie-break; v1 ships with HORIZON_TICKS=0 so there is no income estimate to be wrong",
    "SIMPLIFIES net-net: deletes three scattered ad-hoc stress heuristics (has_sufficient_energy, has_excess_energy, the walls-only 10k storage gate) and replaces the implicit emergent ordering with one auditable ledger a console log line can print per room",
    "Waterfall demands are flow-scaled (burn-rate × window) for S2-S5 and stock-exact for S0, so healthy rooms fund everything (graceful no-op) and the arbiter only bites under genuine scarcity — no new behavior to tune in the common case"
  ],
  "cons": [
    "Does not fix transfer-lane matching itself: inside the ACTIVE mask, nearest-still-wins (haulbehavior.rs:154-175) — the ledger removes competing demand under stress but a healthy-room High spawn can still lose to a nearer Medium lab; full intra-mask priority ordering remains a separate (Angle 1-shaped) fix",
    "try_draw grants are first-come in specs entity iteration order within the tick — deterministic per run but not value-optimal among same-sink claimants (same class as the existing pending-ticket bookkeeping; acceptable, but a purist global assignment it is not)",
    "Burn-rate demand estimates for S2-S5 are approximations (live WORK counts × engine constants); a wrong estimate shifts how much surplus lower sinks see for one tick — bounded and self-correcting, but the constants (ALLOC_WINDOW, SPAWN_WAVE_RESERVE) need sim calibration before the defaults are trusted (EP-4.6)",
    "The map_structure_repair_priority signature change fans out to every repair call site in one commit — mechanical but the largest single-commit surface in the design",
    "Bounded residual leak at stress onset: in-flight pickups/deliveries registered the tick before collapse complete under EP-3.8 park-don't-tear-down semantics (≤ one carry per creep)",
    "Adds one more cross-cutting Resource that future mission authors must remember to consult when adding a new energy sink — mitigated by the ledger being the documented single entry point, but it is a convention to police in review"
  ],
  "wfv_impact": "None — no WORLD_FORMAT_VERSION bump (stays 23). The ledger is an ephemeral Resource rebuilt each tick (the EconomySnapshot/RepairQueue class, explicitly outside the game_loop.rs:494-512 persistence tuple), the feature flag is serde-default additive in Memory._features (not WFV-governed), and all consumer changes are read-side gates. The design deliberately rejects the two tempting variants that WOULD force a bump: adding a TransferPriority tier above High (variants ride serialized HaulState tickets, transfersystem.rs:1029-1034) and persisting a measured-income EWMA (income stays stateless; if a smoothed income signal is ever wanted, it must be recomputable-per-tick from live creep WORK counts, per EP-5.4).",
  "cpu_notes": "One additional Always-stage pass per owned room per tick: pure integer arithmetic over data EconomyAssessmentSystem and the RoomData structure cache already computed — O(rooms × sink count), no pathfinding, no room scans, no allocations beyond one small HashMap rebuild. Consumer-side cost is O(1) map lookups replacing work: under stress the gates SKIP the opportunistic repair in-range scans (repairbehavior.rs fallback), skip whole Build/Upgrade/Repair state machines, and shrink the RepairQueue — net CPU under collapse is expected negative (fewer intents, fewer scans), which is exactly when the CPU budget matters most. Intent count can only decrease (gates suppress repair/build/upgrade intents; they never add intents). Classified StageClass::Always because it feeds the never-shed spawn/haul set (EP-4.3); its own cost is small enough (~µs-scale integer math per room) that Always is safe.",
  "sim_evaluation_hook": "The economy sim (EconWorld overlay on screeps-sim-core per the sim-infra recon, with harvest/store/spawn/build/upgrade resolution) imports `energy_ledger::allocate` directly — same crate-level pure kernel, zero mirror. Scenario family: post-wipe bootstrap states realized from `screeps-rover-eval/resources/captured-layouts.json` (with the ~5-line `required_rcl` capture extension) at RCL 1-4, decayed roads seeded so Medium/High road-repair targets are abundant (the operator's exact symptom preconditions), plus a healthy 200k-storage control room. A/B arms: ledger ON vs OFF (kill-switch path), and a sweep over SPAWN_WAVE_RESERVE / ALLOC_WINDOW / HORIZON_TICKS via the tuning.rs coordinate-descent idiom. Metrics through stats.rs verbatim: (1) headline `T_recover` = ticks until energy_available == energy_capacity_available is sustained, reported as η = T*_analytic/T_realized (T* = harvest-throughput-bound lower bound, the H-metric architecture, 0-scored on non-completion); (2) `T_RCL(N)` time-to-RCL-N for the rush variant; (3) the leak audit as a HARD GATE: energy spent on sub-Critical repair during any tick where S0 demand is unmet must be 0 with ledger ON (the IntentAudit-style zero-tolerance sentinel); (4) the parity gate: healthy-control scenario must show ZERO behavioral diff ON vs OFF (EP-2.2). Expected movement: η_recover up materially on collapse scenarios, leak-gate 0, healthy H unchanged; determinism fence (spread-0 over 5 rounds) applies since the kernel is integer-pure. Live complement: ibex-eval colony_health econ term on a Docker soak validates the sim's prediction before any MMO deploy."
}