{
  "overview": "Build the offline ECONOMY simulator as two new crates layered on screeps-sim-core — `screeps-econ-engine` (engine-mechanics resolution: sources/minerals, spawn, stores/transfer/build/repair/decay, controller/RCL, labs) and `screeps-econ-eval` (scenario corpus, metrics, CLI bench, determinism fence) — plus one new pure decision crate `screeps-econ-decision` that defines the four energy-prioritization kernels the priority redesign will ship. This mirrors ADR 0033's proven shape exactly: sim-core kernel underneath, an overlay world crate beside screeps-combat-engine, an eval crate beside screeps-rover-eval reusing its stats.rs bootstrap-CI and scenario/tuning idioms, a `fast|full` CLI bench, and a spread-0 determinism fence as a ship-blocker. Headline metrics are (a) T_recover — bootstrap recovery time from a defined collapse state to a defined recovered state, and (b) T_RCL(N) — greenfield RCL-rush curves — both expressed as efficiency ratios η = T*_oracle/T_realized against analytic energy-throughput lower bounds, aggregated as H = ΣWη/ΣW with seeded bootstrap 95% CI (the 0033 §D5.4 objective-function discipline, same e/t currency). The scenario corpus seeds from the 13 real foreman-planned rooms in screeps-rover-eval/resources/captured-layouts.json (confirmed present), with a ~5-line capture_layout extension to emit per-structure required_rcl (confirmed available on foreman's Plan). The sim drives extracted PURE KERNELS, not the live mission/job code — recon proved bot economy logic is hard-coupled to JS Creep handles and the specs ECS — but the kernels ARE the redesign surface, so the A/B milestone evaluates the exact code the live bot will adopt. The operator's observed disease (energy leaking into road repair while extensions sit empty) is reproduced offline as an explicit M1 gate before any cure is scored.",
  "architecture": "CRATE LAYOUT (anti-duplication dispositions per 0033 §D2):\n\nNET-NEW `screeps-econ-engine` — the economy overlay, mirroring screeps-combat-engine's relationship to sim-core. `EconWorld { movement: MovementState, sources, minerals, controller, spawns, extensions, containers, storage, sites, roads, labs, dropped, tick_ledger }`; `EconIntents` embeds `MoveIntents` plus economy verbs (Harvest, Transfer, Withdraw, Pickup, Build, Repair, UpgradeController, SpawnCreep, RunReaction, Boost); `resolve_econ_tick` calls sim-core `resolve_movement` at the movement point of its pipeline via the `Simulation` trait (screeps-sim-core/src/sim.rs:15-26 — 'No change to this crate is needed to add a layer', verified). Per-resource creep stores are layered here; sim-core's `carry_used` scalar stays the movement-weight input (derived as store total). Intent simultaneity follows the bot's own pipeline model (jobs/actions.rs Pipeline A work-intent + Pipeline D transfer + movement per tick), with deviations from the real engine's per-intent pipelines documented inline. Every mechanic constant carries an engine-mechanics.md or engine-source citation pinned by a unit test (the spawn_throughput.rs precedent).\n\nNET-NEW `screeps-econ-decision` — JS-free pure crate, the analog of screeps-combat-decision. Contains the `EconomyView` (DTO read seam: RoomEconDto, CreepEconDto, StoreDto, DemandDto) + `EconomyIntent` write seam, and the four priority kernels (see seam definition below). The live bot adds this as a dependency exactly like screeps-combat-decision (screeps-ibex/Cargo.toml:36 precedent). REUSE-AS-IS via dependency: `spawn_throughput::spawn_step` from screeps-combat-decision (already a citation-pinned pure mirror of live spawnsystem.rs including the head-of-line energy-banking break — the central scarcity mechanism under test).\n\nNET-NEW `screeps-econ-eval` — scenarios/metrics/bench/fence, mirroring screeps-rover-eval file-for-file: scenario.rs (curated catalog + seeded procedural generate(seed)), metrics.rs (η-vs-oracle ratios), an oracle.rs (analytic energy-throughput lower bounds), tuning.rs-style env-driven #[ignore] policy sweep, bin/econ_bench.rs (`cargo run --release -p screeps-econ-eval --bin econ_bench -- fast|full`, env overrides, per-scenario lines + per-family + pooled headline, non-zero exit on gate failure). REUSE-AS-IS via dependency on screeps-rover-eval as a library: stats.rs (seeded bootstrap_weighted_mean_ci + Summary — pure math, verbatim), base_traffic.rs layout loaders + terrain decode + `base_scenario()` realization, and the IntentAudit/deadlock sentinel pattern. If the rover-eval dep proves irritating, stats.rs is promoted to sim-core later (REUSE-AFTER-MOVE fallback, one reviewed move). REUSE-AS-IS: sim-core `rover_driver` (feature `rover`) so every sim creep moves via the REAL rover MovementSystem with live-parity stuck/shove/priority behavior and the tuned reuse=20 — movement fidelity is inherited, not re-implemented. REUSE-AS-IS: sim-core rng.rs (SplitMix64, zero ambient entropy). Determinism fence: `econ_is_deterministic_over_rounds` (5 rounds, spread must be 0), cloned from screeps-combat-eval/src/tournament.rs:1163-1198; EP-6.13 discipline from day 0 (no HashMap iteration reaches a decision or an emitted ordering — BTreeMap/sort at boundaries; quantize before discrete decisions).\n\nREAL-LOGIC-VS-KERNEL DECISION (explicit): the sim drives EXTRACTED PURE KERNELS, not the existing mission/job code. Justification from recon: bot economy logic is hard-coupled to the JS game API and specs ECS — `JobExecutionRuntimeData.owner: &'a Creep` (jobs/jobsystem.rs:44-55), jobs are screeps-machine FSMs reading live creep.store()/room()/ECS entities, spawnsystem reads game::rooms() and live StructureSpawn, the transfer system is ECS+screeps::*-bound. Running it headless is infeasible short-term (combat needed a dedicated extraction to get there). This is acceptable BECAUSE the code under test in the A/B is the priority policy, not the FSM plumbing: the harness re-implements thin worker FSM shells (idle→pickup→deliver→work), while all four DECISIONS route through the kernel seam. KERNEL-EXTRACTION SEAM (the path to real-bot-logic, and the operator's ask (a) made concrete): (K1) transfer-demand kernel — given RoomEconDto, emit the deposit/withdraw demand set with priorities (replaces RoomTransferMission's static TransferPriority::High registrations, room_transfer.rs:426-462); (K2) hauler-task kernel — given a creep's cargo + the demand set, pick the next ticket (replaces the nearest-wins-inside-flat-ACTIVE-mask logic, transfersystem.rs:1674-1877 + haulbehavior.rs:154-175); (K3) repair-admission kernel — given room posture + structure + creep, yes/no on repairing now (gates tick_opportunistic_repair, repairbehavior.rs:111, and BuildJob's repair states, build.rs:56-104); (K4) spawn-request kernel — given room posture, emit the (role, body, f32 priority) request set consumed by spawn_step (fixes capacity-sized-replacement-bodies head-of-line stalls, source_mining.rs:394-398 class). The M1 BASELINE policy is these four kernels transcribed verbatim from the recon's citations (static High refill, nearest-wins matching, ungated opportunistic repair, capacity-sized replacements) so the A-arm is behaviorally faithful to today's bot; candidate redesigns are alternative kernel implementations. Live adoption (M5) = the bot's missions/jobs calling K1–K4 through an ephemeral EnergyPosture resource (EconomyAssessmentSystem slot at game_loop.rs:82, Read<> plumbing per the RepairQueue pattern at jobsystem.rs:29→41), kill-switched via features.rs, recomputed per tick — NO WORLD_FORMAT_VERSION bump (the ecs recon confirmed the reset-free path; the hard constraint is never reshaping serialized HaulJobContext/HaulState/TransferPriority). This dissolves the mirror-fork risk: after M5 there is exactly one implementation of the policy (EP-2.6), consumed by both live bot and sim, the combat-decision pattern replicated.",
  "systems": [
    {
      "name": "Sim source & mineral system",
      "mechanics": "Sources: energy pool 3000 (owned/reserved) / 1500 (neutral) / 4000 (SK) per cycle; harvest = HARVEST_POWER 2 e/WORK/tick decrementing the pool; 300-tick regen with the timer starting at the first harvest below cap (engine-mechanics.md:445 — the timer subtlety materially affects sustained income and must be modeled, not idealized as flat 10 e/t); harvested energy lands in the creep store, overflow beyond store capacity drops to ground. Minerals: 4 density tiers {1:15000, 2:35000, 3:70000, 4:100000} (CORRECTION to the prompt's '6-density' — there are four), HARVEST_MINERAL_POWER 1/WORK/intent, requires extractor, EXTRACTOR_COOLDOWN 5 ticks between intents, MINERAL_REGEN_TIME 50000 after exhaustion.",
      "v1_scope": "Sources FULLY modeled in M0 — headline-critical for both metrics (income is the denominator of every oracle). Minerals STUBBED until M6 (static amount + extractor cooldown only): irrelevant to T_recover and to T_RCL(N≤6) energy arithmetic."
    },
    {
      "name": "Sim spawn system",
      "mechanics": "spawnCreep debits energy ATOMICALLY at intent time (fails whole if available < body cost — there is NO 1 e/t drain during spawning; prompt framing corrected per engine-mechanics.md:257); draw order spawns-first then extensions-closest-first; busy for CREEP_SPAWN_TIME 3 × body.length ticks, one creep per spawn; spawned creep emerges to an adjacent tile (blocked-exit slip coarse-modeled via movement occupancy); each spawn SELF-CHARGES +1 e/tick while room spawn+extension energy < 300 (engine-mechanics.md:279 — the mechanism that makes a fully drained room recoverable and the true floor of every bootstrap oracle); spawn capacity 300; extension counts by RCL {2:5, 3:10, 4:20, 5:30, 6:40, 7:50, 8:60} at capacity 50 (RCL≤6) / 100 (RCL7) / 200 (RCL8), room max 12,900 at RCL8; energy_available / energy_capacity_available semantics. Queue policy: descending f32 priority with the head-of-line energy-banking break (affordable-by-capacity-but-not-by-energy head request blocks all lower requests that tick) — reused from the spawn_throughput::spawn_step pure mirror, citation-pinned to live spawnsystem.rs:379-436.",
      "v1_scope": "FULLY modeled in M0 — the entire mechanism layer of the headline metrics. Renew and directional spawning stubbed (renew is gated live behind a 10k stored floor and is irrelevant under collapse; add in M6 if steady-state family needs it)."
    },
    {
      "name": "Sim economy system (stores, transfer, build, repair, decay)",
      "mechanics": "Per-resource stores on creeps/containers/storage/spawns/extensions/labs; transfer/withdraw/pickup adjacency-1 intents; one work-intent + one transfer-intent + movement per creep per tick (matching the bot's own Pipeline A/D model in jobs/actions.rs — coarser than the engine's full per-intent pipelines; deviations documented). Build: BUILD_POWER 5 progress/WORK/tick at 1 energy per progress point (5 e/WORK/t while building); sites complete at structure progress totals; construction site placement follows the foreman plan's required_rcl schedule. Repair: REPAIR_POWER 100 hits/WORK/tick at REPAIR_COST 0.01 e/hit (1 e/WORK/t). Decay (all verified against the engine clone): roads lose ROAD_DECAY_AMOUNT 100 hits per ROAD_DECAY_TIME 1000 ticks, ×5 on swamp, ×150 on wall/tunnel (roads/tick.js:10-31; hitsMax 5000/25000/750000 respectively); plus ROAD_WEAROUT per-step traffic wear (damage × body size per creep step — REQUIRED, this is what regenerates the repair-bait under the sim's own traffic and keeps the leak scenario honest); containers lose CONTAINER_DECAY 5000 hits per CONTAINER_DECAY_TIME_OWNED 500 ticks when controller level > 0, per 100 ticks unowned (containers/tick.js:10-31; hits 250k); dropped resources decay ceil(amount/1000)/tick (engine-mechanics.md:431); creep death at TTL drops store to ground (tombstones stubbed to direct ground drop). Ramparts (300 hits/100t) only if a scenario includes them. Towers stubbed v1 (energy sink flagged off; add for steady-state family if needed).",
      "v1_scope": "Stores/transfer/pickup + road & container decay + traffic wearout in M0–M1 (required to express the collapse family and the repair leak). Build system + site scheduling in M2 (required for greenfield RCL rush). Tower/rampart mechanics deferred."
    },
    {
      "name": "Sim controller & RCL progression system",
      "mechanics": "upgradeController: UPGRADE_CONTROLLER_POWER 1 e/WORK/tick within range 3 (RCL8 15 e/t cap — moot below 8); progress table CONTROLLER_LEVELS {1:200, 2:45000, 3:135000, 4:405000, 5:1215000, 6:3645000, 7:10935000}; downgrade clock decrements 1/tick from CONTROLLER_DOWNGRADE {1:20K, 2:10K, 3:20K, 4:40K, 5:80K, 6:120K, 7:150K, 8:200K}, each upgrade action restores +100 capped at full, level-up resets the clock to HALF-max (engine-mechanics.md:228 — the half-max reset shapes early-rush downgrade pressure and must not be idealized); level-down on clock expiry halves progress. RCL gates structure counts (extensions, towers, storage at 4, labs at 6+) via the plan's required_rcl schedule.",
      "v1_scope": "FULLY modeled in M2 — first-class because T_RCL(N) is a headline metric and downgrade pressure is a scenario axis (Family D). Only the ≤RCL7 progress rows matter for v1 tick caps."
    },
    {
      "name": "Sim lab system",
      "mechanics": "runReaction: consumes LAB_REACTION_AMOUNT 5 of each input compound, produces 5 output; cooldown is the per-compound REACTION_TIME table (LAB_COOLDOWN 10 is annotated 'not used' in the engine constants — trust REACTION_TIME, engine-mechanics.md:303, :513); lab capacity 3000 mineral + 2000 energy; boostCreep costs LAB_BOOST_MINERAL 30 + LAB_BOOST_ENERGY 20 per body part boosted.",
      "v1_scope": "STUBBED until M6. Labs contribute nothing to T_recover or T_RCL(N≤6) — neither headline metric touches compounds. Built in M6 to honor the explicit operator ask and to enable future mineral-economy/boost-cost metrics (e.g. boost e/t-equivalent pricing feeding the 0033 §D5.4 military-w arm)."
    }
  ],
  "metrics": [
    {
      "name": "T_recover / η_recover / H_recover (HEADLINE)",
      "definition": "COLLAPSE STATE (scenario-defined): room at RCL R with structures per the plan's required_rcl schedule; all creeps dead (or a parameterized 1-harvester skeleton); spawns+extensions at 0 energy; storage energy S0 ∈ {0, 5k, 50k}; roads at h% health ∈ {30%, 60%} (repair bait); downgrade clock at c% ∈ {100%, 25%}. RECOVERED STATE: the first tick t where BOTH (i) energy_available == energy_capacity_available (every spawn and extension full) AND (ii) trailing-300-tick harvest rate ≥ 0.9 × room potential (source_count × 10 e/t) — i.e. refilled AND self-sustaining, so a policy cannot 'win' by refilling once off storage while the mining economy is still dead. T_recover = t − t_collapse; runs hitting the tick cap score η = 0 (catastrophic tail stays visible, 0033 idiom). ORACLE T*_recover: analytic lower bound from the sequential dependency chain — spawn self-charge at 1 e/t (+S0) → first affordable harvester body → harvest ramp at 2 e/WORK/t under source regen → fleet build-out at 3 ticks/part spawn throughput → capacity-deficit refill at net income; pure arithmetic in oracle.rs, no pathing (uses plan haul distances). η_recover = T*_recover/T_recover ∈ (0,1]. Aggregate H_recover = Σ Wη / Σ W with W = T*_recover (harder scenarios weigh more), reported as weighted mean + seeded-bootstrap 95% CI + p05–p95 via rover-eval stats.rs. Regression-tracked baseline, NEVER a ==1 gate (EP-6.7: gates are N-seeded paired diffs vs stored (scenario, seed, SHA) baselines)."
    },
    {
      "name": "T_RCL(N) / η_rcl / H_rcl rush curves (HEADLINE)",
      "definition": "From greenfield (RCL1, one spawn per the foreman plan anchor, virgin sources, no roads/containers — they get built per required_rcl schedule): T_RCL(N) = first tick controller reaches level N, reported as the full curve N = 2..4 in fast mode, N = 5..6 in full mode only (RCL6 needs ~5.4M cumulative energy ≈ 10^5+ ticks; tick cost measured in M2 before committing corpus size). ORACLE T*_RCL(N): energy-conservation lower bound — cumulative energy required (Σ progress + body costs of the minimal saturating fleet + site build costs) divided through a piecewise-max-income schedule (1 e/t self-charge phase → ramping harvest → source-saturated phase), scheduled against spawn throughput (3 t/part). η_rcl(N) = T*/T; H_rcl = ΣWη/ΣW with W = T*_RCL(N)."
    },
    {
      "name": "spawn_energy_blocked_ticks",
      "definition": "Count (and fraction) of ticks where the head-of-line spawn request was affordable-by-capacity but blocked by energy_available (the spawn_step break branch taken) — the direct 'extensions weren't refilled fast enough' signal. Sub-metric: refill_latency distribution = ticks from each energy_available drop below capacity until back-to-full."
    },
    {
      "name": "spawn_idle_frac",
      "definition": "Fraction of ticks each spawn is idle while the request queue is non-empty (idle-with-demand). Distinguishes energy starvation (blocked_ticks high) from demand starvation (queue empty)."
    },
    {
      "name": "extension_deficit_integral",
      "definition": "Time-integral of (energy_capacity_available − energy_available)/energy_capacity_available over the run — 'extension-empty ticks' weighted by depth of deficit. Comparable across RCLs."
    },
    {
      "name": "energy_ledger + conservation audit (HARD GATE)",
      "definition": "Per-tick flow accounting by source {harvested, spawn_self_charge, starting_stock} and by sink {spawn_bodies, upgrade, build, repair_roads, repair_containers, repair_other, decay_lost, dropped_decay_lost, on_creep_at_death}. The ledger must balance exactly every tick (sources − sinks − Δstocks == 0); any imbalance is a sim bug and fails the run — the IntentAudit-class zero-tolerance sentinel for the economy layer."
    },
    {
      "name": "repair_leak_e",
      "definition": "Energy spent on repair intents during ticks where energy_available < energy_capacity_available (i.e. while refill demand exists) — the operator's observed symptom, measured directly in energy units. Split by structure class (roads vs containers vs other). The M1 repro gate requires baseline > 0 on the collapse family with repair bait; the A/B success criterion drives it toward the redesign's intended level without regressing steady-state road upkeep."
    },
    {
      "name": "death_spiral / deadlock sentinels (HARD GATE)",
      "definition": "Deadlock: K consecutive ticks (K=12, rover-eval idiom) with zero energy delta AND zero creep movement while demand exists → run fails loudly, scored η=0, gated to zero occurrences on the corpus. Death-spiral flag: total room energy (stocks + carried) strictly declining over a trailing 500-tick window while spawns idle — diagnostic, surfaced per-scenario."
    },
    {
      "name": "econ_is_deterministic_over_rounds (SHIP-BLOCKER)",
      "definition": "5 identical seeded runs of a fixed corpus slice must produce bit-identical headline reports (spread == 0), cloned from combat-eval tournament.rs:1163-1198. Plus det_reorder (input-order permutation invariance) per 0033 §D7. Fails the bench with non-zero exit."
    }
  ],
  "scenarios": "SEED CORPUS: the 13 real foreman-planned rooms in C:\\code\\screeps-ibex\\screeps-rover-eval\\resources\\captured-layouts.json (verified present: terrain + controller + sources + mineral + full RCL8 structure sets incl. 793 extensions, 50 containers, 39 spawns, 130 labs). PREREQUISITE SLICE (M1): extend capture_layout to serialize `required_rcl` per structure — verified available on foreman's `RoomItem.required_rcl: Option<u8>` (screeps-foreman/src/plan.rs:27) — so any layout can be realized 'as of RCL X' and yields the construction-site schedule for build phases. Rover-eval's base_scenario() terrain realization is reused as-is.\n\nFAMILY C — COLLAPSE (T_recover): parameterized product over (layout ∈ 13) × (RCL ∈ {3,4,5,6}) × (storage S0 ∈ {0, 5k, 50k}) × (creeps ∈ {wiped, 1-harvester skeleton}) × (road health ∈ {30%, 60%}) × (downgrade clock ∈ {full, 25%}); curated ~20-scenario catalog spanning the axes (0033 corpus-size idiom) + seeded procedural variants via generate(seed). The road-health axis is the A/B discriminator: decayed roads are exactly the Medium/High repair bait the recon traced the live leak to, and traffic wearout keeps regenerating it during the run.\n\nFAMILY G — GREENFIELD RCL RUSH (T_RCL(N)): layout + RCL1 + single spawn at the plan anchor + virgin room; structures become placeable per required_rcl; measures the full rush curve. Stresses spawn-throughput scheduling, build-vs-upgrade energy split, and body-size ramping as capacity grows.\n\nFAMILY S — STEADY-STATE NON-REGRESSION: healthy RCL-N room, full fleet, normal creep-TTL churn, roads slowly wearing under traffic; measures refill_latency, spawn_idle_frac, and road-network health over 10k ticks. THE GUARD RAIL: any priority redesign that wins Family C by simply never repairing must not let Family S's road stock collapse or refill latency regress — per-tick-optimal policies get evaluated in the regime where repair IS the right call, honoring the operator's no-hysteresis directive by making the tradeoff visible instead of latching modes.\n\nFAMILY D — DOWNGRADE PRESSURE: Family C start but downgrade clock at 10% — forces the refill-vs-controller-save triage (the live CRITICAL-upgrader case, upgrade.rs:209-222); scores whether the policy avoids losing an RCL (level-down halves progress, making this catastrophically expensive in T_recover terms).\n\nWeights W per 0033: harder/larger scenarios weigh more (W = T*); per-family H reported + pooled H; tick caps per family (C: ~15k, G-fast: ~50k, G-full: measured in M2; S: 10k; D: 15k); non-completion scores 0, never dropped from the result set (EP-6.8 plan-loss accounting).",
  "milestones": [
    {
      "id": "M0",
      "title": "screeps-econ-engine kernel: sources + stores + spawn + conservation",
      "detail": "New crate over sim-core's Simulation trait: EconWorld/EconIntents/resolve_econ_tick with source harvest (2 e/WORK/t, 300t regen with first-harvest timer start), per-resource stores, transfer/withdraw/pickup, spawnCreep (atomic debit, spawns-then-extensions-closest draw, 3 t/part busy, +1 e/t self-charge below 300, extension capacities by RCL), road+container decay + traffic wearout, dropped-resource decay, creep TTL death dropping stores. Energy ledger + exact conservation audit. Determinism fence econ_is_deterministic_over_rounds + det_reorder cloned and green from the first commit. GATE: fence spread 0; conservation exact over randomized intent fuzzing; one unit test per mechanic citing engine-mechanics.md/engine-source line (the spawn_throughput citation-pin idiom). Zero live-bot changes; host-only crate."
    },
    {
      "id": "M1",
      "title": "econ-eval harness + baseline policy + T_recover + the REPRO GATE",
      "detail": "New screeps-econ-eval crate: capture_layout extended to emit required_rcl (re-run the bin, commit the cache); collapse Family C realized from captured layouts; worker FSM shells driving rover_driver movement (real rover MovementSystem, live-parity); scripted BASELINE policy = the four kernels K1–K4 transcribed verbatim from recon citations (static High spawn/extension deposits, nearest-wins flat-ACTIVE hauler matching, ungated opportunistic repair ≥Medium-harvester/≥Low-hauler incl. ticket deduction, capacity-sized replacement bodies + head-of-line banking via spawn_step); T_recover + oracle + η/H via rover-eval stats.rs; econ_bench CLI fast mode with stored (scenario, seed, SHA) baselines. GATE (repro): the baseline reproduces the operator's disease offline — repair_leak_e > 0 on repair-bait scenarios AND T_recover measurably inflated (paired-seed diff, CI excluding zero) with bait ON vs an identical no-bait control. If the sim cannot show the disease, it cannot validate the cure — this gate blocks M4."
    },
    {
      "id": "M2",
      "title": "Controller/RCL + build system + greenfield rush curves",
      "detail": "Controller upgrade/progress/downgrade-clock/level-transitions (half-max reset, level-down progress halving); construction sites + build 5 progress/WORK/t at 1 e/progress, scheduled per required_rcl; Family G (greenfield) + Family D (downgrade pressure) + Family S (steady-state churn); T_RCL(N) curves + oracle. Measure wall-clock tick cost on long-horizon runs and fix fast/full corpus splits from data (EP-4.6: measured, not asserted). GATE: rush curves reported with CI on all 13 layouts to N=4; oracle sanity η ≤ 1+ε everywhere; fence still spread-0; Family S baseline stored."
    },
    {
      "id": "M3",
      "title": "screeps-econ-decision seam at parity (A/A == 0)",
      "detail": "New pure crate: EconomyView/EconomyIntent DTOs + kernels K1 (transfer-demand priorities), K2 (hauler task selection), K3 (repair admission), K4 (spawn request set); harness re-plumbed so ALL four decisions route through the seam; the M1 baseline re-expressed as kernel implementations. GATE (EP-6.4 parity): A/A run — seam-routed baseline vs M1 inline baseline — byte-identical headline reports across the corpus. This freezes the seam (EP-2.1) before any behavior change rides it, and is the exact artifact the live bot adopts."
    },
    {
      "id": "M4",
      "title": "THE A/B: priority-redesign candidates vs baseline (the decision milestone)",
      "detail": "The priority-redesign design(s) from the companion workstream land as alternative K1–K4 kernel implementations (e.g. posture-conditional deposit priorities, priority-ordered hauler matching, energy-gated repair admission, deficit-sized bodies). Tournament idiom (env-driven #[ignore] sweep, coordinate descent over kernel constants); evaluation = N-seeded PAIRED diffs vs baseline per family. SUCCESS CRITERIA: H_recover and H_rcl improve with 95% CI excluding zero on Families C/G/D; Family S non-regression (refill_latency, road-stock health, spawn_idle within baseline CI); repair_leak_e reduced on C; zero sentinel firings. OUTPUT: ranked winner + tuned named constants as a reviewed diff, and the honest possibility of a null result (per-tick-optimal candidates only — no latched emergency mode unless Family S/C oscillation is actually observed in the sim, which would be the operator-directive-sanctioned evidence for adding damping)."
    },
    {
      "id": "M5",
      "title": "Live adoption behind a kill-switch (reset-free)",
      "detail": "Bot consumes the winning kernels: EnergyPostureSystem registered after EconomyAssessmentSystem (game_loop.rs:82 slot, StageClass::Always — feeds the never-shed set), ephemeral Write<EnergyPosture> resource, Read<> plumbed to jobs via the RepairQueue pattern (jobsystem.rs:29→41) and to missions/spawn per existing EconomySnapshot plumbing; K1–K4 called at the recon-mapped sites (room_transfer.rs demand registration, haul idle selection, tick_opportunistic_repair admission, mission spawn requests). features.rs kill-switch (serde-default additive, no Memory migration), default OFF until soak. HARD CONSTRAINT: no serialized-shape change — posture is recomputed per tick; TransferPriority enum, HaulJobContext, HaulState, tickets untouched → NO WORLD_FORMAT_VERSION bump. GATE: 238+ host tests + wasm build green; Docker soak with ibex-eval colony_health (the live complement to the offline prediction) on a staged collapse (kill all creeps via console) showing recovery; flag flipped default-ON only after operator review. Old inline behavior DELETED at the call sites once validated (EP-2.6/EP-10.5), not left flagged-off forever."
    },
    {
      "id": "M6",
      "title": "Minerals + labs + extractor (operator-requested completeness)",
      "detail": "Mineral density tiers/regen/extractor cooldown; lab reactions (5-in/5-out, per-compound REACTION_TIME, 3000+2000 capacities) + boost costs (30 mineral + 20 energy per part); a mineral-economy scenario family (RCL6 room, extractor + lab throughput) and a boost-cost e/t-equivalent diagnostic. Not headline-gated; unblocks future metrics (boost pricing for the 0033 military-w arm, terminal/market sims). GATE: mechanics unit-tested vs engine-mechanics.md §7.4; fence still spread-0."
    }
  ],
  "risks": [
    "Fidelity gap in the FSM shells: the harness re-implements worker state machines rather than running the live jobs layer, so a live divergence outside the four kernels (e.g. ticket-chaining subtleties in get_additional_deliveries) could make sim rankings mispredict live outcomes. Mitigated by routing every DECISION through the seam (the redesign surface is exactly K1–K4), transcribing the baseline from pinned citations, and the M5 Docker-soak colony_health check as the live complement — but state it honestly: the sim validates the policy, not the whole bot.",
    "Oracle looseness: analytic T* lower bounds ignore pathing/congestion, so η will sit systematically below 1 and its absolute level is not meaningful across families. Guarded by EP-6.7 — all gates are paired-seed diffs vs stored baselines, never absolute η thresholds; H is regression-tracked only.",
    "Long-horizon tick cost: RCL-rush to N≥5 implies 10^5+ ticks of full movement sim per scenario; if wall-clock cost is prohibitive the corpus quietly shrinks and loses power. M2 explicitly measures cost before fixing fast/full splits; movement already carries the tuned reuse=20; fallback is fewer layouts × more seeds on long families, never silent scenario dropping (EP-6.8).",
    "spawn_step mirror drift: the queue policy is a mirror of live spawnsystem.rs, not shared code — live changes (or the M4 winner's K4) can drift from it. Citation-pin tests catch textual drift; the real fix is M5 itself, after which the kernel IS the live code and the fork dissolves. Until then, any live spawnsystem change must touch the mirror in the same commit.",
    "Intent-simultaneity coarseness: modeling one work + one transfer intent per tick (the bot's own Pipeline A/D) instead of the engine's full per-intent pipelines could mis-score policies exploiting same-tick combos the bot cannot express anyway. Acceptable because the bot's action model is the binding constraint; deviations documented in econ-engine so nobody trusts the sim for non-ibex agents.",
    "A/B contamination via movement: candidate policies change traffic patterns, which changes road wearout, which changes the repair bait mid-run — a feedback loop that could reward degenerate 'never build roads' behavior. Family S's road-stock non-regression criterion and the build-schedule realism (roads come from the foreman plan, not the policy) bound this.",
    "Scope creep into sibling ADR territory: ADR 0006 owns the live colony-health sim, ADR 0007 hauling, ADR 0011 spawn orchestration, ADR 0033 movement. The ADR needs a Division-of-ownership paragraph scoping this to the OFFLINE economy bench + the K1–K4 pure kernels; live mission/job restructuring beyond the M5 call-site adoption belongs to those ADRs.",
    "WFV hazard in M5: any temptation to persist posture, add fields to HaulJobContext/HaulState, or reorder TransferPriority variants forces a WORLD_FORMAT_VERSION bump and a loud full reset (serialized tickets embed the priority enum). Design constraint is binding: ephemeral resource + read-side gates only; if the winning design genuinely needs a serialized shape change, it must be surfaced to the operator as an explicit reset decision, not slipped in.",
    "Determinism regressions: economy state is naturally map-shaped (stores by structure id); one HashMap iteration reaching a decision re-introduces the ~1% noise class the rover fence caught three times. BTreeMap/sorted-boundary discipline (EP-6.13) from M0, fence in every milestone gate, quantize-before-discrete-decision for all f64 kernel outputs.",
    "Null-result risk on the A/B: the head-of-line banking already reserves in-structure energy for the top spawn; if the true live bottleneck is something the corpus under-expresses (e.g. CPU-tier shedding, multi-room interactions), the sim may show small effects. The M1 repro gate de-risks this — if the disease does not reproduce offline with measurable T_recover inflation, the sim design (not the redesign) gets revisited before M4 spends effort."
  ]
}