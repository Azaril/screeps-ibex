{
  "name": "Energy Sink Market — unified e/t EV bids (rho) over the existing transfer/repair lanes",
  "overview": "Every energy sink (spawn/extension refill, repair target, build site, upgrade, tower, lab, storage) computes a per-tick bid rho = energy-equivalent value returned per energy spent, in the codebase's one sanctioned currency (e/t, ADR 0032/0038/0033 §D5.4). Storage is the numeraire (rho = 1.0 exactly); refill's bid is the marginal spawn-throughput ROI (~10-15x when sources are unsaturated post-wipe, decaying to ~1 when saturated); road repair's bid is rebuild-cost-avoided x death-imminence (~0.3-0.7 for a half-health road). Stress behavior EMERGES: with empty extensions the room's opportunity floor is the refill bid, so every drive-by repair, builder repair state, and tower repair shot is priced out with zero modes, zero latching — exactly the per-tick-optimal directive. When the room is healthy the floor falls to 1.0 and quantized bids reproduce today's priorities, so the system degrades to current behavior by construction.",
  "mechanism": "WHAT IS COMPUTED. A new pure kernel `screeps-ibex/src/sink_economics.rs` (sibling of `room_economics.rs`, same \"PURE kernel — NO game::* reads\" contract; never edits `room_net_roi`'s body per ADR 0038 §7) exports bid functions, all f64 quantized to milli-rho (`quantize_milli`) by callers before any discrete decision (determinism-fence doctrine):\n- `refill_bid(f: &RefillFacts) -> f64` = 1.0 + MARGINAL_HARVEST_ROI x U, where U = unsaturated source fraction = max(0, 1 - fielded_harvest_income/potential_income), fielded_harvest_income = 2.0 x fielded_harvest_work (HARVEST_POWER), potential = source_count x 10 e/t (same constants as `room_economics.rs`). MARGINAL_HARVEST_ROI ~= (HARVEST_POWER x CREEP_LIFE_TIME - marginal_work_body_cost)/marginal_work_body_cost ~= 14 (named constant, sim-calibrated per EP-4.6). Post-wipe U~=1 -> rho~=15; saturated room U=0 -> rho=1.0 (energy in the bank is worth face value). Optional second term +GAMMA_QUEUE when the prev-tick spawn queue head was capacity-affordable but energy-blocked (read from `SpawnQueueSnapshot`, extended with `head_blocked: bool` — ephemeral).\n- `repair_bid(f: &RepairFacts) -> f64` = (rebuild_cost_per_hit / REPAIR_COST) x min(1, HORIZON/ticks_to_death), with ticks_to_death = hits/decay_per_tick, HORIZON = CREEP_LIFE_TIME = 1500. Engine numbers make this self-calibrating: plain/swamp road = 0.06 e/hit rebuild vs 0.01 repair -> base 6.0, but a 50% road has 25,000 ticks to death -> rho~=0.36 (below storage — correctly deferred); an owned-room container (0.02 e/hit, 50 hits/tick decay) at 50% has 2,500 ticks -> rho~=1.2 and rising (correctly repaired). Roads cross rho=1.0 only near ~18% health — per-tick optimal, no threshold table. Walls/ramparts stay OUTSIDE the market on the existing siege-gated `WallRepairMission` path (defense is never-shed, EP-4.3).\n- `build_bid(kind) -> f64` = declared per-structure-type constant table (spawn/extension/container/storage 5-10, tower 3, road 1.2, decor 0.5) — honest policy constants per the 0033 §D5.4 idiom, tournament-calibrated.\n- `upgrade_bid(ttd, ttd_max) -> f64` = V_UPGRADE (~1.0) normally; DOWNGRADE_OVERRIDE (large, survival-gated) when ttd < ttd_max/2 — preserves the existing CRITICAL upgrader semantics.\n\nWHEN / BY WHICH SYSTEM. New `EnergyMarketSystem` (StageClass::Always) registered at `game_loop.rs:83`, immediately after `EconomyAssessmentSystem` (:82) and before all missions/jobs. It reads `EconomySnapshot` (`military/economy.rs:27-51` — stored_energy, spawn_energy vs spawn_energy_capacity, free_spawns, prev_tick_queue_depth all already there) plus a new `fielded_harvest_work: u32` field added to `RoomEconomyData` (one extra pass in `EconomyAssessmentSystem` summing WORK parts of creeps holding Harvest/StaticMine jobs — ephemeral). It writes a new ephemeral specs Resource `EnergyMarket { rooms: HashMap<Entity, RoomMarket> }` (explicit `world.insert` next to `EconomySnapshot` at `game_loop.rs:893`), where `RoomMarket { enabled: bool, rho_refill_q: u32, rho_floor_q: u32, rho_upgrade_q: u32 }` — rho_floor = rho_refill while spawn_energy < spawn_energy_capacity; else 1000 milli if a storage with free capacity exists; else rho_upgrade. All values pre-quantized so no float ever reaches a decision.\n\nCONSUMED WHERE (five read sites, each a small diff):\n1. `tick_opportunistic_repair` (`jobs/utility/repairbehavior.rs:111`) gains one gate: skip unless quantized repair_bid(best in-range target) >= rho_floor_q. Because ALL three drive-by call sites (`jobs/harvest.rs:225,264`, `jobs/haul.rs:229,246`, `jobs/staticmine.rs:144`) route through this primitive, one change closes the operator-observed leak everywhere, including the harvester-en-route-to-spawn ticket drain. Plumbing: add `energy_market: Read<'a, EnergyMarket>` to `JobSystemData` and `&'a EnergyMarket` to `JobExecutionSystemData` (`jobs/jobsystem.rs:17-42`) — the exact existing `repair_queue` pattern.\n2. `RepairQueue` entries gain a `rho_q: u32` computed by `LocalBuildMission::pre_run_mission` (`missions/localbuild.rs:181-217`, which already iterates every damaged structure); `RepairQueue::get_best_target` (`repairqueue.rs:54-78`) orders by rho_q (tie-break: lowest hits then object id — deterministic) and takes a `min_rho_q` filter = rho_floor_q. This replaces `map_normal_priority`/`map_high_value_priority` as the ordering for civilian repair (the RepairPriority enum stays for walls). `get_repairer_priority` (`localbuild.rs:112-122`) requests a repairer-builder only when best rho_q >= rho_floor_q — killing the perverse post-collapse \"decayed roads spawn a HIGH builder\".\n3. Deposit priorities become quantized bids: `RoomTransferMission::request_transfer_for_spawns/extensions` (`room_transfer.rs:426-462`) sets bucket = bucket_for(rho_refill_q) via fixed thresholds (>=4000 milli High, >=1500 Medium, >=700 Low, else None); `missions/tower.rs:176-191` and `missions/labs.rs:431-439` likewise (tower hostile bid = large constant -> High, peaceful ~0.5 -> Low — reproducing today's values). The serialized `TransferPriority` enum is deliberately UNTOUCHED (it rides in in-flight `TransferDepositTicket`s inside serialized `HaulState`); the market only decides which existing bucket each per-tick request lands in. `MissionSystemData` gains the `EnergyMarket` read alongside `economy` (`missions/missionsystem.rs:45`).\n4. Intra-bucket ordering fix: `get_new_delivery_current_resources_state` (`jobs/utility/haulbehavior.rs:129-179`) iterates High->Medium->Low tiers instead of flat-nearest across the ACTIVE mask (recon leak (a)); `get_additional_deliveries` (`haulbehavior.rs:248-364`) chains at mask ALL only when no unmet High deposit availability remains in-room, else ACTIVE (leak (b)). Both are behavioral-only, no serialization impact.\n5. `BuildJob::Idle` (`jobs/build.rs:56-103`) reorders by bid: if creep carries energy and rho_refill_q >= HIGH threshold, try `get_new_delivery_state(High)` FIRST (builders become emergency refillers under stress — emergent, not a mode); its repair states pass min_rho_q = rho_floor_q, deleting the unconditional repair-at-ANY-priority arm. Tower repair (`missions/tower.rs:426-453`) gets the same rho gate.\n\nKILL-SWITCH: `features.market.energy_bids: bool` (serde-default false; `features.rs` nested-struct convention, no Memory migration). `EnergyMarketSystem` publishes `enabled = features.market.energy_bids`; every read site branches: disabled -> exact current constants (static High for refill, RepairPriority thresholds, no floor gate). EP-2.2 parity first: land plumbing with the flag off, prove no diff, then flip after sim + Docker soak.",
  "state_changes": "All new state is ephemeral, recomputed per tick: (1) `EnergyMarket` specs Resource (new, world.insert at game_loop.rs:893 vicinity, NOT in the serialize tuple at game_loop.rs:494-512); (2) `RoomEconomyData.fielded_harvest_work: u32` + `SpawnQueueSnapshot.head_blocked: bool` — both fields of the explicitly ephemeral `EconomySnapshot` (economy.rs:58 \"not serialized\"); (3) `rho_q: u32` on RepairQueue entries — RepairQueue is cleared/rebuilt every tick (repairqueue.rs, game_loop.rs:84); (4) `features.market.energy_bids` — additive serde-default Memory._features field, no migration; (5) new pure module `sink_economics.rs` — code only. Deliberately NOT changed: `TransferPriority` enum and `TransferWithdrawTicket`/`TransferDepositTicket` shapes (serialized inside `HaulState`/`JobData`, game_loop.rs:501), `HaulJobContext` (incl. keeping `allow_repair` as-is — the market gate sits inside the shared primitive, not the serialized flag), `RoomData`, all mission saveload shapes. The one tempting-but-forbidden move — carrying raw rho on transfer tickets instead of quantizing into the existing 4 buckets at request time — is explicitly rejected to stay reset-free.",
  "pros": [
    "Zero modes / zero hysteresis: stress prioritization is the per-tick argmax of one currency, exactly satisfying the operator's per-tick-optimal directive; nothing latches, so there is no mode-exit bug class",
    "One mechanism closes all four recon leak paths (drive-by repair, builder repair-at-any-priority, intra-High nearest-wins diversion, secondary-chaining-to-storage) — versus four bespoke patches",
    "Reuses the sanctioned e/t currency and pure-kernel pattern (room_economics.rs / claim_economics.rs / 0033 SS D5.4 role bids): no new unit invented, and future sinks (terminal sends, nuker, powerspawn, labs) get priced by adding one function, not one subsystem",
    "Degrades gracefully twice over: healthy rooms produce today's priorities from the same formulas (refill demand amount->0 and rho->1), and the kill-switch reverts every read site to current constants for parity landing (EP-2.2)",
    "No WORLD_FORMAT_VERSION bump and no reset: every new datum is per-tick recomputed, per EP-5.4 and the ADR-0038 precedent",
    "Directly sim-evaluable: the bid kernel is pure from day one, so the offline economy sim A/Bs the actual shipped policy (spawn_throughput/room_economics mirror precedent), and constants are calibrated by the 0033 tuning.rs tournament pattern that already SHIPPED a rover default",
    "Engine arithmetic does most of the calibration for free: rebuild-cost-per-hit x imminence naturally ranks containers above roads and prices half-health roads below storage without any hand-tuned threshold table",
    "CPU is neutral-to-negative under stress: the floor gate early-outs the range-3 repair scan before it runs, precisely when CPU matters most"
  ],
  "cons": [
    "Calibration surface: ~8 named constants (MARGINAL_HARVEST_ROI, V_UPGRADE, DOWNGRADE_OVERRIDE, build-bid table, 3 bucket thresholds); mispricing V_UPGRADE or the refill bid can starve the controller or upgraders in mid-game rooms — mitigated by the downgrade override and sim tournament, but the risk is real until soaked",
    "Quantizing rho into the existing 4 TransferPriority buckets loses resolution inside High (a 15-bid extension and a 6-bid hostile-tower refill both land High, and nearest-wins persists within the bucket); full resolution needs ticket reshaping = WFV bump, explicitly deferred",
    "Emergent behavior is harder to debug than an explicit emergency mode: 'why did the hauler skip the lab' requires reading per-room bids — needs a one-line console/HUD readout of RoomMarket to be operable",
    "Imminence-priced road repair means roads deliberately ride near ~18% health; functionally safe (decay is deterministic) but visually alarming, and any decay-modeling error in RepairFacts kills roads before the sim would notice without a dedicated scenario",
    "The offline A/B exercises the pure bid kernel plus mirrored selection logic, not the JS-coupled job cascade itself (jobsystem.rs owner: &Creep); mirror-drift risk persists until an EconomyView DTO seam is extracted (the combat-decision pattern) — scoped as follow-on, not blocker",
    "Does not fix the adjacent post-wipe defect of capacity-sized replacement bodies head-of-line-blocking the spawn queue (todo.md line 18); the market makes energy REACH spawns faster but a separate body-sizing fix is still needed for full recovery speed",
    "Touches five read sites across jobs/missions/queues in one initiative — wider blast radius than a single-gate patch, though each site is a small flag-guarded diff"
  ],
  "wfv_impact": "No WORLD_FORMAT_VERSION bump (stays 23). Every new datum is an ephemeral per-tick Resource or a field on an already-ephemeral snapshot/queue, none of which ride the serialize tuple (game_loop.rs:494-512). The serialized shapes that WOULD force a bump — TransferPriority variants (embedded in in-flight TransferDepositTicket/TransferWithdrawTicket inside HaulState in JobData at game_loop.rs:501), HaulJobContext, RoomData, mission ConvertSaveload shapes — are all deliberately frozen by the design: the market quantizes bids into the existing 4-bucket enum at request time and gates behavior at read sites only. Feature flag is additive serde-default in Memory._features (outside WFV entirely).",
  "cpu_notes": "EnergyMarketSystem: O(owned rooms) pure f64 arithmetic (~a dozen ops/room, <<0.01 CPU) in the Always pre-pass; it must be StageClass::Always since it feeds spawn/haul (the never-shed set, EP-4.3). fielded_harvest_work: one join over creep jobs in EconomyAssessmentSystem, O(creeps) part-count sums — microseconds, same order as work that system already does. Repair bids: ~5 flops per damaged structure inside LocalBuildMission::pre_run_mission's EXISTING per-tick structure iteration — no new scan. Read sites are one Resource lookup + u32 compare each; the tick_opportunistic_repair gate runs BEFORE the range-3 target scan, so under stress (floor > max civilian repair bid) it skips the scan entirely — net CPU savings exactly when the room is CPU-poor. No pathfinding, no new per-tick allocations beyond one small HashMap<Entity, RoomMarket> rebuilt per tick (decisions read single per-room quantized values; no HashMap iteration reaches a decision, per EP-6.13). Intent-side: fewer repair intents under stress = lower intent CPU.",
  "sim_evaluation_hook": "The planned econ overlay (EconWorld composing sim-core MovementState per the sim.rs layering contract, with harvest/store/spawn/build/upgrade resolution from engine-mechanics.md SS7.4 and the required_rcl re-capture of the 13 captured-layouts.json rooms) runs scenario families {post-wipe RCL-reboot, RCL2 bootstrap rush, energy-crash-with-storage, container-death, healthy steady-state}. The sim's creep policy calls the SAME `sink_economics.rs` bid functions plus a mirror of the selection cascade (the spawn_throughput.rs citation-pinned-mirror pattern), driven through rover_driver for live-parity movement. A/B = identical scenario+seed with features.market.energy_bids on vs off (off = today's hard-coded priorities mirrored), N seeds, paired diffs per EP-6.7 — never absolute thresholds. Headline metric that moves: recovery efficiency eta = T*_recover/T_recover where T_recover = ticks until spawn_energy==capacity sustained AND source saturation >=95% (and eta_RCL = T*_rcl_n/T_rcl_n for rush scenarios), T* = the analytic energy-throughput lower bound; aggregated as H = Sum(W*eta)/Sum(W) with W = gross e/t x T*, through stats.rs seeded-bootstrap CI verbatim. Diagnostic metrics: E_repair_stress/E_total (energy spent on repair while spawn_energy<capacity — expected to drop to ~0 with the market on) and deposit-bucket transition count per room per 100 ticks (the oscillation watch: only if this correlates with realized eta loss does any damping get considered, per the no-preemptive-hysteresis directive). Constants calibrated by the tuning.rs coordinate-descent tournament over {MARGINAL_HARVEST_ROI, bucket thresholds, build-bid table} against pooled H, with the 0033 gates-first ranked_key idiom and the determinism fence (spread 0 over 5 rounds) underneath."
}