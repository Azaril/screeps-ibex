{
  "name": "Per-Tick Refill Precedence (PTRP): tier-faithful delivery + stress-gated repair, one pure kernel, zero new serialized state",
  "overview": "Refill priority becomes binding, not nominal: spawns/extensions already register at TransferPriority::High, but the carried-cargo delivery path flattens the priority mask and picks by distance, and drive-by repair burns spawn-bound energy with no room-energy check. PTRP (1) makes the deliver-current-cargo path iterate priority tiers High→Medium→Low like select_pickup_and_delivery already does, and (2) raises the effective minimum repair priority to Critical whenever a pure per-tick kernel says the room has an unmet refill deficit and no stored-energy buffer. No new modes, no latching, no new enum tiers, no serialized-shape change — every decision is recomputed each tick from EconomySnapshot fields that already exist, mirroring the one sanctioned precedent (controller-link deposit priority as a function of fill, room_transfer.rs:56-93).",
  "mechanism": "FOUR concrete changes, all read-side/selection-side.\n\n(A) New pure kernel `screeps-ibex/src/energy_stress.rs` (precedent: `room_economics.rs` — \"functionally PURE, no game::* reads\"). Two functions, integer math only (quantize-before-discrete-decision doctrine, no floats reaching a branch):\n- `refill_deficit_q(energy_available: u32, energy_capacity: u32) -> u32` = `1000 - (energy_available*1000)/energy_capacity` (per-mille deficit; 0 = extensions/spawns full).\n- `repair_allowance(deficit_q: u32, stored_energy: u32) -> RepairAllowance { Unrestricted, CriticalOnly }`:\n  `Unrestricted` iff `stored_energy >= REPAIR_UNRESTRICTED_STORED_ENERGY (10_000)` OR `deficit_q <= REPAIR_UNRESTRICTED_MAX_DEFICIT_Q (100 = 10%)`; else `CriticalOnly`.\n  The 10k floor deliberately reuses the RENEW_MIN_ROOM_ENERGY=10_000 precedent (spawnsystem.rs:20); both constants are named per EP-4.6 and calibrated by the sim tournament, not asserted. Inputs come from `RoomEconomyData` (military/economy.rs:27-51: `spawn_energy`, `spawn_energy_capacity`, `stored_energy`), which `EconomyAssessmentSystem` already rebuilds every tick at game_loop.rs:82 before all missions/jobs — no new system, no new Resource; add a `RoomEconomyData::repair_allowance(&self)` convenience method. THIS IS THE ESCALATION FUNCTION: as energy_available/energy_capacity drops below 90% with no stored buffer, every repair sink in the room simultaneously drops out of contention (effective min priority jumps to Critical), which is equivalent to refill escalating past them — realized without adding a tier above High, because TransferPriority variants ride serialized inside in-flight HaulState tickets (TransferWithdrawTicket/TransferDepositTicket via JobData ConvertSaveload, game_loop.rs:494-512) and adding a variant would force a WFV bump.\n\n(B) Tier-faithful delivery for carried cargo (the single highest-leverage change — makes the EXISTING High on spawns/extensions actually win). `TransferQueue::select_deliveries` (transfer/transfersystem.rs:1674-1710) and its consumer `get_new_delivery_current_resources_state` (jobs/utility/haulbehavior.rs:129-179) currently collect all ACTIVE-mask deposits FLAT and pick nearest — a hauler beside a Medium lab (missions/labs.rs:431-439) or Low controller container delivers there while spawns sit empty across the room. Change: iterate masks [HIGH, MEDIUM, LOW] in order (same tier discipline generate_active_priorities at transfer/utility.rs:34-98 already imposes on the new-pickup path, select_pickup_and_delivery transfersystem.rs:2154-2188); take the first tier with any candidate; within the tier keep the existing nearest-wins scoring. Deterministic tie-break on equal range: order by `(linear_range, target_pos.packed(), TransferTarget discriminant)` — position-stable, no HashMap iteration order reaches the decision (EP-6.13). Same tier-ordering applied to the HaulState::Idle step-1 call site (jobs/haul.rs:86-95). Graceful degradation is structural: a full spawn/extension registers amount = free capacity = 0, so when the room is healthy the High tier is empty and selection falls through to exactly today's behavior.\n\n(C) Stress-gated repair via ONE primitive + the shared selectors. Thread `RepairAllowance` as an effective-minimum raise: under `CriticalOnly`, the caller's minimum priority is `max(caller_min, RepairPriority::Critical)`. Apply inside `tick_opportunistic_repair` (jobs/utility/repairbehavior.rs:111-171) and `select_repair_structure[_and_priority]`/`select_repair_structure_in_range` (jobs/utility/repair.rs:162-232). That one seam covers every leak role from recon: harvester drive-by repair before harvest and during High spawn deliveries (jobs/harvest.rs:225, 264-266 — the direct spawn-ticket drain via consume_resource_from_deposits), harvester Idle full-Repair state (harvest.rs:177-185), remote-hauler Delivery/MoveToRoom repair (jobs/haul.rs:229-248), static miner (jobs/staticmine.rs:144), and BuildJob's repair-before-build-before-pickup arms (jobs/build.rs:56-104) — note Critical still passes, so a spawn/container below 50% (map_high_value_priority, repair.rs:39-53) is still repaired mid-collapse, which is correct. Plumbing: add `economy: Read<'a, EconomySnapshot>` to `JobSystemData` (jobs/jobsystem.rs:17-34) and `&EconomySnapshot` to `JobExecutionSystemData` (jobsystem.rs:36-42), the exact pattern `repair_queue: Read<RepairQueue>` already uses (jobsystem.rs:29→41); jobs resolve room Entity via `EntityMappingData::get_room` as tick_opportunistic_repair already does (repairbehavior.rs:123-124). Systems run sequentially (run_now + maintain, game_loop.rs:188-211) so Read-in-jobs vs Write-in-missions cannot conflict.\n\n(D) Two mission-side consumers of the same allowance (missions already receive `economy: Write<EconomySnapshot>`, missionsystem.rs:45): (1) `LocalBuildMission::get_repairer_priority` (localbuild.rs:112-122) returns no repair-driven builder request under CriticalOnly unless the best target is Critical — kills the perverse collapse loop where decayed roads (<25% → High, repair.rs:23-37) trigger a SPAWN_PRIORITY_HIGH repairer body that competes for the exact spawn energy being hoarded; construction-driven builder count and all bootstrap-first-creep paths (source_mining.rs:394, localbuild.rs:255, haul.rs:229, upgrade.rs:292) are untouched. (2) `TowerMission` peaceful repair (missions/tower.rs:426-453) raises its min to Critical under CriticalOnly (10 energy/shot on roads plus induced refill hauling); tower deposit priority stays as-is (High under hostiles = never-shed defense per EP-4.3; peaceful Low now correctly loses to High refills via change B).\n\nExplicit non-changes (scope discipline, fewest moving parts): no deposit-registration site changes priority (after B, Medium/Low sinks are only served when no High demand exists — the per-tick optimum, no demotion mechanism needed); secondary-delivery chaining to storage (haulbehavior.rs:248-364) unchanged (energy in storage is not lost); builder/upgrader energy PICKUP is not gated (post-wipe extension construction IS the compounding investment; the head-of-line spawn banking at spawnsystem.rs:428-436 already reserves structure-side energy); the capacity-sized-replacement-body stall (todo.md:18) is a separate defect for a sibling slice.\n\nKill-switch (see state_changes): flag off ⇒ select_deliveries uses the flat path and repair_allowance returns Unrestricted unconditionally ⇒ bit-identical to today (EP-2.2 parity landing, flip after sim verification).",
  "state_changes": "NEW: `energy_stress.rs` pure module (two fns + two named constants + RepairAllowance enum — all compile-time, nothing stored). CHANGED (ephemeral only): `RoomEconomyData` gains a derived `repair_allowance()` method over its existing fields (EconomySnapshot is explicitly \"rebuilt each tick, not serialized\", economy.rs:58 — no serialization impact); `JobSystemData`/`JobExecutionSystemData` gain a `Read<EconomySnapshot>` (SystemData is never serialized). NEW FEATURE FLAGS: `features.energy.tiered_delivery: bool` and `features.energy.repair_stress_gate: bool` in a new `EnergyFeatures` group in features.rs, `#[serde(default)]` additive — Memory._features round-trip is self-healing (features.rs:791-813), no migration. DELIBERATELY NOT TOUCHED: `TransferPriority` enum (no new variant — variants are serialized inside in-flight HaulState tickets via JobData ConvertSaveload, game_loop.rs:501 + transfersystem.rs:1029-1034); `HaulJobContext`/`HaulState` shapes; `RoomData`; all four priority queues remain ephemeral per-tick Resources. Zero bytes of new persisted state.",
  "pros": [
    "Attacks the root inconsistency rather than adding a compensator: the carried-cargo path becomes tier-consistent with select_pickup_and_delivery, so the change is closer to a bug fix than a feature — the system gets MORE uniform, not more special-cased (the operator's simplicity ask)",
    "Zero new serialized state and zero WFV impact by construction — the design explicitly routes around the TransferPriority-variant trap the recon flagged",
    "Fully compliant with the per-tick-optimal directive: no latched emergency mode, no hysteresis; every gate is a pure function of this tick's EconomySnapshot, recomputed after the queue-clear/rebuild cycle that already exists (transfersystem.rs:2459, repairqueue clear at game_loop.rs:84)",
    "One primitive seam (tick_opportunistic_repair + the two shared repair selectors) closes all five leak roles at once — harvester, home hauler, remote hauler, static miner, builder — plus towers and the repairer-spawn loop via two mission reads; ~6 files touched total",
    "Healthy-room degradation is structural, not conditional: full spawns register zero-amount deposits (empty High tier) and either allowance branch returns Unrestricted, so flag-on behavior converges to current behavior without any 'mode exit' logic",
    "Pure kernel + integer quantization is host-unit-testable and sim-consumable, and both feature flags allow independent rollback (tiered delivery vs repair gate)",
    "Recovers harvest throughput too: under stress, the Pipeline-A repair-before-harvest skip (jobs/actions.rs:28-31, harvest.rs:225) stops firing, so WORK-heavy bootstrap creeps stop forfeiting 2 e/WORK/tick harvest ticks to road repair"
  ],
  "cons": [
    "The two allowance constants (10k stored floor, 10% deficit slack) are policy numbers needing sim calibration (EP-4.6); a wrong slack value either re-opens the leak or starves road upkeep in busy-but-healthy rooms whose extensions cycle below 90% during spawn bursts",
    "Strict tier ordering is distance-blind ACROSS tiers: a hauler can cross the room for a 50-energy extension while an adjacent Medium lab waits — bounded (High refill demand caps at 12.9k/room) and per-tick correct, but movement-inefficient in edge cases the sim must quantify",
    "Binary allowance can flip tick-to-tick near the threshold (repairs one tick, gated the next) — harmless because repair is a per-tick action with no committed work to tear down, and per the standing directive we add no damping unless oscillation is actually observed and harmful",
    "Does not arbitrate the withdraw-side race: builders/upgraders still pull from the same containers/storage at ALL priorities (jobs/build.rs:84-93, upgrade.rs:112-122) that haulers need for refill supply — mitigated by head-of-line spawn banking but not eliminated (that is Angle-2/3 budget-arbiter territory)",
    "Does not fix the capacity-sized replacement-body head-of-line stall after a wipe (todo.md:18, source_mining.rs:397) — PTRP makes energy ARRIVE at spawns faster but a 1800-cost miner request can still bank it; needs a sibling slice",
    "The tier-ordered selection rule lives in the JS-coupled transfersystem, so the offline sim either consumes the bot crate host-side or pins a spawn_throughput-style citation-pinned mirror — mirror drift is a real (if precedented) risk"
  ],
  "wfv_impact": "None — no WORLD_FORMAT_VERSION bump. The serialized tuple (game_loop.rs:494-512, fingerprinted by WORLD_FORMAT_VERSION=23 at game_loop.rs:679) is untouched: EconomySnapshot is ephemeral by charter (economy.rs:58), the four queues are rebuilt per tick and never serialized, feature flags are serde-default additive in Memory._features (outside the bincode tuple), and the design deliberately does NOT add a TransferPriority variant or any field to HaulJobContext/HaulState/RoomData precisely because those DO ride the tuple (tickets embed TransferPriority, transfersystem.rs:934-938/1029-1034; JobData at game_loop.rs:501). All behavior change is in per-tick recomputed selection/gating logic. Breaking-change label per EP-5.2: Behavioral (flag-gated), not Memory-format.",
  "cpu_notes": "Net ~zero, likely slightly negative (saves CPU). Kernel = a handful of integer ops per owned room per tick, computed from fields EconomyAssessmentSystem already gathers at game_loop.rs:82. Tier-ordered select_deliveries iterates the same candidate set as today, just grouped by 3 masks — same asymptotics, small constant; the tie-break sort is over an already-small per-room candidate list. One extra Read<> in JobSystemData is free. No pathfinding changes (pricing/policy only, per the pathfinding directive). Intent-budget upside: under stress the bot emits FEWER intents (suppressed repair intents at 0.2 CPU each across every WORK creep near a decayed road, fewer tower repair actions, no extra repairer spawn), and harvesters stop wasting Pipeline-A ticks. All touched systems keep their existing StageClass; nothing new enters the shed set (repair gating feeds haul/spawn, the never-shed lane, EP-4.3).",
  "sim_evaluation_hook": "The planned econ overlay (EconWorld over screeps-sim-core per the sim-infra recon, ADR-0033 pattern) A/Bs flag-off vs flag-on on a bootstrap-collapse scenario family: initial state = one spawn + planned extensions + roads pre-decayed to 40% hits (Medium repair priority, the observed leak class) + stored_energy 0, layouts from the 13 captured foreman rooms (captured-layouts.json, with the ~5-line required_rcl capture extension), scripted fleet running the same PTRP kernel the bot uses (energy_stress.rs is pure/JS-free and the bot crate already compiles host-side; the tiny tier-selection rule gets a citation-pinned mirror a la spawn_throughput.rs if crate-graph friction appears). Headline metric that moves: T_recover = ticks from crash-state until energy_available == energy_capacity_available is first sustained, scored as eta = T*_bound / T_recover where T*_bound is the source-throughput lower bound (10 e/t/source feeding refill demand, room_net_roi-style accounting), aggregated H = sum(W*eta)/sum(W) with stats.rs seeded-bootstrap CI, 0-scored on non-recovery within tick_cap so the catastrophic tail stays visible; secondary metric time-to-RCL-2/3 for the RCL-rush ask. Expected direction: flag-on strictly raises H on the collapse family (energy leaked to roads is the dominant modeled loss). Guard family: healthy-room steady-state scenarios must show flag-on == flag-off within CI (parity gate, EP-2.2) plus a road-health floor metric to prove upkeep is not starved when rich. Constants tournament: coordinate-descent sweep over (stored floor, deficit slack) using the tuning.rs idiom, gates-first ranked_key, determinism fence spread-0 underneath."
}