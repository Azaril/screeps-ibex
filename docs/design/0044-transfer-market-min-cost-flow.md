# ADR 0044 — The transfer market is a min-cost flow: the reduced-cost bid (V·U − source_floor − haul)

- **Status:** Accepted (2026-07-08, William Archbell) — design of record from the transfer-EV deep-dive (11-agent workflow: map + web research on other bots/OR theory/Screeps mechanics + multi-lens design + adversarial verify). Implementation phased below; the sim-experiment plan is a **hard prerequisite** for validating the remote-room fix (the sim is single-room today). Supersedes the interim `.min(refill_roi_cap)` hard cap (ADR 0043 A1 stopgap).
- **Date:** 2026-07-08
- **Deciders:** William Archbell
- **Related:** ADR 0040 (the e/t currency + transfer market + `market_pass`/`matching`), ADR 0038 (`room_net_roi` haul coefficient), ADR 0043 (the band-normalization ledger — this is the principled foundation A1 was reaching for); memory [[sim-determinism-fence]], [[no-one-off-pathfinding-algorithms]], [[prefer-per-tick-optimal-over-hysteresis]].
- **One line:** The transfer (hauling) market is the decentralized solution to a **min-cost flow**: each sink prices energy at its own marginal downstream value (a shadow price `V·U`), storage-at-par is the numeraire/outside option, and each delivery is chosen by its **reduced cost** `bid − source_floor − haul(d)`. Haul is BOTH an **accept/reject subtraction** (is this arc profitable at all? — the piece missing today) AND, separately, the `service_ticks` **divisor** (which scarce carrier-tick to spend — already present). This is exactly the operator's `V·U − haul`; "maximize units-in-flight" is refuted (a diagnostic, not a maximand) and "minimize latency" is refined (weighted by urgency U).

## Context — what the market optimizes today, and the gap

`matching.rs` scores each carrier→sink arc by **value-density** `v = bid·amount / service_ticks` and greedily takes the highest-density feasible arc. That already IS `V·U/haul`, folded: V (downstream value) = the sink bid (refill inherits body-ROI, build/repair/upgrade inherit their completion value), U (urgency) is baked into the bid (deficit-scaled refill floor, repair imminence), and haul is the `service_ticks` **divisor**.

The gap the operator flagged (remote rooms): **haul is only ever a divisor, which can never make an arc net-negative** — so an EV-negative remote haul is still assigned if nothing better exists, and the ADR 0043 A1 `.min(refill_roi_cap)` cap **flattens** every starved lane (home and remote) to one indistinguishable number. The min-cost-flow theory (LP duality), the field (Overmind's `dq/dt` + net-of-haul remote curves, storage-as-hub consensus), and Screeps mechanics (remote energy is net-of-distance with an empirical break-even ~+8.7 e/t at the door, ~0 past ~250 tiles) all agree: a beyond-break-even remote delivery must be **rejectable**, not merely deprioritized. That requires a haul **subtraction**.

## Decision — the corrected two-stage reduced-cost model

All bids milli-e/t, par = `STORAGE_BID` = `BID_SCALE` = 1000. **Sink prices stay pure `V·U` shadow prices** (one price serves near AND far carriers); the per-arc haul term is applied at **selection**, not baked into the kernel. Survival vetoes (downgrade clock, tower-under-attack, container<50%) stay **outside** the market.

### Stage 1 — admission (the new piece)
Keep an arc `source → sink` (distance `d`) iff:
```
delivered = bid_sink − source_floor − haul_milli(d)  > 0
```
else **decline** (leave the energy at the source). This is the reduced-cost / par-after-haul reject floor the divisor form lacks.

### Stage 2 — allocation (unchanged)
Among admitted arcs, maximize the **existing** density `Σ bid_sink·flow / service_ticks` (distance in the divisor only). The subtraction gates accept/reject + remote ordering; the divisor allocates scarce carrier-ticks. **Never write these as one divided expression** — that double-charges distance.

### The pieces (with the adversarial fixes baked in)

- **`haul_milli(d)` — an INTEGER kernel** in `sink_economics.rs` (NOT a reuse of the f64 `room_net_roi` function — that would put a float in the exact-rational ordering and lives in the wrong crate; **determinism blocker**). Transcribe only the coefficient: `haul_milli(d) = (2·d·(CARRY_COST+MOVE_COST)·BID_SCALE) / (CARRY_CAPACITY·CREEP_LIFE_TIME)` in saturating u32 ≈ `round(d·2.667)` for a plain 1:1 body, times an integer **road factor** `road_q/1000` (seed 500 for roaded lanes; sweep). `d` = the **structural source→sink leg** (`pickup.pos.get_range_to(sink.pos)` — market.rs:231's second term), NOT the carrier-approach leg the divisor uses and NOT the composite `service_ticks`. Gate at **pickup-commit** (empty-hauler edges) where `d` is well-defined and declining genuinely leaves energy banked; do **not** re-gate already-loaded cargo (declining strands it). No new pathfinding (reuse the Position range — [[no-one-off-pathfinding-algorithms]]).

- **`source_floor` — the outside option, source-dependent** (the "decline is free" premise fails for a saturating source): **par (1000)** when the pickup source is lossless (storage/terminal); **~0** when it is a saturating buffer (source container above a fill threshold, or dropped energy — declining there means overflow/decay, not lossless hold). This is the newsvendor/base-stock treatment applied source-side and it dissolves the remote-chronic-starvation degenerate case.

- **NO `room_net_roi` multiplicative tilt.** It is redundant — a structurally-poor remote already prices low via `haul(d)` AND via its own lower sink bids (a low-throughput remote spawns cheaper bodies → lower refill ROI). Adding the tilt double-counts distance and reintroduces the float-in-ordering blocker. If a non-distance room-quality signal is later shown necessary, add it as an **integer per-mille** kernel, never the f64 function.

- **Refill — thread the exact inputs, retire the cap as a discriminator** (ADR 0043 A1 done right). Replace the live `refill_bid(&consts, None, deficit, 300).min(refill_roi_cap)` with: (a) `top_blocked_roi_milli` = `body_roi_milli` of the head-of-line unaffordable request (V = the ROI of the creep the energy *becomes*, bounded); (b) `next_body_cost_e` = the **exact cheapest-queued-body** cost (from the spawn queue). With the true (larger) denominator the derived floor is **naturally sub-cap**; the bid = `clamp(top_blocked_roi, floor, cap)` is bounded-by-V. The `refill_roi_cap` **stays** only as an inert **degenerate-body guard** (a 1-part body bidding a near-∞ ROI), paired with a `body_cost` floor / `w` clamp inside `body_roi_milli` so the degenerate infinity is bounded at the source. **MUST ship atomically:** dropping the call-site `.min()` *without* fixing `next_body_cost` lets a big home lane bid ~40×par and starve everything — keep the `300` fallback + `.min()` until the queue is threaded.

- **Remote sinks:** the SAME sink-price formulas, and the per-arc `haul_milli(d)` subtraction is the sole remote discriminator — a high-V remote refill now correctly outbids a mid-V near sink *when genuinely worth the haul*, and a beyond-break-even one is DECLINED, not flattened to the cap.

### Keep unchanged (all confirmed theory-correct against the code)
`instant_spawnability_premium` (base-stock underage U), `buffer_deposit_bid = base·(free/cap)²` (the (s,S) holding discount; overage ≈ 0 in-structure → correctly biases buffer-ahead for spawn bursts), `imminence_q`/`repair_bid` (V·U for repair), `upgrade_bid` + `V_UPGRADE` with its `CONTROLLER_MAX_UPGRADE_PER_TICK` saturation, `opportunity_floor`/`admit_use_withdraw` (the VCG/complementary-slackness outside option gating Use-lane withdraws), all survival VETOES outside the market, and the **exact-rational `u128` greedy** (no float reaches an ordering).

## Verdicts on the operator's candidate framings (from the research)
- **`V·U − haul`: CONFIRMED** — it is precisely the per-arc reduced cost of the flow objective (LP duality), and mirrors Overmind's proven split of sink `multiplier` from the `dq/dt` matcher.
- **"Maximize units-in-flight": REFUTED** — Little's Law `L = λW` is an identity, not an objective; at fixed conversion throughput, more in-flight energy means more latency + holding cost. It is a **diagnostic** (rising haul backlog ⇒ W rising ⇒ sinks starved), never a maximand.
- **"Minimize mined→used latency": REFINED** — latency matters only *weighted by value-at-risk*, which is exactly what U (stockout-imminence) already encodes. A global minimize-W over-hauls cheap far deliveries and burns CPU.

## The sim-experiment plan (hard prerequisite: the sim is single-room today)

`screeps-econ-eval` is single-room (`layout.room` one RoomName; the remote arm is inert at `max_distance==0`), so **the remote-flattening hypothesis cannot be measured on the current corpus.** Add first:
- **Family R** — one home + N remotes at real path-distances `d ∈ {10,40,90,150,210,260}` (straddling the ~200–250 break-even), each with its own source(s), so `service_ticks` and `haul_milli(d)` actually vary.
- **Instruments (diagnostics):** (B) mined→used energy dwell (tag at harvest, record at consume) mean/p95; (C) units-in-flight + carrier-utilization; (D) a realized-haul-cost ledger (carrier move-ticks × upkeep — "net of haul" MEASURED); (E) wasted/idle/re-hauled energy.
- **One shared `arc_admitted()` filter** applied in BOTH `matching.rs` `greedy_assign` AND `econ-eval` `oracle_best_fp` (drop `delivered ≤ 0` edges), then rank survivors by the same density — otherwise `match_optimality_gap` measures the greedy failing to optimize an objective it isn't running (an artifact).

**Arms** (new OBJECTIVE axis): A0 band-priced baseline · A1 capped-rate (current: refill EV + flat cap + haul-as-divisor) · A2 reduced-cost (exact `next_body_cost`+`top_blocked_roi`, cap demoted, per-arc haul subtraction, source-floor) · A3 = A2 + all-sinks-EV (containers/controller/build/repair live) · sensitivities: subtraction-only vs divisor-only; source-floor par-everywhere vs container→0.

**Measure** — economy: `T_recover` (η), `T_RCL(N)`, `H`, `extension_deficit_integral`, deficit-episode p05–p95, `spawn_idle_frac`, `repair_leak_e`, income e/t. Transfer optimality: `match_optimality_gap` (permille, vs the reduced-cost oracle), mined→used latency, in-flight/utilization, realized-haul net-of-value, wasted energy, flap.

**Success gate:** on Family R, remote high-ROI refills SERVED when profitable and DECLINED past break-even (zero realized `delivered<0`; A1 shows EV-negative remote hauls); remote deficit-episode p95 drops; **no** single-room `T_recover`/`T_RCL`/`H`/`repair_leak_e` regression (bootstrap CI); gap within the greedy budget under the shared-objective oracle; determinism fence green throughout. If A2 over-hauls at scale (D shows realized haul-e rising faster than delivered value) → add a CPU/fleet-size cap and re-run.

## The six adversarial fixes (naive synthesis → shipped model)
1. **BLOCKER** — `room_net_roi` float tilt in the ordering → **drop the tilt** (haul subtraction already prices far/poor remotes; integer per-mille kernel only if ever needed).
2. **MAJOR** — `d` must be the source→sink structural leg, gated at pickup-commit (not the carrier-approach leg the divisor uses; don't gate loaded cargo).
3. **MAJOR** — `source_floor` = par for lossless storage, ~0 for a saturating buffer ("decline is free" fails for a filling source container → overflow/decay).
4. **MAJOR** — one shared `arc_admitted()` in BOTH greedy and oracle, or `match_optimality_gap` is an artifact.
5. **MINOR** — thread `next_body_cost` + drop `.min()` **atomically**; keep both until then (load-bearing).
6. **MINOR** — state the objective as two stages, not one divided expression (else distance is double-charged).

## Phasing
- **P0 (kernel + core, unit-testable, no WFV):** `haul_milli(d)` integer kernel + `source_floor` + the two-stage `arc_admitted()` in `matching.rs` + the `body_cost`/`w` clamp in `body_roi_milli`. Drop the `room_net_roi` tilt from consideration.
- **P1 (refill exact inputs) — SHIPPED 2026-07-08.** Two parts, both landed:
  - **Kernel deficit-bound** (`refill_bid`): the `instant_spawnability_premium` floor now uses `min(lane_deficit, next_body_cost)` as its deficit input, capping the floor at `[par, 2·par]` (filling the *next* body is worth up to 2×par; energy beyond is buffer, priced by `buffer_deposit_bid`). This is what killed the ~40×-par deep-lane explosion (RCL8 empty lane) at the source and let the external `.min(cap)` retire — the internal cap is now the sole ceiling and `top_blocked_roi` is the true driver. *(Found reading the kernel: the naive "thread the exact `next_body_cost`" framing was wrong — the floor is a **lower** clamp bound, so on a deep lane it explodes regardless of how exactly `next_body_cost` is measured; the deficit itself had to be bounded.)*
  - **Live exact inputs — no-lag ordering solution.** `SpawnRequest.priority` already IS the body ROI in the shared `BID_SCALE` currency (M5b), so `top_blocked_roi` = the head-of-line queued bid — no `w` reconstruction. Threading it to the flush-time refill generator (which can only read `room_data`, never the `SpawnQueue` resource) is purely a **system-ordering** problem, not an inherent one-tick lag: the canonical generator flush is `build_econ_snapshot` at the top of `RunJobSystem`, which runs *after* `RunMissionSystem` + `SquadManagerSystem` have registered every spawn. A dedicated **`SpawnRefillPricingSystem`** sits between the last spawn producer and `RunJobSystem`; it **reads** the now-complete `SpawnQueue` and **writes** a *separate* `RefillPricingCache` resource (`RefillPricingContext { next_body_cost_e = cheapest queued body, top_blocked_roi_milli = head-of-line bid }`) — so the queue stays a pure producer/consumer (never mutated for pricing) and the published data can never be stale queue state. The refill generator captures the room's `Rc` cell from that cache at registration (the cache is the required bridge — the generator can't read resources — mirroring the `supply_structure_cache` precedent); refill is *not* separable into a demand-registering system because `execute_demands` prices it as one target-type inside the room's full lazy haul-demand scan. Same tick, fully fresh. No WFV (cache + cells are transient). The `.min(cap)` and the `REFILL_NEXT_BODY_COST_FALLBACK_E=300` stopgap are deleted.
  - Still sim-validate in P2 (arm A2 vs A1) that the bounded floor + real head-of-line ROI beats the old cap-pinned behavior on `T_recover`/spawn-idle.
- **P2 (sim + experiments):** Family R + instruments B–E + the shared `arc_admitted()` in the oracle; run arms A0–A3 + sensitivities; validate the success gate. This is the gate for the remote fix.
  - **TRUE-DISTANCE MIGRATION (audit finding, 2026-07-08 — a required P2 prerequisite).** The shared market kernel (`screeps-econ-decision/src/market.rs`, used by BOTH sim and live) prices `service_ticks` AND the ADR 0044 haul `d` as **Chebyshev `get_range_to`** (a deliberate "linear-range stand-in," `market.rs:16`/`baseline.rs:592`), not true path distance. `Position::get_range_to` returns a straight-line *global-coordinate* Chebyshev distance across rooms — it does NOT panic — so cross-room the market silently prices a remote at (e.g.) `d≈40` when the true routed path is `90+`. `haul_milli(d)` then underprices the far haul and the admission **serves remotes that are actually beyond break-even** — exactly the failure this ADR prevents. So the reduced-cost admission cannot be validated on remotes until `d` (and `service_ticks`) become TRUE path distance. Fix: the pure kernel must take distances (or a distance oracle) as INPUT rather than computing `get_range_to`; the adapter supplies rover-backed true `travel_ticks`. **This changes the live path too — and is a live improvement** (live currently underprices far hauls). Secondary approximations (mis-*rank* cross-room but not the core pricing): nearest-source/nearest-delivery by linear range (`runner.rs:1578`, `baseline.rs:413`); `spawn_policy` `room_manhattan_distance`/`max_distance` are coarse sizing heuristics (leave as room-count); repair/controller `<= 3` range gates are correct as Chebyshev adjacency.
  - **Distance caching + CPU benchmark (operator concern, 2026-07-08 — gate the true-distance migration on it).** Per-edge true pathfinding every tick would blow the live CPU budget, so the migration must be cheap-by-caching, and the frequency MUST be benchmarked before it ships. Design: (1) **the caching largely already exists — reuse it, don't reinvent.** `PathfinderService` (the single mission-side pathfinder) already has a TTL'd, CPU-pool-bounded (~20 CPU/tick) **cached inter-room route distance** (`travel_ticks(from_room, to_room)` / `route_distance`; "room exits are static"), degrading to "no path" under pressure; rover has `CostMatrixCache` (per-room matrices). (2) **Hybrid distance oracle:** same-room legs stay Chebyshev `get_range_to` (O(1), small error inside 50×50); cross-room legs = intra-room Chebyshev offsets + the CACHED room-level `travel_ticks` (the dominant term straight-line Chebyshev misses). No per-tile pathfinding in the hot path. (3) **Static-pair amortization:** the decision-critical distances are between STATIC structures (source containers/storage → spawn/extension sinks; remote source containers → home), so the route cache hits after first compute and only a road/structure change invalidates — steady-state pathfinds ≈ 0. (4) **Kernel change:** `market_pass` takes a `dist` oracle (closure/trait) instead of computing `get_range_to`; the live adapter backs it with `PathfinderService`, the sim adapter with `RoverMover::travel_ticks` (mirroring the caching so the arms measure like-for-like). (5) **Benchmark, as the ship gate:** instrument pathfinds/tick + cache hit-rate + CPU-pool usage (`PathfinderService::snapshot`); the migration ships only if steady-state stays within the pool. If it doesn't, coarsen — precompute a per-room static source→sink distance matrix on layout/road change (O(1) reads), longer route TTL. The `d` buckets the ADR measures ({10,40,90,150,210,260}) are coarse enough that room-level `travel_ticks` + intra-room Chebyshev is ample granularity.
  - **Multi-room mover — reuse the combat sim's rover-backed movement (recon-confirmed).** The shared primitives already exist in `screeps-sim-core` (`resolve_moves_via_system_with(movement_state, &[SimMoveRequest], cache, cost_source, config)` + `resolve_movement` edge-exit relocation) and `screeps-rover` (`PathfindingProvider`, `CostMatrixDataSource`), and combat-agent already drives multi-room movement through them (`CombatWorldCostSource`). Replace econ-eval's single-room `AnalyticMover` with a `RoverMover: Mover` that plans via rover + walks the shared kernel to produce the same trace contract, plus an `EconCostMatrixSource: CostMatrixDataSource` reading obstacles from `EconWorld` per room (mirrors `CombatWorldCostSource`). The `Mover` trait + `runner.rs` call sites stay unchanged. `EconWorld.movement` is already the shared multi-room `MovementState`.
  - **Multi-room approach (operator directive, 2026-07-08): FULL geometric multi-room, generalize-and-share — NOT a bespoke econ mover or an analytic distance-abstraction.** Reconnaissance verdict: `EconWorld` (in `screeps-econ-engine`) is already position-based and multi-room-capable, and the K1–K4 kernels + the engine resolver already handle cross-room `Position`s; the *only* blocker is the sim mover's single-room assertion (`screeps-econ-eval/src/movement.rs:125`). The combat simulation already does multi-room movement and `screeps-rover` owns pathfinding ([[no-one-off-pathfinding-algorithms]]), so the econ sim's `AnalyticMover` must be replaced by the SAME shared, rover-backed multi-room pathfinding the combat sim uses — lifting/generalizing whatever is common rather than reinventing a cross-room mover in econ-eval. Pathfinding changes land in rover. Arm design: the reduced-cost admission (`source_floor`/`haul` on the sim edges) and all-sinks-EV must be **config-gated** on new `MarketArmCfg` flags (default off) so the existing arms stay byte-identical; A0 = `baseline`, A1 = `market` (admission off), A2 = market + admission, A3 = A2 + all-sinks. The sim already prices refill via the shared `econ::refill_bid` with exact plan inputs (`refill_bid_from_plans`) — so P1's kernel deficit-bound already applies to both sim and live (they were at parity; P1 lifted live *up* to the sim).
- **P3 (all-sinks activation — ADR 0043 A1/A8):** route `buffer_deposit_bid`/`upgrade_bid`/`build_bid`/`repair_bid` to live registration so the whole economy is EV-priced, validated by A3.
- **Deferred / open:** haul-coefficient road-factor calibration against the empirical break-even curve; in-flight demand-netting (Overmind's discount-by-inbound) — adds a DTO field, only if per-tick greedy re-pricing proves insufficient; a CPU/fleet-size cap — only if instrument D shows over-hauling.
- **FOLLOW-UP — loaded-carrier deliver reachability (operator 2026-07-09, leave for now):** the empty-carrier pickup+deliver arc is reachability-gated by the `dist` oracle (`None` ⇒ skip), but a LOADED carrier choosing a sink prices its carrier→sink leg as dynamic Chebyshev (`matching`/kernel loaded path, `market.rs` `service = get_range_to(dpos)+1`), NOT through the oracle — so a loaded carrier could be assigned a genuinely-unreachable sink. Deliberate today (the approach leg moves every tick — intentionally not routed/cached), and the movement layer handles the unreachable assignment at runtime (creep can't path → re-decides). If it proves a problem, the clean fix is a STATIC per-sink reachability check (is the sink connected to the room's walkable graph — a cacheable property independent of carrier position), gating the loaded deliver the same way `None` gates the empty pickup. Not a correctness hole in practice (owned-room sinks are reachable); a completeness gap. Track.
- **REVISIT — the `RefillPricingCache` `Rc<RefCell<>>` bridge (operator flag, 2026-07-08):** the shared-cell mechanism (published by `SpawnRefillPricingSystem`, captured by the transfer refill generator) makes the cell's lifetime and read/write ownership non-obvious — the same objection that applies to the `supply_structure_cache` precedent it mirrors. It exists only because the lazy generator closure can read `room_data` but not a resource. Cleaner alternatives to evaluate when the transfer-generator architecture is next touched: (a) extend `TransferRequestSystemData` with an explicit `refill_context(room)` accessor backed by a plain (non-`Rc`) resource reference held in `TransferQueueGeneratorData` — explicit dataflow, no interior mutability, but threads the resource through the ~18 `TransferQueueGeneratorData` construction sites; (b) lift refill-demand registration out of the lazy generator into a system with direct `SpawnQueue` + `TransferQueue` access (blocked today because `execute_demands` prices refill as one target-type inside the room's full haul-demand scan — would need that scan split or shared). Not a correctness issue; a clarity/maintainability one. Track until the generator seam is refactored.

## Build progress + resume points (LIVING — update every increment; survives auto-compaction)

**Shipped to `master`:** ADR + P0 kernel (`7bb947f`) · P0-core admission (`2f5445c`) · P1 refill-from-live-queue (`57999c5`, `7e7750d`) · `Rc` revisit note (`42c3dd8`) · P2 direction/audit/caching (`4b6c1f6`, `e23bdd8`, `ce2e7ce`). All wasm-building; econ-decision/spawn/transfer/econ-eval suites green.

**P2 STATUS (2026-07-09):** steps 1 (mover), 3 (Family R multi-room), 5 (arms + admission validation) DONE + committed + tested. The admission's DECLINE is proven unambiguously at the kernel (`market::admission_declines_far_par_sink`, `76d1c2c`/`b9956a2`); the in-sim economic decline needs a saturated/par-sink variant (deferred, pairs with realistic terrain). Remaining: step 4 (instruments), the saturated Family R variant + economic-decline runs, step 2 (true-distance, pairs with realistic terrain), realistic terrain gen, foreman-plans validation. Commits: `ee782eb` `e541e55` `3f11dc4` `76d1c2c` `b9956a2`.

**P2 build order + status:**
1. **RoverMover + EconCostMatrixSource** (econ-eval) — ✅ DONE (`ee782eb`). `RoverMover` in `movement.rs` (routes via `screeps_rover::LocalPathfinder::search` + multi-room `walk_trace_multi`; `room_cost_matrix` bakes terrain; `step_direction` cross-room; memo; `Mover::invalidate_from(&MovementState)`). 2 tests (single-room reachability + cross-room border trace) + full econ-eval suite green (46). NOT yet wired into the runner — that happens with Family R (needs `Box<dyn Mover>` selection at `runner.rs:256` + a multi-room scenario). No `EconCostMatrixSource`/`CostMatrixDataSource` needed (search used directly).
   - **Impl decided:** RoverMover = `screeps_rover::LocalPathfinder::search(...)` (CONFIRMED genuine multi-room A* over room-qualified Positions, offline/no-JS, returns full `Vec<Position>` — `local_pathfinder.rs:438`) for the ROUTE, then a **multi-room-generalized `walk_trace`** (current `walk_trace` at `movement.rs:89` uses single-room `MovementState{terrain,..}`; generalize to a `rooms` map with edges PASSABLE so `resolve_movement`'s edge-exit relocation fires) for the fatigue trace. Swaps `screeps_rover_eval::oracle::optimal_path` (single-room, `room_grid_dijkstra`) → `LocalPathfinder::search`. Keeps the `Mover` trait's trace/travel_ticks contract + memoization.
   - `search` signature: `search(origin, goal, range:u32, room_callback: &mut dyn FnMut(RoomName)->Option<LocalCostMatrix>, max_ops, plain_cost=2, swamp_cost=10) -> PathfindingResult{path,incomplete}`. CONFIRMED (`local_pathfinder.rs:12,176,609`): headless has NO `Terrain` — the caller **bakes terrain into the `LocalCostMatrix`**: 255=impassable (walls + blocker structures), a swamp cost for swamp tiles, 1 for roads, 0=plain default. So a per-room `LocalCostMatrix` builder reads `EconWorld.movement.rooms`/default terrain (walls/swamps) + `EconWorld.roads` (cost 1) + blocker structures (255). **Use `search` DIRECTLY (not the combat driver/`CostMatrixDataSource`)** — econ's analytic tier ignores contention by design (module docs `movement.rs:15`), so it needs rover's multi-room *pathfinding* but not the driver's traffic management. This still honours "pathfinding in rover" (reuses `LocalPathfinder`) with the smallest change. So `EconCostMatrixSource` is just a cached per-room `LocalCostMatrix` builder (no `CostMatrixDataSource` impl needed).
   - **Runner touch:** runner uses `&mut dyn Mover` (runner.rs:264/665/791/1446/1664). `MovementState` + `SimTerrain` are `Clone`; `SimTerrain{walls,swamps,roads}` + `fatigue_rate(x,y)` (road 1/plain 2/swamp 10 — terrain fully describes fatigue; cost matrix bakes walls=255/swamp=cost/road=1, terrain-only like `optimal_path`). **Keep `AnalyticMover` for single-room families C/D/G/S (no regression risk — `LocalPathfinder.search` may give different single-room paths than `optimal_path`); add `RoverMover` for Family R; select via `Box<dyn Mover>`** at `runner.rs:256`. Change trait `invalidate_from(&SimTerrain)` → `invalidate_from(&MovementState)` (multi-room terrain; AnalyticMover adapts via `.terrain`, no behavior change); runner call `runner.rs:330` → `mover.invalidate_from(&world.movement)`.
   - **RoverMover impl (in movement.rs, reuses `direction_between`/`body_class`/`TRACE_TICK_CAP`):** holds cloned `default_terrain` + `rooms: HashMap<RoomName,SimTerrain>` + lazy `matrices: HashMap<RoomName,LocalCostMatrix>` + `LocalPathfinder` + memo `HashMap<(Position,Position,u8,BodyClass,u32), Option<Rc<Vec<Position>>>>`. `trace`: in-range→empty; else destructure self for disjoint borrows, `pathfinder.search(from,to,range, &mut |room| build/cache matrix from terrain_for(room), max_ops, 2, 10)`; `incomplete`→None; then `walk_trace_multi` (like `walk_trace` at movement.rs:89 but `MovementState{terrain:default, rooms:clone, ..}` — edges PASSABLE so `resolve_movement` edge-exit fires; `direction_between` must handle cross-room adjacent tiles — verify). `travel_ticks` = trace len (the true distance the migration consumes). Matrix builder: `LocalCostMatrix::new()`, set swamp tiles→SWAMP cost, road tiles→1, wall tiles→255 (`cm.set(RoomXY, u8)`).
2. **True-distance migration** — ✅ DONE (sim) (`ab4230f`). Kernel `market_pass` takes a `dist` oracle for the pickup→sink (structural haul) leg (carrier→pickup approach stays cheap Chebyshev); SIM backs it with the multi-room mover (true routed distance, `routed ≫ Chebyshev` on realistic terrain). LIVE backs it with `get_range_to` for now — the cached `PathfinderService` oracle is the tracked follow-up (CPU benchmark; a backing choice, NOT a flag). **END STATE, NO FEATURE FLAGS (operator directive):** the reduced-cost admission (`source_floor` + haul subtraction) + true distance are ALWAYS on — removed the `admission`/`true_distance` toggles from `MarketArmCfg`, the `market` arm IS the end state, dropped the A/B arms. Parity holds (open fixture: mover-dist == Chebyshev). The sweep tunes the CONSTANTS around this end-state market.
   - **2b. LIVE full-tile true distance — ✅ DONE (`c73e4e5`), bring sim↔live together (operator-chosen 2026-07-09, "Full-tile, cached (exact)").** Shipped: `HaulDistance` trait (narrow seam in `transfer/market_adapter.rs`; transfer sees ONLY this — no rover types) threaded through `run_room_market`→`select_market_pickup_and_delivery`→`get_new_market_pickup_and_delivery_state`→`haul.rs` Idle. Live impl `pathing/hauldistance.rs`: `HaulDistanceService` resource (`(pickup,sink)`→routed-ticks memo, TTL 1500) + `RoverDistanceOracle`. On a miss it pathfinds via rover's engine-backed `ScreepsPathfinder` (terrain read NATIVELY — no wasm re-parse) with the STRUCTURE overlay from the SHARED `CostMatrixCache` (operator: "use the cost matrix cache, it's much more optimal") — static obstacle set (no creeps/sites) ⇒ cacheable; range 1; Chebyshev fallback on incomplete. Wired into `JobSystemData` (`WriteExpect<CostMatrixCache>` + `Write<HaulDistanceService>`); per-tick counters reset at the pass top, exported via `PathingMetrics.haul_dist_{computes,hits,cached_pairs}` (the CPU **ship-gate benchmark** — expect computes→~0 steady-state). NO WFV (transient resource + `#[serde(default)]` telemetry). Sim keeps its pure-Rust `LocalPathfinder`; both are rover `PathfindingProvider`s finding shortest walkable tile paths ⇒ same distance MODEL ⇒ tuning transfers.
   - **2b-unreachable. NO-PATH ⇒ NOT A TRANSFER OPTION (operator 2026-07-09: "if unreachable a creep can't deliver — shouldn't be a transfer option").** The `dist` oracle now returns `Option<u32>`; `None` = no path. The kernel SKIPS that (pickup, sink) arc instead of the old Chebyshev fallback (which fabricated a cheap straight-line price for an unservable haul — admitting deliveries a creep physically cannot make). Sim: `travel_ticks` returns `None` directly (no `unwrap_or_else`). Live: `HaulDistanceService` caches the `Option` (caching a `None` stops re-searching a doomed/too-long pair every tick — a 20k-op-exhausted search is `incomplete`⇒`None`⇒declined, which is correct since such a haul is beyond break-even anyway). Pinned by `market::unreachable_pickup_is_not_a_transfer_option` (None⇒declined, reachable⇒served). Boundary: the LOADED-carrier deliver leg still uses the dynamic Chebyshev carrier→sink approach (not the oracle), so it isn't reachability-gated — the movement layer handles a genuinely-unreachable assigned sink at runtime; revisit if it proves a problem.
   - **2b-fix. STRUCTURE-SINK range bug (operator caught 2026-07-09: "sim also needs to account for structures").** CORRECTION to an earlier wrong note: the sim IS structure-aware — `realize()` (`layout.rs:174`) folds every blocking structure (spawns/extensions/storage/towers/labs/links) from the REAL captured foreman layout into `terrain.walls`, RCL-staged (`included_at`), and inserts the home terrain into the multi-room `rooms` map (`scenario.rs:530`); roads→cost 1, containers walkable — well-aligned with live's cost matrix. BUT because a refill SINK tile is thus IMPASSABLE, the sim's `market_pass` priced it with `travel_ticks(pickup, sink, range=0)`, which is UNREACHABLE onto a wall ⇒ silent Chebyshev fallback ⇒ the DOMINANT refill haul was straight-line, defeating the migration on realistic terrain. Fix: range 1 (deliver-ADJACENT), matching the live oracle (which already used range 1). Pinned by `scenario::structure_sink_reachable_at_range_one_not_zero` (range 0 → `None`, range 1 → routed). Residual (accurate, second-order): sim doesn't fold ramparts (live's `get_structure_costs` may); container cost sim-plains(2) vs live(2) — equal. All suites green (bot 301 / econ-eval 52 / econ-decision 77), wasm builds. **NEXT: watch the live benchmark under load; run the constants sweep on the corrected model; consider a foreman-layout × RCL validation sweep (infra already exists — `realize` at any RCL over the 13 captured layouts).**
   - **2b (original design). LIVE full-tile true distance (operator-chosen 2026-07-09, "Full-tile, cached (exact)").** Rejected the room-level `travel_ticks` hybrid: `travel_ticks = hops×50` (`pathfinderservice.rs:89`, engine `find_route`) is a FLAT per-room cost with ZERO intra-room terrain, and `find_route` is JS-only (sim can't call it). On the realistic Family R the remotes sit in a LINE ⇒ `hops×50 ≈ straight Chebyshev`, so a room-hybrid would be ~Chebyshev and would NOT price the intra-room cave detour (`routed≫Chebyshev` WITHIN the corridor) — the whole realistic-terrain signal. Only full-tile rover pathing captures it. So **LIVE computes the pickup→sink haul distance with the SAME rover tile-A* as the sim mover** (`screeps_rover::LocalPathfinder::search`), wrapped in a **per-static-pair distance cache** (structures don't move ⇒ steady-state pathfinds ≈ 0; **benchmark pathfinds/tick + cache hit-rate as the ship gate**). Exact sim==live parity ⇒ tournament tuning transfers. Sim side already done (`ab4230f`).
   - **2c. COUPLING CONSTRAINT (operator 2026-07-09):** "make the systems available to each other AS LONG AS we maintain clean separation of responsibilities, encapsulation, loose coupling." ⇒ the transfer/market layer must depend on a NARROW `DistanceOracle` abstraction (a trait method like `haul_distance(from,to)->u32`), NOT on rover/`PathfinderService`/cost-matrix internals. The rover-backed cache is the concrete impl living in the pathing layer; it's injected into the market pass through a clean seam (extend `TransferRequestSystemData` with a distance accessor, OR pass `&mut dyn DistanceOracle`). No rover types leak into `transfer/`. (Awaiting the live-rover infra map — Explore agent — to pick the seam + which cost-matrix cache to reuse.)
3. **Family R scenario** — home + remotes at real distances `d ∈ {10,40,90,150,210,260}`. ✅ DONE (3a `e541e55`, 3b `3f11dc4`). Multi-room world builds; `run_family_r` runs it end-to-end through `RoverMover` (2000 ticks, no panic, creeps reach remote rooms — cross-room mining live). Initial harvesters round-robin across sources when remotes exist (single-room families byte-identical). `RunGoal::Horizon`.

**RE-ORDERING INSIGHT (2026-07-09):** on the synthetic STRAIGHT channel corridor, true routed distance ≈ Chebyshev `get_range_to` (`d=40→42`, `d=150→154`). So the admission (already wired in P0-core with Chebyshev `d`) is CORRECTLY priced on Family R *as-is* — the true-distance migration (step 2) only changes pricing where `routed ≫ Chebyshev`, i.e. REALISTIC terrain. **So step 2 pairs with the deferred realistic-terrain work; the admission (steps 4/5) can and should be validated on Family R NOW with Chebyshev `d`.** New order: **4 (instruments) + 5 (arms) → validate admission on Family R → then step 2 with realistic terrain.**
   - **3a DONE:** `FamilyRScenario`/`instantiate`/`remote_catalog` in `scenario.rs`; healthy home + synthetic remotes over a 3-wide walled CHANNEL corridor (+ carved 1-wide home east exit). Two RoverMover fixes: off-world rooms impassable (callback `None`); channel forces a straight highway (no diagonal room-corner crossings the walk can't follow). Test: instantiates multi-room + mover reaches remotes at ≈true distance (40→42, 150→154). NOTE synthetic-corridor terrain is a stopgap (realistic-terrain gen tracked as deferred).
   - **3b NEXT — wire + mine:** (a) runner `Box<dyn Mover>` — `RoverMover::new(&world.movement)` when `remote_distances` non-empty; add a `run_family_r`/`RunGoal` path (see `runner.rs:254` `run_scenario` + `RunOptions`). (b) Remote mining economics: the sim must assign harvesters to remote sources + haul remote container→home; `max_distance` must become `>0` (currently 0 gates out the ≥Low repair arm + remote-hauler sizing — `spawn_policy`/`runner.rs:813`). Verify harvest (`runner.rs:1578` nearest-by-linear-range) assigns remotes. This is where the actual remote-haul flow (the thing the admission prices) comes alive.
   - **Plan:** new `FamilyRScenario` struct + `instantiate_family_r` (mirrors `RushScenario`/`instantiate` at `scenario.rs:164`). Realize the HOME layout (a real captured layout at RCL≥4 w/ storage) via `realize`; then add **SYNTHETIC remotes** — for each target `d`, a `SimSource` + source `SimContainer` placed in a remote room over OPEN terrain (`SimTerrain::default`) at a position whose true routed distance from the home spawn ≈ `d` (room-distance×50 + intra-room offset; d≈10 = far corner of home room, d≈40 = adjacent room near border, …, d≈260 ≈ 5 rooms). Populate `world.movement.rooms[remote_room] = open terrain`. Return `(world, terrain, LayoutInfo)` — LayoutInfo likely needs `rooms`/remote metadata (extend or keep home `room` + a remote list).
   - **Runner wiring:** `runner.rs:256` → `Box<dyn Mover>`: `RoverMover::new(&world.movement)` when the scenario is multi-room (remotes present), else `AnalyticMover`. Pass `&mut *mover`.
   - **Remote mining integration (the big part):** the sim must actually mine remotes + haul home. K1–K4 are position-based (multi-room-ready per recon) BUT: harvest picks "nearest source by linear range" (`runner.rs:1578`) — remote sources need harvesters assigned; hauler sizing/repair gate on `max_distance` (`spawn_policy` / `runner.rs:813`, currently 0 = single-room) — must become `>0` for remotes so remote-hauler behavior + the ≥Low repair arm activate. Verify the harvest/haul FSM routes remote container→home via `RoverMover` (true cross-room distance).
   - Distances are the axis the true-distance migration (step 2) then prices; open remotes make `d` predictable (routed ≈ Chebyshev on open terrain, but the market currently uses Chebyshev so step 2's effect shows where routed≠Chebyshev — for the cleanest signal, add some remote-room walls so routed > Chebyshev, exposing the admission's mispricing under Chebyshev).
4. **Instruments B–E** — mined→used latency, in-flight/utilization, realized-haul-cost, wasted energy. ⬜ (next concrete step; additive metrics on the runner tick loop — recon mapped hooks at `runner.rs` steps 2.75/3/5/6).
5. **Arms A0–A3** — `MarketArmCfg` flags (`admission`, `all_sinks_ev`), default off. A0=baseline, A1=market(admission off), A2=+admission, A3=+all-sinks. ✅ 5a config-gate (`76d1c2c`) + 5b validation (`b9956a2`). `haul_road_q` threaded through `market_pass` (0 disables haul); `admission: bool` on `MarketArmCfg` (default false, reverts P0-core's inadvertent always-on; existing arms byte-identical); A2 arm `market-adm` added; live always plains. Admission decline proven at kernel; A1/A2 in-sim ~equal on the profitable corridor (correct). A3 (all_sinks_ev) is P3, still ⬜.
   - **Config-gate plan:** admission = `source_floor>0` AND `haul_cost>0`; disable via `source_floor=0` + `haul_road_q=0` (`haul_milli(d,0)=0`). Thread a `haul_road_q: u32` param through the shared `market_pass` (`screeps-econ-decision/src/market.rs:159`, replacing the hardcoded `HAUL_ROAD_Q_PLAINS_PERMILLE` at `:262`). Callers: 7 econ-decision self-tests (pass `PLAINS` = unchanged), econ-eval `market.rs:390` (pass `if cfg.admission {PLAINS} else {0}` + gate `MarketPickup.source_floor` on `cfg.admission`), live via `market_adapter` (from `transfersystem.rs:2013` `RoomMarketInput`) → pass `PLAINS` (live admission ON, shipped). Add `admission: bool` + `all_sinks_ev: bool` to `MarketArmCfg` **default false** — this ALSO reverts P0-core's inadvertent always-on admission in the existing sim "market" arm (restoring ADR-0040-tournament byte-identity). A1=existing market(admission false), A2=+admission true, A3=+all_sinks.
   - **⚠ VALIDATION FINDING (2026-07-09) — the decline needs the RIGHT scenario.** `haul_milli(d)=d·8/3` milli: `d=260→~693` (0.69 e/e). A hungry REFILL sink bids ~8000 (8×) ⇒ `delivered≫0` ⇒ every remote SERVED (correct — remote energy is a bargain vs an 8× refill; refill break-even d≈3000 tiles = never). The admission's DECLINE only bites for **par-value sinks** (storage rebalance bid 1000 → break-even d≈375) or where **routed≫Chebyshev** (realistic terrain). So the success-gate "beyond-break-even declined" must be measured on a **saturated-home / par-sink** Family R variant (home storage full so remote energy competes at par) OR under realistic terrain — NOT the hungry-refill corridor (which correctly serves all). Add a `saturated` Family R variant (skip the 200k storage stock, or fill it to cap so refill is satisfied and remotes price at par).
6. **Validate** — success gate + no single-room regression + CPU benchmark. ⬜

**FOREMAN-LAYOUT × RCL VALIDATION SWEEP — ✅ DONE (2026-07-09).** `tournament::foreman_rcl_sweep` + `foreman_rcl_sweep_full` (`--ignored --nocapture`): every captured foreman layout realized at each RCL stage it supports (13 layouts × RCL 1..full-build ≈ 104 runs), run to the 10k guard-rail horizon under the END-STATE market (structure-aware true routed distance + unreachable-arc exclusion). Health invariants: not deadlocked; roads not collapsed; at RCL≥4 the market GENERATES edges (refill/haul sinks reachable — the structure-reachability proof) and no permanent refill deficit. **Result: all healthy** — edges 2k–28k across the corpus, road 0.96–1.00, no deadlocks. One transitional flag (`E11N11-rcl3` permanent deficit) resolved as a threshold artifact: the pre-storage container economy (RCL 1–3) is inherently haul-tight (peers show equal/worse `defp95`), so the permanent-deficit check is gated to RCL≥4 (where stocked storage should always keep the spawn servable). Fast gated SMOKE (`foreman_layouts_healthy_across_rcls`, ~3s) guards against rot. This validates the structure-aware/unreachable-excluding market on the real-layout corpus before the constants sweep tunes against it.

**CONSTANTS SWEEP ON THE CORRECTED MODEL — ✅ RUN (2026-07-09).** `tune_market_constants` coordinate descent (fast corpus, 75s) over the end-state market (structure-aware true routed distance + unreachable exclusion). **Key result: the corrected model needs NO haul-constant retune** — every distance-related axis held its default (`build_bid_road=250`, `refill_roi_cap=10000`, `build_bid_extension=8000`, `v_upgrade=2000`). Baseline C/G/D H = 0.29/0.19/0.17; the incumbent (k4 bodies) already lifts to 0.40/0.196/0.18. The ONLY axis the descent moved was `imminence_horizon_ticks` 1500→750 — a repair-TIMING constant unrelated to haul distance, and a C↔D tradeoff (D 0.18→0.24, C 0.40→0.36, gates green). **Recommendation: do NOT adopt it now** — it's orthogonal to the distance work, a fast-corpus tradeoff (not full adjudication), and trades the primary recovery family (C) for the downgrade family (D). The sweep's real value here is the confirmation that the true-distance/structure-aware/unreachable changes did NOT destabilize the tuning — the shipped constants remain optimal. A dedicated full-corpus adjudication of `imminence_horizon` is a separate, optional follow-up.

**Deferred P2+ / future validation (operator notes, 2026-07-08/09):**
- **Foreman-generated room plans across RCL stages** — eventually run REAL foreman room plans at a variety of RCL stages through the economy + transfer sim, to prove the economy/transfer model on realistic layouts (not just the healthy-home + synthetic-remote corridor). A stronger end-to-end validation than the hand-built corpora.
- **Realistic terrain generation** — ⏳ generator DONE + econ wired; combat/pathfinding + foreman-RCL breadth remaining.
  - ✅ **Shared generator** `screeps_sim_core::terrain_gen` (submodule `9b5c99c`→`bf30cfe`, superproject `eab74c6`→`76da260`): cellular-automata cave walls + swamp + natural-cave connectivity, deterministic in seed, in sim-core so ALL sims share it.
  - ✅ **Seam alignment correct-by-construction** (operator catch): the kernel/engine relocates a creep across an exit tile WITHOUT a wall check (`sim-core tick.rs:53-76`), so seams MUST match or a creep lands on a wall. `generate_terrain_for_room(room, world_seed, connect, params)` derives each seam's exit range from a deterministic ORDER-INDEPENDENT function of the two rooms (`seam_range(id_a,id_b)`) — adjacent rooms' opposing exits match by construction, vary per seam, NO fix-up carving (mirrors the engine's global-continuous generation). `seam_range_between(a,b)` aligns a captured room to a generated neighbour. Tests: two independently-generated adjacent rooms share a matching seam tile-for-tile; Family R seam-invariant (every open edge tile has a walkable mirror).
  - ✅ **Econ wired** (`4604e63`, `76da260`): `FamilyRScenario::realistic()` — seam-derived cave corridor, `routed ≫ Chebyshev` proven. Synthetic channel kept as the fast default.
  - ✅ **Cross-sim wiring + deviation analysis (operator ask, 2026-07-09).** Wired the shared generator into all three sims + added `connected_open(terrain, start)` helper (`sim-core 224d990`) for placing on connected terrain. Findings per sim:
    - **PATHING** (`rover-eval` `generate_realistic`, `db32580`): ✅ NO deviation. Over 30 cave seeds rover stays PERFECTLY optimal (worst R_fatigue = R_ticks = 1.0000), zero ops-cap hits, all solvable. `LocalPathfinder` matches the oracle on hard terrain — the route-optimality + ops gates are safe, no re-tune.
    - **COMBAT** (`combat-eval` `Bed::Generated`, submodule `23379d6` / super `6d80028`): ⚠ REAL deviations. (1) mirror-symmetric self-play net swings ±~500 (open ≈ 0) — realistic terrain EXPOSES a position/order bias trivial rooms hid (fairness/determinism look warranted); (2) the tuned `open_combat` edge over `default` is a consistent −835 on OpenField but ranges −750..+890 across cave seeds — the tuning does NOT generalise to chokepoints. → **RE-TUNE NEEDED:** add `Bed::Generated` to `BASKET` + re-run the kernel grid sweep (`SquadTacticParams`/`KernelParams`: `cohesion_k`, `discohesion_coef`, `approach_coef`, kite `w_future`/`w_prox`); re-pin the canaries (EXP-KITE-1 melee=0, EXP-COHESION-1 worst≤8, EXP-POS-* survivor/pairwise) which will shift on chokepoints. Multi-hour tournament pass.
    - **ECONOMY** (`Family R realistic()` already wired): re-tune focus — `haul_milli` is a LINEAR distance model, optimistic once `routed ≫ Chebyshev` (realistic remotes) → the true-distance migration (step 2) pairs here; sweep-sensitive: `build_bid_road_milli`, `refill_roi_cap_milli`, `build_bid_extension_milli` (via `tune_market_constants`), guarded by the Family S non-regression bands (`deficit_per_tick`, `road_stock`, `flap`). Long sweep pass.
  - ⬜ **Combat sim**: wire `generate_terrain` into combat scenarios (`screeps-combat-eval`) so positioning/pathing is proven on real chokepoints (operator: combat rooms are too easy). Combat uses the same `SimTerrain`/`MovementState`, so it's a scenario-construction change.
  - ⬜ **Pathfinding sim** (`screeps-rover-eval`): use generated terrain in its corpora where trivial.
  - ⬜ **Foreman layouts × RCL breadth**: the econ home rooms already use foreman `captured_layouts` at the scenario RCL; broaden the corpora to foreman plans across a range of RCLs (operator ask), and use generated terrain for the non-captured (remote) rooms — which is now what `realistic()` does.
  - This makes `routed ≫ Chebyshev` normal → the credible **step 2 (true-distance)** validation runs on `realistic()` Family R.

**Key resume facts (file:line):**
- Shared movement primitives: `screeps-sim-core/src/rover_driver.rs:248` `resolve_moves_via_system_with(movement_state, &[SimMoveRequest], cache, cost_source, config)`; `SimMoveRequest` at `rover_driver.rs:86`; `resolve_movement` edge-exit relocation in `screeps-sim-core/src/tick.rs:25`. `EconWorld.movement` is already the shared multi-room `MovementState`.
- Rover traits: `screeps-rover/src/traits.rs:76` `CostMatrixDataSource {get_structure_costs, get_construction_site_costs, get_creep_costs}`; `PathfindingProvider` at `traits.rs:20`.
- Combat reference to mirror: `CombatWorldCostSource` at `screeps-combat-agent/src/pathing.rs:351` (`from_world`); driver wrapper `resolve_moves_via_system` at `pathing.rs:523`.
- Econ mover seam: `Mover` trait `screeps-econ-eval/src/movement.rs:53` (`trace`/`invalidate_from`/`travel_ticks`); single-room assert `movement.rs:125`; instantiated `runner.rs:256`, invalidated `runner.rs:330`, used in `step_worker`.
- Distance cache to reuse (live): `PathfinderService` `screeps-ibex/src/pathing/pathfinderservice.rs` — `travel_ticks(from_room,to_room,tick):206`, `route_distance:201`, `nearest_by_path:171`, `snapshot:161`, CPU pool `pool_for_tier:44`.
- Kernel distance to migrate: `screeps-econ-decision/src/market.rs` — `service` `:209`/`:241`, haul `d` `:262`, `nearest_refill` `:182` (all `get_range_to`).
- Arm-config seam: `MarketArmCfg` `screeps-econ-eval/src/market.rs:60`; sim refill via `refill_bid_from_plans` `:502` (already shared-kernel parity with live).

## Consequences
Turns the transfer market from "value-density with haul-as-divisor-only + a flattening cap" into the correct decentralized min-cost flow: remotes priced by real value − haul (served when worth it, declined past break-even), the cap inert, the whole economy EV-priced. All integer/deterministic (the one float — the `room_net_roi` tilt — was dropped). The remote fix is **gated on the sim gaining a multi-room corpus** (P2) — until then it cannot be validated, so P2 blocks the remote deploy. Every kept kernel is confirmed theory-correct.
