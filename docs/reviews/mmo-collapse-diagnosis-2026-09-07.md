# Live MMO economic collapse — diagnosis (shardX, 2026-09-07)

> Operator report (2026-09-07): "The MMO has been underperforming and seems to be having room resource starvation (e.g. ramparts are not able to be kept alive)". Measured at tick ~5484870: 7 owned rooms (W5N48 + W16N52 lost since 2026-08-24), 46 creeps, storage energy 0 in every room, spawns idle, W13N51 ramparts fully decayed, 720k oxygen hoarded. Method: live console/seg-57 reads + a six-lane code investigation, each lane adversarially verified, then synthesized. Evidence bundle and per-agent transcripts are in the session scratchpad; the numbers below are the live ones. Tracker: UNOWNED-8.

# MMO collapse diagnosis — 2026-09-07 synthesis (6 lanes, verified findings only)

## 1. Causal chain

Two independent roots. **A** stops energy from moving; **B** guarantees that once a lane is below one hauler body it can never spawn the cheaper income body that would refill it. Every other surviving finding is downstream of, or amplifies, A×B.

| # | Role | Mechanism (file:line) | Live number it explains | Status |
|---|---|---|---|---|
| A | **ROOT — movement wedge** | Rover path-ops pool = `pathfinding_cpu_budget` 20 × 1000 = 20,000 ops/tick (ibex `pathing/movementsystem.rs:546-567`). Each search **reserves** `rooms×2000` (×2 when stuck) before running, never refunded (`screeps-rover/src/movementsystem.rs:1811`; stats = cap−remaining `:584-586`) → pool caps **count** at 10 in-room / 5 stuck searches, not CPU. `allowed_ops==0` → `PathNotFound` (`:1803-1810`); incomplete → `PathNotFound` (`:1843-1845`). A `PathNotFound` creep is inserted as `stationary_occupant` with `allow_shove=false, allow_swap=false` (`:482-506`, `:977-989`) = immovable post. Failed creeps re-enter `needs_path` next tick and outnumber the cap/5 first-path reserve (`:1420-1432`). Haul job layer holds its ticket through failure — `tick_delivery` only re-issues `move_to`, no adjacent-sink fallback (`jobs/utility/haulbehavior.rs:604-617`). | seg-57 t=5484939/5485286: `ops_used 20000 == ops_pool 20000`, `repath_count 5` (=5×4000 stuck grants), `move_failures 23/43`, `wasted_moves 0`, CPU 26-66/140, bucket 10000. 38/44 creeps static across 3 samples. W13N52 hauler 5483823 (19C, 550e) at (9,19) **adjacent** to empty extensions >180 ticks, lane 301/12500, 3 spawns idle; engine `findPath` ignoring creeps = 0-8 steps for every stuck pair. **Release on TTL death** (46→40 creeps, t≈5485350-5485414): `ops_used 6000`, `move_failures 0`, W13N52 container 2000→100, lane 301→900, storage 0→1595, three spawns fired. | CONFIRMED (episodic — clears when posts die, re-wedges; 5485609 read shows 6000/20000, 2 failures) |
| B | **ROOT — spawn queue deadlock** | `spawnsystem.rs:497-499` `break` on first `body_cost > available` (only `> capacity` is `continue`d, `:493`). Hauler bid = `band.max(body_roi).min(CRITICAL−1)`; `body_roi_milli` = carry·50·1000·1500/(carry·100) = 750,000 for any 1:1 body → **99,999 always** (`spawn_policy.rs:291-297`, `haul.rs:265-273`). Hauler sized to `energy_capacity` once any hauler exists (`haul.rs:229-237`; 1300e RCL4 / 1800e RCL5 / 2000e RCL8). Link/container miners bid **75,000** HIGH (`source_mining.rs:457,:491`) despite comments/tests asserting CRITICAL. No starvation arm (`haul.rs:280` TODO). Renew needs creep at range 1 of a free spawn (`spawnsystem.rs:551-553`) — impossible under A. | Live queues: W5N49 e=300 `[Critical 100000·250 Harvester, High 99999·1800 Haul, High 75000·550 Container Miner ×2…]` with containers 2000/2000, sources 3000/3000. W16N51 (only source, miner expired) `[High 99999·1800 Haul, High 75000·600 Link Miner…]`; 1750/1800 idle at 5485528 with no hostiles. W9N46 spent 2600e on 2×13C13M haulers with **0 miners**. Post-release (no hostiles, no freeze) `busy=0` in 6/7 rooms — B persists without A. | CONFIRMED |
| C | AMPLIFIER | Empty-roster bootstrap bodies: harvester `energy_available.max(300)` → 250e `mmcw` (`spawn_policy.rs:198-204`); hauler → 300e `cmcmcm` (`haul.rs:229-233`); builder `cwmm` (`localbuild.rs:227-231`). Every later body capacity-sized. Home harvester tries haul pickup before harvesting (`harvest.rs:104-127`) → another stuck mini-hauler under A. | 7× `mmcw`, 3× `cmcmcm` live. W13N51 lane +278e in 376 ticks = 0.74 e/t → 1250e replacement needs ~1700 ticks > 1500-tick life. 20 e/t potential vs 0.74 realised. | CONFIRMED |
| D | AMPLIFIER | Defense fan-out: `DEFENSE_CADENCE=2` (`war.rs:279`), any A/RA/W/C/H hostile incl. border tile → Secure CRITICAL, TTL 60, no in-room-tile test (`war.rs:604-637`, `war_decision.rs:235-240`). `queue_slot_spawn` queues the shared-token body to **every** home within `MAX_SPAWN_DISTANCE=10` (all 7 rooms), sized min(best capacity, 3000), banks because tower ≥10e (`damage.rs:74-81`). Bid ≈75.5-76.6k (not 99,999): `est_ticks` never set for defense → defaults `MAX_TRAVEL_BUDGET=1000` (`set_est_ticks` only at `war.rs:1759`). Same default makes `should_abandon_forming` (`squad_manager.rs:1900-1918`) fire for any multi-member owned-room defense (r_o ≤1071 vs burn 1467-2267) → GaveUp after 20 reconciles → `NEVER_DEPARTED_GIVEUP_LIMIT=2` marks **own** room unwinnable 2000-20000 ticks (`:2134-2151`, `objective_queue.rs:56-58`). `can_afford_military` is offense-only (`war.rs:1655`). | `iamgqr` 20M+14RA+6H (dps 140/heal 72) at W7N47 (16,0); 24+9 Secure lines/240 ticks. 7 live defenders + 1 tombstone ≈ **8,400e** from 1300-cap, 0-storage rooms; `squad_count=1, engaged=0`. `NEVER-DEPARTED-BREAKER obj=740 room=W2N49` → W2N49 now has no in-room defender. | CONFIRMED (bid corrected; give-up mechanism reversed from lane draft) |
| E | AMPLIFIER (W7N47/W2N49 only) | Any hostile → tower deposit at `SURVIVAL_BID` 1,000,000 (`tower.rs:176-197`, `sink_economics.rs:42,:425-431`); duplicate from `wall_repair.rs:127-155` (every rampart <100k). Floor → 1e6, upgrader/builder shed. Towers are empty when hostiles arrive because peace refill 500 (`TOWER_PEACE_MILLI`, `sink_economics.rs:149`) < storage numeraire 1000 → never refilled in any storage room. | `floor=1000000` 27/90 (W7N47), 18/90 (W2N49). Tower series W7N47 630→0 then back to 1000 with no hostile: ~1000e per poke pulled from a 174-388/1300 lane. Peace towers empire-wide: 0-7e. | CONFIRMED |
| F | AMPLIFIER | Reservers: `desired_reservers=2`, [Claim,Move]×2 = 1300e MEDIUM/LOW (`reserve.rs:187-209`); unseen room = "insufficient" (`:158-164`); no tie to miners. 19 outposts × standing request. | W6N49 `Azaril:482` + W15N52 reserved with 0 miners = 2,600e spent from 300e-lane rooms. 19 Reserve missions, reservers=0, all `allow=true`. | CONFIRMED (spawn attribution inferred) |
| G | MINOR | Terminal energy reserve 10k locked (`terminal.rs:49-52,329-347`), counted in `stored_energy` (`economy.rs:224-227`). | W13N52 terminal 10000 / storage 0; one-shot. | CONFIRMED |
| H | SYMPTOM | Refill floor pinned at `REFILL_ROI_CAP` 10,000 because `spawnsystem.rs:295-301` publishes the head **bid** as `top_blocked_roi_milli` (contract says body ROI). Designed pin; upgraders (2000) shed permanently. Storage 0, ramparts decaying, controllers 33-37k/40k are downstream of A×B. | `floor=10000` 90/90 in 6 rooms; W13N51 0 ramparts; 8 rampart sites at 0. | CONFIRMED, not causal |
| I | SYMPTOM | Ring-4 gate + unknown-leaf BFS (`claim_economics.rs:121-123`, `gather.rs:189-220`) + **scout fleet = 0** (5485573). Blocks re-expansion incl. re-claiming W5N48/W16N52. | 24 candidates d≤3 deferred every tick; 0 at d≥4. | CONFIRMED (expansion lockout only) |

**Latent, not firing:** normal-mode `headroom == cap` refuses all searches if bucket <9500 (`pathing/movementsystem.rs:595`, rover `:1733-1737`); world payload 162.8 KB of 204.8 KB (4/4 chunks, `game_loop.rs:597,617-634`) — overflow truncates → next VM start wipes the world; remote hauler bids also clamp to 99,999 and remote harvester requests are issued per-home ×4 (`source_mining.rs:365-420`).

**Refuted (do not re-investigate):** orphaned creeps (census `orphans=[]` 44/44); VM resets/panics (`vm_starts 2771`, `aborted_ticks 2687` flat over 670 ticks); ADR 0040 D8 military reserve (offense-only caller); haul-lane priority inversion (floor read only by upgrader/builder admission); mineral mining (W13N52 O exhausted, W13N51 no container); boosts/labs (T0, labs empty); remote lane as sink (~1,200e of 30,950e fielded); SK farm / salvage / remote-build (0 missions); defend-gate blocking remotes (`allow_spawning=true` ×19); scout `intel_age` oscillation mechanism (RoomData created for highway rooms; scout now dead); EMERGENCY_WALL/SafeMode; market-off as energy cause (credits cannot import >10k e/20k ticks: `fairvalue.rs:164-173`, `MAX_DEAL_COST_PER_UNIT` 0.25 vs 0.39-0.87 live).

## 2. Most likely trigger (2026-08-24 → 09-07)

**Player `iamgqr` harassing the SW flank (W7N47/W2N49/W5N48) starting right after the 2026-08-23 expansion to 9 rooms**, converting empire storage into defenders via D+E until storage hit 0, at which point B locked every lane below one hauler body and A's wedges stopped the container→extension drip.

| Evidence | Value |
|---|---|
| Defender energy fielded from 0-storage rooms (last ~1500 ticks) | ≈8,400e (7 live + 1 tombstone), 3,400e in W7N47 alone (cap 1300) |
| Fan-out reach | all 7 homes within 10 rooms of W7N47 → up to 3000e bodies banked in W13N52 per poke |
| Re-field churn | forming give-up structurally fires (r_o ≤1071 < burn 1467+) → GaveUp ×2 within 1500 ticks, recycle, re-emit every 2 ticks |
| Tower drain per poke | ~1000e (W7N47 630→0→1000) from a 174-388e lane |
| Lost rooms | W5N48 = tower + 6 ext, **no spawn**, owner null — spawnless colony (`should_abandon_claim`, `utility.rs:110-125`) whose remote build depended on starved homes; W16N52 same flank |
| Timing | 2026-08-22 deploy → 08-23 W5N48 claimed → collapse window opens 08-24 |
| Storage now | 0 in 7/7 rooms; only terminal-locked 16.8k remains |

Not the trigger: the 2026-08-22 `reset.features` one-shot (it did revert `market.sell=true`, orders dated 08-20/21/22, but market is energy-irrelevant); VM resets (flat); segment overflow (no evidence this VM — see §4).

## 3. Interventions

### 3a. Live flips today (console, no deploy) — all wiped by the next `reset.features`; record them

| Rank | Flip | Effect | Risk | Verify on live |
|---|---|---|---|---|
| 1 | `Memory._features.pathing.pathfinding_cpu_budget` 20 → **50** (ceiling `PATHFIND_OPS_CEILING` 50,000; `pathing/movementsystem.rs:546-567`) | 25 in-room / 12 stuck searches per tick instead of 10/5 → far fewer `PathNotFound` posts; current wedges clear without waiting for TTL | CPU only if searches actually run (reservation ≠ cost); bucket 10000, ema 33 — headroom ample | seg-57 `pathing.ops_used < ops_pool`, `move_failures → 0-2`, containers (18,9)/(16,18)/(22,34) drain from 2000, lanes leave 300/301 within 100 ticks |
| 2 | `Memory._features.remote_mine.reserve = false` (`reserve.rs:150-151` — the only consumer) | Removes 19 standing 1300e MEDIUM reserver requests from 6 home queues | None — no outpost has a miner to protect | Room-visual spawn queues lose `Reserve` entries; no new Claim-part bodies |
| 3 | Defense flag off while `iamgqr` kites (`Memory._features.military.defense` — confirm key in `features.rs` before flipping) | Stops the empire-wide 1000-3000e defender fan-out and give-up churn; towers keep firing (independent mission) | Real breach gets towers only; harasser is a border kiter with dps 140 vs empty ramparts — accept for ≤2000 ticks | No `[War] Secure` lines; no new `mrmr`/`mhmh` bodies; W7N47/W13N52 lanes climb |
| — | `market.sell=true` | Liquidates O at ≤10k units/20k ticks; **not** an energy lever | Active-deal path spends terminal energy — enable passive only, or wait | Optional; not for bleeding |

Do **not** flip `fcd`/`boost_military` back — labs are empty, no boost is consuming anything.

### 3b. Code fixes, ranked

| Rank | Fix | File:line | Expected effect | Risk | Verify |
|---|---|---|---|---|---|
| 1 | Refund actual pathfinder ops after search (`remaining += allowed − result.ops`); do **not** insert a budget-miss `PathNotFound` creep as an unshoveable `stationary_occupant` (keep shoveable, or re-try next tick without posting) | `screeps-rover/src/movementsystem.rs:1811-1850`, `:482-506`, `:977-989` | Pool caps CPU not count; no immovable posts from budget misses → wedges cannot form | Determinism fence must stay green; shove semantics for genuinely blocked creeps | seg-57 `ops_used` ≪ `ops_pool` at same repath count; add Failed/Stuck split to `move_failures` (`pathing/movementsystem.rs:611-620`) |
| 2 | Miners → `SPAWN_BID_CRITICAL`; hauler bid need-scaled (`w = min(unfulfilled/round_trip, carry·50)`), never above miners; starvation mode: size any replacement body from `energy_available` when lane < requested body for > one spawn cycle | `source_mining.rs:457,:491`; `spawn_policy.rs:291-297,198-204`; `haul.rs:229-237,280`; fix tests `squad_manager.rs:4593,4605` | B dies: a 550e miner spawns ahead of an 1800e hauler; a 300e hauler is always fieldable | Over-spawning small bodies in steady state — keep capacity sizing once lane ≥ body | W5N49/W16N51 queues show miner at head; no lane sits at 300 with a full container >100 ticks |
| 3 | Loaded hauler on `Failed` move: drop ticket, reselect; if adjacent to any needy refill structure, transfer now | `jobs/utility/movebehavior.rs:16-26`, `haulbehavior.rs:456-505,580-660` | Cargo never held through a wedge; answers the (9,19) hauler | Ticket churn if Failed is transient — gate on ≥2 consecutive Failed | No creep with `store>0` static >20 ticks adjacent to an extension <50 |
| 4 | Defense: set `est_ticks` for defense objectives (onsite window); exempt in-room defense from forming give-up; Secure only for hostiles inside the room (not x/y∈{0,49}); restrict shared-token fan-out to ≤2 nearest homes; suppress re-emit while own room is marked unwinnable | `war.rs:604-637,1759`; `squad_manager.rs:1900-1918,2134-2151,2753-2905` | One harasser costs one room ≤1 body, not 7 rooms × 3000e; no own-room unwinnable marks | Slower response to a real in-room raid — bounded by tower coverage | `[War] Secure` count/240 ticks ≪ 33; no NEVER-DEPARTED-BREAKER on owned rooms |
| 5 | Tower pricing: peace refill > storage numeraire (e.g. 1500 up to a firing reserve); SURVIVAL bid only when `tower_fire` would fire and tower < reserve; delete duplicate SURVIVAL in wall_repair | `sink_economics.rs:149,:425-431`; `tower.rs:176-197`; `wall_repair.rs:127-155` | Towers arrive full at the next poke; survival lane stops draining spawn lanes | Storage fills marginally slower | Peace tower energy >500 in all rooms; no `floor=1000000` samples |
| 6 | Reservers: require ≥1 alive miner; 1×[Claim,Move] when cap <2600; unseen room uses last-seen reservation − elapsed | `reserve.rs:158-164,187-209` | No 1300e bodies for dead outposts | None | — |
| 7 | Serialization: chunk at 100 KB or re-add seg 54; abort write (keep last good) on overflow; persist overflow/deser tick in `Memory._metrics` | `segments.rs:43`; `game_loop.rs:597,617-634,876-921` | Removes the next-VM-start wipe; ceiling 400 KB | WFV bump is fine per operator policy | seg-57 `segment_chunks_used` ≤ 2 of 4 |
| 8 | Finite normal-mode headroom (~20 CPU); key loaded-carrier reachability memo by sink, skip when adjacent, bound cache | `pathing/movementsystem.rs:595`; `market.rs:~231`; `hauldistance.rs:28-33,101-118` | No total freeze on bucket dip; no 1500-tick cached `None` blinding a sink | — | `haul_dist_cached_pairs` stops growing; hits >0 |
| 9 | Expansion: exempt previously-owned rooms (own structures present) from ring gate; treat zero d≥4 candidates as covered; scout bid floors at CRITICAL-lite under fleet=0 | `claim_economics.rs:121-123`; `claim.rs:974-980,602-631`; `scoutassignment.rs:683-767` | W5N48/W16N52 reclaimable; fleet never 0 | — | `[ClaimOp]` stops deferring d=1 own-structure rooms |
| 10 | Sweep `game::construction_sites()` in non-owned, non-target rooms; publish real body ROI of an unaffordable head as `top_blocked_roi_milli`; gate terminal energy reserve on `market.sell||buy`; room-loss ledger; SpawnQueue head INFO log | `construction.rs`; `spawnsystem.rs:295-301`; `terminal.rs:329-347`; `colony.rs:199-215` | Hygiene + observability | — | — |

## 4. Unknown — and the read that settles each

| Unknown | Exact read |
|---|---|
| Which creeps form the wedge (Failed vs Stuck, which posts) and whether searches are doomed or merely capped | Add a `[Rover]` per-creep line (creep, rooms_on_route, allowed_ops, result Ok/Incomplete/PathNotFound) behind a debug flag, or split `move_failures` into `failed`/`stuck` in seg-57; until then: `room.visual.export()` FAILED markers per owned room for 50 consecutive ticks + `Game.creeps[n].memory` for those creeps |
| Why hauler 5483823 delivered 385e to storage (par 1000) instead of the adjacent 10,000-bid extensions | Debug trace of `select_market_pickup_and_delivery` for one loaded hauler; count `None` entries in the `haul_distance` cache (`hauldistance.rs`) |
| When storage went 0 — dates the trigger | Stats-history seg 56: `stored_energy` per room by tick 2026-08-22 → 09-07; look for the step and the coincident Secure/tombstone activity |
| Loss mechanism for W5N48 / W16N52 (unclaim vs downgrade vs raid) | `Game.notify` email history 08-24..09-07 for `Colony: abandoning` / spawn-destroyed / downgrade notices; W16N52 needs one scout visit (structures, controller `ticksToDowngrade`) |
| Did the payload cross 204,800 bytes during the 9-room period and wipe on a reset | seg 56 / eval metrics archive for `segment_chunks_used=5` or `deser_failures>0` samples; `Memory._metrics.vm_starts` delta across 08-24 |
| Did `reset.features` change any pathing/defense value from the pre-08-22 live tune | Diff `Memory._features` against `features.rs` defaults; `pathfinding_cpu_budget`, `bucket_burst_threshold`, `remote_mine.search_radius` |
| Is W2N49 currently `unwinnable`-marked (no in-room defender) and for how long | `[ClaimOp]`/objective-queue debug dump (`claim.rs:2394`-style) for `mark_unwinnable` expiry on W2N49 |

---

## Appendix — per-lane findings after adversarial verification

Each lane was investigated by one agent (code + live reads) and then independently re-checked by a verifier instructed to refute. Status is the investigator's; the verdict column is the verifier's.

### H3 — minerals and the switched-off market

| Finding | Status | Weight | Verifier |
|---|---|---|---|
| External market is OFF by compiled default (ADR 0012 deliberate, NOT ADR 0040 M5) — and the 2026-08-22 reset.features one-shot silently reverted a live sell=true override | CONFIRMED | SYMPTOM | UPHELD |
| The 20,000 O in each terminal is the terminal.rs tiered sale float (5k transfer + 10k passive + 5k active) — hauled in every tick, re-requested for sale every tick, never sold | CONFIRMED | SYMPTOM | UPHELD |
| Terminal ENERGY reserve strands 10,000 e per terminal room at Medium haul priority while spawns starve — 16,807 e locked live, and it is counted as 'stored_energy' | CONFIRMED | AMPLIFIER | DOWNGRADED → CONFIRMED (mechanism); overstated (impact)/MINOR AMPLIFIER (one-time 16.8k e locked; no ongoing drain; renew leak ~20 e/renew, W13N52 only) |
| Mineral mining has NO energy-health gate in code, but it is currently INERT live — it is not consuming spawn energy or hauler capacity today | REFUTED | UNRELATED | UPHELD |
| Boost/labs pipeline (boost_military=true) burns nothing here: the supply clamp resolves to T0 because no tier's six-compound set is stocked, so no BoostRequest is ever filed and labs sit empty | REFUTED | UNRELATED | UPHELD |
| Even with sell/buy flipped ON, the ADR 0012 exposure caps and the deal-distance ceiling make the market unable to either liquidate the hoard or import energy in useful time; the stale W13N51 order additionally blocks new O sells from that room | CONFIRMED | UNRELATED | UPHELD |

Verifier: missed within the lane — Precise order dating (own console read, Game.market.orders[].createdTimestamp): W13N52 O sell 2026-08-20 20:25 UTC, W13N51 O sell 2026-08-21 04:13 UTC, W12N51 U sell 2026-08-22 13:56 UTC — the market was demonstrably live-on until ~9 h before the reset.features commit (2026-08-22 22:43 UTC). This closes the lane's first open question in the affirmative and refines its Aug 16-19 estimate; historical tick rate was ~2.2 s/tick, not the 2.67 measured now.; Coupling hazard in the proposed minimal fix (flip market.sell=true): the ACTIVE-deal path (ordersystem.rs:322-436) spends TERMINAL energy on transfer cost (available_transfer_energy = terminal energy from terminal.rs pre_run), and the Medium 10k reserve refill (finding 3) would then re-request energy from a starving room to replenish it. Today no O buy order is within the 0.25 e/unit ceiling so no deal would fire, but the book changes; if sell is flipped during the energy crisis the reserve refill should be gated too (or only passive sells enabled — passive fills cost the seller no energy).; W13N52's terminal holds K 9,475 and U 9,865 while storage has 525/135 (< 10k desired), so terminal.rs's 'make available' branch files perpetual priority-None (par) withdraw requests terminal->storage for K and U every tick; harmless (par bids are served last) but it is dead per-tick request churn in the same class as the discarded sale requests.; The intra-empire terminal-send path (terminal.rs run_mission, outgoing energy band 50,000 above the 200k desired storage) is the empire's only mechanism for moving energy from a rich room to a starving one, and it is structurally inert here because no room's storage exceeds 200k (all are 0-385 e). Whatever recovery plan is chosen, the market/terminal subsystem offers NO energy-rebalancing lever at these storage levels — worth stating explicitly so no lane counts on it.; seg-57 stored_energy (W13N52 = 10,000, W13N51 = 6,807) equals terminal energy exactly; any lane or dashboard reading stored_energy as 'spendable' is reading terminal-locked energy. The economy.rs:224-229 sum also includes containers, so the phantom is not only terminal — but live it is 100% terminal.

**Terminal ENERGY reserve strands 10,000 e per terminal room at Medium haul priority while spawns starve — 16,807 e locked live, and it is counted as 'stored_energy'** — mechanism: screeps-ibex/src/missions/terminal.rs:49-52 get_desired_reserve_terminal_amount(Energy) = OrderQueue::maximum_transfer_energy() = 10,000 (ordersystem.rs:57-59). The HAUL generator :329-347 'Ensure a reserve' files a TransferDepositRequest to the terminal at TransferPriority::Medium (= BID_TIER_MEDIUM 2,000 milli, screeps-econ-decision/src/sink_economics.rs:524) for the shortfall up to 10k regardless of storage energy. The only energy withdraw from the terminal (:407-430) is for amounts ABOVE the 10k reserve, so the 10k can never return to storage/spawns. The reserve exists solely to pay active-deal/terminal-send transfer costs, which never happen (finding 1). Side effect: military/economy.rs:227 adds terminal energy into stored_energy, so W13N52's seg-57 stored_energy=10,000 is 100% terminal-locked; spawnsystem.rs:22 RENEW_MIN_ROOM_ENERGY=10,000 therefore lets W13N52 renew on phantom energy, and the 20% military reserve (economy.rs:88) is computed on it.

Live evidence: H3ORD @ tick 5,485,227: W13N52 storage energy 0 / terminal energy 10,000 / energyAvailable 301 of 12,500; W13N51 storage 0 / terminal 6,807 / energyAvailable 877 of 2,300. Seg-57 stored_energy W13N52=10,000, W13N51=6,807 — identical to terminal energy. Market floor lines show refill unmet at 10,000 in both rooms.

Minimal fix: Gate the reserve deposit on the market actually being able to use it: in terminal.rs:329-347 request the energy reserve only when features.market.sell||buy (or when a terminal transfer is pending), and let the 'make available' withdraw (:407-430) release the reserve when storage energy < some floor. Separately stop counting terminal energy as stored_energy for renew (economy.rs:227 / spawnsystem.rs:22).

### H4 — defense, repair and rampart decay

| Finding | Status | Weight | Verifier |
|---|---|---|---|
| Rampart/structure decay is unfunded repair: peacetime tower refill priced BELOW par and builders have no energy source (storage 0, cannot harvest) | CONFIRMED | SYMPTOM | UPHELD |
| Player border-kiter re-triggers a CRITICAL Secure objective every 2 ticks; defender bodies bid 99,999 and head-of-line-block every economy spawn in the home while the room banks for an unwinnable squad | CONFIRMED | AMPLIFIER | DOWNGRADED → CONFIRMED (bid ~75.5k-76.6k, not 99,999; blocks HIGH/MEDIUM economy only)/AMPLIFIER |
| Any hostile creep flips tower refill to SURVIVAL_BID=1,000,000, making it the room's opportunity floor: upgraders/builders are shed and hauling is redirected to a tower that then drains to 0 against an unkillable kiter | CONFIRMED | AMPLIFIER | UPHELD |
| Global 100-construction-site cap is exhausted by 67 sites in non-owned (mostly invisible) rooms that the bot never removes, blocking all rampart/wall/extension placement in W2N49, W9N46, W16N51 | CONFIRMED | AMPLIFIER | DOWNGRADED → CONFIRMED/UNRELATED (minor, intermittent) |
| EMERGENCY_WALL_HITS=100k is unreachable/meaningless at these RCLs and WallRepairMission is effectively a log line + a duplicate SURVIVAL bid; SafeMode correctly not armed | REFUTED | UNRELATED | UPHELD |
| The 20% military reserve (ADR 0040 D8) is not on the defense path at all — defense spending is ungated by stored energy | REFUTED | UNRELATED | UPHELD |
| ADR 0042 opportunity_floor hardcoded 0 plus DEFENSE_TARGET_VALUE=1e6 means the economic give-up can never fire for a defense squad; give-ups come only from the lifecycle deadline, producing never-departed churn | PLAUSIBLE | AMPLIFIER | REFUTED → CONFIRMED (different mechanism: economic give-up structurally fires for owned-room defense)/AMPLIFIER |

Verifier: missed within the lane — est_ticks is never set for DEFENSE objectives (only offense at war.rs:1759) so every defense r_o is divided by MAX_TRAVEL_BUDGET=1000 — this single default makes the forming give-up fire for essentially any multi-member owned-room defense and is the root of the never-departed breaker firing on W2N49 (finding 7 corrected); it also flattens the defense spawn bid to ~75.5k.; Peacetime tower refill (500 milli) structurally loses to the STORAGE numeraire (1000, always registered) — in any room with a storage the tower can only ever refill via the SURVIVAL lane; combined with localbuild.rs:166-168 excluding ramparts, an RCL<=6 storage room has NO peacetime rampart funder at all, so rampart decay-to-zero (W13N51: 0 ramparts) is the design outcome, not a stress symptom.; The shared-token defense request fans out to EVERY home within MAX_SPAWN_DISTANCE=10 rooms (queue_slot_spawn) — all 7 owned rooms are within 10 of W7N47 — so one border harasser places a 1000-1200e body at the head of every room's spawn queue above the HIGH economy bulk; the lane framed this as a per-home effect.; After mark_unwinnable(W2N49) the Phase C claim filter (squad_manager.rs:2662) makes the re-emitted Secure unclaimable, so no squad and no spawn banking exist for it — the every-2-tick '[War] Secure ... W2N49' line is inert log noise for 2000-20000 ticks while W2N49 has no in-room defender at all (only towers). Answers the lane's open question 3.; A claim-time EV gate DOES exist (Phase C ev_of_claim > COMMIT_EV_THRESHOLD=1.0, p_win-weighted pairing_ev over asset=energy_capacity and threat=estimated_attack_dps); the lane should have asked why a 3-member squad vs a 72-hps self-healer passes it rather than asserting no gate exists.; Fresh read @5485528 (no hostiles anywhere, TTL 60 lapsed): busy=0 in 6/7 rooms, W16N51 1750/1800 idle, W13N52 900/12500 idle — the idle-spawn symptom persists with no defense request in any queue, so the spawn/haul lane (capacity-sized bodies + head-of-line break, container->extension refill) owns the idle spawns; this lane's finding 2 is an amplifier during pokes, not the cause.; Construction-site cap is intermittently binding (fresh read 90/100 with 67 non-owned) — the lane's '100 exactly' was a moment-in-time read; the sweep is still warranted.

**Player border-kiter re-triggers a CRITICAL Secure objective every 2 ticks; defender bodies bid 99,999 and head-of-line-block every economy spawn in the home while the room banks for an unwinnable squad** — mechanism: operations/war.rs:279 DEFENSE_CADENCE=2; run_defense_scan (war.rs:380-470) emits a DefenseNeed for any owned room whose hostile has an Attack/RangedAttack/Work/Claim/Heal part (war_decision.rs:235-240) — a creep standing on the border tile counts. war.rs:604-637 requests Secure{room} at the kernel priority (CRITICAL=100 for an in-room threat) with TTL 60 (war.rs:35) and NO winnability or affordability gate (decide_doctrine/plan_engagement composition is taken as-is; can_afford_military is only used by offense at war.rs:1655). squad_manager.rs:363-377 spawn_priority_for: r_o_completed uses DEFENSE_TARGET_VALUE=1_000_000 (war.rs:47) so forming_completion_bid saturates at SPAWN_BID_CRITICAL-1 and +DEFENSE_SPAWN_EDGE => every defense slot bids 99,999, above haulers/builders/upgraders (<=75,000). queue_slot_spawn (squad_manager.rs:2753-2905) sizes the body to min(best in-range capacity, 3000) and, because the room's tower has >=10 energy, damage.rs:74-81 returns Wait (bank to capacity). spawnsystem.rs:497-503: a head-of-line request with body_cost > available but <= capacity `break`s — nothing below it spawns that tick. Squads that never depart give up (R22 breaker squad_manager.rs:2134-2151), are retired (retire_squad 2718) and members orphaned then recycled (jobs/squad_combat.rs:944-952).

Live evidence: H4G @5485317: hostile 'iamgqr' at W7N47 (16,0) body 20M+14RA+6H, 4000 hits (dps 140 / heal 72 — the exact '[War] Secure ... W7N47 prio=100 (dps=140, heal=72, count=1)' line recurring in every tail); W2N49 variant dps=170 heal=84. Bundle tail: 24+9 Secure lines in 240 ticks; tail4: 6 W7N47 lines in 60s. Live bodies (H4B @5485197): 7 military creeps ~7,200e (W7N47: mrmrmrmrmr 1000e + mhmhmhmh x2 2400e; W13N51 mrmrmrmrmr ttl 28 never left home; W16N51 mhmhmhmh; W3N50 mrmrmrmr in transit; W2N49 mrmrmrmr spawning) + a dead 1200e healer tombstone in W2N49 (deathTime 5485180) — ~8,400e of defenders from 1300-capacity rooms with 0 storage; our 5-RA squad (50 dps) cannot out-damage a 72 hps self-healer. seg-57 cohesion: squad_count=1, engaged_squads=0. Tail3: '[Lifecycle] NEVER-DEPARTED-BREAKER squad=Entity(651) obj=740 room=W2N49 — 2 consecutive GaveUp generations without departing'; two 'INTEGRITY: dead squad ref Entity(639) scrubbed from SquadCombatJob' orphans. Spawns: W7N47 idle at 174-388/1300 with 250e runt economy creeps; SNAP1 busy=0 in all 7 rooms.

Minimal fix: Gate the owned-room Secure request in war.rs:604-637 on plan_engagement(...).winnable() (or p_win over the threat's heal) and on the threat being inside the room (not a border tile), and stop pinning the bid by replacing DEFENSE_TARGET_VALUE with the protected room's real value so forming_completion_bid orders defense below income-critical economy when the threat is a harasser; reuse the living garrison (Reassign) instead of re-fielding per poke.

**Any hostile creep flips tower refill to SURVIVAL_BID=1,000,000, making it the room's opportunity floor: upgraders/builders are shed and hauling is redirected to a tower that then drains to 0 against an unkillable kiter** — mechanism: missions/tower.rs:176-197 registers a tower deposit at tower_refill_bid(consts, !creeps.hostile().is_empty()) which is SURVIVAL_BID=1_000_000 (sink_economics.rs:42, 425-427) for ANY hostile creep (room/data.rs:1033 hostile() is every hostile, armed or not). missions/wall_repair.rs:127-155 registers the same SURVIVAL deposit whenever any rampart/wall < EMERGENCY_WALL_HITS=100k (wall_repair.rs:20) — true for every rampart in the empire (W13N52 RCL8 avg 21,880), so it is a pure duplicate. transfersystem.rs:1649-1680 publish_market_floor sets the room floor to the highest materially-unmet deposit bid => 1,000,000; consumer_admission.rs:65-73 (upgrader bid 2000) and :118-135 (builder priced sites) then shed via admit_use_withdraw (sink_economics.rs:471-473). Deposit selection is bid-ordered, so haulers fill the tower ahead of spawn/extension refill (10,000). Tower then spends it firing/repairing (tower.rs:356-378 / :401-423).

Live evidence: [market] W7N47 floor=1000000 unmet=[1000000, 10000, 1000] in 27 of 90 samples, W2N49 in 18 of 90 (bundle); seg-57 W2N49 market_opportunity_floor=1000000. Tower energy series: W7N47 630 (@5484870) -> 360 (@5485148) -> 280 (@5485188) -> 260 (@5485198) -> 0 (@5485316); W2N49 5 (@5484870) -> 770 -> 696 -> 936 (@5485198) -> 850 (@5485316) — i.e. ~1,000e hauled to each tower per episode while extensions sit at 3 filled in W7N47. No '[Tower] Confirmed tower drain' line in any tail, so the drain conservation (tower.rs:258-297) never engaged against the (16,0) border poker. Meanwhile W7N47 energy_available 174-388/1300, W2N49 rampart repair queue at Critical unserved.

Minimal fix: Price the hostile-time tower refill by expected kill value (tower_fire kernel: full_tower_damage vs heal; hold-fire => no SURVIVAL bid), require the hostile to be inside the room (not x/y==0/49) and armed (hostile_warrants_defender), and delete the duplicate SURVIVAL generator in wall_repair.rs:127-155 (or make EMERGENCY_WALL_HITS RCL-relative).

**Global 100-construction-site cap is exhausted by 67 sites in non-owned (mostly invisible) rooms that the bot never removes, blocking all rampart/wall/extension placement in W2N49, W9N46, W16N51** — mechanism: screeps-foreman/src/plan.rs:556-572 execute_operations logs and skips ERR_FULL; missions/construction.rs:453-482 only budgets against the per-room cap (features.construction.max_construction_sites=10). No code path ever calls ConstructionSite.remove(): the only global enumeration is room/createroomsystem.rs:26 (creates RoomData for site rooms). Sites left by lost/abandoned claims and remote-build in rooms we no longer see persist forever and count against the player-wide 100 cap.

Live evidence: H4C @5485197: Object.keys(Game.constructionSites).length = 100 exactly. Owned rooms hold 33 (W13N51 10, W13N52 10, W5N49 8, W9N46 5); 67 are in non-owned rooms, 47 of them in rooms with NO visibility (H4E: W18N51 9 incl. a spawn site at 6018 progress, W18N53 9 incl. spawn s0, W15N53 6 incl. spawn s5700, W11N56 7, W11N57 5, W12N57 5, W17N54 6) plus W17N52 5, W11N52 5, W15N52 3, W13N53 2, W13N55 2, W22N48 1, W22N49 2. Tails: 'create_construction_site failed in W2N49: Road/Rampart/Wall -> Full' 64+27+18 times ('Construction W2N49 (RCL 4): 109 create-ops, 0 sites created, 0 sites already in room'), W9N46 Rampart -> Full x15, W16N51 Rampart -> Full x7. So yes, the cap blocks rampart placement in three rooms — though placed ramparts would not be built or held anyway (finding 1).

Minimal fix: Add a periodic sweep over game::construction_sites(): remove any site whose room is not owned by us and not an active remote-mining/claim target (or invisible for > N ticks / progress 0 for > N ticks), so the 100-cap is available to owned rooms.

**ADR 0042 opportunity_floor hardcoded 0 plus DEFENSE_TARGET_VALUE=1e6 means the economic give-up can never fire for a defense squad; give-ups come only from the lifecycle deadline, producing never-departed churn** — mechanism: squad_manager.rs:1900-1918 should_abandon_forming(r_o, burn, 0): with r_o computed from DEFENSE_TARGET_VALUE=1_000_000 (war.rs:47) the completed rate dwarfs any burn, so the R_net give-up is inert for defense; the squad instead runs to the lease/forming-budget GaveUp (lifecycle.rs:384-393 precedence) and, never having departed its home==target room, trips NEVER_DEPARTED_GIVEUP_LIMIT=2 (squad_manager.rs:279, :2134-2151) which mark_unwinnable()s our OWN room for 2000-20000 ticks (objective_queue.rs:56-58) and Phase C then filters it (squad_manager.rs:2662).

Live evidence: '[Lifecycle] NEVER-DEPARTED-BREAKER squad=Entity(651, Generation(11)) obj=ObjectiveId(740) room=W2N49 — 2 consecutive GaveUp generations without departing; backing the room off' (tail3, ~tick 5485150-5485225) while '[War] Secure objective for threat room W2N49 prio=100 (dps=170, heal=84)' keeps re-emitting; W2N49 tombstone of our own mhmhmhmh healer at 5485180.

Minimal fix: Feed a real opportunity floor (the room's published market floor) and the protected room's true value into r_o for defense so unwinnable harasser responses are declined economically before a squad is fielded; do not let the never-departed breaker back off an owned room without also suppressing the re-emission in war.rs (else the objective keeps re-asserting and the spawn queue keeps banking).

### H5 — stability and the lost rooms

| Finding | Status | Weight | Verifier |
|---|---|---|---|
| VM resets / aborted ticks are HISTORICAL lifetime counters — the bot is not resetting or panicking now | REFUTED | UNRELATED | UPHELD |
| Serialize aborts are not losing economic state per tick (rate is zero); the only lossy path is a full world wipe on deserialize failure | REFUTED | UNRELATED | UPHELD |
| World payload is at 80% of a 200 KB ceiling; the 5th chunk is silently dropped and the NEXT VM start would wipe the world | PLAUSIBLE | AMPLIFIER | DOWNGRADED → CONFIRMED mechanism, NOT YET TRIGGERED/LATENT RISK (unrelated to the current collapse; becomes a wipe on the next VM start only if the payload crosses 204800 bytes) |
| Movement ops pool 'exhausted' every tick is grant accounting, not CPU — but it starves later creeps of first paths (PathNotFound) each tick | CONFIRMED | AMPLIFIER | DOWNGRADED → PLAUSIBLE (real accounting flaw; incidence intermittent, impact unmeasured)/MINOR / UNPROVEN |
| W5N48 and W16N52 were the two rooms lost; W5N48 was a SPAWNLESS built colony (tower + 5 extensions still ours), lost via unclaim-on-contest or controller downgrade — no record survives | PLAUSIBLE | SYMPTOM | UPHELD |
| Ring-4 deferral can never lift: the BFS stops at unknown rooms so no candidate >= 4 hops exists, and 'covered' requires a scout fleet that is one dying creep — the empire (including its own lost rooms) cannot re-expand | CONFIRMED | ROOT | DOWNGRADED → CONFIRMED gate / PLAUSIBLE 'never lifts' (conditional on zero scouting)/ROOT of the expansion lockout only; UNRELATED to the economic collapse of existing rooms |
| The single scout is border-oscillating between W0N50 and W0N51 instead of touring, so unknown rooms never get seen | PLAUSIBLE | AMPLIFIER | REFUTED → OBSERVED, mechanism UNEXPLAINED/MINOR (superseded by fleet = 0) |

Verifier: missed within the lane — Scout fleet is now ZERO (live probe tick 5485573: no 1-MOVE creeps; the lone scout 5483928-0 has expired) and no scout is in any spawn (5485591 spawning bodies: mwwwww, cm x18, wcmmwwwwwwwww); spawn stores sit at 1-244 energy across 9 spawns. Coverage / far-candidate discovery is impossible until a 50-energy scout bid wins in the starved queue — this answers the lane's own open question and is the real blocker behind finding 6, within the lane's subsystem.; CPU is spiky, not flat: seg-57 at tick 5485609 shows cpu.used 84.8/140 (vs the lane's 27-30). Not a stability fault (bucket 10000, no resets), but the 'cpu 27-30' characterization understates per-tick variance.; The 162 KB payload driver was never measured: room_plan_data is serialized (game_loop.rs:883) and prefetch_candidate_plans pushes plans for the top candidate rooms every Scouting tick, so plan data for the ~24 non-owned candidates rides in the world payload; plus 203 missions for 43 creeps. The chunk ceiling is approached by frontier size, not just owned-room count — relevant to finding 3's growth risk.; Finding 3's 'silently dropped' is wrong in wording: the overflow branch logs error! ('Not enough segments available...') every tick; what is missing is a persistent record and an abort-instead-of-truncate. The verified live headroom is 20.5% of the 204800-byte ceiling, not 26%.; W5N48 live state correction: SIX extensions (not five) + tower, all still returned by FIND_MY_STRUCTURES at controller level 0 / owner null; W16N52 remains invisible. expansion_avoidance cooldown (5000 ticks, heap-only) has long expired, so both rooms are back in the deferred candidate list as the tail shows.; The seg-57 move_failures counter cannot distinguish PathNotFound from Stuck>=threshold (ibex movementsystem.rs:611-620), and `repath_count` counts only successful searches (rover:515-521) — both are needed before any pool-starvation claim can be attributed; the lane's saturated samples were not representative (6000/20000 with 2 failures at 5485609).

**World payload is at 80% of a 200 KB ceiling; the 5th chunk is silently dropped and the NEXT VM start would wipe the world** — mechanism: segments.rs:43 COMPONENT_SEGMENTS = [50,51,52,53] (seg 54 freed 2026-06-12, segments.rs:19); game_loop.rs:597 chunk_count = len/51200, line 599 warns when chunk_count+1 >= 4 (i.e. from 3 chunks); game_loop.rs:617-634: chunks beyond the 4th hit the `else` branch — error!('Not enough segments available...') and the bytes are DROPPED, the 4 segments hold a truncated payload every tick. The running VM is unaffected (world lives in the heap) but the next VM start (deploy, global reset, or the first future panic) runs deserialize_world → rmp error → game_loop.rs:902-920 clears every component storage: all missions, operations, room plans, haul tickets, squads gone (a loud reset with no WFV bump). Engine segments are 100 KB; the 50 KB chunking (game_loop.rs:597/617) halves usable capacity.

Live evidence: Tail: 'Serialized world state near segment capacity: 4 of 4 chunk(s) used (161920..162768 encoded bytes)' every tick; seg-57 segment_chunks_used 4 / budget 4; live len50/51/52 = 51200, len53 = 9636. 162 KB at 7 rooms / 43 creeps / 202 missions; headroom to 204800 bytes is ~42 KB (~26%). The empire was 9 rooms with more creeps before 2026-08-24 — whether it crossed the ceiling then (and got wiped on a reset) is not recorded (deser_failures is environment-lifetime).

Minimal fix: Chunk at 100 KB (engine segment max) or re-add seg 54 to COMPONENT_SEGMENTS; make an overflow abort the write (keep the previous good payload) instead of writing a truncated one; persist a deser-failure/overflow tick in Memory._metrics.

**Movement ops pool 'exhausted' every tick is grant accounting, not CPU — but it starves later creeps of first paths (PathNotFound) each tick** — mechanism: screeps-rover/src/movementsystem.rs:1800-1811: max_ops = rooms_on_route*2000*mult (min MAX_PATHFIND_OPS); allowed_ops = max_ops.min(remaining); remaining -= allowed_ops BEFORE the search, and the actual PathFinder ops are never refunded (no `pathfinding_ops_budget_remaining +=` anywhere); tick_stats (rover:581-587) reports cap - remaining, which ibex emits as pathing.ops_used/ops_pool (metrics.rs:437-438). Cap = pathfinding_cpu_budget 20 -> 20000 ops (ibex pathing/movementsystem.rs:546-556, feature pathing.pathfinding_cpu_budget=20). When remaining reaches 0, every further needs_path creep that tick gets Err(PathNotFound) (rover:1808-1810) -> MovementResult::Failed -> move_failures (ibex movementsystem.rs:611-620); the first-path reserve (rover:1431-1433, cap/5) only protects against repaths, not against 5 first-paths consuming 4000 each.

Live evidence: Both seg-57 samples: ops_used 20000 == ops_pool 20000 with repath_count exactly 5 (5 searches x 4000-op grants = the whole pool) and move_failures 22-23 of 43 creeps per tick, while cpu.used is only 27-30/140 (so it is not real pathfinding CPU). Live position samples 5485348 -> 5485364 (16 ticks): 22 of 36 creeps did not move a tile — most are miners/upgraders legitimately parked on containers, but the lone scout (W0N51 9,49), a 'wmcm' remote builder on the W7N47 (49,34) exit tile, a hauler at W6N47 (4,39), and the 4-creep W7N47 squad at y=3-4 are stationary. wasted_moves 0 (creeps that do issue moves do move).

Minimal fix: Charge the pool with the search's actual returned ops (refund allowed_ops - result.ops) in rover movementsystem.rs after the search at ~1815; alternatively cap per-search grant at cap/N_pending. Add a Failed-vs-Stuck breakdown to seg-57 move_failures.

**Ring-4 deferral can never lift: the BFS stops at unknown rooms so no candidate >= 4 hops exists, and 'covered' requires a scout fleet that is one dying creep — the empire (including its own lost rooms) cannot re-expand** — mechanism: claim_economics.rs:121-123 may_claim_below_ring = distance >= ring(4) || scouting_covered. operations/claim.rs:974-980 defers every below-ring candidate (the logged line). room/gather.rs:189-220: the discover BFS records a room with no RoomData as an unknown LEAF (line 219) and never expands past it, so with 13+ unknown rooms at distance <= 3 the candidate frontier is capped at distance 3 — no far candidate can ever be produced, and every claim is gated on `covered` = scouting_coverage_complete (claim.rs:602-631): all candidates scored AND every unknown room seen within VISIBILITY_TIMEOUT 20000 ticks (claim.rs:184) unless currently in unreachable backoff (visibilitysystem.rs:225-243, base 2000 ticks, expires and the room re-enters the requirement). What completes it: the ScoutAssignmentSystem fleet physically visiting every unknown room (scoutassignment.rs:529-550 demand set, 683-767 EV spawn bid of a 1-MOVE scout from a spawn-bearing home). Cycle: Idle (discover_interval 500-1500) -> Scouting (<= 2500) -> Select window 400 ticks (claim.rs:73, verbose per tick) -> Idle, forever.

Live evidence: Tail: 24 distinct 'candidate X at distance 1-3 < ring 4 deferred' lines x 90 ticks in 240 s; NO candidate at distance >= 4 and never a 'reachable ring covered' line. The deferred list includes the two rooms we just lost (W5N48 d=1, W16N52 d=1 — our tower + extensions still stand in W5N48). H5MEM: W6N49/W4N50 no visibility, W0N51 is a controller-less highway room (an unknown-room leaf). The fleet is ONE scout, creep 5483928-0 body 'm': lane H6 saw it at W0N51 (9,49) ttl 282 and W0N50 (9,0) ttl 245; my samples W0N51 (9,49) ttl 82 then 66 — >= 200 ticks ping-ponging on one border exit, ttl 66 at last read, no pending scout spawn visible (spawns hold 13-300 energy).

Minimal fix: Exempt previously-owned rooms / rooms holding our own structures from the ring gate (or treat them as far); treat 'BFS produced zero candidates >= ring' as covered=true (a boxed-in frontier is exactly what ADR 0038 D9 said must still expand); optionally let the BFS expand through unknown rooms with a distance penalty.

**The single scout is border-oscillating between W0N50 and W0N51 instead of touring, so unknown rooms never get seen** — mechanism: jobs/scout.rs:52-62 targets the first tour stop != current room; jobs/utility/movebehavior.rs:153 declares arrival on room membership. scoutassignment.rs:489-496 intel_age() returns u32::MAX for any room without a RoomData entity (mapping miss), so such a room stays in the demand set (entry_needs_service, line 155-156) even while the scout is standing in it; if two adjacent unknown rooms both stay 'unseen' this way, the scout in A targets B, crosses, then targets A — the exact edge ping-pong the movebehavior.rs:144-152 comment warns about, now arising from demand rather than from the arrival test. Highway rooms (no controller) are the suspects; whether visibilitysystem.rs:491/515 creates RoomData for them is unverified.

Live evidence: Creep 5483928-0 ('m'): W0N51 (9,49) ttl 282 [H6A], W0N50 (9,0) ttl 245 [H6B], W0N51 (9,49) ttl 82 and 66 [H5POS 5485348/5485364] — same exit tile pair for >= 200 ticks, fatigue 0 (not terrain-stuck). W0N51 reported 'noctl' (highway). Meanwhile every ClaimOp cycle ends uncovered.

Minimal fix: Verify with the ClaimOp debug dump (claim.rs:2394 prints unknown_rooms) and a tour dump; ensure a visible room always gets a RoomData entity / fresh dynamic visibility before the assigner runs, and drop a tour stop the tick the scout is inside it regardless of entity presence.

### H6 — remote mining, outposts, SK farm, salvage/derelict as an energy sink

| Finding | Status | Weight | Verifier |
|---|---|---|---|
| Remote lane is NOT an energy sink: 19 outposts exist but field ~4 creeps (~1.2k of 31k fielded body-energy); remote income is zero, remote sources sit untouched | REFUTED | UNRELATED | UPHELD |
| Remote spawn requests are starved by head-of-line blocking behind unaffordable full-capacity local bodies; remote requests bid MEDIUM/LOW so they never spawn while energy_available stays at 7-388 | CONFIRMED | SYMPTOM | UPHELD |
| Remote rooms can never get containers, so remote mining is structurally the 4-walking-harvesters-per-source model with near-zero net yield and storage-only delivery | CONFIRMED | AMPLIFIER | DOWNGRADED → CONFIRMED (mechanism); delivery claim corrected/AMPLIFIER (minor — recovery ceiling, not a current drain) |
| Reservers are spawned (1300e = a whole RCL4 room) for outposts with zero miners, and are re-requested whenever the outpost is merely out of view | PLAUSIBLE | AMPLIFIER | UPHELD |
| Defend-gate (militarily_active) is NOT blocking remote spawning — and it is inert for unseen rooms, including two outposts last seen with hostile combat creeps | REFUTED | UNRELATED | UPHELD |
| SK farm is not a sink: no SourceKeeperFarm mission exists and none can — no RCL>=6 home is within max_range 2 of an SK ring room | REFUTED | UNRELATED | UPHELD |
| Salvage/derelict/raid/remote-build are not sinks: zero missions of each kind are live, and the breach gate would veto on stored energy anyway | REFUTED | UNRELATED | UPHELD |

Verifier: missed within the lane — Remote hauler bids are ROI-clamped to CRITICAL-1 (spawn_policy.rs:291-298 hauler_bid = band.max(roi).min(99,999)), not MEDIUM/LOW: the moment any outpost registers a pickup (unfulfilled>0), haul.rs:229-237 sizes the replacement remote hauler at min(home capacity) ([W,M]+[C,M]x up to 20) and it bids ABOVE HIGH — it would become a new capacity-sized head-of-line blocker in the home queue, the same shape the lane blames on local haulers. Only the frozen unfulfilled=0 keeps this dormant.; Harvester spawn requests carry token=None and are issued once per home room per source (source_mining.rs:365-420, with current_source_room_harvesters counted per home): an outpost source reachable from N homes can be funded N x 4 times. Latent amplifier once spawns recover; also inflates the queue clutter count the lane reports.; Remote HARVESTERS deliver via target_filters::all at HIGH transfer priority (jobs/harvest.rs:166-175), i.e. straight into spawn/extensions — the one remote channel that helps refill; the lane's 'storage-only' income model applies to haulers only.; Remote haul-mission stats only refresh when the outpost is visible (haul.rs:184-187 `room_visible` predicate), so with no creep giving vision unfulfilled_hauling is frozen at 0: the two fielded remote haulers (700e) idle in Wait for their whole life and the mission cannot re-size until something restores vision. Dead bodies today, an oscillation risk later.; features.remote_mine.harvest is not consulted by SourceMining/LocalSupply (grep: no remote_mine reference in missions/localsupply/*); only reserve.rs checks remote_mine.reserve. The operator flag cannot switch remote harvesting off — hygiene, but relevant if the recommended 'gate outposts on home economy' fix is attempted via features.; MiningOutpost missions persist when intel is stale: can_run_mission (missions/miningoutpost.rs:66-92) returns true whenever updated_within(1000) is false, so once an outpost enters Mine it keeps requesting harvesters/reservers indefinitely with no fresh scouting — the lane's 'even when out of view' point applies to all three children, not just reservers.

**Remote rooms can never get containers, so remote mining is structurally the 4-walking-harvesters-per-source model with near-zero net yield and storage-only delivery** — mechanism: No code places construction sites outside the home ConstructionMission (missions/construction.rs:445, one per Colony = 7; screeps-foreman/src/plan.rs is the only create_construction_site caller). Hence source_mining.rs:466 (`!source_containers.is_empty() && (local || any_home_room_has_storage)` -> container miners) is unreachable for outposts and the harvester branch at source_mining.rs:370-393 always runs: DESIRED_HARVESTERS_PER_SOURCE=4 (spawn_policy.rs:207) of [M,M,C,W]xN carrying 50e per round trip. Remote haulers are storage_delivery_only (haul.rs:277) so any remote energy bypasses spawn/extension refill entirely and needs a second local haul.

Live evidence: Dump: container_miners=0 and link_miners=0 on all 22 remote sources; live H6C: W16N52 containers [], W5N48 [], W0N51 []; W6N49/W6N47 not visible (no creeps keeping vision). The two live remote haulers have 150e and 50e carry capacity respectively and sit in state=Wait at home (nothing registered for pickup: unfufilled_hauling=0 in all remote haul missions). Remote sources full (1500/1500) = zero remote income to the empire.

Minimal fix: Plan + place source containers in outpost rooms (a remote-plan arm of the ConstructionMission / RoomTransfer registration, ADR 0009/0044 territory) and let remote haulers deliver to spawn/extensions when home storage is empty (haul.rs:277). Until then remote_mine.harvest yields nothing and should not be counted as income.

**Reservers are spawned (1300e = a whole RCL4 room) for outposts with zero miners, and are re-requested whenever the outpost is merely out of view** — mechanism: missions/reserve.rs:187 desired_reservers=2; body [Claim,Move] x up to 2 (:199-201) = 1300e regardless of home RCL; priority SPAWN_BID_MEDIUM for the first, LOW for the second (:205-209). reserve.rs:158-164 decides 'sufficient reservation' from game::rooms().get(room) — an unseen room reads as insufficient, so every outpost without vision requests reservers every tick. Nothing ties the request to whether the outpost has any miner/hauler. At equal bid, SpawnQueue::request (spawnsystem.rs:223-237) inserts the newer request AHEAD of existing equal bids, so a 1300e reserver at MEDIUM can head-of-line-block cheaper MEDIUM/LOW bodies (upgraders lerp MEDIUM->LOW, spawn_policy.rs:443-447) once HIGH is satisfied.

Live evidence: W6N49 controller reservation 'Azaril:482' (SNAP3) and W15N52 dump intel res=m: two reservers (2x1300e) were funded from W5N49 (cap 1800) / W16N51 (cap 1800) within the last ~1000-1500 ticks for outposts that have 0 harvesters/miners/containers — 2,600e wasted, ~130 ticks of both W5N49 sources' full output, in rooms whose storage is 0 and spawn energy is 300. 19 Reserve missions all allow=true reservers=0 => 19 standing 1300e MEDIUM requests spread over the 6 home queues (W16N51 x5, W5N49 x4, W9N46 x4, W13N51 x3, W7N47 x3, W2N49 x1).

Minimal fix: In reserve.rs: request a reserver only when the outpost has >=1 alive miner/harvester (or registered pickup), size to 1x[Claim,Move] (650e) when home capacity < 2600, and treat 'room not visible' as 'use last-seen reservation ticks minus elapsed' instead of 'insufficient'.

### H2 — unified energy market (ADR 0040/0042/0043/0044) as the cause of the MMO economic collapse

| Finding | Status | Weight | Verifier |
|---|---|---|---|
| The universal 10000 bid / floor is the spawn+extension refill lane pinned at REFILL_ROI_CAP (by construction, whenever any spawn request is queued) | CONFIRMED | SYMPTOM | UPHELD |
| The 1,000,000 bid in W2N49/W7N47 is SURVIVAL_BID tower refill while a hostile player creep is visible; it outranks the refill lane 100:1 and prices every consumer out in those two rooms | CONFIRMED | AMPLIFIER | DOWNGRADED → CONFIRMED (mechanism)/MINOR AMPLIFIER at most in W2N49/W7N47 only; working as designed |
| No priority inversion inside the transfer market: the floor gates only Use-lane consumer withdraws; the refill lane is the top-priced haul sink and storage has no reserve bid | REFUTED | UNRELATED | UPHELD |
| The empire is not haul-limited by pricing but by execution: provider containers are FULL, miners idle on full containers, sources sit unharvested at 3000, and haulers stand still for 20-130 ticks next to empty spawns — the rover per-tick path-ops pool is saturated every tick and ~half the creeps report move failures | PLAUSIBLE | ROOT | UPHELD |
| Spawn-queue head-of-line banking with capacity-sized bodies freezes the lane at 300 and blocks the cheap hauler that could unfreeze it (the 'inversion' exists in the spawn queue, not the market) | PLAUSIBLE | AMPLIFIER | DOWNGRADED → PLAUSIBLE/WEAK AMPLIFIER (secondary to finding 4; miner 'capacity-sized' claim incorrect) |
| Terminal energy reserve hoards 16.8k energy in two rooms while their spawn lanes sit at 13/300 | CONFIRMED | AMPLIFIER | UPHELD |
| 720k oxygen unsold: market selling is switched off in Memory._features, so the terminal mission's passive-sale requests are never executed | CONFIRMED | UNRELATED | UPHELD |
| The 20% military reserve does not withhold energy from the market; with storage at 0 it only refuses offense squads | REFUTED | UNRELATED | UPHELD |

Verifier: missed within the lane — Peaceful tower refill is priced BELOW the storage numeraire (TOWER_PEACE_MILLI 500 at sink_economics.rs:149 vs STORAGE_BID 1000; tower.rs:184-198). Storage free capacity (~1M) is always a materially-unmet par deposit, so in density ranking a peaceful tower never wins an edge while a storage exists: SNAP2 shows towerE 5-10 in every room without a hostile, including W13N52 (6 towers, 7 energy total) and W2N49 at 5 before the harasser arrived. Towers are therefore empty when hostiles show up, which is exactly what forces the 1e6 survival lane to drain a starving spawn lane (finding 2). ADR 0043 A5 defect, defense-relevant, market-lane owned.; The refill pricing input is a unit mismatch, not a tuning choice: spawnsystem.rs:295-301 publishes requests[0].priority() (a band bid >= 25_000) as top_blocked_roi_milli where sink_economics.rs:317-319 and room_transfer.rs:199 specify body_roi_milli of the head request. Consequence: refill_bid is pinned at the 10000 cap in every room with any queued request, so the opportunity floor is 10000 everywhere and upgrader Use-lane draws (2000) are permanently shed (consumer_admission.rs:60-73) except under the downgrade veto — an always-on GCL/RCL progress loss the lane labelled cosmetic.; Loaded-carrier reachability probe keys the haul-distance memo on the carrier's CURRENT position: market.rs ~:225-233 calls dist(c.pos, dpos) for every loaded hauler decision; hauldistance.rs:101-115 caches (from,to) for DIST_TTL=1500 with no size bound and runs a fresh engine PathFinder (SEARCH_MAX_OPS 20k, CPU-charged) on every miss. That is why seg-57 shows 10165 cached pairs with hits=0 and computes=4 per tick (unbounded memo growth), and a single transient incomplete search caches None so a loaded hauler on that tile is declined EVERY sink for 1500 ticks — a market-lane candidate for the unexplained W13N52 hauler holding 550-581e adjacent to an empty Spawn9 (open question 2), which the path-ops finding cannot explain (range-1 delivery needs no path).; Recovery lever not flagged: market.buy=false / buy_energy=false with 531M credits (features.rs:150-160, ordersystem.rs:463-472) — energy import into the W13N52/W13N51 terminals is switchable at the console, though it is still gated by the 10k terminal reserve (finding 6) and by hauler execution (finding 4).; Cross-lane but load-bearing for market state persistence: seg-57 faults show panics_caught 2687 == serialize_skipped_aborted 2687 and the serialized world is at 4 of 4 chunks (~162.7k of 204.8k bytes, 'near segment capacity' logged every tick). If a tick overflows the chunk budget the HaulJob/ticket state is not persisted, and with vm_starts=2771 any VM reset then rebuilds haulers from stale or missing state — a plausible contributor to tickets that never execute; needs the global/serialization lane.

**The 1,000,000 bid in W2N49/W7N47 is SURVIVAL_BID tower refill while a hostile player creep is visible; it outranks the refill lane 100:1 and prices every consumer out in those two rooms** — mechanism: screeps-ibex/src/missions/tower.rs:184-198 registers every tower's free capacity at `tower_refill_bid(consts, are_hostile_creeps)` = SURVIVAL_BID = 1_000_000 when ANY hostile creep is in the room (sink_economics.rs:42, :423-431); wall_repair.rs:127-156 adds the same 1e6 tower deposit when hostiles are present AND a rampart/wall < 100k. In market_pass (screeps-econ-decision/src/market.rs:200-300, matching.rs:44-50) edge density = bid*amount/service, so a 1e6 tower sink beats the 10000 refill aggregate for every hauler edge; the published floor becomes 1e6 so `admit_use_withdraw`/builder admission (jobs/utility/consumer_admission.rs:65-133) reject upgrade (2000) and all build classes (<=10000).

Live evidence: W2N49: 72 lines floor=10000 vs 17+1 lines floor=1000000; W7N47: 63 vs 27 — the flip tracks hostile visibility. Live MKA t=5485234: hostile `iamgqr` 48-part creep in W2N49 and 40-part in W7N47 (the [War] Secure threat rooms, dps 170/40); W2N49 tower 1000/1000 (kept full at the expense of the lane: spawn energy 500->300), W7N47 tower 630->110 (firing), W7N47 sources 3000/3000 (miners fled, containers 0/0/777), W7N47 has 18 walls <100k. seg-57 W2N49 market_opportunity_floor=1000000.

Minimal fix: Scope the survival lane: price tower refill at SURVIVAL_BID only while the tower is actually below a firing reserve (e.g. free > TOWER_CAPACITY/2) and/or cap survival tower demand at one tower-load per pass, so a full-time harasser cannot starve the spawn lane of a 1300-capacity room; keep the 1e6 for the wall-breach case only.

**The empire is not haul-limited by pricing but by execution: provider containers are FULL, miners idle on full containers, sources sit unharvested at 3000, and haulers stand still for 20-130 ticks next to empty spawns — the rover per-tick path-ops pool is saturated every tick and ~half the creeps report move failures** — mechanism: (Cross-lane — pathing — but it is what the market's assignments run into.) jobs/staticmine.rs:225-226: a static miner returns `wait(1)` when its container cannot absorb one harvest, so full containers stop income at the source. Hauler tickets execute via movement: rover screeps-rover/src/movementsystem.rs:1800-1811 allocates `allowed_ops = min(rooms*2000 (<=20_000), pool_remaining)` from a per-tick pool DEFAULT_PATHFIND_OPS_BUDGET = 20_000 (:21) and returns `PathNotFound` when the pool is empty (:1804-1806); a single 10+-room first-path search (max_ops = rooms*2000 capped 20_000) can drain the whole tick's pool. screeps-ibex/src/pathing/movementsystem.rs:611-620 counts `MovementResult::Failed | Stuck>=threshold` as `move_failures`. The haul-distance oracle (pathing/hauldistance.rs) uses the ENGINE pathfinder with its own 20k cap and is not the limiter.

Live evidence: MKA t=5485234: source containers 2000/2000 in W5N49 (16,18),(38,30); W16N51 (22,34); W13N51 (9,9); W9N46 (33,39),(41,16); W13N52 (18,9); sources full at 3000/3000 in W5N49 (both), W16N51, W7N47 (both) with miners parked on the containers (5W/0C/1M at (16,18),(38,30)). Haulers stationary: W16N51 0/3/3 at (16,29)->(16,28) over 97 ticks with 0 energy, 7 tiles from a full container and adjacent to a 250/300 spawn; W13N52 0/19/19 carried 550 from t=5485235 to 5485421 (>180 ticks, died with 581 aboard) while standing adjacent to Spawn9 at 13/300 (engine PathFinder path length 0); W9N46 0/3/3 at (37,26) unmoved 97 ticks; spawn lanes frozen at exactly 300/301 in W5N49/W16N51/W13N52 from t=5484870 through 5485234. Engine PathFinder (MKE, t=5485331, own-rampart/container-walkable matrix) finds complete paths for all sampled pairs (4-7 tiles) — geometry is fine. seg-57 pathing at t=5484939 AND t=5485286: ops_used=20000 of ops_pool=20000 (saturated both samples), move_failures=23/22 per tick out of 43 creeps, wasted_moves=0 (no intents issued for them), CPU only 66/26 of 140, bucket 10000, governor normal — starvation of path ops, not CPU. Three MOVE-only scouts sat at W7N47 (40-42,3-4) for >=163 ticks; findRoute W13N51->W18N52 (a live ClaimOp ring-4 candidate) = 12 rooms = a 20k-op search.

Minimal fix: Pathing lane: cap per-search ops well below the tick pool (e.g. <= pool/4) and make the pool per-priority (haul/miner first-paths before scout/claim far routes), or run far cross-room routes on `findRoute` waypoints; verify by watching seg-57 ops_used drop below ops_pool and move_failures -> ~0, then containers drain and spawn lanes refill without any market change.

**Spawn-queue head-of-line banking with capacity-sized bodies freezes the lane at 300 and blocks the cheap hauler that could unfreeze it (the 'inversion' exists in the spawn queue, not the market)** — mechanism: screeps-ibex/src/spawnsystem.rs:483-489: requests are consumed in descending bid order; `if body_cost > energy_capacity {continue}` else `if body_cost > available_energy {break}` — a CRITICAL/HIGH request sized to `energy_capacity_available` banks at the head and nothing cheaper behind it spawns. Miner replacements are capacity-sized (missions/localsupply/source_mining.rs:300,449,485 `source_miner_body(.., energy_capacity_available(), ..)`), harvester replacements too (spawn_policy.rs:198-206), and the hauler is capacity-sized once ANY hauler exists (missions/haul.rs:229-236 `min(max_energy)`; `hauler_body` spawn_policy.rs:232-256 with no K4 deficit pick). ADR 0007 item 4 / K4 pricing is not applied to these bodies.

Live evidence: Spawns idle (busy=0 all 7 rooms at t=5484870) with lanes at exactly 300/301 for >=360 ticks in W5N49/W16N51/W13N52 while 200-300e haulers would be affordable; the moment W13N52's lane refilled (826 at t=5485363) it spawned a 2000e 0/20/20 hauler and a 15W/1C/2M miner (MKI t=5485421), and W13N51/W2N49 spawned 10W/1C/2M miners — all capacity-sized. RENEW is also off everywhere (spawnsystem.rs:22,474 needs stored>=10k).

Minimal fix: Size the head-of-line body to what the lane can afford within a short horizon (K4 `deficit_priced_pick` with income) or skip-over a banking request when a cheaper same-role-critical request (hauler/harvester) is affordable now; both keep the descending order and only change the `break` at spawnsystem.rs:487.

**Terminal energy reserve hoards 16.8k energy in two rooms while their spawn lanes sit at 13/300** — mechanism: missions/terminal.rs:405-425 makes terminal energy available to haulers only above `terminal_reserve_threshold.end()` = `OrderQueue::maximum_transfer_energy()` = 10_000 (transfer/ordersystem.rs:56-58; terminal.rs:50-54), and only when storage < desired 200k (constants.rs:5). Below 10k nothing is registered as a withdraw.

Live evidence: SNAP5: W13N52 terminal energy exactly 10000, W13N51 6807, both storages 0; W13N52 spawns at 13/13/13 and extensions 262 for >=360 ticks; seg-57 reports W13N52 stored_energy 10000 (all of it terminal, untouchable).

Minimal fix: When the room's refill lane is materially unmet (floor >= refill bid), drop the terminal energy reserve to 0 (or register the reserve at a par withdraw) so the market can pull it into the lane.

### H1 — energy flow and the spawn deadlock

| Finding | Status | Weight | Verifier |
|---|---|---|---|
| Empire-wide creep movement freeze starves every spawn lane (haulers hold cargo, miners never reach containers) | CONFIRMED | ROOT | UPHELD |
| Spawn queue head-of-line banking + hauler bid pinned at 99,999 + capacity-sized replacement bodies turns any inflow stall into a hard spawn deadlock (miners only bid 75,000) | CONFIRMED | AMPLIFIER | UPHELD |
| The 1W1C2M (250e) and 3C3M (300e) creeps are the 'first creep of an empty roster' bootstrap bodies; they recur because the freeze keeps zeroing rosters and they cannot bank their own replacements | CONFIRMED | AMPLIFIER | UPHELD |
| Market opportunity floor is pinned at the 10,000 cap in every room because the refill 'ROI' published is the head request's BID; haulers are not gated by it but all consumer self-fetch and repair are shed (ramparts decay, controllers run down) | CONFIRMED | AMPLIFIER | DOWNGRADED → PLAUSIBLE (mechanism half-wrong: bid-vs-ROI is a no-op; pinning at cap under a blocked queue is designed)/SYMPTOM |
| Orphaned creeps (creeps with no ECS job) — REFUTED as a cause | REFUTED | UNRELATED | UPHELD |
| Known tracker items: 0040 D8 military reserve and 0042 squad opportunity_floor=0 are not on the spawn-lane path; 0043 harvester lerp and 0007 hauler-count sizing are minor contributors | CONFIRMED | SYMPTOM | UPHELD |
| Latent: in non-burst mode the movement system refuses every path search (headroom == cap), so a bucket dip below 9,500 would freeze all movement outright | PLAUSIBLE | AMPLIFIER | DOWNGRADED → PLAUSIBLE (latent, deliberate per code comment, not firing)/UNRELATED to this incident (latent amplifier only under a bucket dip) |

Verifier: missed within the lane — The 20,000-op pool is a LIVE-TUNABLE feature, not a constant: screeps-ibex/src/pathing/movementsystem.rs:546-567 derives it from Memory._features.pathing.pathfinding_cpu_budget (=20 in SNAP8) x 1000, with PATHFIND_OPS_CEILING 50,000 and governor tier scaling; raising it to 50 (50k ops = 25 in-room searches/tick) is a zero-deploy mitigation the lane never surfaced, and MIN_PATHFIND_OPS=2000 only guarantees ONE search when the budget is zero.; Ops-pool accounting is reservation-without-refund (screeps-rover/src/movementsystem.rs:1811; ops_consumed = cap - remaining at :584-586): a trivially cheap adjacent-tile search still reserves 2000/4000 ops, so the pool caps SEARCH COUNT (10, or 5 stuck-escalated) not CPU. Refunding the pathfinder's actual ops after the search is the smallest root-cause fix in the rover and is absent from the lane's minimal_fix list.; The first-path pool reserve (rover :1420-1432, NEEDS_PATH_OPS_RESERVE_DIV=5) already exists and was designed against exactly this 'permanently pathless' wedge; the live failure shows it is defeated when Failed creeps (which re-enter needs_path) exceed the reserve's 1-2 searches — the lane should cite it and explain why it did not hold rather than describe first-path starvation as unguarded.; Doc/test drift on the miner band: spawn_policy.rs hauler_bid/harvester_priority comments and squad_manager.rs tests (:4593, :4605) all assert 'CRITICAL miners' while source_mining.rs requests SPAWN_BID_HIGH — any fix to finding 2 must update those assertions or the tests will encode the wrong invariant.; W13N52 renew eligibility: stored_energy (storage+terminal+containers) ~13,100 >= RENEW_MIN_ROOM_ENERGY 10,000, so the renew pass (spawnsystem.rs:540-566) was armed but structurally useless — it requires the creep within range 1 of a free spawn, which a frozen creep cannot satisfy; the lane's 'renew is off everywhere' should read 'renew cannot fire under a freeze'.; tick_delivery (haulbehavior.rs:604-617) has no adjacent-target fallback: a loaded hauler standing next to an empty 10,000-bid extension will hold cargo for a non-adjacent ticket (storage at par) — this is the concrete reason the (9,19) hauler delivered 385e to storage after release; the lane listed it as an open question but the code answers it (ticket target is bound at selection; no re-price against adjacent sinks).

**Empire-wide creep movement freeze starves every spawn lane (haulers hold cargo, miners never reach containers)** — mechanism: Rover movement: a creep whose path search fails is inserted as `stationary_occupant` (screeps-rover/src/movementsystem.rs:482-506: allow_shove=false, allow_swap=false) at the Pass-1 Err branch (movementsystem.rs:976-989), i.e. an immovable post. A search fails with PathNotFound when the per-tick ops pool is drained (movementsystem.rs:1803-1814 `allowed_ops = max_ops.min(pool_remaining)`; 0 -> PathNotFound) or the search is incomplete (1844). Each doomed search still burns its allowed ops (2000 x rooms x2 when stuck), so a handful of blocked creeps drain the 20000-op pool every tick and every other creep's first-path (`needs_path`, 1433) also gets PathNotFound -> more immovable posts. The wedge only clears when posts die of TTL (renew is off everywhere: spawnsystem.rs:470-475 needs stored_energy >= 10_000, all rooms have 0). Job layer holds its ticket through the failure: tick_pickup / tick_delivery (screeps-ibex/src/jobs/utility/haulbehavior.rs:456-505, 580-660) only ever re-issue move_to; no cargo is abandoned or redirected on a Failed result.

Live evidence: seg-57 t=5484939: pathing ops_used=20000 == ops_pool=20000, move_failures=23 (of 43 creeps), wasted_moves=0, repath_count=5. Position samples t~5485150/5485246/5485275: 38 of 44 creeps identical (only scouts/1 harvester moved). W13N52 hauler 5483823 (19C19M, 550 carried) static at (9,19) ADJACENT to empty extensions (10,19)/(10,20)/(10,18) for >100 ticks while lane sat at 301/12500; room visual export shows the rover FAILED marker (red circle r=0.2 = visualize_failed, screeps-ibex/src/pathing/movementsystem.rs:268-282) at (9,19) and at (17,9). W7N47 container miners 5484213-1/5484289 parked at (37,38)/(38,40) next to spawn (38,39) while their containers (31,37)/(36,45) held 0/0 and both sources sat at 3000/3000. Buffers full everywhere: W13N52 container (18,9) 2000, links 800/799/449, 615 dropped, storage 0; W5N49 containers 2000/2000, sources 3000/3000; W16N51 container 2000 + links 800/799; W9N46 2000/1998. Lane inflow W13N52 = 0 e/t: energyAvailable 301 at t=5484870 and 301 (262 ext + 3x13 spawn) at t=5485246. RELEASE: 6 creeps expired (46->40) between t~5485350-5485400; seg-57 t=5485414: ops_used 6000, move_failures 0, haul_dist_hits 8; container (18,9) 2000->100, links 800->50 / 799->97, lane 301->438 (t=5485401) ->900 (t=5485469), storage 0->385->1595; W13N52 spawned 20C20M hauler 5485344 (2000e), upgrader 5485378 (1650e), builder 5485382; W7N47 miners reached (31,37)/(36,45); W16N51 lane 300->1475; W9N46 spawned two 13C13M haulers.

Minimal fix: Movement lane owns the wedge itself; for the H1 seam: (a) on `check_movement_failure` (jobs/utility/movebehavior.rs:16-26) a Pickup/Delivery hauler must drop its ticket and reselect — a loaded hauler adjacent to any needy refill structure should transfer immediately rather than hold cargo on a failed move; (b) rover: a PathNotFound creep must stay shoveable (stationary_occupant allow_shove=false is what turns a budget miss into a permanent post) and doomed searches must be storm-damped so they stop draining the shared pool every tick.

**Spawn queue head-of-line banking + hauler bid pinned at 99,999 + capacity-sized replacement bodies turns any inflow stall into a hard spawn deadlock (miners only bid 75,000)** — mechanism: spawnsystem.rs:497-499 `if body_cost > available_energy { break; }` — the first unaffordable request blocks EVERYTHING behind it (only `> energy_capacity` is skipped, :493). haul.rs:229-237: once `haulers` is non-empty `energy_to_use = min(energy_capacity_available)` -> hauler_body 20 repeats (spawn_policy.rs:232-256) = 2000e at RCL8, 1800e at RCL5, 1300e at RCL4. haul.rs:265-266 + spawn_policy.rs:291-297: logistics_rate = carry*50*1000, body_cost = carry*100 -> body_roi_milli = 750,000 for ANY size, so `band.max(roi).min(SPAWN_BID_CRITICAL-1)` = 99,999 always, independent of need. Link/container miners bid SPAWN_BID_HIGH = 75,000 (source_mining.rs:457, :491), not CRITICAL — contradicting the spawn_policy.rs:284-297 comment 'logistics never preempts income'. Replacement harvesters lerp CRITICAL->HIGH by roster fill (spawn_policy.rs:214-224) so 1/4 = 93,750 < 99,999. Header of spawn_policy.rs (lines 15-16) records this S6 head-of-line defect as 'deliberately preserved — M4 owns the fix'. haul.rs:280 TODO: no starvation/bootstrap handling.

Live evidence: Live spawn queues (room visual export): W5N49 e=300: ['Critical 100000 · 250 · Harvester', 'High 99999 · 1800 · Haul', 'High 75000 · 550 · Container Miner', 'High 75000 · 550 · Container Miner', ...] — both income bodies (550e) blocked behind a 1800e hauler while containers sit at 2000/2000 and sources at 3000/3000. W16N51 e=1167..1475: ['High 99999 · 1800 · Haul', 'High 75000 · 600 · Link Miner', ...] — the room's ONLY source (21,34) at 3000 with container 2000 + links 800/799 full, miner 5483877 expired ~t=5485397, replacement blocked behind the hauler. W9N46 e=915 -> spent on two 13C13M haulers (5485338, 5485466 = 2600e) while both sources have no miner: queue ['High 75000 · 1150 · Upgrader', 'High 75000 · 550 · Container Miner' x2, ...]. W13N52 (cap 12500) needed a 2000e head while its lane was 301 for 400+ ticks (finding 1) — 3 spawns idle at 13e each; the 2000e hauler spawned only after the release (5485344). Hauler count wanted vs has (spawn_policy.rs:258-266, cap 3 local): W13N52 wants 3 had 1; W16N51 wants 3 has 1 (3C3M); W5N49 has 2 wants 3; W9N46 had 1 now 3.

Minimal fix: Make the hauler bid need-scaled (price marginal unmet throughput, not carry*50 — e.g. w = min(unfulfilled/round_trip, carry*50)), put link/container miners at SPAWN_BID_CRITICAL as the comments already claim, and size a replacement hauler from energy_available when the lane has had no inflow (haul.rs:280 TODO) so a 300e hauler can always be fielded under starvation.

**The 1W1C2M (250e) and 3C3M (300e) creeps are the 'first creep of an empty roster' bootstrap bodies; they recur because the freeze keeps zeroing rosters and they cannot bank their own replacements** — mechanism: spawn_policy.rs:198-204 harvester_body_energy: total_harvesting_creeps==0 -> energy_available.max(300) -> harvester_body [M,M,C,W]x1 = 250e ('mmcw'); every later harvester is capacity-sized (1250e at cap>=1250). haul.rs:229-233: haulers empty -> max(energy_available, 300) -> [C,M]x3 = 300e ('cmcmcm'); localbuild.rs:227-231 (builders empty & bid>=HIGH -> 'cwmm' 250e) and upgrade.rs:201-205 (downgrade_risk & empty) are the same arm. harvest.rs:104-127: a home harvester first tries haul pickup/delivery before harvesting, so under the freeze it becomes another stuck mini-hauler.

Live evidence: SNAP4/QB bodies: 'mmcw' 1W1C2M in W13N51 x3 (5483777, 5484325, 5484594), W13N52 (5483783), W7N47 (5483790), W9N46 (5484500), W5N49 spawned another 'mmcw' 5485454 at t=5485454; 'cmcmcm' 3C3M haulers in W13N51 (5484618), W16N51 (5484256), W9N46 (5484449); 'cwmm' builder 5485382 in W13N52 at t=5485382. W13N51 queue shows the follow-ups: 'Low 37500 · 1250 · Harvester' (3/4 roster lerp) behind 'High 75000 · 550 · Container Miner'. Recovery rate implied: 1W1C2M = 2 e/t harvest, 50 carry, ~55 ticks/cycle ~= 0.9 e/t each; W13N51 lane rose 599->877 (+278) over 376 ticks = 0.74 e/t during the freeze -> a 1250e replacement needs ~1700 ticks > the 1500-tick life of the bootstrap creeps; W13N52 lane 0 e/t. Potential once creeps move: 2 sources x 10 e/t = 20 e/t (W13N52 observed ~22 e/t post-release incl. buffer drain: 2000 hauler + 1650 upgrader + 250 builder + 600 lane in ~200 ticks).

Minimal fix: Size ALL replacement civilian bodies from max(energy_available, min viable) whenever the lane has been below the requested body for > one spawn cycle (a starvation mode), not only when the roster is empty; keep capacity sizing for the steady state.

**Market opportunity floor is pinned at the 10,000 cap in every room because the refill 'ROI' published is the head request's BID; haulers are not gated by it but all consumer self-fetch and repair are shed (ramparts decay, controllers run down)** — mechanism: spawnsystem.rs:291-304 RefillPricingCache::publish_from sets `top_blocked_roi_milli = Some(requests[0].priority())` — the head request's milli bid (25,000..100,000), for ANY non-empty queue, affordable or not, not a body ROI. sink_economics.rs:323-334 refill_bid clamps it to REFILL_ROI_CAP_MILLI 10,000 (:141) -> spawn/extension deposit bid = 10,000 (room_transfer.rs:206-212, :417-418) -> opportunity_floor = 10,000 (transfersystem.rs:1649-1678). Haulers: carrier_gate opportunity 0 admits (market.rs:143-149); reduced cost 10000-1000-haul(d) > 0 for storage/terminal sources (sink_economics.rs:268-282; source_floor transfersystem.rs:112-128) so spawn fill is NOT gated. Consumers: upgrader V_UPGRADE 2,000 < 10,000 (consumer_admission.rs:65-75, veto only below 50% clock), builder extension 8,000 < 10,000 (consumer_admission.rs:118-135), repair via admit_repair (sink_economics.rs:478) -> all priced out whenever any spawn request is queued.

Live evidence: [market] lines: floor=10000 in all 7 rooms (90/90 samples), including W9N46 at 1059/1300 energy and W13N52 with 10 labs/6 towers; seg-57 market_opportunity_floor=10000 everywhere; W2N49/W7N47 floor=1000000 = SURVIVAL_BID tower refill under the Secure-objective hostiles (sink_economics.rs:425-431). Consequences visible: ramparts W13N51 0 (walls 14), W16N51 1 @4201, W9N46 1 @3501, W5N49 min 601; controller clocks W9N46 33153, W2N49 35185, W7N47 37542 (RCL4 full 40000) while containers hold 2000/2000; upgrade progress W13N52 0, W9N46 88% stalled. Terminal energy (W13N52 10000, W13N51 6807) is a par-floor haul source (terminal.rs:423-455 registers Haul withdraws) — not locked, just never moved during the freeze.

Minimal fix: Publish the head's real body ROI (body_roi_milli of its w, and only for an UNAFFORDABLE head) as top_blocked_roi_milli instead of its bid; alternatively floor-material the refill node on the deficit the next affordable body needs, so the floor falls to par once the lane can spawn something.

**Latent: in non-burst mode the movement system refuses every path search (headroom == cap), so a bucket dip below 9,500 would freeze all movement outright** — mechanism: screeps-ibex/src/pathing/movementsystem.rs:595 `pathfinding_headroom = if normal_mode { Some(movement_cap) } else { Some(80.0) }`; rover generate_path movementsystem.rs:1733-1737 `if used + headroom > *max { PathNotFound }` — with headroom == max this refuses any search once movement has used > 0 CPU in the tick. normal_mode holds when bucket < bucket_burst_threshold (9500, Memory._features.pathing) so budget_ceiling == cpu_limit (:523-527).

Live evidence: Not currently firing: bucket 10000, bucket_trend ~0, so budget_ceiling = tick_limit 500 (burst) and headroom 80. Explains why the empire depends on a full bucket; any CPU excursion (e.g. the 4/tick 20k-op haul-distance computes, hauldistance.rs:35 and :101-118, keyed by the moving creep's position via market.rs:231 -> 10,165 cached pairs, 0 hits during the freeze) that drains the bucket would convert to a total freeze.

Minimal fix: Set a finite normal-mode headroom (e.g. the largest single search cost, ~20 CPU) and key the loaded-carrier reachability memo by the sink only (or skip it when the creep is already adjacent).
